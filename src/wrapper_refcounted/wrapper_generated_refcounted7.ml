(* THIS FILE IS AUTOMATICALLY GENERATED, DO NOT EDIT BY HAND! *)

open! Core
open C_ffi
open Torch_wrapper_types
open Torch_refcounted_bindings.Type_defs

[%%c {| #include "torch_api.h" |}]

let special_modified_bessel_i1_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_modified_bessel_i1_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_modified_bessel_k0 (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_modified_bessel_k0(%{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_modified_bessel_k0_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_modified_bessel_k0_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_modified_bessel_k1 (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_modified_bessel_k1(%{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_modified_bessel_k1_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_modified_bessel_k1_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_multigammaln (self @ local) ~p =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_multigammaln(%{self : tensor value}, %{p : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_multigammaln_out ~(out @ local) (self @ local) ~p =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_multigammaln_out(%{out : tensor value}, %{self : tensor value}, %{p : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_ndtr (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_ndtr(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_ndtr_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_ndtr_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_ndtri (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_ndtri(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_ndtri_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_ndtri_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_polygamma ~n (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_polygamma(%{n : int}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_polygamma_out ~(out @ local) ~n (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_polygamma_out(%{out : tensor value}, %{n : int}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_psi (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_special_psi(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_psi_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_psi_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_round (self @ local) ~decimals =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_round(%{self : tensor value}, %{decimals : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_round_out ~(out @ local) (self @ local) ~decimals =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_round_out(%{out : tensor value}, %{self : tensor value}, %{decimals : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_scaled_modified_bessel_k0 ~(x @ local) =
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_scaled_modified_bessel_k0(%{x : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_scaled_modified_bessel_k0_out ~(out @ local) ~(x @ local) =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_scaled_modified_bessel_k0_out(%{out : tensor value}, %{x : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_scaled_modified_bessel_k1 ~(x @ local) =
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_scaled_modified_bessel_k1(%{x : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_scaled_modified_bessel_k1_out ~(out @ local) ~(x @ local) =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_scaled_modified_bessel_k1_out(%{out : tensor value}, %{x : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_t ~(x @ local) ~(n @ local) =
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_t(%{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_t_n_scalar ~(x @ local) ~n =
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_t_n_scalar(%{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_t_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_t_n_scalar_out(%{out : tensor value}, %{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_t_out ~(out @ local) ~(x @ local) ~(n @ local) =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_t_out(%{out : tensor value}, %{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_t_x_scalar ~x ~(n @ local) =
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_t_x_scalar(%{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_t_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  let out = globalize_tensor out in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_t_x_scalar_out(%{out : tensor value}, %{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_u ~(x @ local) ~(n @ local) =
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_u(%{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_u_n_scalar ~(x @ local) ~n =
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_u_n_scalar(%{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_u_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_u_n_scalar_out(%{out : tensor value}, %{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_u_out ~(out @ local) ~(x @ local) ~(n @ local) =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_u_out(%{out : tensor value}, %{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_u_x_scalar ~x ~(n @ local) =
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_u_x_scalar(%{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_u_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  let out = globalize_tensor out in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_u_x_scalar_out(%{out : tensor value}, %{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_v ~(x @ local) ~(n @ local) =
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_v(%{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_v_n_scalar ~(x @ local) ~n =
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_v_n_scalar(%{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_v_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_v_n_scalar_out(%{out : tensor value}, %{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_v_out ~(out @ local) ~(x @ local) ~(n @ local) =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_v_out(%{out : tensor value}, %{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_v_x_scalar ~x ~(n @ local) =
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_v_x_scalar(%{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_v_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  let out = globalize_tensor out in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_v_x_scalar_out(%{out : tensor value}, %{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_w ~(x @ local) ~(n @ local) =
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_w(%{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_w_n_scalar ~(x @ local) ~n =
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_w_n_scalar(%{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_w_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_w_n_scalar_out(%{out : tensor value}, %{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_w_out ~(out @ local) ~(x @ local) ~(n @ local) =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_w_out(%{out : tensor value}, %{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_w_x_scalar ~x ~(n @ local) =
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_w_x_scalar(%{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_shifted_chebyshev_polynomial_w_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  let out = globalize_tensor out in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_shifted_chebyshev_polynomial_w_x_scalar_out(%{out : tensor value}, %{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_sinc (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_sinc(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_sinc_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_sinc_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_softmax (self @ local) ~dim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_special_softmax(%{self : tensor value}, %{dim : int}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_spherical_bessel_j0 ~(x @ local) =
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_spherical_bessel_j0(%{x : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_spherical_bessel_j0_out ~(out @ local) ~(x @ local) =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_spherical_bessel_j0_out(%{out : tensor value}, %{x : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_xlog1py (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_special_xlog1py(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_xlog1py_other_scalar (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_xlog1py_other_scalar(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_xlog1py_other_scalar_out ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_xlog1py_other_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_xlog1py_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_special_xlog1py_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_xlog1py_self_scalar self (other @ local) =
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_special_xlog1py_self_scalar(%{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_xlog1py_self_scalar_out ~(out @ local) self (other @ local) =
  let out = globalize_tensor out in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_special_xlog1py_self_scalar_out(%{out : tensor value}, %{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_xlogy (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_special_xlogy(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_xlogy_other_scalar (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_xlogy_other_scalar(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_xlogy_other_scalar_out ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_xlogy_other_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_xlogy_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_special_xlogy_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_xlogy_self_scalar self (other @ local) =
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_special_xlogy_self_scalar(%{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_xlogy_self_scalar_out ~(out @ local) self (other @ local) =
  let out = globalize_tensor out in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_special_xlogy_self_scalar_out(%{out : tensor value}, %{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_zeta (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_special_zeta(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_zeta_other_scalar (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_zeta_other_scalar(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_zeta_other_scalar_out ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_zeta_other_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_zeta_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_special_zeta_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_zeta_self_scalar self (other @ local) =
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_special_zeta_self_scalar(%{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_zeta_self_scalar_out ~(out @ local) self (other @ local) =
  let out = globalize_tensor out in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_special_zeta_self_scalar_out(%{out : tensor value}, %{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let split (self @ local) ~split_size ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_split(%{self : tensor value}, %{split_size : int}, %{dim : int}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let split_copy (self @ local) ~split_size ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_split_copy(%{self : tensor value}, %{split_size : int}, %{dim : int}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let split_copy_tensor_out ~(out @ local) (self @ local) ~split_size ~dim =
  let out = Torch_local_iterators.List.map_local_input out ~f:globalize_tensor in
  let self = globalize_tensor self in
  [%c.alloc
    {|atg_split_copy_tensor_out(%{out : tensor list value}, %{self : tensor value}, %{split_size : int}, %{dim : int});|}]
;;

let split_sizes (self @ local) ~split_size ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_split_sizes(%{self : tensor value}, %{split_size : int list value}, %{dim : int}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let split_with_sizes (self @ local) ~split_sizes ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_split_with_sizes(%{self : tensor value}, %{split_sizes : int list value}, %{dim : int}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let split_with_sizes_copy (self @ local) ~split_sizes ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_split_with_sizes_copy(%{self : tensor value}, %{split_sizes : int list value}, %{dim : int}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let split_with_sizes_copy_out ~(out @ local) (self @ local) ~split_sizes ~dim =
  let out = Torch_local_iterators.List.map_local_input out ~f:globalize_tensor in
  let self = globalize_tensor self in
  [%c.alloc
    {|atg_split_with_sizes_copy_out(%{out : tensor list value}, %{self : tensor value}, %{split_sizes : int list value}, %{dim : int});|}]
;;

let sqrt (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_sqrt(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sqrt_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_sqrt_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sqrt_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_sqrt_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let square (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_square(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let square_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_square_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let square_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_square_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let squeeze (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_squeeze(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let squeeze_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_squeeze_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let squeeze_copy (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_squeeze_copy(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let squeeze_copy_dim (self @ local) ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_squeeze_copy_dim(%{self : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let squeeze_copy_dim_out ~(out @ local) (self @ local) ~dim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_squeeze_copy_dim_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let squeeze_copy_dims (self @ local) ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_squeeze_copy_dims(%{self : tensor value}, %{dim : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let squeeze_copy_dims_out ~(out @ local) (self @ local) ~dim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_squeeze_copy_dims_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let squeeze_copy_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_squeeze_copy_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let squeeze_dim (self @ local) ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_squeeze_dim(%{self : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let squeeze_dim_ (self @ local) ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_squeeze_dim_(%{self : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let squeeze_dims (self @ local) ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_squeeze_dims(%{self : tensor value}, %{dim : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let squeeze_dims_ (self @ local) ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_squeeze_dims_(%{self : tensor value}, %{dim : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sspaddmm ?beta ?alpha (self @ local) ~(mat1 @ local) ~(mat2 @ local) =
  let self = globalize_tensor self in
  let mat1 = globalize_tensor mat1 in
  let mat2 = globalize_tensor mat2 in
  [%c.alloc
    ({|CAMLreturn(atg_sspaddmm(%{self : tensor value}, %{mat1 : tensor value}, %{mat2 : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sspaddmm_out
  ?beta
  ?alpha
  ~(out @ local)
  (self @ local)
  ~(mat1 @ local)
  ~(mat2 @ local)
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let mat1 = globalize_tensor mat1 in
  let mat2 = globalize_tensor mat2 in
  [%c.alloc
    ({|CAMLreturn(atg_sspaddmm_out(%{out : tensor value}, %{self : tensor value}, %{mat1 : tensor value}, %{mat2 : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let stack (tensors @ local) ~dim =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_stack(%{tensors : tensor list value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let stack_out ~(out @ local) (tensors @ local) ~dim =
  let out = globalize_tensor out in
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_stack_out(%{out : tensor value}, %{tensors : tensor list value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let std (self @ local) ~unbiased =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_std(%{self : tensor value}, Int_val(%{unbiased : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let std_correction (self @ local) ~dim ~correction ~keepdim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_std_correction(%{self : tensor value}, %{dim : int list option value}, %{correction : scalar value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let std_correction_out ~(out @ local) (self @ local) ~dim ~correction ~keepdim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_std_correction_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list option value}, %{correction : scalar value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let std_dim (self @ local) ~dim ~unbiased ~keepdim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_std_dim(%{self : tensor value}, %{dim : int list option value}, Int_val(%{unbiased : bool value}), Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let std_mean (self @ local) ~unbiased =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_std_mean(%{self : tensor value}, Int_val(%{unbiased : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let std_mean_correction (self @ local) ~dim ~correction ~keepdim =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_std_mean_correction(%{self : tensor value}, %{dim : int list option value}, %{correction : scalar value}, Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let std_mean_correction_out
  ~(out0 @ local)
  ~(out1 @ local)
  (self @ local)
  ~dim
  ~correction
  ~keepdim
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_std_mean_correction_out(%{out0 : tensor value}, %{out1 : tensor value}, %{self : tensor value}, %{dim : int list option value}, %{correction : scalar value}, Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let std_mean_dim (self @ local) ~dim ~unbiased ~keepdim =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_std_mean_dim(%{self : tensor value}, %{dim : int list option value}, Int_val(%{unbiased : bool value}), Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let std_out ~(out @ local) (self @ local) ~dim ~unbiased ~keepdim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_std_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list option value}, Int_val(%{unbiased : bool value}), Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let stft
  (self @ local)
  ~n_fft
  ~hop_length
  ~win_length
  ~(window @ local)
  ~normalized
  ~onesided
  ~return_complex
  ~align_to_window
  =
  let self = globalize_tensor self in
  let window = [%globalize: tensor option] window in
  [%c.alloc
    ({|CAMLreturn(atg_stft(%{self : tensor value}, %{n_fft : int}, %{hop_length : int option value}, %{win_length : int option value}, %{window : tensor option value}, Int_val(%{normalized : bool value}), Int_val(%{onesided : bool value}), Int_val(%{return_complex : bool value}), Int_val(%{align_to_window : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let stft_center
  (self @ local)
  ~n_fft
  ~hop_length
  ~win_length
  ~(window @ local)
  ~center
  ~pad_mode
  ~normalized
  ~onesided
  ~return_complex
  ~align_to_window
  =
  let self = globalize_tensor self in
  let window = [%globalize: tensor option] window in
  [%c.alloc
    ({|CAMLreturn(atg_stft_center(%{self : tensor value}, %{n_fft : int}, %{hop_length : int option value}, %{win_length : int option value}, %{window : tensor option value}, Int_val(%{center : bool value}), String_val(%{pad_mode : string value}), Int_val(%{normalized : bool value}), Int_val(%{onesided : bool value}), Int_val(%{return_complex : bool value}), Int_val(%{align_to_window : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let stride (self @ local) ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_stride(%{self : tensor value}, %{dim : int}));|} : int64 value)]
;;

let sub ?alpha (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_sub(%{self : tensor value}, %{other : tensor value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sub_ ?alpha (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_sub_(%{self : tensor value}, %{other : tensor value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sub_out ?alpha ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_sub_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sub_scalar ?alpha (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_sub_scalar(%{self : tensor value}, %{other : scalar value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sub_scalar_ ?alpha (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_sub_scalar_(%{self : tensor value}, %{other : scalar value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sub_scalar_out ?alpha ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_sub_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let subtract ?alpha (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_subtract(%{self : tensor value}, %{other : tensor value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let subtract_ ?alpha (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_subtract_(%{self : tensor value}, %{other : tensor value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let subtract_out ?alpha ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_subtract_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let subtract_scalar ?alpha (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_subtract_scalar(%{self : tensor value}, %{other : scalar value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let subtract_scalar_ ?alpha (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_subtract_scalar_(%{self : tensor value}, %{other : scalar value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sum (self @ local) ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_sum(%{self : tensor value}, %{dtype : int}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sum_dim_intlist (self @ local) ~dim ~keepdim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_sum_dim_intlist(%{self : tensor value}, %{dim : int list option value}, Int_val(%{keepdim : bool value}), %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sum_intlist_out ~(out @ local) (self @ local) ~dim ~keepdim ~dtype =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_sum_intlist_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list option value}, Int_val(%{keepdim : bool value}), %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sum_out ~(out @ local) (self @ local) ~dtype =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_sum_out(%{out : tensor value}, %{self : tensor value}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sum_to_size (self @ local) ~size =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_sum_to_size(%{self : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let svd (self @ local) ~some ~compute_uv =
  let self = globalize_tensor self in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_svd(%{self : tensor value}, Int_val(%{some : bool value}), Int_val(%{compute_uv : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let svd_u ~(u @ local) ~(s @ local) ~(v @ local) (self @ local) ~some ~compute_uv =
  let u = globalize_tensor u in
  let s = globalize_tensor s in
  let v = globalize_tensor v in
  let self = globalize_tensor self in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_svd_u(%{u : tensor value}, %{s : tensor value}, %{v : tensor value}, %{self : tensor value}, Int_val(%{some : bool value}), Int_val(%{compute_uv : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let swapaxes (self @ local) ~axis0 ~axis1 =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_swapaxes(%{self : tensor value}, %{axis0 : int}, %{axis1 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let swapaxes_ (self @ local) ~axis0 ~axis1 =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_swapaxes_(%{self : tensor value}, %{axis0 : int}, %{axis1 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let swapdims (self @ local) ~dim0 ~dim1 =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_swapdims(%{self : tensor value}, %{dim0 : int}, %{dim1 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let swapdims_ (self @ local) ~dim0 ~dim1 =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_swapdims_(%{self : tensor value}, %{dim0 : int}, %{dim1 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let tr (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_t(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let t_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_t_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let t_copy (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_t_copy(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let t_copy_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_t_copy_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let take (self @ local) ~(index @ local) =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  [%c.alloc
    ({|CAMLreturn(atg_take(%{self : tensor value}, %{index : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let take_along_dim (self @ local) ~(indices @ local) ~dim =
  let self = globalize_tensor self in
  let indices = globalize_tensor indices in
  [%c.alloc
    ({|CAMLreturn(atg_take_along_dim(%{self : tensor value}, %{indices : tensor value}, %{dim : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let take_along_dim_out ~(out @ local) (self @ local) ~(indices @ local) ~dim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let indices = globalize_tensor indices in
  [%c.alloc
    ({|CAMLreturn(atg_take_along_dim_out(%{out : tensor value}, %{self : tensor value}, %{indices : tensor value}, %{dim : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let take_out ~(out @ local) (self @ local) ~(index @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  [%c.alloc
    ({|CAMLreturn(atg_take_out(%{out : tensor value}, %{self : tensor value}, %{index : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let tan (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_tan(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let tan_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_tan_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let tan_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_tan_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let tanh (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_tanh(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let tanh_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_tanh_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let tanh_backward ~(grad_output @ local) ~(output @ local) =
  let grad_output = globalize_tensor grad_output in
  let output = globalize_tensor output in
  [%c.alloc
    ({|CAMLreturn(atg_tanh_backward(%{grad_output : tensor value}, %{output : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let tanh_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  ~(output @ local)
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let output = globalize_tensor output in
  [%c.alloc
    ({|CAMLreturn(atg_tanh_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{output : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let tanh_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_tanh_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let tensor_split (self @ local) ~sections ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_tensor_split(%{self : tensor value}, %{sections : int}, %{dim : int}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let tensor_split_indices (self @ local) ~indices ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_tensor_split_indices(%{self : tensor value}, %{indices : int list value}, %{dim : int}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let tensor_split_tensor_indices_or_sections
  (self @ local)
  ~(tensor_indices_or_sections @ local)
  ~dim
  =
  let self = globalize_tensor self in
  let tensor_indices_or_sections = globalize_tensor tensor_indices_or_sections in
  [%c.alloc
    ({|CAMLreturn(atg_tensor_split_tensor_indices_or_sections(%{self : tensor value}, %{tensor_indices_or_sections : tensor value}, %{dim : int}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let tensordot (self @ local) (other @ local) ~dims_self ~dims_other =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_tensordot(%{self : tensor value}, %{other : tensor value}, %{dims_self : int list value}, %{dims_other : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let tensordot_out ~(out @ local) (self @ local) (other @ local) ~dims_self ~dims_other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_tensordot_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}, %{dims_self : int list value}, %{dims_other : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let threshold (self @ local) ~threshold ~value =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_threshold(%{self : tensor value}, %{threshold : scalar value}, %{value : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let threshold_ (self @ local) ~threshold ~value =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_threshold_(%{self : tensor value}, %{threshold : scalar value}, %{value : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let threshold_backward ~(grad_output @ local) (self @ local) ~threshold =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_threshold_backward(%{grad_output : tensor value}, %{self : tensor value}, %{threshold : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let threshold_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~threshold
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_threshold_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{threshold : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let threshold_out ~(out @ local) (self @ local) ~threshold ~value =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_threshold_out(%{out : tensor value}, %{self : tensor value}, %{threshold : scalar value}, %{value : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let tile (self @ local) ~dims =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_tile(%{self : tensor value}, %{dims : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let to_ (self @ local) ~device =
  let self = globalize_tensor self in
  let device = Device.to_int device in
  [%c.alloc
    ({|CAMLreturn(atg_to(%{self : tensor value}, %{device : int}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let to_dense (self @ local) ~dtype ~masked_grad =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_to_dense(%{self : tensor value}, %{dtype : int}, Int_val(%{masked_grad : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let to_dense_backward ~(grad @ local) (input @ local) ~masked_grad =
  let grad = globalize_tensor grad in
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg_to_dense_backward(%{grad : tensor value}, %{input : tensor value}, Int_val(%{masked_grad : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let to_device (self @ local) ~device ~dtype ~non_blocking ~copy =
  let self = globalize_tensor self in
  let device = Device.to_int device in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_to_device(%{self : tensor value}, %{device : int}, %{dtype : int}, Int_val(%{non_blocking : bool value}), Int_val(%{copy : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let to_dtype (self @ local) ~dtype ~non_blocking ~copy =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_to_dtype(%{self : tensor value}, %{dtype : int}, Int_val(%{non_blocking : bool value}), Int_val(%{copy : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let to_dtype_layout (self @ local) ~options ~non_blocking ~copy =
  let self = globalize_tensor self in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_to_dtype_layout(%{self : tensor value}, %{options_kind : int}, %{options_device : int}, Int_val(%{non_blocking : bool value}), Int_val(%{copy : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let to_mkldnn (self @ local) ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_to_mkldnn(%{self : tensor value}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let to_mkldnn_backward ~(grad @ local) (input @ local) =
  let grad = globalize_tensor grad in
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg_to_mkldnn_backward(%{grad : tensor value}, %{input : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let to_mkldnn_out ~(out @ local) (self @ local) ~dtype =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_to_mkldnn_out(%{out : tensor value}, %{self : tensor value}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let to_other (self @ local) (other @ local) ~non_blocking ~copy =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_to_other(%{self : tensor value}, %{other : tensor value}, Int_val(%{non_blocking : bool value}), Int_val(%{copy : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let to_padded_tensor (self @ local) ~padding ~output_size =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_to_padded_tensor(%{self : tensor value}, %{padding : float}, %{output_size : int list option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let to_padded_tensor_out ~(out @ local) (self @ local) ~padding ~output_size =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_to_padded_tensor_out(%{out : tensor value}, %{self : tensor value}, %{padding : float}, %{output_size : int list option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let topk (self @ local) ~k ~dim ~largest ~sorted =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_topk(%{self : tensor value}, %{k : int}, %{dim : int}, Int_val(%{largest : bool value}), Int_val(%{sorted : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let topk_values
  ~(values @ local)
  ~(indices @ local)
  (self @ local)
  ~k
  ~dim
  ~largest
  ~sorted
  =
  let values = globalize_tensor values in
  let indices = globalize_tensor indices in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_topk_values(%{values : tensor value}, %{indices : tensor value}, %{self : tensor value}, %{k : int}, %{dim : int}, Int_val(%{largest : bool value}), Int_val(%{sorted : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let totype (self @ local) ~scalar_type =
  let self = globalize_tensor self in
  let scalar_type = Kind.packed_to_int scalar_type in
  [%c.alloc
    ({|CAMLreturn(atg_totype(%{self : tensor value}, %{scalar_type : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let trace (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_trace(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let trace_backward ~(grad @ local) ~sizes =
  let grad = globalize_tensor grad in
  [%c.alloc
    ({|CAMLreturn(atg_trace_backward(%{grad : tensor value}, %{sizes : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let trace_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_trace_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let transpose (self @ local) ~dim0 ~dim1 =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_transpose(%{self : tensor value}, %{dim0 : int}, %{dim1 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let transpose_ (self @ local) ~dim0 ~dim1 =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_transpose_(%{self : tensor value}, %{dim0 : int}, %{dim1 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let transpose_copy (self @ local) ~dim0 ~dim1 =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_transpose_copy(%{self : tensor value}, %{dim0 : int}, %{dim1 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let transpose_copy_int_out ~(out @ local) (self @ local) ~dim0 ~dim1 =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_transpose_copy_int_out(%{out : tensor value}, %{self : tensor value}, %{dim0 : int}, %{dim1 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let trapezoid ~(y @ local) ~(x @ local) ~dim =
  let y = globalize_tensor y in
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_trapezoid(%{y : tensor value}, %{x : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let trapezoid_dx ?dx ~(y @ local) ~dim () =
  let y = globalize_tensor y in
  [%c.alloc
    ({|CAMLreturn(atg_trapezoid_dx(%{y : tensor value}, %{dx : scalar option value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let trapz ~(y @ local) ~(x @ local) ~dim =
  let y = globalize_tensor y in
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_trapz(%{y : tensor value}, %{x : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let trapz_dx ~(y @ local) ~dx ~dim =
  let y = globalize_tensor y in
  [%c.alloc
    ({|CAMLreturn(atg_trapz_dx(%{y : tensor value}, %{dx : float}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let triangular_solve (self @ local) ~(a @ local) ~upper ~transpose ~unitriangular =
  let self = globalize_tensor self in
  let a = globalize_tensor a in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_triangular_solve(%{self : tensor value}, %{a : tensor value}, Int_val(%{upper : bool value}), Int_val(%{transpose : bool value}), Int_val(%{unitriangular : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let triangular_solve_x
  ~(x @ local)
  ~(m @ local)
  (self @ local)
  ~(a @ local)
  ~upper
  ~transpose
  ~unitriangular
  =
  let x = globalize_tensor x in
  let m = globalize_tensor m in
  let self = globalize_tensor self in
  let a = globalize_tensor a in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_triangular_solve_x(%{x : tensor value}, %{m : tensor value}, %{self : tensor value}, %{a : tensor value}, Int_val(%{upper : bool value}), Int_val(%{transpose : bool value}), Int_val(%{unitriangular : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let tril (self @ local) ~diagonal =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_tril(%{self : tensor value}, %{diagonal : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let tril_ (self @ local) ~diagonal =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_tril_(%{self : tensor value}, %{diagonal : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let tril_indices ~row ~col ~offset ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_tril_indices(%{row : int}, %{col : int}, %{offset : int}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let tril_indices_out ~(out @ local) ~row ~col ~offset =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_tril_indices_out(%{out : tensor value}, %{row : int}, %{col : int}, %{offset : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let tril_out ~(out @ local) (self @ local) ~diagonal =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_tril_out(%{out : tensor value}, %{self : tensor value}, %{diagonal : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let triplet_margin_loss
  ~(anchor @ local)
  ~(positive @ local)
  ~(negative @ local)
  ~margin
  ~p
  ~eps
  ~swap
  ~reduction
  =
  let anchor = globalize_tensor anchor in
  let positive = globalize_tensor positive in
  let negative = globalize_tensor negative in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_triplet_margin_loss(%{anchor : tensor value}, %{positive : tensor value}, %{negative : tensor value}, %{margin : float}, %{p : float}, %{eps : float}, Int_val(%{swap : bool value}), %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let triu (self @ local) ~diagonal =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_triu(%{self : tensor value}, %{diagonal : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let triu_ (self @ local) ~diagonal =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_triu_(%{self : tensor value}, %{diagonal : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let triu_indices ~row ~col ~offset ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_triu_indices(%{row : int}, %{col : int}, %{offset : int}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let triu_indices_out ~(out @ local) ~row ~col ~offset =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_triu_indices_out(%{out : tensor value}, %{row : int}, %{col : int}, %{offset : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let triu_out ~(out @ local) (self @ local) ~diagonal =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_triu_out(%{out : tensor value}, %{self : tensor value}, %{diagonal : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let true_divide (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_true_divide(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let true_divide_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_true_divide_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let true_divide_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_true_divide_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let true_divide_scalar (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_true_divide_scalar(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let true_divide_scalar_ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_true_divide_scalar_(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let trunc (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_trunc(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let trunc_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_trunc_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let trunc_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_trunc_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let type_as (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_type_as(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let unbind (self @ local) ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_unbind(%{self : tensor value}, %{dim : int}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let unbind_copy (self @ local) ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_unbind_copy(%{self : tensor value}, %{dim : int}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let unbind_copy_int_out ~(out @ local) (self @ local) ~dim =
  let out = Torch_local_iterators.List.map_local_input out ~f:globalize_tensor in
  let self = globalize_tensor self in
  [%c.alloc
    {|atg_unbind_copy_int_out(%{out : tensor list value}, %{self : tensor value}, %{dim : int});|}]
;;

let unflatten (self @ local) ~dim ~sizes =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_unflatten(%{self : tensor value}, %{dim : int}, %{sizes : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let unflatten_dense_tensors ~(flat @ local) (tensors @ local) =
  let flat = globalize_tensor flat in
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_unflatten_dense_tensors(%{flat : tensor value}, %{tensors : tensor list value}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let unfold (self @ local) ~dimension ~size ~step =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_unfold(%{self : tensor value}, %{dimension : int}, %{size : int}, %{step : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let unfold_backward ~(grad_in @ local) ~input_sizes ~dim ~size ~step =
  let grad_in = globalize_tensor grad_in in
  [%c.alloc
    ({|CAMLreturn(atg_unfold_backward(%{grad_in : tensor value}, %{input_sizes : int list value}, %{dim : int}, %{size : int}, %{step : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let unfold_backward_out ~(out @ local) ~(grad_in @ local) ~input_sizes ~dim ~size ~step =
  let out = globalize_tensor out in
  let grad_in = globalize_tensor grad_in in
  [%c.alloc
    ({|CAMLreturn(atg_unfold_backward_out(%{out : tensor value}, %{grad_in : tensor value}, %{input_sizes : int list value}, %{dim : int}, %{size : int}, %{step : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let unfold_copy (self @ local) ~dimension ~size ~step =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_unfold_copy(%{self : tensor value}, %{dimension : int}, %{size : int}, %{step : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let unfold_copy_out ~(out @ local) (self @ local) ~dimension ~size ~step =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_unfold_copy_out(%{out : tensor value}, %{self : tensor value}, %{dimension : int}, %{size : int}, %{step : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let uniform (self @ local) ~from ~to_ =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_uniform(%{self : tensor value}, %{from : float}, %{to_ : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let uniform_ (self @ local) ~from ~to_ =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_uniform_(%{self : tensor value}, %{from : float}, %{to_ : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let uniform_out ~(out @ local) (self @ local) ~from ~to_ =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_uniform_out(%{out : tensor value}, %{self : tensor value}, %{from : float}, %{to_ : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let unique_consecutive (self @ local) ~return_inverse ~return_counts ~dim =
  let self = globalize_tensor self in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_unique_consecutive(%{self : tensor value}, Int_val(%{return_inverse : bool value}), Int_val(%{return_counts : bool value}), %{dim : int option value}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let unique_consecutive_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  (self @ local)
  ~return_inverse
  ~return_counts
  ~dim
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let self = globalize_tensor self in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_unique_consecutive_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{self : tensor value}, Int_val(%{return_inverse : bool value}), Int_val(%{return_counts : bool value}), %{dim : int option value}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let unique_dim (self @ local) ~dim ~sorted ~return_inverse ~return_counts =
  let self = globalize_tensor self in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_unique_dim(%{self : tensor value}, %{dim : int}, Int_val(%{sorted : bool value}), Int_val(%{return_inverse : bool value}), Int_val(%{return_counts : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let unique_dim_consecutive (self @ local) ~dim ~return_inverse ~return_counts =
  let self = globalize_tensor self in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_unique_dim_consecutive(%{self : tensor value}, %{dim : int}, Int_val(%{return_inverse : bool value}), Int_val(%{return_counts : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let unique_dim_consecutive_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  (self @ local)
  ~dim
  ~return_inverse
  ~return_counts
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let self = globalize_tensor self in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_unique_dim_consecutive_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{self : tensor value}, %{dim : int}, Int_val(%{return_inverse : bool value}), Int_val(%{return_counts : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let unique_dim_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  (self @ local)
  ~dim
  ~sorted
  ~return_inverse
  ~return_counts
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let self = globalize_tensor self in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_unique_dim_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{self : tensor value}, %{dim : int}, Int_val(%{sorted : bool value}), Int_val(%{return_inverse : bool value}), Int_val(%{return_counts : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let unsafe_chunk (self @ local) ~chunks ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_unsafe_chunk(%{self : tensor value}, %{chunks : int}, %{dim : int}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let unsafe_split (self @ local) ~split_size ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_unsafe_split(%{self : tensor value}, %{split_size : int}, %{dim : int}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let unsafe_split_tensor_out ~(out @ local) (self @ local) ~split_size ~dim =
  let out = Torch_local_iterators.List.map_local_input out ~f:globalize_tensor in
  let self = globalize_tensor self in
  [%c.alloc
    {|atg_unsafe_split_tensor_out(%{out : tensor list value}, %{self : tensor value}, %{split_size : int}, %{dim : int});|}]
;;

let unsafe_split_with_sizes (self @ local) ~split_sizes ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_unsafe_split_with_sizes(%{self : tensor value}, %{split_sizes : int list value}, %{dim : int}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let unsafe_split_with_sizes_out ~(out @ local) (self @ local) ~split_sizes ~dim =
  let out = Torch_local_iterators.List.map_local_input out ~f:globalize_tensor in
  let self = globalize_tensor self in
  [%c.alloc
    {|atg_unsafe_split_with_sizes_out(%{out : tensor list value}, %{self : tensor value}, %{split_sizes : int list value}, %{dim : int});|}]
;;

let unsqueeze (self @ local) ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_unsqueeze(%{self : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let unsqueeze_ (self @ local) ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_unsqueeze_(%{self : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let unsqueeze_copy (self @ local) ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_unsqueeze_copy(%{self : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let unsqueeze_copy_out ~(out @ local) (self @ local) ~dim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_unsqueeze_copy_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_bicubic2d (self @ local) ~output_size ~align_corners ~scales_h ~scales_w =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_bicubic2d(%{self : tensor value}, %{output_size : int list value}, Int_val(%{align_corners : bool value}), %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_bicubic2d_backward
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~align_corners
  ~scales_h
  ~scales_w
  =
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_bicubic2d_backward(%{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, Int_val(%{align_corners : bool value}), %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_bicubic2d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~align_corners
  ~scales_h
  ~scales_w
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_bicubic2d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, Int_val(%{align_corners : bool value}), %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_bicubic2d_out
  ~(out @ local)
  (self @ local)
  ~output_size
  ~align_corners
  ~scales_h
  ~scales_w
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_bicubic2d_out(%{out : tensor value}, %{self : tensor value}, %{output_size : int list value}, Int_val(%{align_corners : bool value}), %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_bicubic2d_vec (input @ local) ~output_size ~align_corners ~scale_factors =
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_bicubic2d_vec(%{input : tensor value}, %{output_size : int list option value}, Int_val(%{align_corners : bool value}), %{scale_factors : float list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_bilinear2d (self @ local) ~output_size ~align_corners ~scales_h ~scales_w =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_bilinear2d(%{self : tensor value}, %{output_size : int list value}, Int_val(%{align_corners : bool value}), %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_bilinear2d_backward
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~align_corners
  ~scales_h
  ~scales_w
  =
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_bilinear2d_backward(%{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, Int_val(%{align_corners : bool value}), %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_bilinear2d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~align_corners
  ~scales_h
  ~scales_w
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_bilinear2d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, Int_val(%{align_corners : bool value}), %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_bilinear2d_out
  ~(out @ local)
  (self @ local)
  ~output_size
  ~align_corners
  ~scales_h
  ~scales_w
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_bilinear2d_out(%{out : tensor value}, %{self : tensor value}, %{output_size : int list value}, Int_val(%{align_corners : bool value}), %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_bilinear2d_vec (input @ local) ~output_size ~align_corners ~scale_factors =
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_bilinear2d_vec(%{input : tensor value}, %{output_size : int list option value}, Int_val(%{align_corners : bool value}), %{scale_factors : float list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_bilinear2d_vec_out
  ~(out @ local)
  (input @ local)
  ~output_size
  ~align_corners
  ~scale_factors
  =
  let out = globalize_tensor out in
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_bilinear2d_vec_out(%{out : tensor value}, %{input : tensor value}, %{output_size : int list option value}, Int_val(%{align_corners : bool value}), %{scale_factors : float list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_linear1d (self @ local) ~output_size ~align_corners ~scales =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_linear1d(%{self : tensor value}, %{output_size : int list value}, Int_val(%{align_corners : bool value}), %{scales : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_linear1d_backward
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~align_corners
  ~scales
  =
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_linear1d_backward(%{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, Int_val(%{align_corners : bool value}), %{scales : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_linear1d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~align_corners
  ~scales
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_linear1d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, Int_val(%{align_corners : bool value}), %{scales : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_linear1d_out
  ~(out @ local)
  (self @ local)
  ~output_size
  ~align_corners
  ~scales
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_linear1d_out(%{out : tensor value}, %{self : tensor value}, %{output_size : int list value}, Int_val(%{align_corners : bool value}), %{scales : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_linear1d_vec (input @ local) ~output_size ~align_corners ~scale_factors =
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_linear1d_vec(%{input : tensor value}, %{output_size : int list option value}, Int_val(%{align_corners : bool value}), %{scale_factors : float list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_nearest1d (self @ local) ~output_size ~scales =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_nearest1d(%{self : tensor value}, %{output_size : int list value}, %{scales : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_nearest1d_backward ~(grad_output @ local) ~output_size ~input_size ~scales =
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_nearest1d_backward(%{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, %{scales : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_nearest1d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~scales
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_nearest1d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, %{scales : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_nearest1d_out ~(out @ local) (self @ local) ~output_size ~scales =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_nearest1d_out(%{out : tensor value}, %{self : tensor value}, %{output_size : int list value}, %{scales : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_nearest1d_vec (input @ local) ~output_size ~scale_factors =
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_nearest1d_vec(%{input : tensor value}, %{output_size : int list option value}, %{scale_factors : float list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_nearest2d (self @ local) ~output_size ~scales_h ~scales_w =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_nearest2d(%{self : tensor value}, %{output_size : int list value}, %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_nearest2d_backward
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~scales_h
  ~scales_w
  =
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_nearest2d_backward(%{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_nearest2d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~scales_h
  ~scales_w
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_nearest2d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_nearest2d_out ~(out @ local) (self @ local) ~output_size ~scales_h ~scales_w =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_nearest2d_out(%{out : tensor value}, %{self : tensor value}, %{output_size : int list value}, %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_nearest2d_vec (input @ local) ~output_size ~scale_factors =
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_nearest2d_vec(%{input : tensor value}, %{output_size : int list option value}, %{scale_factors : float list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_nearest2d_vec_out ~(out @ local) (input @ local) ~output_size ~scale_factors =
  let out = globalize_tensor out in
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_nearest2d_vec_out(%{out : tensor value}, %{input : tensor value}, %{output_size : int list option value}, %{scale_factors : float list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_nearest3d (self @ local) ~output_size ~scales_d ~scales_h ~scales_w =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_nearest3d(%{self : tensor value}, %{output_size : int list value}, %{scales_d : float option value}, %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_nearest3d_backward
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~scales_d
  ~scales_h
  ~scales_w
  =
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_nearest3d_backward(%{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, %{scales_d : float option value}, %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_nearest3d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~scales_d
  ~scales_h
  ~scales_w
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_nearest3d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, %{scales_d : float option value}, %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_nearest3d_out
  ~(out @ local)
  (self @ local)
  ~output_size
  ~scales_d
  ~scales_h
  ~scales_w
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_nearest3d_out(%{out : tensor value}, %{self : tensor value}, %{output_size : int list value}, %{scales_d : float option value}, %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_nearest3d_vec (input @ local) ~output_size ~scale_factors =
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_nearest3d_vec(%{input : tensor value}, %{output_size : int list option value}, %{scale_factors : float list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_trilinear3d
  (self @ local)
  ~output_size
  ~align_corners
  ~scales_d
  ~scales_h
  ~scales_w
  =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_trilinear3d(%{self : tensor value}, %{output_size : int list value}, Int_val(%{align_corners : bool value}), %{scales_d : float option value}, %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_trilinear3d_backward
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~align_corners
  ~scales_d
  ~scales_h
  ~scales_w
  =
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_trilinear3d_backward(%{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, Int_val(%{align_corners : bool value}), %{scales_d : float option value}, %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_trilinear3d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~align_corners
  ~scales_d
  ~scales_h
  ~scales_w
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_trilinear3d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, Int_val(%{align_corners : bool value}), %{scales_d : float option value}, %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_trilinear3d_out
  ~(out @ local)
  (self @ local)
  ~output_size
  ~align_corners
  ~scales_d
  ~scales_h
  ~scales_w
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_trilinear3d_out(%{out : tensor value}, %{self : tensor value}, %{output_size : int list value}, Int_val(%{align_corners : bool value}), %{scales_d : float option value}, %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let upsample_trilinear3d_vec (input @ local) ~output_size ~align_corners ~scale_factors =
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg_upsample_trilinear3d_vec(%{input : tensor value}, %{output_size : int list option value}, Int_val(%{align_corners : bool value}), %{scale_factors : float list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let value_selecting_reduction_backward
  ~(grad @ local)
  ~dim
  ~(indices @ local)
  ~sizes
  ~keepdim
  =
  let grad = globalize_tensor grad in
  let indices = globalize_tensor indices in
  [%c.alloc
    ({|CAMLreturn(atg_value_selecting_reduction_backward(%{grad : tensor value}, %{dim : int}, %{indices : tensor value}, %{sizes : int list value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let values (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_values(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let values_copy (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_values_copy(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let values_copy_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_values_copy_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let vander ~(x @ local) ~n ~increasing =
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_vander(%{x : tensor value}, %{n : int option value}, Int_val(%{increasing : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let var (self @ local) ~unbiased =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_var(%{self : tensor value}, Int_val(%{unbiased : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let var_correction (self @ local) ~dim ~correction ~keepdim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_var_correction(%{self : tensor value}, %{dim : int list option value}, %{correction : scalar value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let var_correction_out ~(out @ local) (self @ local) ~dim ~correction ~keepdim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_var_correction_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list option value}, %{correction : scalar value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let var_dim (self @ local) ~dim ~unbiased ~keepdim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_var_dim(%{self : tensor value}, %{dim : int list option value}, Int_val(%{unbiased : bool value}), Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let var_mean (self @ local) ~unbiased =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_var_mean(%{self : tensor value}, Int_val(%{unbiased : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let var_mean_correction (self @ local) ~dim ~correction ~keepdim =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_var_mean_correction(%{self : tensor value}, %{dim : int list option value}, %{correction : scalar value}, Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let var_mean_correction_out
  ~(out0 @ local)
  ~(out1 @ local)
  (self @ local)
  ~dim
  ~correction
  ~keepdim
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_var_mean_correction_out(%{out0 : tensor value}, %{out1 : tensor value}, %{self : tensor value}, %{dim : int list option value}, %{correction : scalar value}, Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let var_mean_dim (self @ local) ~dim ~unbiased ~keepdim =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_var_mean_dim(%{self : tensor value}, %{dim : int list option value}, Int_val(%{unbiased : bool value}), Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let var_out ~(out @ local) (self @ local) ~dim ~unbiased ~keepdim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_var_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list option value}, Int_val(%{unbiased : bool value}), Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let vdot (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_vdot(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let vdot_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_vdot_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let view (self @ local) ~size =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_view(%{self : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let view_as (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_view_as(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let view_as_complex (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_view_as_complex(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let view_as_complex_copy (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_view_as_complex_copy(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let view_as_complex_copy_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_view_as_complex_copy_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let view_as_real (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_view_as_real(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let view_as_real_copy (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_view_as_real_copy(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let view_as_real_copy_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_view_as_real_copy_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let view_copy (self @ local) ~size =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_view_copy(%{self : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let view_copy_dtype (self @ local) ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_view_copy_dtype(%{self : tensor value}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let view_copy_dtype_out ~(out @ local) (self @ local) ~dtype =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_view_copy_dtype_out(%{out : tensor value}, %{self : tensor value}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let view_copy_out ~(out @ local) (self @ local) ~size =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_view_copy_out(%{out : tensor value}, %{self : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let view_dtype (self @ local) ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_view_dtype(%{self : tensor value}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let vsplit (self @ local) ~sections =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_vsplit(%{self : tensor value}, %{sections : int}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let vsplit_array (self @ local) ~indices =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_vsplit_array(%{self : tensor value}, %{indices : int list value}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let vstack (tensors @ local) =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_vstack(%{tensors : tensor list value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let vstack_out ~(out @ local) (tensors @ local) =
  let out = globalize_tensor out in
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_vstack_out(%{out : tensor value}, %{tensors : tensor list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let where ~(condition @ local) =
  let condition = globalize_tensor condition in
  [%c.alloc
    ({|CAMLreturn(atg_where(%{condition : tensor value}));|} : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let where_scalar ~(condition @ local) self other =
  let condition = globalize_tensor condition in
  [%c.alloc
    ({|CAMLreturn(atg_where_scalar(%{condition : tensor value}, %{self : scalar value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let where_scalarother ~(condition @ local) (self @ local) other =
  let condition = globalize_tensor condition in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_where_scalarother(%{condition : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let where_scalarself ~(condition @ local) self (other @ local) =
  let condition = globalize_tensor condition in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_where_scalarself(%{condition : tensor value}, %{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let where_self ~(condition @ local) (self @ local) (other @ local) =
  let condition = globalize_tensor condition in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_where_self(%{condition : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let where_self_out ~(out @ local) ~(condition @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let condition = globalize_tensor condition in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_where_self_out(%{out : tensor value}, %{condition : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let xlogy (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_xlogy(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let xlogy_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_xlogy_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let xlogy_outscalar_other ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_xlogy_outscalar_other(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let xlogy_outscalar_self ~(out @ local) self (other @ local) =
  let out = globalize_tensor out in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_xlogy_outscalar_self(%{out : tensor value}, %{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let xlogy_outtensor ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_xlogy_outtensor(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let xlogy_scalar_other (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_xlogy_scalar_other(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let xlogy_scalar_other_ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_xlogy_scalar_other_(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let xlogy_scalar_self self (other @ local) =
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_xlogy_scalar_self(%{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let zero (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_zero(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let zero_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_zero_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let zero_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_zero_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let zeros ~size ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_zeros(%{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let zeros_like (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_zeros_like(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let zeros_like_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_zeros_like_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let zeros_out ~(out @ local) ~size =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_zeros_out(%{out : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;
