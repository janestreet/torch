(* THIS FILE IS AUTOMATICALLY GENERATED, DO NOT EDIT BY HAND! *)

open! Core
open C_ffi
open Torch_wrapper_types
open Torch_refcounted_bindings.Type_defs

[%%c {| #include "torch_api.h" |}]

let reflection_pad3d_backward ~(grad_output @ local) (self @ local) ~padding =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_reflection_pad3d_backward(%{grad_output : tensor value}, %{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let reflection_pad3d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~padding
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_reflection_pad3d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let reflection_pad3d_out ~(out @ local) (self @ local) ~padding =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_reflection_pad3d_out(%{out : tensor value}, %{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let relu (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_relu(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let relu6 (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_relu6(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let relu6_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_relu6_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let relu_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_relu_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let relu_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_relu_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let remainder (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_remainder(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let remainder_ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_remainder_(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let remainder_scalar_out ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_remainder_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let remainder_scalar_tensor self (other @ local) =
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_remainder_scalar_tensor(%{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let remainder_scalar_tensor_out ~(out @ local) self (other @ local) =
  let out = globalize_tensor out in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_remainder_scalar_tensor_out(%{out : tensor value}, %{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let remainder_tensor (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_remainder_tensor(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let remainder_tensor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_remainder_tensor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let remainder_tensor_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_remainder_tensor_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let renorm (self @ local) ~p ~dim ~maxnorm =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_renorm(%{self : tensor value}, %{p : scalar value}, %{dim : int}, %{maxnorm : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let renorm_ (self @ local) ~p ~dim ~maxnorm =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_renorm_(%{self : tensor value}, %{p : scalar value}, %{dim : int}, %{maxnorm : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let renorm_out ~(out @ local) (self @ local) ~p ~dim ~maxnorm =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_renorm_out(%{out : tensor value}, %{self : tensor value}, %{p : scalar value}, %{dim : int}, %{maxnorm : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let repeat (self @ local) ~repeats =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_repeat(%{self : tensor value}, %{repeats : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let repeat_interleave ~(repeats @ local) ~output_size =
  let repeats = globalize_tensor repeats in
  [%c.alloc
    ({|CAMLreturn(atg_repeat_interleave(%{repeats : tensor value}, %{output_size : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let repeat_interleave_self_int (self @ local) ~repeats ~dim ~output_size =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_repeat_interleave_self_int(%{self : tensor value}, %{repeats : int}, %{dim : int option value}, %{output_size : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let repeat_interleave_self_tensor (self @ local) ~(repeats @ local) ~dim ~output_size =
  let self = globalize_tensor self in
  let repeats = globalize_tensor repeats in
  [%c.alloc
    ({|CAMLreturn(atg_repeat_interleave_self_tensor(%{self : tensor value}, %{repeats : tensor value}, %{dim : int option value}, %{output_size : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let repeat_interleave_tensor_out ~(out @ local) ~(repeats @ local) ~output_size =
  let out = globalize_tensor out in
  let repeats = globalize_tensor repeats in
  [%c.alloc
    ({|CAMLreturn(atg_repeat_interleave_tensor_out(%{out : tensor value}, %{repeats : tensor value}, %{output_size : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let repeat_out ~(out @ local) (self @ local) ~repeats =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_repeat_out(%{out : tensor value}, %{self : tensor value}, %{repeats : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let replication_pad1d (self @ local) ~padding =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_replication_pad1d(%{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let replication_pad1d_backward ~(grad_output @ local) (self @ local) ~padding =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_replication_pad1d_backward(%{grad_output : tensor value}, %{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let replication_pad1d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~padding
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_replication_pad1d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let replication_pad1d_out ~(out @ local) (self @ local) ~padding =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_replication_pad1d_out(%{out : tensor value}, %{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let replication_pad2d (self @ local) ~padding =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_replication_pad2d(%{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let replication_pad2d_backward ~(grad_output @ local) (self @ local) ~padding =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_replication_pad2d_backward(%{grad_output : tensor value}, %{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let replication_pad2d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~padding
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_replication_pad2d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let replication_pad2d_out ~(out @ local) (self @ local) ~padding =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_replication_pad2d_out(%{out : tensor value}, %{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let replication_pad3d (self @ local) ~padding =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_replication_pad3d(%{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let replication_pad3d_backward ~(grad_output @ local) (self @ local) ~padding =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_replication_pad3d_backward(%{grad_output : tensor value}, %{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let replication_pad3d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~padding
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_replication_pad3d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let replication_pad3d_out ~(out @ local) (self @ local) ~padding =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_replication_pad3d_out(%{out : tensor value}, %{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let requires_grad_ (self @ local) ~requires_grad =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_requires_grad_(%{self : tensor value}, Int_val(%{requires_grad : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let reshape (self @ local) ~shape =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_reshape(%{self : tensor value}, %{shape : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let reshape_as (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_reshape_as(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let resize (self @ local) ~size =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_resize(%{self : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let resize_ (self @ local) ~size =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_resize_(%{self : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let resize_as (self @ local) ~(the_template @ local) =
  let self = globalize_tensor self in
  let the_template = globalize_tensor the_template in
  [%c.alloc
    ({|CAMLreturn(atg_resize_as(%{self : tensor value}, %{the_template : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let resize_as_ (self @ local) ~(the_template @ local) =
  let self = globalize_tensor self in
  let the_template = globalize_tensor the_template in
  [%c.alloc
    ({|CAMLreturn(atg_resize_as_(%{self : tensor value}, %{the_template : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let resize_as_out ~(out @ local) (self @ local) ~(the_template @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let the_template = globalize_tensor the_template in
  [%c.alloc
    ({|CAMLreturn(atg_resize_as_out(%{out : tensor value}, %{self : tensor value}, %{the_template : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let resize_as_sparse (self @ local) ~(the_template @ local) =
  let self = globalize_tensor self in
  let the_template = globalize_tensor the_template in
  [%c.alloc
    ({|CAMLreturn(atg_resize_as_sparse(%{self : tensor value}, %{the_template : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let resize_as_sparse_ (self @ local) ~(the_template @ local) =
  let self = globalize_tensor self in
  let the_template = globalize_tensor the_template in
  [%c.alloc
    ({|CAMLreturn(atg_resize_as_sparse_(%{self : tensor value}, %{the_template : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let resize_as_sparse_out ~(out @ local) (self @ local) ~(the_template @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let the_template = globalize_tensor the_template in
  [%c.alloc
    ({|CAMLreturn(atg_resize_as_sparse_out(%{out : tensor value}, %{self : tensor value}, %{the_template : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let resize_out ~(out @ local) (self @ local) ~size =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_resize_out(%{out : tensor value}, %{self : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let resolve_conj (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_resolve_conj(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let resolve_neg (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_resolve_neg(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let retains_grad (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_retains_grad(%{self : tensor value}));|} : bool value)]
;;

let rms_norm (input @ local) ~normalized_shape ~(weight @ local) ~eps =
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  [%c.alloc
    ({|CAMLreturn(atg_rms_norm(%{input : tensor value}, %{normalized_shape : int list value}, %{weight : tensor option value}, %{eps : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rnn_relu
  (input @ local)
  ~(hx @ local)
  ~(params @ local)
  ~has_biases
  ~num_layers
  ~dropout
  ~train
  ~bidirectional
  ~batch_first
  =
  let input = globalize_tensor input in
  let hx = globalize_tensor hx in
  let params = Torch_local_iterators.List.map_local_input params ~f:globalize_tensor in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_rnn_relu(%{input : tensor value}, %{hx : tensor value}, %{params : tensor list value}, Int_val(%{has_biases : bool value}), %{num_layers : int}, %{dropout : float}, Int_val(%{train : bool value}), Int_val(%{bidirectional : bool value}), Int_val(%{batch_first : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let rnn_relu_cell
  (input @ local)
  ~(hx @ local)
  ~(w_ih @ local)
  ~(w_hh @ local)
  ~(b_ih @ local)
  ~(b_hh @ local)
  =
  let input = globalize_tensor input in
  let hx = globalize_tensor hx in
  let w_ih = globalize_tensor w_ih in
  let w_hh = globalize_tensor w_hh in
  let b_ih = [%globalize: tensor option] b_ih in
  let b_hh = [%globalize: tensor option] b_hh in
  [%c.alloc
    ({|CAMLreturn(atg_rnn_relu_cell(%{input : tensor value}, %{hx : tensor value}, %{w_ih : tensor value}, %{w_hh : tensor value}, %{b_ih : tensor option value}, %{b_hh : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rnn_relu_data
  ~(data @ local)
  ~(batch_sizes @ local)
  ~(hx @ local)
  ~(params @ local)
  ~has_biases
  ~num_layers
  ~dropout
  ~train
  ~bidirectional
  =
  let data = globalize_tensor data in
  let batch_sizes = globalize_tensor batch_sizes in
  let hx = globalize_tensor hx in
  let params = Torch_local_iterators.List.map_local_input params ~f:globalize_tensor in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_rnn_relu_data(%{data : tensor value}, %{batch_sizes : tensor value}, %{hx : tensor value}, %{params : tensor list value}, Int_val(%{has_biases : bool value}), %{num_layers : int}, %{dropout : float}, Int_val(%{train : bool value}), Int_val(%{bidirectional : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let rnn_tanh
  (input @ local)
  ~(hx @ local)
  ~(params @ local)
  ~has_biases
  ~num_layers
  ~dropout
  ~train
  ~bidirectional
  ~batch_first
  =
  let input = globalize_tensor input in
  let hx = globalize_tensor hx in
  let params = Torch_local_iterators.List.map_local_input params ~f:globalize_tensor in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_rnn_tanh(%{input : tensor value}, %{hx : tensor value}, %{params : tensor list value}, Int_val(%{has_biases : bool value}), %{num_layers : int}, %{dropout : float}, Int_val(%{train : bool value}), Int_val(%{bidirectional : bool value}), Int_val(%{batch_first : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let rnn_tanh_cell
  (input @ local)
  ~(hx @ local)
  ~(w_ih @ local)
  ~(w_hh @ local)
  ~(b_ih @ local)
  ~(b_hh @ local)
  =
  let input = globalize_tensor input in
  let hx = globalize_tensor hx in
  let w_ih = globalize_tensor w_ih in
  let w_hh = globalize_tensor w_hh in
  let b_ih = [%globalize: tensor option] b_ih in
  let b_hh = [%globalize: tensor option] b_hh in
  [%c.alloc
    ({|CAMLreturn(atg_rnn_tanh_cell(%{input : tensor value}, %{hx : tensor value}, %{w_ih : tensor value}, %{w_hh : tensor value}, %{b_ih : tensor option value}, %{b_hh : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rnn_tanh_data
  ~(data @ local)
  ~(batch_sizes @ local)
  ~(hx @ local)
  ~(params @ local)
  ~has_biases
  ~num_layers
  ~dropout
  ~train
  ~bidirectional
  =
  let data = globalize_tensor data in
  let batch_sizes = globalize_tensor batch_sizes in
  let hx = globalize_tensor hx in
  let params = Torch_local_iterators.List.map_local_input params ~f:globalize_tensor in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_rnn_tanh_data(%{data : tensor value}, %{batch_sizes : tensor value}, %{hx : tensor value}, %{params : tensor list value}, Int_val(%{has_biases : bool value}), %{num_layers : int}, %{dropout : float}, Int_val(%{train : bool value}), Int_val(%{bidirectional : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let roll (self @ local) ~shifts ~dims =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_roll(%{self : tensor value}, %{shifts : int list value}, %{dims : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let roll_out ~(out @ local) (self @ local) ~shifts ~dims =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_roll_out(%{out : tensor value}, %{self : tensor value}, %{shifts : int list value}, %{dims : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rot90 (self @ local) ~k ~dims =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_rot90(%{self : tensor value}, %{k : int}, %{dims : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rot90_out ~(out @ local) (self @ local) ~k ~dims =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_rot90_out(%{out : tensor value}, %{self : tensor value}, %{k : int}, %{dims : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let round (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_round(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let round_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_round_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let round_decimals (self @ local) ~decimals =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_round_decimals(%{self : tensor value}, %{decimals : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let round_decimals_ (self @ local) ~decimals =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_round_decimals_(%{self : tensor value}, %{decimals : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let round_decimals_out ~(out @ local) (self @ local) ~decimals =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_round_decimals_out(%{out : tensor value}, %{self : tensor value}, %{decimals : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let round_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_round_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let row_indices (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_row_indices(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let row_indices_copy (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_row_indices_copy(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let row_indices_copy_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_row_indices_copy_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let row_stack (tensors @ local) =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_row_stack(%{tensors : tensor list value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let row_stack_out ~(out @ local) (tensors @ local) =
  let out = globalize_tensor out in
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_row_stack_out(%{out : tensor value}, %{tensors : tensor list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rrelu ?lower ?upper (self @ local) ~training =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_rrelu(%{self : tensor value}, %{lower : scalar option value}, %{upper : scalar option value}, Int_val(%{training : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rrelu_ ?lower ?upper (self @ local) ~training =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_rrelu_(%{self : tensor value}, %{lower : scalar option value}, %{upper : scalar option value}, Int_val(%{training : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rrelu_with_noise ?lower ?upper (self @ local) ~(noise @ local) ~training =
  let self = globalize_tensor self in
  let noise = globalize_tensor noise in
  [%c.alloc
    ({|CAMLreturn(atg_rrelu_with_noise(%{self : tensor value}, %{noise : tensor value}, %{lower : scalar option value}, %{upper : scalar option value}, Int_val(%{training : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rrelu_with_noise_ ?lower ?upper (self @ local) ~(noise @ local) ~training =
  let self = globalize_tensor self in
  let noise = globalize_tensor noise in
  [%c.alloc
    ({|CAMLreturn(atg_rrelu_with_noise_(%{self : tensor value}, %{noise : tensor value}, %{lower : scalar option value}, %{upper : scalar option value}, Int_val(%{training : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rrelu_with_noise_backward
  ~(grad_output @ local)
  (self @ local)
  ~(noise @ local)
  ~lower
  ~upper
  ~training
  ~self_is_result
  =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let noise = globalize_tensor noise in
  [%c.alloc
    ({|CAMLreturn(atg_rrelu_with_noise_backward(%{grad_output : tensor value}, %{self : tensor value}, %{noise : tensor value}, %{lower : scalar value}, %{upper : scalar value}, Int_val(%{training : bool value}), Int_val(%{self_is_result : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rrelu_with_noise_backward_out
  ~(out @ local)
  ~(grad_output @ local)
  (self @ local)
  ~(noise @ local)
  ~lower
  ~upper
  ~training
  ~self_is_result
  =
  let out = globalize_tensor out in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let noise = globalize_tensor noise in
  [%c.alloc
    ({|CAMLreturn(atg_rrelu_with_noise_backward_out(%{out : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{noise : tensor value}, %{lower : scalar value}, %{upper : scalar value}, Int_val(%{training : bool value}), Int_val(%{self_is_result : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rrelu_with_noise_functional ?lower ?upper (self @ local) ~(noise @ local) ~training =
  let self = globalize_tensor self in
  let noise = globalize_tensor noise in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_rrelu_with_noise_functional(%{self : tensor value}, %{noise : tensor value}, %{lower : scalar option value}, %{upper : scalar option value}, Int_val(%{training : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let rrelu_with_noise_out
  ?lower
  ?upper
  ~(out @ local)
  (self @ local)
  ~(noise @ local)
  ~training
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let noise = globalize_tensor noise in
  [%c.alloc
    ({|CAMLreturn(atg_rrelu_with_noise_out(%{out : tensor value}, %{self : tensor value}, %{noise : tensor value}, %{lower : scalar option value}, %{upper : scalar option value}, Int_val(%{training : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rsqrt (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_rsqrt(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rsqrt_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_rsqrt_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rsqrt_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_rsqrt_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rsub ?alpha (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_rsub(%{self : tensor value}, %{other : tensor value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rsub_scalar ?alpha (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_rsub_scalar(%{self : tensor value}, %{other : scalar value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rsub_scalar_out ?alpha ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_rsub_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rsub_tensor_out ?alpha ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_rsub_tensor_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let scalar_tensor ~s ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_scalar_tensor(%{s : scalar value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let scalar_tensor_out ~(out @ local) ~s =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_scalar_tensor_out(%{out : tensor value}, %{s : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let scaled_dot_product_attention
  ~(query @ local)
  ~(key @ local)
  ~(value @ local)
  ~(attn_mask @ local)
  ~dropout_p
  ~is_causal
  ~scale
  ~enable_gqa
  =
  let query = globalize_tensor query in
  let key = globalize_tensor key in
  let value = globalize_tensor value in
  let attn_mask = [%globalize: tensor option] attn_mask in
  [%c.alloc
    ({|CAMLreturn(atg_scaled_dot_product_attention(%{query : tensor value}, %{key : tensor value}, %{value : tensor value}, %{attn_mask : tensor option value}, %{dropout_p : float}, Int_val(%{is_causal : bool value}), %{scale : float option value}, Int_val(%{enable_gqa : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let scatter (self @ local) ~dim ~(index @ local) ~(src @ local) =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_scatter(%{self : tensor value}, %{dim : int}, %{index : tensor value}, %{src : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let scatter_ (self @ local) ~dim ~(index @ local) ~(src @ local) =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_scatter_(%{self : tensor value}, %{dim : int}, %{index : tensor value}, %{src : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let scatter_add (self @ local) ~dim ~(index @ local) ~(src @ local) =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_scatter_add(%{self : tensor value}, %{dim : int}, %{index : tensor value}, %{src : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let scatter_add_ (self @ local) ~dim ~(index @ local) ~(src @ local) =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_scatter_add_(%{self : tensor value}, %{dim : int}, %{index : tensor value}, %{src : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let scatter_add_out ~(out @ local) (self @ local) ~dim ~(index @ local) ~(src @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_scatter_add_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, %{index : tensor value}, %{src : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let scatter_reduce (self @ local) ~dim ~(index @ local) ~(src @ local) ~reduce =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_scatter_reduce(%{self : tensor value}, %{dim : int}, %{index : tensor value}, %{src : tensor value}, String_val(%{reduce : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let scatter_reduce_ (self @ local) ~dim ~(index @ local) ~(src @ local) ~reduce =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_scatter_reduce_(%{self : tensor value}, %{dim : int}, %{index : tensor value}, %{src : tensor value}, String_val(%{reduce : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let scatter_reduce_out
  ~(out @ local)
  (self @ local)
  ~dim
  ~(index @ local)
  ~(src @ local)
  ~reduce
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_scatter_reduce_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, %{index : tensor value}, %{src : tensor value}, String_val(%{reduce : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let scatter_src_out ~(out @ local) (self @ local) ~dim ~(index @ local) ~(src @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_scatter_src_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, %{index : tensor value}, %{src : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let scatter_value (self @ local) ~dim ~(index @ local) ~value =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  [%c.alloc
    ({|CAMLreturn(atg_scatter_value(%{self : tensor value}, %{dim : int}, %{index : tensor value}, %{value : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let scatter_value_ (self @ local) ~dim ~(index @ local) ~value =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  [%c.alloc
    ({|CAMLreturn(atg_scatter_value_(%{self : tensor value}, %{dim : int}, %{index : tensor value}, %{value : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let scatter_value_out ~(out @ local) (self @ local) ~dim ~(index @ local) ~value =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  [%c.alloc
    ({|CAMLreturn(atg_scatter_value_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, %{index : tensor value}, %{value : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let scatter_value_reduce (self @ local) ~dim ~(index @ local) ~value ~reduce =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  [%c.alloc
    ({|CAMLreturn(atg_scatter_value_reduce(%{self : tensor value}, %{dim : int}, %{index : tensor value}, %{value : scalar value}, String_val(%{reduce : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let scatter_value_reduce_ (self @ local) ~dim ~(index @ local) ~value ~reduce =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  [%c.alloc
    ({|CAMLreturn(atg_scatter_value_reduce_(%{self : tensor value}, %{dim : int}, %{index : tensor value}, %{value : scalar value}, String_val(%{reduce : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let scatter_value_reduce_out
  ~(out @ local)
  (self @ local)
  ~dim
  ~(index @ local)
  ~value
  ~reduce
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  [%c.alloc
    ({|CAMLreturn(atg_scatter_value_reduce_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, %{index : tensor value}, %{value : scalar value}, String_val(%{reduce : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let searchsorted
  ~(sorted_sequence @ local)
  (self @ local)
  ~out_int32
  ~right
  ~side
  ~(sorter @ local)
  =
  let sorted_sequence = globalize_tensor sorted_sequence in
  let self = globalize_tensor self in
  let sorter = [%globalize: tensor option] sorter in
  [%c.alloc
    ({|CAMLreturn(atg_searchsorted(%{sorted_sequence : tensor value}, %{self : tensor value}, Int_val(%{out_int32 : bool value}), Int_val(%{right : bool value}), %{side : string option value}, %{sorter : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let searchsorted_scalar
  ~(sorted_sequence @ local)
  self
  ~out_int32
  ~right
  ~side
  ~(sorter @ local)
  =
  let sorted_sequence = globalize_tensor sorted_sequence in
  let sorter = [%globalize: tensor option] sorter in
  [%c.alloc
    ({|CAMLreturn(atg_searchsorted_scalar(%{sorted_sequence : tensor value}, %{self : scalar value}, Int_val(%{out_int32 : bool value}), Int_val(%{right : bool value}), %{side : string option value}, %{sorter : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let searchsorted_scalar_out
  ~(out @ local)
  ~(sorted_sequence @ local)
  self
  ~out_int32
  ~right
  ~side
  ~(sorter @ local)
  =
  let out = globalize_tensor out in
  let sorted_sequence = globalize_tensor sorted_sequence in
  let sorter = [%globalize: tensor option] sorter in
  [%c.alloc
    ({|CAMLreturn(atg_searchsorted_scalar_out(%{out : tensor value}, %{sorted_sequence : tensor value}, %{self : scalar value}, Int_val(%{out_int32 : bool value}), Int_val(%{right : bool value}), %{side : string option value}, %{sorter : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let searchsorted_tensor_out
  ~(out @ local)
  ~(sorted_sequence @ local)
  (self @ local)
  ~out_int32
  ~right
  ~side
  ~(sorter @ local)
  =
  let out = globalize_tensor out in
  let sorted_sequence = globalize_tensor sorted_sequence in
  let self = globalize_tensor self in
  let sorter = [%globalize: tensor option] sorter in
  [%c.alloc
    ({|CAMLreturn(atg_searchsorted_tensor_out(%{out : tensor value}, %{sorted_sequence : tensor value}, %{self : tensor value}, Int_val(%{out_int32 : bool value}), Int_val(%{right : bool value}), %{side : string option value}, %{sorter : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let segment_reduce
  ~(data @ local)
  ~reduce
  ~(lengths @ local)
  ~(indices @ local)
  ~(offsets @ local)
  ~axis
  ~unsafe
  ~initial
  =
  let data = globalize_tensor data in
  let lengths = [%globalize: tensor option] lengths in
  let indices = [%globalize: tensor option] indices in
  let offsets = [%globalize: tensor option] offsets in
  [%c.alloc
    ({|CAMLreturn(atg_segment_reduce(%{data : tensor value}, String_val(%{reduce : string value}), %{lengths : tensor option value}, %{indices : tensor option value}, %{offsets : tensor option value}, %{axis : int}, Int_val(%{unsafe : bool value}), %{initial : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let segment_reduce_out
  ~(out @ local)
  ~(data @ local)
  ~reduce
  ~(lengths @ local)
  ~(indices @ local)
  ~(offsets @ local)
  ~axis
  ~unsafe
  ~initial
  =
  let out = globalize_tensor out in
  let data = globalize_tensor data in
  let lengths = [%globalize: tensor option] lengths in
  let indices = [%globalize: tensor option] indices in
  let offsets = [%globalize: tensor option] offsets in
  [%c.alloc
    ({|CAMLreturn(atg_segment_reduce_out(%{out : tensor value}, %{data : tensor value}, String_val(%{reduce : string value}), %{lengths : tensor option value}, %{indices : tensor option value}, %{offsets : tensor option value}, %{axis : int}, Int_val(%{unsafe : bool value}), %{initial : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let select (self @ local) ~dim ~index =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_select(%{self : tensor value}, %{dim : int}, %{index : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let select_backward ~(grad_output @ local) ~input_sizes ~dim ~index =
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg_select_backward(%{grad_output : tensor value}, %{input_sizes : int list value}, %{dim : int}, %{index : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let select_backward_out ~(out @ local) ~(grad_output @ local) ~input_sizes ~dim ~index =
  let out = globalize_tensor out in
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg_select_backward_out(%{out : tensor value}, %{grad_output : tensor value}, %{input_sizes : int list value}, %{dim : int}, %{index : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let select_copy (self @ local) ~dim ~index =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_select_copy(%{self : tensor value}, %{dim : int}, %{index : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let select_copy_int_out ~(out @ local) (self @ local) ~dim ~index =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_select_copy_int_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, %{index : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let select_scatter (self @ local) ~(src @ local) ~dim ~index =
  let self = globalize_tensor self in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_select_scatter(%{self : tensor value}, %{src : tensor value}, %{dim : int}, %{index : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let select_scatter_out ~(out @ local) (self @ local) ~(src @ local) ~dim ~index =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_select_scatter_out(%{out : tensor value}, %{self : tensor value}, %{src : tensor value}, %{dim : int}, %{index : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let selu (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_selu(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let selu_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_selu_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let set (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_set(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let set_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_set_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let set_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_set_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let set_requires_grad (self @ local) ~r =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_set_requires_grad(%{self : tensor value}, Int_val(%{r : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let set_source_tensor (self @ local) ~(source @ local) =
  let self = globalize_tensor self in
  let source = globalize_tensor source in
  [%c.alloc
    ({|CAMLreturn(atg_set_source_tensor(%{self : tensor value}, %{source : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let set_source_tensor_ (self @ local) ~(source @ local) =
  let self = globalize_tensor self in
  let source = globalize_tensor source in
  [%c.alloc
    ({|CAMLreturn(atg_set_source_tensor_(%{self : tensor value}, %{source : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let set_source_tensor_out ~(out @ local) (self @ local) ~(source @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let source = globalize_tensor source in
  [%c.alloc
    ({|CAMLreturn(atg_set_source_tensor_out(%{out : tensor value}, %{self : tensor value}, %{source : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let set_source_tensor_storage_offset_
  (self @ local)
  ~(source @ local)
  ~storage_offset
  ~size
  ~stride
  =
  let self = globalize_tensor self in
  let source = globalize_tensor source in
  [%c.alloc
    ({|CAMLreturn(atg_set_source_tensor_storage_offset_(%{self : tensor value}, %{source : tensor value}, %{storage_offset : int}, %{size : int list value}, %{stride : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sgn (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_sgn(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sgn_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_sgn_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sgn_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_sgn_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sigmoid (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_sigmoid(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sigmoid_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_sigmoid_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sigmoid_backward ~(grad_output @ local) ~(output @ local) =
  let grad_output = globalize_tensor grad_output in
  let output = globalize_tensor output in
  [%c.alloc
    ({|CAMLreturn(atg_sigmoid_backward(%{grad_output : tensor value}, %{output : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sigmoid_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  ~(output @ local)
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let output = globalize_tensor output in
  [%c.alloc
    ({|CAMLreturn(atg_sigmoid_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{output : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sigmoid_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_sigmoid_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sign (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_sign(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sign_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_sign_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sign_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_sign_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let signbit (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_signbit(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let signbit_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_signbit_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let silu (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_silu(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let silu_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_silu_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let silu_backward ~(grad_output @ local) (self @ local) =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_silu_backward(%{grad_output : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let silu_backward_grad_input ~(grad_input @ local) ~(grad_output @ local) (self @ local) =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_silu_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let silu_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_silu_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sin (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_sin(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sin_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_sin_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sin_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_sin_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sinc (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_sinc(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sinc_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_sinc_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sinc_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_sinc_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sinh (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_sinh(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sinh_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_sinh_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sinh_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_sinh_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let size (self @ local) ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_size(%{self : tensor value}, %{dim : int}));|} : int64 value)]
;;

let slice (self @ local) ~dim ~start ~end_ ~step =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_slice(%{self : tensor value}, %{dim : int}, %{start : int option value}, %{end_ : int option value}, %{step : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let slice_backward ~(grad_output @ local) ~input_sizes ~dim ~start ~end_ ~step =
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg_slice_backward(%{grad_output : tensor value}, %{input_sizes : int list value}, %{dim : int}, %{start : int}, %{end_ : int}, %{step : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let slice_backward_out
  ~(out @ local)
  ~(grad_output @ local)
  ~input_sizes
  ~dim
  ~start
  ~end_
  ~step
  =
  let out = globalize_tensor out in
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg_slice_backward_out(%{out : tensor value}, %{grad_output : tensor value}, %{input_sizes : int list value}, %{dim : int}, %{start : int}, %{end_ : int}, %{step : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let slice_copy (self @ local) ~dim ~start ~end_ ~step =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_slice_copy(%{self : tensor value}, %{dim : int}, %{start : int option value}, %{end_ : int option value}, %{step : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let slice_copy_tensor_out ~(out @ local) (self @ local) ~dim ~start ~end_ ~step =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_slice_copy_tensor_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, %{start : int option value}, %{end_ : int option value}, %{step : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let slice_inverse (self @ local) ~(src @ local) ~dim ~start ~end_ ~step =
  let self = globalize_tensor self in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_slice_inverse(%{self : tensor value}, %{src : tensor value}, %{dim : int}, %{start : int option value}, %{end_ : int option value}, %{step : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let slice_scatter (self @ local) ~(src @ local) ~dim ~start ~end_ ~step =
  let self = globalize_tensor self in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_slice_scatter(%{self : tensor value}, %{src : tensor value}, %{dim : int}, %{start : int option value}, %{end_ : int option value}, %{step : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let slice_scatter_out ~(out @ local) (self @ local) ~(src @ local) ~dim ~start ~end_ ~step
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_slice_scatter_out(%{out : tensor value}, %{self : tensor value}, %{src : tensor value}, %{dim : int}, %{start : int option value}, %{end_ : int option value}, %{step : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let slogdet (self @ local) =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_slogdet(%{self : tensor value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let slogdet_out ~(sign @ local) ~(logabsdet @ local) (self @ local) =
  let sign = globalize_tensor sign in
  let logabsdet = globalize_tensor logabsdet in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_slogdet_out(%{sign : tensor value}, %{logabsdet : tensor value}, %{self : tensor value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let slow_conv3d
  (self @ local)
  ~(weight @ local)
  ~kernel_size
  ~(bias @ local)
  ~stride
  ~padding
  =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_slow_conv3d(%{self : tensor value}, %{weight : tensor value}, %{kernel_size : int list value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let slow_conv3d_out
  ~(out @ local)
  (self @ local)
  ~(weight @ local)
  ~kernel_size
  ~(bias @ local)
  ~stride
  ~padding
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_slow_conv3d_out(%{out : tensor value}, %{self : tensor value}, %{weight : tensor value}, %{kernel_size : int list value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let slow_conv_dilated2d
  (self @ local)
  ~(weight @ local)
  ~kernel_size
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_slow_conv_dilated2d(%{self : tensor value}, %{weight : tensor value}, %{kernel_size : int list value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let slow_conv_dilated2d_out
  ~(out @ local)
  (self @ local)
  ~(weight @ local)
  ~kernel_size
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_slow_conv_dilated2d_out(%{out : tensor value}, %{self : tensor value}, %{weight : tensor value}, %{kernel_size : int list value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let slow_conv_dilated3d
  (self @ local)
  ~(weight @ local)
  ~kernel_size
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_slow_conv_dilated3d(%{self : tensor value}, %{weight : tensor value}, %{kernel_size : int list value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let slow_conv_dilated3d_out
  ~(out @ local)
  (self @ local)
  ~(weight @ local)
  ~kernel_size
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_slow_conv_dilated3d_out(%{out : tensor value}, %{self : tensor value}, %{weight : tensor value}, %{kernel_size : int list value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let slow_conv_transpose2d
  (self @ local)
  ~(weight @ local)
  ~kernel_size
  ~(bias @ local)
  ~stride
  ~padding
  ~output_padding
  ~dilation
  =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_slow_conv_transpose2d(%{self : tensor value}, %{weight : tensor value}, %{kernel_size : int list value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{output_padding : int list value}, %{dilation : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let slow_conv_transpose2d_out
  ~(out @ local)
  (self @ local)
  ~(weight @ local)
  ~kernel_size
  ~(bias @ local)
  ~stride
  ~padding
  ~output_padding
  ~dilation
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_slow_conv_transpose2d_out(%{out : tensor value}, %{self : tensor value}, %{weight : tensor value}, %{kernel_size : int list value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{output_padding : int list value}, %{dilation : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let slow_conv_transpose3d
  (self @ local)
  ~(weight @ local)
  ~kernel_size
  ~(bias @ local)
  ~stride
  ~padding
  ~output_padding
  ~dilation
  =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_slow_conv_transpose3d(%{self : tensor value}, %{weight : tensor value}, %{kernel_size : int list value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{output_padding : int list value}, %{dilation : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let slow_conv_transpose3d_out
  ~(out @ local)
  (self @ local)
  ~(weight @ local)
  ~kernel_size
  ~(bias @ local)
  ~stride
  ~padding
  ~output_padding
  ~dilation
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_slow_conv_transpose3d_out(%{out : tensor value}, %{self : tensor value}, %{weight : tensor value}, %{kernel_size : int list value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{output_padding : int list value}, %{dilation : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let smm (self @ local) ~(mat2 @ local) =
  let self = globalize_tensor self in
  let mat2 = globalize_tensor mat2 in
  [%c.alloc
    ({|CAMLreturn(atg_smm(%{self : tensor value}, %{mat2 : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let smooth_l1_loss (self @ local) ~(target @ local) ~reduction ~beta =
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_smooth_l1_loss(%{self : tensor value}, %{target : tensor value}, %{reduction : int}, %{beta : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let smooth_l1_loss_backward
  ~(grad_output @ local)
  (self @ local)
  ~(target @ local)
  ~reduction
  ~beta
  =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_smooth_l1_loss_backward(%{grad_output : tensor value}, %{self : tensor value}, %{target : tensor value}, %{reduction : int}, %{beta : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let smooth_l1_loss_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~(target @ local)
  ~reduction
  ~beta
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_smooth_l1_loss_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{target : tensor value}, %{reduction : int}, %{beta : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let smooth_l1_loss_out ~(out @ local) (self @ local) ~(target @ local) ~reduction ~beta =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_smooth_l1_loss_out(%{out : tensor value}, %{self : tensor value}, %{target : tensor value}, %{reduction : int}, %{beta : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let soft_margin_loss (self @ local) ~(target @ local) ~reduction =
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_soft_margin_loss(%{self : tensor value}, %{target : tensor value}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let soft_margin_loss_backward
  ~(grad_output @ local)
  (self @ local)
  ~(target @ local)
  ~reduction
  =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_soft_margin_loss_backward(%{grad_output : tensor value}, %{self : tensor value}, %{target : tensor value}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let soft_margin_loss_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~(target @ local)
  ~reduction
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_soft_margin_loss_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{target : tensor value}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let soft_margin_loss_out ~(out @ local) (self @ local) ~(target @ local) ~reduction =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_soft_margin_loss_out(%{out : tensor value}, %{self : tensor value}, %{target : tensor value}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let softmax (self @ local) ~dim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_softmax(%{self : tensor value}, %{dim : int}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let softmax_int_out ~(out @ local) (self @ local) ~dim ~dtype =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_softmax_int_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let softplus ?beta ?threshold (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_softplus(%{self : tensor value}, %{beta : scalar option value}, %{threshold : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let softplus_backward ~(grad_output @ local) (self @ local) ~beta ~threshold =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_softplus_backward(%{grad_output : tensor value}, %{self : tensor value}, %{beta : scalar value}, %{threshold : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let softplus_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~beta
  ~threshold
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_softplus_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{beta : scalar value}, %{threshold : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let softplus_out ?beta ?threshold ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_softplus_out(%{out : tensor value}, %{self : tensor value}, %{beta : scalar option value}, %{threshold : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let softshrink ?lambd (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_softshrink(%{self : tensor value}, %{lambd : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let softshrink_backward ~(grad_output @ local) (self @ local) ~lambd =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_softshrink_backward(%{grad_output : tensor value}, %{self : tensor value}, %{lambd : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let softshrink_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~lambd
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_softshrink_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{lambd : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let softshrink_out ?lambd ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_softshrink_out(%{out : tensor value}, %{self : tensor value}, %{lambd : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sort (self @ local) ~dim ~descending =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_sort(%{self : tensor value}, %{dim : int}, Int_val(%{descending : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let sort_stable (self @ local) ~stable ~dim ~descending =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_sort_stable(%{self : tensor value}, Int_val(%{stable : bool value}), %{dim : int}, Int_val(%{descending : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let sort_values ~(values @ local) ~(indices @ local) (self @ local) ~dim ~descending =
  let values = globalize_tensor values in
  let indices = globalize_tensor indices in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_sort_values(%{values : tensor value}, %{indices : tensor value}, %{self : tensor value}, %{dim : int}, Int_val(%{descending : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let sort_values_stable
  ~(values @ local)
  ~(indices @ local)
  (self @ local)
  ~stable
  ~dim
  ~descending
  =
  let values = globalize_tensor values in
  let indices = globalize_tensor indices in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_sort_values_stable(%{values : tensor value}, %{indices : tensor value}, %{self : tensor value}, Int_val(%{stable : bool value}), %{dim : int}, Int_val(%{descending : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let sparse_bsc_tensor
  ~(ccol_indices @ local)
  ~(row_indices @ local)
  ~(values @ local)
  ~options
  =
  let ccol_indices = globalize_tensor ccol_indices in
  let row_indices = globalize_tensor row_indices in
  let values = globalize_tensor values in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_bsc_tensor(%{ccol_indices : tensor value}, %{row_indices : tensor value}, %{values : tensor value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_bsc_tensor_ccol_row_value_size
  ~(ccol_indices @ local)
  ~(row_indices @ local)
  ~(values @ local)
  ~size
  ~options
  =
  let ccol_indices = globalize_tensor ccol_indices in
  let row_indices = globalize_tensor row_indices in
  let values = globalize_tensor values in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_bsc_tensor_ccol_row_value_size(%{ccol_indices : tensor value}, %{row_indices : tensor value}, %{values : tensor value}, %{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_bsr_tensor
  ~(crow_indices @ local)
  ~(col_indices @ local)
  ~(values @ local)
  ~options
  =
  let crow_indices = globalize_tensor crow_indices in
  let col_indices = globalize_tensor col_indices in
  let values = globalize_tensor values in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_bsr_tensor(%{crow_indices : tensor value}, %{col_indices : tensor value}, %{values : tensor value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_bsr_tensor_crow_col_value_size
  ~(crow_indices @ local)
  ~(col_indices @ local)
  ~(values @ local)
  ~size
  ~options
  =
  let crow_indices = globalize_tensor crow_indices in
  let col_indices = globalize_tensor col_indices in
  let values = globalize_tensor values in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_bsr_tensor_crow_col_value_size(%{crow_indices : tensor value}, %{col_indices : tensor value}, %{values : tensor value}, %{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_compressed_tensor
  ~(compressed_indices @ local)
  ~(plain_indices @ local)
  ~(values @ local)
  ~options
  =
  let compressed_indices = globalize_tensor compressed_indices in
  let plain_indices = globalize_tensor plain_indices in
  let values = globalize_tensor values in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_compressed_tensor(%{compressed_indices : tensor value}, %{plain_indices : tensor value}, %{values : tensor value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_compressed_tensor_comp_plain_value_size
  ~(compressed_indices @ local)
  ~(plain_indices @ local)
  ~(values @ local)
  ~size
  ~options
  =
  let compressed_indices = globalize_tensor compressed_indices in
  let plain_indices = globalize_tensor plain_indices in
  let values = globalize_tensor values in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_compressed_tensor_comp_plain_value_size(%{compressed_indices : tensor value}, %{plain_indices : tensor value}, %{values : tensor value}, %{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_coo_tensor ~size ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_coo_tensor(%{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_coo_tensor_indices ~(indices @ local) ~(values @ local) ~options ~is_coalesced =
  let indices = globalize_tensor indices in
  let values = globalize_tensor values in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_coo_tensor_indices(%{indices : tensor value}, %{values : tensor value}, %{options_kind : int}, %{options_device : int}, Int_val(%{is_coalesced : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_coo_tensor_indices_size
  ~(indices @ local)
  ~(values @ local)
  ~size
  ~options
  ~is_coalesced
  =
  let indices = globalize_tensor indices in
  let values = globalize_tensor values in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_coo_tensor_indices_size(%{indices : tensor value}, %{values : tensor value}, %{size : int list value}, %{options_kind : int}, %{options_device : int}, Int_val(%{is_coalesced : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_coo_tensor_size_out ~(out @ local) ~size =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_coo_tensor_size_out(%{out : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_csc_tensor
  ~(ccol_indices @ local)
  ~(row_indices @ local)
  ~(values @ local)
  ~options
  =
  let ccol_indices = globalize_tensor ccol_indices in
  let row_indices = globalize_tensor row_indices in
  let values = globalize_tensor values in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_csc_tensor(%{ccol_indices : tensor value}, %{row_indices : tensor value}, %{values : tensor value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_csc_tensor_ccol_row_value_size
  ~(ccol_indices @ local)
  ~(row_indices @ local)
  ~(values @ local)
  ~size
  ~options
  =
  let ccol_indices = globalize_tensor ccol_indices in
  let row_indices = globalize_tensor row_indices in
  let values = globalize_tensor values in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_csc_tensor_ccol_row_value_size(%{ccol_indices : tensor value}, %{row_indices : tensor value}, %{values : tensor value}, %{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_csr_tensor
  ~(crow_indices @ local)
  ~(col_indices @ local)
  ~(values @ local)
  ~options
  =
  let crow_indices = globalize_tensor crow_indices in
  let col_indices = globalize_tensor col_indices in
  let values = globalize_tensor values in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_csr_tensor(%{crow_indices : tensor value}, %{col_indices : tensor value}, %{values : tensor value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_csr_tensor_crow_col_value_size
  ~(crow_indices @ local)
  ~(col_indices @ local)
  ~(values @ local)
  ~size
  ~options
  =
  let crow_indices = globalize_tensor crow_indices in
  let col_indices = globalize_tensor col_indices in
  let values = globalize_tensor values in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_csr_tensor_crow_col_value_size(%{crow_indices : tensor value}, %{col_indices : tensor value}, %{values : tensor value}, %{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_dim (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_sparse_dim(%{self : tensor value}));|} : int64 value)]
;;

let sparse_mask (self @ local) ~(mask @ local) =
  let self = globalize_tensor self in
  let mask = globalize_tensor mask in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_mask(%{self : tensor value}, %{mask : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_mask_out ~(out @ local) (self @ local) ~(mask @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let mask = globalize_tensor mask in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_mask_out(%{out : tensor value}, %{self : tensor value}, %{mask : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_resize (self @ local) ~size ~sparse_dim ~dense_dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_resize(%{self : tensor value}, %{size : int list value}, %{sparse_dim : int}, %{dense_dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_resize_ (self @ local) ~size ~sparse_dim ~dense_dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_resize_(%{self : tensor value}, %{size : int list value}, %{sparse_dim : int}, %{dense_dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_resize_and_clear (self @ local) ~size ~sparse_dim ~dense_dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_resize_and_clear(%{self : tensor value}, %{size : int list value}, %{sparse_dim : int}, %{dense_dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_resize_and_clear_ (self @ local) ~size ~sparse_dim ~dense_dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_resize_and_clear_(%{self : tensor value}, %{size : int list value}, %{sparse_dim : int}, %{dense_dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_resize_and_clear_out ~(out @ local) (self @ local) ~size ~sparse_dim ~dense_dim
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_resize_and_clear_out(%{out : tensor value}, %{self : tensor value}, %{size : int list value}, %{sparse_dim : int}, %{dense_dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_resize_out ~(out @ local) (self @ local) ~size ~sparse_dim ~dense_dim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_resize_out(%{out : tensor value}, %{self : tensor value}, %{size : int list value}, %{sparse_dim : int}, %{dense_dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_sampled_addmm ?beta ?alpha (self @ local) ~(mat1 @ local) ~(mat2 @ local) =
  let self = globalize_tensor self in
  let mat1 = globalize_tensor mat1 in
  let mat2 = globalize_tensor mat2 in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_sampled_addmm(%{self : tensor value}, %{mat1 : tensor value}, %{mat2 : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let sparse_sampled_addmm_out
  ?beta
  ?alpha
  ~(out @ local)
  (self @ local)
  ~(mat1 @ local)
  ~(mat2 @ local)
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let mat1 = globalize_tensor mat1 in
  let mat2 = globalize_tensor mat2 in
  [%c.alloc
    ({|CAMLreturn(atg_sparse_sampled_addmm_out(%{out : tensor value}, %{self : tensor value}, %{mat1 : tensor value}, %{mat2 : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_airy_ai ~(x @ local) =
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_airy_ai(%{x : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_airy_ai_out ~(out @ local) ~(x @ local) =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_airy_ai_out(%{out : tensor value}, %{x : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_bessel_j0 (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_bessel_j0(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_bessel_j0_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_bessel_j0_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_bessel_j1 (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_bessel_j1(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_bessel_j1_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_bessel_j1_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_bessel_y0 (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_bessel_y0(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_bessel_y0_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_bessel_y0_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_bessel_y1 (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_bessel_y1(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_bessel_y1_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_bessel_y1_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_t ~(x @ local) ~(n @ local) =
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_t(%{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_t_n_scalar ~(x @ local) ~n =
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_t_n_scalar(%{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_t_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_t_n_scalar_out(%{out : tensor value}, %{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_t_out ~(out @ local) ~(x @ local) ~(n @ local) =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_t_out(%{out : tensor value}, %{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_t_x_scalar ~x ~(n @ local) =
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_t_x_scalar(%{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_t_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  let out = globalize_tensor out in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_t_x_scalar_out(%{out : tensor value}, %{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_u ~(x @ local) ~(n @ local) =
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_u(%{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_u_n_scalar ~(x @ local) ~n =
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_u_n_scalar(%{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_u_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_u_n_scalar_out(%{out : tensor value}, %{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_u_out ~(out @ local) ~(x @ local) ~(n @ local) =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_u_out(%{out : tensor value}, %{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_u_x_scalar ~x ~(n @ local) =
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_u_x_scalar(%{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_u_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  let out = globalize_tensor out in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_u_x_scalar_out(%{out : tensor value}, %{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_v ~(x @ local) ~(n @ local) =
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_v(%{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_v_n_scalar ~(x @ local) ~n =
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_v_n_scalar(%{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_v_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_v_n_scalar_out(%{out : tensor value}, %{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_v_out ~(out @ local) ~(x @ local) ~(n @ local) =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_v_out(%{out : tensor value}, %{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_v_x_scalar ~x ~(n @ local) =
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_v_x_scalar(%{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_v_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  let out = globalize_tensor out in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_v_x_scalar_out(%{out : tensor value}, %{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_w ~(x @ local) ~(n @ local) =
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_w(%{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_w_n_scalar ~(x @ local) ~n =
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_w_n_scalar(%{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_w_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_w_n_scalar_out(%{out : tensor value}, %{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_w_out ~(out @ local) ~(x @ local) ~(n @ local) =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_w_out(%{out : tensor value}, %{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_w_x_scalar ~x ~(n @ local) =
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_w_x_scalar(%{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_chebyshev_polynomial_w_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  let out = globalize_tensor out in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_chebyshev_polynomial_w_x_scalar_out(%{out : tensor value}, %{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_digamma (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_digamma(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_digamma_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_digamma_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_entr (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_entr(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_entr_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_entr_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_erf (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_special_erf(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_erf_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_erf_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_erfc (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_erfc(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_erfc_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_erfc_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_erfcx (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_erfcx(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_erfcx_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_erfcx_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_erfinv (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_erfinv(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_erfinv_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_erfinv_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_exp2 (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_exp2(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_exp2_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_exp2_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_expit (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_expit(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_expit_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_expit_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_expm1 (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_expm1(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_expm1_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_expm1_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_gammainc (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_special_gammainc(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_gammainc_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_special_gammainc_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_gammaincc (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_special_gammaincc(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_gammaincc_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_special_gammaincc_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_gammaln (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_gammaln(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_gammaln_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_gammaln_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_hermite_polynomial_h ~(x @ local) ~(n @ local) =
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_hermite_polynomial_h(%{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_hermite_polynomial_h_n_scalar ~(x @ local) ~n =
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_hermite_polynomial_h_n_scalar(%{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_hermite_polynomial_h_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_hermite_polynomial_h_n_scalar_out(%{out : tensor value}, %{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_hermite_polynomial_h_out ~(out @ local) ~(x @ local) ~(n @ local) =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_hermite_polynomial_h_out(%{out : tensor value}, %{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_hermite_polynomial_h_x_scalar ~x ~(n @ local) =
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_hermite_polynomial_h_x_scalar(%{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_hermite_polynomial_h_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  let out = globalize_tensor out in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_hermite_polynomial_h_x_scalar_out(%{out : tensor value}, %{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_hermite_polynomial_he ~(x @ local) ~(n @ local) =
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_hermite_polynomial_he(%{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_hermite_polynomial_he_n_scalar ~(x @ local) ~n =
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_hermite_polynomial_he_n_scalar(%{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_hermite_polynomial_he_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_hermite_polynomial_he_n_scalar_out(%{out : tensor value}, %{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_hermite_polynomial_he_out ~(out @ local) ~(x @ local) ~(n @ local) =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_hermite_polynomial_he_out(%{out : tensor value}, %{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_hermite_polynomial_he_x_scalar ~x ~(n @ local) =
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_hermite_polynomial_he_x_scalar(%{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_hermite_polynomial_he_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  let out = globalize_tensor out in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_hermite_polynomial_he_x_scalar_out(%{out : tensor value}, %{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_i0 (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_special_i0(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_i0_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_i0_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_i0e (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_special_i0e(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_i0e_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_i0e_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_i1 (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_special_i1(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_i1_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_i1_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_i1e (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_special_i1e(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_i1e_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_i1e_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_laguerre_polynomial_l ~(x @ local) ~(n @ local) =
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_laguerre_polynomial_l(%{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_laguerre_polynomial_l_n_scalar ~(x @ local) ~n =
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_laguerre_polynomial_l_n_scalar(%{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_laguerre_polynomial_l_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_laguerre_polynomial_l_n_scalar_out(%{out : tensor value}, %{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_laguerre_polynomial_l_out ~(out @ local) ~(x @ local) ~(n @ local) =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_laguerre_polynomial_l_out(%{out : tensor value}, %{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_laguerre_polynomial_l_x_scalar ~x ~(n @ local) =
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_laguerre_polynomial_l_x_scalar(%{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_laguerre_polynomial_l_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  let out = globalize_tensor out in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_laguerre_polynomial_l_x_scalar_out(%{out : tensor value}, %{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_legendre_polynomial_p ~(x @ local) ~(n @ local) =
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_legendre_polynomial_p(%{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_legendre_polynomial_p_n_scalar ~(x @ local) ~n =
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_legendre_polynomial_p_n_scalar(%{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_legendre_polynomial_p_n_scalar_out ~(out @ local) ~(x @ local) ~n =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_special_legendre_polynomial_p_n_scalar_out(%{out : tensor value}, %{x : tensor value}, %{n : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_legendre_polynomial_p_out ~(out @ local) ~(x @ local) ~(n @ local) =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_legendre_polynomial_p_out(%{out : tensor value}, %{x : tensor value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_legendre_polynomial_p_x_scalar ~x ~(n @ local) =
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_legendre_polynomial_p_x_scalar(%{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_legendre_polynomial_p_x_scalar_out ~(out @ local) ~x ~(n @ local) =
  let out = globalize_tensor out in
  let n = globalize_tensor n in
  [%c.alloc
    ({|CAMLreturn(atg_special_legendre_polynomial_p_x_scalar_out(%{out : tensor value}, %{x : scalar value}, %{n : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_log1p (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_log1p(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_log1p_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_log1p_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_log_ndtr (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_log_ndtr(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_log_ndtr_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_log_ndtr_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_log_softmax (self @ local) ~dim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_special_log_softmax(%{self : tensor value}, %{dim : int}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_logit (self @ local) ~eps =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_logit(%{self : tensor value}, %{eps : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_logit_out ~(out @ local) (self @ local) ~eps =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_logit_out(%{out : tensor value}, %{self : tensor value}, %{eps : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_logsumexp (self @ local) ~dim ~keepdim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_logsumexp(%{self : tensor value}, %{dim : int list value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_logsumexp_out ~(out @ local) (self @ local) ~dim ~keepdim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_logsumexp_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_modified_bessel_i0 (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_modified_bessel_i0(%{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_modified_bessel_i0_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_modified_bessel_i0_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let special_modified_bessel_i1 (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_special_modified_bessel_i1(%{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;
