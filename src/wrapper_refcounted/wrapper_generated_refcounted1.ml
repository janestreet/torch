(* THIS FILE IS AUTOMATICALLY GENERATED, DO NOT EDIT BY HAND! *)

open! Core
open C_ffi
open Torch_wrapper_types
open Torch_refcounted_bindings.Type_defs

[%%c {| #include "torch_api.h" |}]

let _scaled_dot_product_efficient_attention
  ~(query @ local)
  ~(key @ local)
  ~(value @ local)
  ~(attn_bias @ local)
  ~compute_log_sumexp
  ~dropout_p
  ~is_causal
  ~scale
  =
  let query = globalize_tensor query in
  let key = globalize_tensor key in
  let value = globalize_tensor value in
  let attn_bias = [%globalize: tensor option] attn_bias in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg__scaled_dot_product_efficient_attention(%{query : tensor value}, %{key : tensor value}, %{value : tensor value}, %{attn_bias : tensor option value}, Int_val(%{compute_log_sumexp : bool value}), %{dropout_p : float}, Int_val(%{is_causal : bool value}), %{scale : float option value}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let _scaled_dot_product_flash_attention_backward
  ~(grad_out @ local)
  ~(query @ local)
  ~(key @ local)
  ~(value @ local)
  ~(out @ local)
  ~(logsumexp @ local)
  ~(cum_seq_q @ local)
  ~(cum_seq_k @ local)
  ~max_q
  ~max_k
  ~dropout_p
  ~is_causal
  ~(philox_seed @ local)
  ~(philox_offset @ local)
  ~scale
  =
  let grad_out = globalize_tensor grad_out in
  let query = globalize_tensor query in
  let key = globalize_tensor key in
  let value = globalize_tensor value in
  let out = globalize_tensor out in
  let logsumexp = globalize_tensor logsumexp in
  let cum_seq_q = globalize_tensor cum_seq_q in
  let cum_seq_k = globalize_tensor cum_seq_k in
  let philox_seed = globalize_tensor philox_seed in
  let philox_offset = globalize_tensor philox_offset in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__scaled_dot_product_flash_attention_backward(%{grad_out : tensor value}, %{query : tensor value}, %{key : tensor value}, %{value : tensor value}, %{out : tensor value}, %{logsumexp : tensor value}, %{cum_seq_q : tensor value}, %{cum_seq_k : tensor value}, %{max_q : int}, %{max_k : int}, %{dropout_p : float}, Int_val(%{is_causal : bool value}), %{philox_seed : tensor value}, %{philox_offset : tensor value}, %{scale : float option value}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _scaled_dot_product_flash_attention_for_cpu
  ~(query @ local)
  ~(key @ local)
  ~(value @ local)
  ~dropout_p
  ~is_causal
  ~(attn_mask @ local)
  ~scale
  =
  let query = globalize_tensor query in
  let key = globalize_tensor key in
  let value = globalize_tensor value in
  let attn_mask = [%globalize: tensor option] attn_mask in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__scaled_dot_product_flash_attention_for_cpu(%{query : tensor value}, %{key : tensor value}, %{value : tensor value}, %{dropout_p : float}, Int_val(%{is_causal : bool value}), %{attn_mask : tensor option value}, %{scale : float option value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _scaled_dot_product_flash_attention_for_cpu_backward
  ~(grad_out @ local)
  ~(query @ local)
  ~(key @ local)
  ~(value @ local)
  ~(out @ local)
  ~(logsumexp @ local)
  ~dropout_p
  ~is_causal
  ~(attn_mask @ local)
  ~scale
  =
  let grad_out = globalize_tensor grad_out in
  let query = globalize_tensor query in
  let key = globalize_tensor key in
  let value = globalize_tensor value in
  let out = globalize_tensor out in
  let logsumexp = globalize_tensor logsumexp in
  let attn_mask = [%globalize: tensor option] attn_mask in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__scaled_dot_product_flash_attention_for_cpu_backward(%{grad_out : tensor value}, %{query : tensor value}, %{key : tensor value}, %{value : tensor value}, %{out : tensor value}, %{logsumexp : tensor value}, %{dropout_p : float}, Int_val(%{is_causal : bool value}), %{attn_mask : tensor option value}, %{scale : float option value}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _scaled_grouped_mm
  (self @ local)
  ~(mat2 @ local)
  ~(scale_a @ local)
  ~(scale_b @ local)
  ~(offs @ local)
  ~(bias @ local)
  ~(scale_result @ local)
  ~out_dtype
  ~use_fast_accum
  =
  let self = globalize_tensor self in
  let mat2 = globalize_tensor mat2 in
  let scale_a = globalize_tensor scale_a in
  let scale_b = globalize_tensor scale_b in
  let offs = [%globalize: tensor option] offs in
  let bias = [%globalize: tensor option] bias in
  let scale_result = [%globalize: tensor option] scale_result in
  let out_dtype = Kind.packed_to_int out_dtype in
  [%c.alloc
    ({|CAMLreturn(atg__scaled_grouped_mm(%{self : tensor value}, %{mat2 : tensor value}, %{scale_a : tensor value}, %{scale_b : tensor value}, %{offs : tensor option value}, %{bias : tensor option value}, %{scale_result : tensor option value}, %{out_dtype : int}, Int_val(%{use_fast_accum : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _scaled_mm
  (self @ local)
  ~(mat2 @ local)
  ~(scale_a @ local)
  ~(scale_b @ local)
  ~(bias @ local)
  ~(scale_result @ local)
  ~out_dtype
  ~use_fast_accum
  =
  let self = globalize_tensor self in
  let mat2 = globalize_tensor mat2 in
  let scale_a = globalize_tensor scale_a in
  let scale_b = globalize_tensor scale_b in
  let bias = [%globalize: tensor option] bias in
  let scale_result = [%globalize: tensor option] scale_result in
  let out_dtype = Kind.packed_to_int out_dtype in
  [%c.alloc
    ({|CAMLreturn(atg__scaled_mm(%{self : tensor value}, %{mat2 : tensor value}, %{scale_a : tensor value}, %{scale_b : tensor value}, %{bias : tensor option value}, %{scale_result : tensor option value}, %{out_dtype : int}, Int_val(%{use_fast_accum : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _scaled_mm_out
  ~(out @ local)
  (self @ local)
  ~(mat2 @ local)
  ~(scale_a @ local)
  ~(scale_b @ local)
  ~(bias @ local)
  ~(scale_result @ local)
  ~out_dtype
  ~use_fast_accum
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let mat2 = globalize_tensor mat2 in
  let scale_a = globalize_tensor scale_a in
  let scale_b = globalize_tensor scale_b in
  let bias = [%globalize: tensor option] bias in
  let scale_result = [%globalize: tensor option] scale_result in
  let out_dtype = Kind.packed_to_int out_dtype in
  [%c.alloc
    ({|CAMLreturn(atg__scaled_mm_out(%{out : tensor value}, %{self : tensor value}, %{mat2 : tensor value}, %{scale_a : tensor value}, %{scale_b : tensor value}, %{bias : tensor option value}, %{scale_result : tensor option value}, %{out_dtype : int}, Int_val(%{use_fast_accum : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _scatter_reduce
  (self @ local)
  ~dim
  ~(index @ local)
  ~(src @ local)
  ~reduce
  ~include_self
  =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg__scatter_reduce(%{self : tensor value}, %{dim : int}, %{index : tensor value}, %{src : tensor value}, String_val(%{reduce : string value}), Int_val(%{include_self : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _scatter_reduce_
  (self @ local)
  ~dim
  ~(index @ local)
  ~(src @ local)
  ~reduce
  ~include_self
  =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg__scatter_reduce_(%{self : tensor value}, %{dim : int}, %{index : tensor value}, %{src : tensor value}, String_val(%{reduce : string value}), Int_val(%{include_self : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _scatter_reduce_two_out
  ~(out @ local)
  (self @ local)
  ~dim
  ~(index @ local)
  ~(src @ local)
  ~reduce
  ~include_self
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg__scatter_reduce_two_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, %{index : tensor value}, %{src : tensor value}, String_val(%{reduce : string value}), Int_val(%{include_self : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _segment_reduce_backward
  ~(grad @ local)
  ~(output @ local)
  ~(data @ local)
  ~reduce
  ~(lengths @ local)
  ~(offsets @ local)
  ~axis
  ~initial
  =
  let grad = globalize_tensor grad in
  let output = globalize_tensor output in
  let data = globalize_tensor data in
  let lengths = [%globalize: tensor option] lengths in
  let offsets = [%globalize: tensor option] offsets in
  [%c.alloc
    ({|CAMLreturn(atg__segment_reduce_backward(%{grad : tensor value}, %{output : tensor value}, %{data : tensor value}, String_val(%{reduce : string value}), %{lengths : tensor option value}, %{offsets : tensor option value}, %{axis : int}, %{initial : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _segment_reduce_backward_out
  ~(out @ local)
  ~(grad @ local)
  ~(output @ local)
  ~(data @ local)
  ~reduce
  ~(lengths @ local)
  ~(offsets @ local)
  ~axis
  ~initial
  =
  let out = globalize_tensor out in
  let grad = globalize_tensor grad in
  let output = globalize_tensor output in
  let data = globalize_tensor data in
  let lengths = [%globalize: tensor option] lengths in
  let offsets = [%globalize: tensor option] offsets in
  [%c.alloc
    ({|CAMLreturn(atg__segment_reduce_backward_out(%{out : tensor value}, %{grad : tensor value}, %{output : tensor value}, %{data : tensor value}, String_val(%{reduce : string value}), %{lengths : tensor option value}, %{offsets : tensor option value}, %{axis : int}, %{initial : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _shape_as_tensor (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__shape_as_tensor(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _slow_conv2d_backward
  ~(grad_input @ local)
  ~(grad_weight @ local)
  ~(grad_bias @ local)
  ~(grad_output @ local)
  (self @ local)
  ~(weight @ local)
  ~kernel_size
  ~stride
  ~padding
  =
  let grad_input = globalize_tensor grad_input in
  let grad_weight = globalize_tensor grad_weight in
  let grad_bias = globalize_tensor grad_bias in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__slow_conv2d_backward(%{grad_input : tensor value}, %{grad_weight : tensor value}, %{grad_bias : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{weight : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _sobol_engine_draw
  ~(quasi @ local)
  ~n
  ~(sobolstate @ local)
  ~dimension
  ~num_generated
  ~dtype
  =
  let quasi = globalize_tensor quasi in
  let sobolstate = globalize_tensor sobolstate in
  let dtype = Kind.packed_to_int dtype in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__sobol_engine_draw(%{quasi : tensor value}, %{n : int}, %{sobolstate : tensor value}, %{dimension : int}, %{num_generated : int}, %{dtype : int}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _sobol_engine_ff_ (self @ local) ~n ~(sobolstate @ local) ~dimension ~num_generated =
  let self = globalize_tensor self in
  let sobolstate = globalize_tensor sobolstate in
  [%c.alloc
    ({|CAMLreturn(atg__sobol_engine_ff_(%{self : tensor value}, %{n : int}, %{sobolstate : tensor value}, %{dimension : int}, %{num_generated : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sobol_engine_initialize_state_ (self @ local) ~dimension =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__sobol_engine_initialize_state_(%{self : tensor value}, %{dimension : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sobol_engine_scramble_ (self @ local) ~(ltm @ local) ~dimension =
  let self = globalize_tensor self in
  let ltm = globalize_tensor ltm in
  [%c.alloc
    ({|CAMLreturn(atg__sobol_engine_scramble_(%{self : tensor value}, %{ltm : tensor value}, %{dimension : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _softmax (self @ local) ~dim ~half_to_float =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__softmax(%{self : tensor value}, %{dim : int}, Int_val(%{half_to_float : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _softmax_backward_data ~(grad_output @ local) ~(output @ local) ~dim ~input_dtype =
  let grad_output = globalize_tensor grad_output in
  let output = globalize_tensor output in
  let input_dtype = Kind.packed_to_int input_dtype in
  [%c.alloc
    ({|CAMLreturn(atg__softmax_backward_data(%{grad_output : tensor value}, %{output : tensor value}, %{dim : int}, %{input_dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _softmax_backward_data_out
  ~(grad_input @ local)
  ~(grad_output @ local)
  ~(output @ local)
  ~dim
  ~input_dtype
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let output = globalize_tensor output in
  let input_dtype = Kind.packed_to_int input_dtype in
  [%c.alloc
    ({|CAMLreturn(atg__softmax_backward_data_out(%{grad_input : tensor value}, %{grad_output : tensor value}, %{output : tensor value}, %{dim : int}, %{input_dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _softmax_out ~(out @ local) (self @ local) ~dim ~half_to_float =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__softmax_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, Int_val(%{half_to_float : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_addmm ?beta ?alpha (self @ local) ~(mat1 @ local) ~(mat2 @ local) =
  let self = globalize_tensor self in
  let mat1 = globalize_tensor mat1 in
  let mat2 = globalize_tensor mat2 in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_addmm(%{self : tensor value}, %{mat1 : tensor value}, %{mat2 : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_addmm_out
  ?beta
  ?alpha
  ~(out @ local)
  (self @ local)
  ~(mat1 @ local)
  ~(mat2 @ local)
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let mat1 = globalize_tensor mat1 in
  let mat2 = globalize_tensor mat2 in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_addmm_out(%{out : tensor value}, %{self : tensor value}, %{mat1 : tensor value}, %{mat2 : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_broadcast_to (self @ local) ~size =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_broadcast_to(%{self : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_broadcast_to_copy (self @ local) ~size =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_broadcast_to_copy(%{self : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_broadcast_to_copy_out ~(out @ local) (self @ local) ~size =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_broadcast_to_copy_out(%{out : tensor value}, %{self : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_bsc_tensor_unsafe
  ~(ccol_indices @ local)
  ~(row_indices @ local)
  ~(values @ local)
  ~size
  ~options
  =
  let ccol_indices = globalize_tensor ccol_indices in
  let row_indices = globalize_tensor row_indices in
  let values = globalize_tensor values in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_bsc_tensor_unsafe(%{ccol_indices : tensor value}, %{row_indices : tensor value}, %{values : tensor value}, %{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_bsr_tensor_unsafe
  ~(crow_indices @ local)
  ~(col_indices @ local)
  ~(values @ local)
  ~size
  ~options
  =
  let crow_indices = globalize_tensor crow_indices in
  let col_indices = globalize_tensor col_indices in
  let values = globalize_tensor values in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_bsr_tensor_unsafe(%{crow_indices : tensor value}, %{col_indices : tensor value}, %{values : tensor value}, %{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_compressed_tensor_unsafe
  ~(compressed_indices @ local)
  ~(plain_indices @ local)
  ~(values @ local)
  ~size
  ~options
  =
  let compressed_indices = globalize_tensor compressed_indices in
  let plain_indices = globalize_tensor plain_indices in
  let values = globalize_tensor values in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_compressed_tensor_unsafe(%{compressed_indices : tensor value}, %{plain_indices : tensor value}, %{values : tensor value}, %{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_compressed_tensor_with_dims
  ~nnz
  ~dense_dim
  ~size
  ~blocksize
  ~index_dtype
  ~options
  =
  let index_dtype = Kind.packed_to_int index_dtype in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_compressed_tensor_with_dims(%{nnz : int}, %{dense_dim : int}, %{size : int list value}, %{blocksize : int list value}, %{index_dtype : int}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_coo_tensor_unsafe
  ~(indices @ local)
  ~(values @ local)
  ~size
  ~options
  ~is_coalesced
  =
  let indices = globalize_tensor indices in
  let values = globalize_tensor values in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_coo_tensor_unsafe(%{indices : tensor value}, %{values : tensor value}, %{size : int list value}, %{options_kind : int}, %{options_device : int}, Int_val(%{is_coalesced : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_coo_tensor_with_dims ~sparse_dim ~dense_dim ~size ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_coo_tensor_with_dims(%{sparse_dim : int}, %{dense_dim : int}, %{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_coo_tensor_with_dims_and_tensors
  ~sparse_dim
  ~dense_dim
  ~size
  ~(indices @ local)
  ~(values @ local)
  ~options
  ~is_coalesced
  =
  let indices = globalize_tensor indices in
  let values = globalize_tensor values in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_coo_tensor_with_dims_and_tensors(%{sparse_dim : int}, %{dense_dim : int}, %{size : int list value}, %{indices : tensor value}, %{values : tensor value}, %{options_kind : int}, %{options_device : int}, Int_val(%{is_coalesced : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_coo_tensor_with_dims_and_tensors_out
  ~(out @ local)
  ~sparse_dim
  ~dense_dim
  ~size
  ~(indices @ local)
  ~(values @ local)
  ~is_coalesced
  =
  let out = globalize_tensor out in
  let indices = globalize_tensor indices in
  let values = globalize_tensor values in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_coo_tensor_with_dims_and_tensors_out(%{out : tensor value}, %{sparse_dim : int}, %{dense_dim : int}, %{size : int list value}, %{indices : tensor value}, %{values : tensor value}, Int_val(%{is_coalesced : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_coo_tensor_with_dims_out ~(out @ local) ~sparse_dim ~dense_dim ~size =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_coo_tensor_with_dims_out(%{out : tensor value}, %{sparse_dim : int}, %{dense_dim : int}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_csc_tensor_unsafe
  ~(ccol_indices @ local)
  ~(row_indices @ local)
  ~(values @ local)
  ~size
  ~options
  =
  let ccol_indices = globalize_tensor ccol_indices in
  let row_indices = globalize_tensor row_indices in
  let values = globalize_tensor values in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_csc_tensor_unsafe(%{ccol_indices : tensor value}, %{row_indices : tensor value}, %{values : tensor value}, %{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_csr_prod (self @ local) ~dim ~keepdim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_csr_prod(%{self : tensor value}, %{dim : int list value}, Int_val(%{keepdim : bool value}), %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_csr_prod_dim_dtype_out ~(out @ local) (self @ local) ~dim ~keepdim ~dtype =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_csr_prod_dim_dtype_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list value}, Int_val(%{keepdim : bool value}), %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_csr_sum (self @ local) ~dim ~keepdim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_csr_sum(%{self : tensor value}, %{dim : int list value}, Int_val(%{keepdim : bool value}), %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_csr_sum_dim_dtype_out ~(out @ local) (self @ local) ~dim ~keepdim ~dtype =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_csr_sum_dim_dtype_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list value}, Int_val(%{keepdim : bool value}), %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_csr_tensor_unsafe
  ~(crow_indices @ local)
  ~(col_indices @ local)
  ~(values @ local)
  ~size
  ~options
  =
  let crow_indices = globalize_tensor crow_indices in
  let col_indices = globalize_tensor col_indices in
  let values = globalize_tensor values in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_csr_tensor_unsafe(%{crow_indices : tensor value}, %{col_indices : tensor value}, %{values : tensor value}, %{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_log_softmax (self @ local) ~dim ~half_to_float =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_log_softmax(%{self : tensor value}, %{dim : int}, Int_val(%{half_to_float : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_log_softmax_backward_data
  ~(grad_output @ local)
  ~(output @ local)
  ~dim
  (self @ local)
  =
  let grad_output = globalize_tensor grad_output in
  let output = globalize_tensor output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_log_softmax_backward_data(%{grad_output : tensor value}, %{output : tensor value}, %{dim : int}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_log_softmax_backward_data_out
  ~(out @ local)
  ~(grad_output @ local)
  ~(output @ local)
  ~dim
  (self @ local)
  =
  let out = globalize_tensor out in
  let grad_output = globalize_tensor grad_output in
  let output = globalize_tensor output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_log_softmax_backward_data_out(%{out : tensor value}, %{grad_output : tensor value}, %{output : tensor value}, %{dim : int}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_log_softmax_int (self @ local) ~dim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_log_softmax_int(%{self : tensor value}, %{dim : int}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_log_softmax_out ~(out @ local) (self @ local) ~dim ~half_to_float =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_log_softmax_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, Int_val(%{half_to_float : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_mask_projection (self @ local) ~(mask @ local) ~accumulate_matches =
  let self = globalize_tensor self in
  let mask = globalize_tensor mask in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_mask_projection(%{self : tensor value}, %{mask : tensor value}, Int_val(%{accumulate_matches : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_mask_projection_out
  ~(out @ local)
  (self @ local)
  ~(mask @ local)
  ~accumulate_matches
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let mask = globalize_tensor mask in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_mask_projection_out(%{out : tensor value}, %{self : tensor value}, %{mask : tensor value}, Int_val(%{accumulate_matches : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_mm ~(sparse @ local) ~(dense @ local) =
  let sparse = globalize_tensor sparse in
  let dense = globalize_tensor dense in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_mm(%{sparse : tensor value}, %{dense : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_mm_reduce ~(sparse @ local) ~(dense @ local) ~reduce =
  let sparse = globalize_tensor sparse in
  let dense = globalize_tensor dense in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_mm_reduce(%{sparse : tensor value}, %{dense : tensor value}, String_val(%{reduce : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_mm_reduce_impl (self @ local) (other @ local) ~reduce =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__sparse_mm_reduce_impl(%{self : tensor value}, %{other : tensor value}, String_val(%{reduce : string value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _sparse_semi_structured_apply (input @ local) ~(thread_masks @ local) =
  let input = globalize_tensor input in
  let thread_masks = globalize_tensor thread_masks in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__sparse_semi_structured_apply(%{input : tensor value}, %{thread_masks : tensor value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _sparse_semi_structured_apply_dense (input @ local) ~(thread_masks @ local) =
  let input = globalize_tensor input in
  let thread_masks = globalize_tensor thread_masks in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_semi_structured_apply_dense(%{input : tensor value}, %{thread_masks : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_semi_structured_linear
  (input @ local)
  ~(weight @ local)
  ~(meta @ local)
  ~(bias @ local)
  ~activation
  ~out_dtype
  =
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let meta = globalize_tensor meta in
  let bias = [%globalize: tensor option] bias in
  let out_dtype = Kind.packed_to_int out_dtype in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_semi_structured_linear(%{input : tensor value}, %{weight : tensor value}, %{meta : tensor value}, %{bias : tensor option value}, %{activation : string option value}, %{out_dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_semi_structured_mm
  ~(mat1 @ local)
  ~(mat1_meta @ local)
  ~(mat2 @ local)
  ~out_dtype
  =
  let mat1 = globalize_tensor mat1 in
  let mat1_meta = globalize_tensor mat1_meta in
  let mat2 = globalize_tensor mat2 in
  let out_dtype = Kind.packed_to_int out_dtype in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_semi_structured_mm(%{mat1 : tensor value}, %{mat1_meta : tensor value}, %{mat2 : tensor value}, %{out_dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_semi_structured_tile (input @ local) ~algorithm ~use_cutlass =
  let input = globalize_tensor input in
  let result0, result1, result2, result3, result4 =
    [%c.alloc
      ({|CAMLreturn(atg__sparse_semi_structured_tile(%{input : tensor value}, String_val(%{algorithm : string value}), Int_val(%{use_cutlass : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3
  , wrap_managed_tensor result4 )
;;

let _sparse_softmax (self @ local) ~dim ~half_to_float =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_softmax(%{self : tensor value}, %{dim : int}, Int_val(%{half_to_float : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_softmax_backward_data
  ~(grad_output @ local)
  ~(output @ local)
  ~dim
  (self @ local)
  =
  let grad_output = globalize_tensor grad_output in
  let output = globalize_tensor output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_softmax_backward_data(%{grad_output : tensor value}, %{output : tensor value}, %{dim : int}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_softmax_backward_data_out
  ~(out @ local)
  ~(grad_output @ local)
  ~(output @ local)
  ~dim
  (self @ local)
  =
  let out = globalize_tensor out in
  let grad_output = globalize_tensor grad_output in
  let output = globalize_tensor output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_softmax_backward_data_out(%{out : tensor value}, %{grad_output : tensor value}, %{output : tensor value}, %{dim : int}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_softmax_int (self @ local) ~dim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_softmax_int(%{self : tensor value}, %{dim : int}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_softmax_out ~(out @ local) (self @ local) ~dim ~half_to_float =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_softmax_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, Int_val(%{half_to_float : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_sparse_matmul (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_sparse_matmul(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_sparse_matmul_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_sparse_matmul_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_sum (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg__sparse_sum(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_sum_backward ~(grad @ local) (self @ local) ~dim =
  let grad = globalize_tensor grad in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_sum_backward(%{grad : tensor value}, %{self : tensor value}, %{dim : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_sum_backward_out ~(out @ local) ~(grad @ local) (self @ local) ~dim =
  let out = globalize_tensor out in
  let grad = globalize_tensor grad in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_sum_backward_out(%{out : tensor value}, %{grad : tensor value}, %{self : tensor value}, %{dim : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_sum_dim (self @ local) ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_sum_dim(%{self : tensor value}, %{dim : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_sum_dim_dtype (self @ local) ~dim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_sum_dim_dtype(%{self : tensor value}, %{dim : int list value}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_sum_dim_out ~(out @ local) (self @ local) ~dim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_sum_dim_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sparse_sum_dtype (self @ local) ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg__sparse_sum_dtype(%{self : tensor value}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _spdiags ~(diagonals @ local) ~(offsets @ local) ~shape =
  let diagonals = globalize_tensor diagonals in
  let offsets = globalize_tensor offsets in
  [%c.alloc
    ({|CAMLreturn(atg__spdiags(%{diagonals : tensor value}, %{offsets : tensor value}, %{shape : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _spdiags_out ~(out @ local) ~(diagonals @ local) ~(offsets @ local) ~shape =
  let out = globalize_tensor out in
  let diagonals = globalize_tensor diagonals in
  let offsets = globalize_tensor offsets in
  [%c.alloc
    ({|CAMLreturn(atg__spdiags_out(%{out : tensor value}, %{diagonals : tensor value}, %{offsets : tensor value}, %{shape : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _spsolve ~(a @ local) ~(b @ local) ~left =
  let a = globalize_tensor a in
  let b = globalize_tensor b in
  [%c.alloc
    ({|CAMLreturn(atg__spsolve(%{a : tensor value}, %{b : tensor value}, Int_val(%{left : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _stack (tensors @ local) ~dim =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg__stack(%{tensors : tensor list value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _stack_out ~(out @ local) (tensors @ local) ~dim =
  let out = globalize_tensor out in
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg__stack_out(%{out : tensor value}, %{tensors : tensor list value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _standard_gamma (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__standard_gamma(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _standard_gamma_grad (self @ local) ~(output @ local) =
  let self = globalize_tensor self in
  let output = globalize_tensor output in
  [%c.alloc
    ({|CAMLreturn(atg__standard_gamma_grad(%{self : tensor value}, %{output : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _standard_gamma_grad_out ~(out @ local) (self @ local) ~(output @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let output = globalize_tensor output in
  [%c.alloc
    ({|CAMLreturn(atg__standard_gamma_grad_out(%{out : tensor value}, %{self : tensor value}, %{output : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _standard_gamma_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__standard_gamma_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_ambiguous_defaults ~(dummy @ local) ~a ~b =
  let dummy = globalize_tensor dummy in
  [%c.alloc
    ({|CAMLreturn(atg__test_ambiguous_defaults(%{dummy : tensor value}, %{a : int}, %{b : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_ambiguous_defaults_b ~(dummy @ local) ~a ~b =
  let dummy = globalize_tensor dummy in
  [%c.alloc
    ({|CAMLreturn(atg__test_ambiguous_defaults_b(%{dummy : tensor value}, %{a : int}, String_val(%{b : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_autograd_multiple_dispatch (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__test_autograd_multiple_dispatch(%{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_autograd_multiple_dispatch_fullcoverage_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__test_autograd_multiple_dispatch_fullcoverage_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_autograd_multiple_dispatch_ntonly (self @ local) ~b =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__test_autograd_multiple_dispatch_ntonly(%{self : tensor value}, Int_val(%{b : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_autograd_multiple_dispatch_view (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__test_autograd_multiple_dispatch_view(%{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_autograd_multiple_dispatch_view_copy (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__test_autograd_multiple_dispatch_view_copy(%{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_autograd_multiple_dispatch_view_copy_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__test_autograd_multiple_dispatch_view_copy_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_check_tensor (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__test_check_tensor(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_functorch_fallback (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg__test_functorch_fallback(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_functorch_fallback_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg__test_functorch_fallback_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_optional_filled_intlist ~(values @ local) ~addends =
  let values = globalize_tensor values in
  [%c.alloc
    ({|CAMLreturn(atg__test_optional_filled_intlist(%{values : tensor value}, %{addends : int list option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_optional_filled_intlist_out ~(out @ local) ~(values @ local) ~addends =
  let out = globalize_tensor out in
  let values = globalize_tensor values in
  [%c.alloc
    ({|CAMLreturn(atg__test_optional_filled_intlist_out(%{out : tensor value}, %{values : tensor value}, %{addends : int list option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_optional_floatlist ~(values @ local) ~addends =
  let values = globalize_tensor values in
  [%c.alloc
    ({|CAMLreturn(atg__test_optional_floatlist(%{values : tensor value}, %{addends : float list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_optional_floatlist_out ~(out @ local) ~(values @ local) ~addends =
  let out = globalize_tensor out in
  let values = globalize_tensor values in
  [%c.alloc
    ({|CAMLreturn(atg__test_optional_floatlist_out(%{out : tensor value}, %{values : tensor value}, %{addends : float list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_optional_intlist ~(values @ local) ~addends =
  let values = globalize_tensor values in
  [%c.alloc
    ({|CAMLreturn(atg__test_optional_intlist(%{values : tensor value}, %{addends : int list option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_optional_intlist_out ~(out @ local) ~(values @ local) ~addends =
  let out = globalize_tensor out in
  let values = globalize_tensor values in
  [%c.alloc
    ({|CAMLreturn(atg__test_optional_intlist_out(%{out : tensor value}, %{values : tensor value}, %{addends : int list option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_parallel_materialize (self @ local) ~num_parallel ~skip_first =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__test_parallel_materialize(%{self : tensor value}, %{num_parallel : int}, Int_val(%{skip_first : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_serialization_subcmul ?alpha (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg__test_serialization_subcmul(%{self : tensor value}, %{other : tensor value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_string_default ~(dummy @ local) ~a ~b =
  let dummy = globalize_tensor dummy in
  [%c.alloc
    ({|CAMLreturn(atg__test_string_default(%{dummy : tensor value}, String_val(%{a : string value}), String_val(%{b : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_warn_in_autograd (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__test_warn_in_autograd(%{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _test_warn_in_autograd_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__test_warn_in_autograd_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _thnn_differentiable_gru_cell_backward
  ~(grad_hy @ local)
  ~(input_gates @ local)
  ~(hidden_gates @ local)
  ~(hx @ local)
  ~(input_bias @ local)
  ~(hidden_bias @ local)
  =
  let grad_hy = globalize_tensor grad_hy in
  let input_gates = globalize_tensor input_gates in
  let hidden_gates = globalize_tensor hidden_gates in
  let hx = globalize_tensor hx in
  let input_bias = [%globalize: tensor option] input_bias in
  let hidden_bias = [%globalize: tensor option] hidden_bias in
  let result0, result1, result2, result3, result4 =
    [%c.alloc
      ({|CAMLreturn(atg__thnn_differentiable_gru_cell_backward(%{grad_hy : tensor value}, %{input_gates : tensor value}, %{hidden_gates : tensor value}, %{hx : tensor value}, %{input_bias : tensor option value}, %{hidden_bias : tensor option value}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3
  , wrap_managed_tensor result4 )
;;

let _thnn_differentiable_lstm_cell_backward
  ~(grad_hy @ local)
  ~(grad_cy @ local)
  ~(input_gates @ local)
  ~(hidden_gates @ local)
  ~(input_bias @ local)
  ~(hidden_bias @ local)
  ~(cx @ local)
  ~(cy @ local)
  =
  let grad_hy = [%globalize: tensor option] grad_hy in
  let grad_cy = [%globalize: tensor option] grad_cy in
  let input_gates = globalize_tensor input_gates in
  let hidden_gates = globalize_tensor hidden_gates in
  let input_bias = [%globalize: tensor option] input_bias in
  let hidden_bias = [%globalize: tensor option] hidden_bias in
  let cx = globalize_tensor cx in
  let cy = globalize_tensor cy in
  let result0, result1, result2, result3, result4 =
    [%c.alloc
      ({|CAMLreturn(atg__thnn_differentiable_lstm_cell_backward(%{grad_hy : tensor option value}, %{grad_cy : tensor option value}, %{input_gates : tensor value}, %{hidden_gates : tensor value}, %{input_bias : tensor option value}, %{hidden_bias : tensor option value}, %{cx : tensor value}, %{cy : tensor value}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3
  , wrap_managed_tensor result4 )
;;

let _thnn_fused_gru_cell
  ~(input_gates @ local)
  ~(hidden_gates @ local)
  ~(hx @ local)
  ~(input_bias @ local)
  ~(hidden_bias @ local)
  =
  let input_gates = globalize_tensor input_gates in
  let hidden_gates = globalize_tensor hidden_gates in
  let hx = globalize_tensor hx in
  let input_bias = [%globalize: tensor option] input_bias in
  let hidden_bias = [%globalize: tensor option] hidden_bias in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__thnn_fused_gru_cell(%{input_gates : tensor value}, %{hidden_gates : tensor value}, %{hx : tensor value}, %{input_bias : tensor option value}, %{hidden_bias : tensor option value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _thnn_fused_gru_cell_backward ~(grad_hy @ local) ~(workspace @ local) ~has_bias =
  let grad_hy = globalize_tensor grad_hy in
  let workspace = globalize_tensor workspace in
  let result0, result1, result2, result3, result4 =
    [%c.alloc
      ({|CAMLreturn(atg__thnn_fused_gru_cell_backward(%{grad_hy : tensor value}, %{workspace : tensor value}, Int_val(%{has_bias : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3
  , wrap_managed_tensor result4 )
;;

let _thnn_fused_gru_cell_backward_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  ~(out3 @ local)
  ~(out4 @ local)
  ~(grad_hy @ local)
  ~(workspace @ local)
  ~has_bias
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let out3 = globalize_tensor out3 in
  let out4 = globalize_tensor out4 in
  let grad_hy = globalize_tensor grad_hy in
  let workspace = globalize_tensor workspace in
  let result0, result1, result2, result3, result4 =
    [%c.alloc
      ({|CAMLreturn(atg__thnn_fused_gru_cell_backward_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{out3 : tensor value}, %{out4 : tensor value}, %{grad_hy : tensor value}, %{workspace : tensor value}, Int_val(%{has_bias : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3
  , wrap_managed_tensor result4 )
;;

let _thnn_fused_gru_cell_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(input_gates @ local)
  ~(hidden_gates @ local)
  ~(hx @ local)
  ~(input_bias @ local)
  ~(hidden_bias @ local)
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let input_gates = globalize_tensor input_gates in
  let hidden_gates = globalize_tensor hidden_gates in
  let hx = globalize_tensor hx in
  let input_bias = [%globalize: tensor option] input_bias in
  let hidden_bias = [%globalize: tensor option] hidden_bias in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__thnn_fused_gru_cell_out(%{out0 : tensor value}, %{out1 : tensor value}, %{input_gates : tensor value}, %{hidden_gates : tensor value}, %{hx : tensor value}, %{input_bias : tensor option value}, %{hidden_bias : tensor option value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _thnn_fused_lstm_cell
  ~(input_gates @ local)
  ~(hidden_gates @ local)
  ~(cx @ local)
  ~(input_bias @ local)
  ~(hidden_bias @ local)
  =
  let input_gates = globalize_tensor input_gates in
  let hidden_gates = globalize_tensor hidden_gates in
  let cx = globalize_tensor cx in
  let input_bias = [%globalize: tensor option] input_bias in
  let hidden_bias = [%globalize: tensor option] hidden_bias in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__thnn_fused_lstm_cell(%{input_gates : tensor value}, %{hidden_gates : tensor value}, %{cx : tensor value}, %{input_bias : tensor option value}, %{hidden_bias : tensor option value}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _thnn_fused_lstm_cell_backward
  ~(grad_hy @ local)
  ~(grad_cy @ local)
  ~(cx @ local)
  ~(cy @ local)
  ~(workspace @ local)
  ~has_bias
  =
  let grad_hy = [%globalize: tensor option] grad_hy in
  let grad_cy = [%globalize: tensor option] grad_cy in
  let cx = globalize_tensor cx in
  let cy = globalize_tensor cy in
  let workspace = globalize_tensor workspace in
  let result0, result1, result2, result3, result4 =
    [%c.alloc
      ({|CAMLreturn(atg__thnn_fused_lstm_cell_backward(%{grad_hy : tensor option value}, %{grad_cy : tensor option value}, %{cx : tensor value}, %{cy : tensor value}, %{workspace : tensor value}, Int_val(%{has_bias : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3
  , wrap_managed_tensor result4 )
;;

let _thnn_fused_lstm_cell_backward_impl
  ~(grad_hy @ local)
  ~(grad_cy @ local)
  ~(cx @ local)
  ~(cy @ local)
  ~(workspace @ local)
  ~has_bias
  =
  let grad_hy = [%globalize: tensor option] grad_hy in
  let grad_cy = [%globalize: tensor option] grad_cy in
  let cx = globalize_tensor cx in
  let cy = globalize_tensor cy in
  let workspace = globalize_tensor workspace in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__thnn_fused_lstm_cell_backward_impl(%{grad_hy : tensor option value}, %{grad_cy : tensor option value}, %{cx : tensor value}, %{cy : tensor value}, %{workspace : tensor value}, Int_val(%{has_bias : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _thnn_fused_lstm_cell_backward_impl_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  ~(grad_hy @ local)
  ~(grad_cy @ local)
  ~(cx @ local)
  ~(cy @ local)
  ~(workspace @ local)
  ~has_bias
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let grad_hy = [%globalize: tensor option] grad_hy in
  let grad_cy = [%globalize: tensor option] grad_cy in
  let cx = globalize_tensor cx in
  let cy = globalize_tensor cy in
  let workspace = globalize_tensor workspace in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__thnn_fused_lstm_cell_backward_impl_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{grad_hy : tensor option value}, %{grad_cy : tensor option value}, %{cx : tensor value}, %{cy : tensor value}, %{workspace : tensor value}, Int_val(%{has_bias : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _thnn_fused_lstm_cell_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  ~(input_gates @ local)
  ~(hidden_gates @ local)
  ~(cx @ local)
  ~(input_bias @ local)
  ~(hidden_bias @ local)
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let input_gates = globalize_tensor input_gates in
  let hidden_gates = globalize_tensor hidden_gates in
  let cx = globalize_tensor cx in
  let input_bias = [%globalize: tensor option] input_bias in
  let hidden_bias = [%globalize: tensor option] hidden_bias in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__thnn_fused_lstm_cell_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{input_gates : tensor value}, %{hidden_gates : tensor value}, %{cx : tensor value}, %{input_bias : tensor option value}, %{hidden_bias : tensor option value}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _to_copy (self @ local) ~options ~non_blocking =
  let self = globalize_tensor self in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg__to_copy(%{self : tensor value}, %{options_kind : int}, %{options_device : int}, Int_val(%{non_blocking : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _to_copy_out ~(out @ local) (self @ local) ~non_blocking =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__to_copy_out(%{out : tensor value}, %{self : tensor value}, Int_val(%{non_blocking : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _to_cpu (tensors @ local) =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg__to_cpu(%{tensors : tensor list value}));|} : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let _to_dense (self @ local) ~dtype ~masked_grad =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg__to_dense(%{self : tensor value}, %{dtype : int}, Int_val(%{masked_grad : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _to_dense_out ~(out @ local) (self @ local) ~dtype ~masked_grad =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg__to_dense_out(%{out : tensor value}, %{self : tensor value}, %{dtype : int}, Int_val(%{masked_grad : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _to_sparse_bsc (self @ local) ~blocksize ~dense_dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__to_sparse_bsc(%{self : tensor value}, %{blocksize : int list value}, %{dense_dim : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _to_sparse_bsc_out ~(out @ local) (self @ local) ~blocksize ~dense_dim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__to_sparse_bsc_out(%{out : tensor value}, %{self : tensor value}, %{blocksize : int list value}, %{dense_dim : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _to_sparse_bsr (self @ local) ~blocksize ~dense_dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__to_sparse_bsr(%{self : tensor value}, %{blocksize : int list value}, %{dense_dim : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _to_sparse_bsr_out ~(out @ local) (self @ local) ~blocksize ~dense_dim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__to_sparse_bsr_out(%{out : tensor value}, %{self : tensor value}, %{blocksize : int list value}, %{dense_dim : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _to_sparse_csc (self @ local) ~dense_dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__to_sparse_csc(%{self : tensor value}, %{dense_dim : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _to_sparse_csc_out ~(out @ local) (self @ local) ~dense_dim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__to_sparse_csc_out(%{out : tensor value}, %{self : tensor value}, %{dense_dim : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _to_sparse_csr (self @ local) ~dense_dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__to_sparse_csr(%{self : tensor value}, %{dense_dim : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _to_sparse_csr_out ~(out @ local) (self @ local) ~dense_dim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__to_sparse_csr_out(%{out : tensor value}, %{self : tensor value}, %{dense_dim : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _to_sparse_semi_structured ~(dense @ local) =
  let dense = globalize_tensor dense in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__to_sparse_semi_structured(%{dense : tensor value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _transform_bias_rescale_qkv ~(qkv @ local) ~(qkv_bias @ local) ~num_heads =
  let qkv = globalize_tensor qkv in
  let qkv_bias = globalize_tensor qkv_bias in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__transform_bias_rescale_qkv(%{qkv : tensor value}, %{qkv_bias : tensor value}, %{num_heads : int}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _transform_bias_rescale_qkv_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  ~(qkv @ local)
  ~(qkv_bias @ local)
  ~num_heads
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let qkv = globalize_tensor qkv in
  let qkv_bias = globalize_tensor qkv_bias in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__transform_bias_rescale_qkv_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{qkv : tensor value}, %{qkv_bias : tensor value}, %{num_heads : int}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _transformer_encoder_layer_fwd
  ~(src @ local)
  ~embed_dim
  ~num_heads
  ~(qkv_weight @ local)
  ~(qkv_bias @ local)
  ~(proj_weight @ local)
  ~(proj_bias @ local)
  ~use_gelu
  ~norm_first
  ~eps
  ~(norm_weight_1 @ local)
  ~(norm_bias_1 @ local)
  ~(norm_weight_2 @ local)
  ~(norm_bias_2 @ local)
  ~(ffn_weight_1 @ local)
  ~(ffn_bias_1 @ local)
  ~(ffn_weight_2 @ local)
  ~(ffn_bias_2 @ local)
  ~(mask @ local)
  ~mask_type
  =
  let src = globalize_tensor src in
  let qkv_weight = globalize_tensor qkv_weight in
  let qkv_bias = globalize_tensor qkv_bias in
  let proj_weight = globalize_tensor proj_weight in
  let proj_bias = globalize_tensor proj_bias in
  let norm_weight_1 = globalize_tensor norm_weight_1 in
  let norm_bias_1 = globalize_tensor norm_bias_1 in
  let norm_weight_2 = globalize_tensor norm_weight_2 in
  let norm_bias_2 = globalize_tensor norm_bias_2 in
  let ffn_weight_1 = globalize_tensor ffn_weight_1 in
  let ffn_bias_1 = globalize_tensor ffn_bias_1 in
  let ffn_weight_2 = globalize_tensor ffn_weight_2 in
  let ffn_bias_2 = globalize_tensor ffn_bias_2 in
  let mask = [%globalize: tensor option] mask in
  [%c.alloc
    ({|CAMLreturn(atg__transformer_encoder_layer_fwd(%{src : tensor value}, %{embed_dim : int}, %{num_heads : int}, %{qkv_weight : tensor value}, %{qkv_bias : tensor value}, %{proj_weight : tensor value}, %{proj_bias : tensor value}, Int_val(%{use_gelu : bool value}), Int_val(%{norm_first : bool value}), %{eps : float}, %{norm_weight_1 : tensor value}, %{norm_bias_1 : tensor value}, %{norm_weight_2 : tensor value}, %{norm_bias_2 : tensor value}, %{ffn_weight_1 : tensor value}, %{ffn_bias_1 : tensor value}, %{ffn_weight_2 : tensor value}, %{ffn_bias_2 : tensor value}, %{mask : tensor option value}, %{mask_type : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _transformer_encoder_layer_fwd_out
  ~(out @ local)
  ~(src @ local)
  ~embed_dim
  ~num_heads
  ~(qkv_weight @ local)
  ~(qkv_bias @ local)
  ~(proj_weight @ local)
  ~(proj_bias @ local)
  ~use_gelu
  ~norm_first
  ~eps
  ~(norm_weight_1 @ local)
  ~(norm_bias_1 @ local)
  ~(norm_weight_2 @ local)
  ~(norm_bias_2 @ local)
  ~(ffn_weight_1 @ local)
  ~(ffn_bias_1 @ local)
  ~(ffn_weight_2 @ local)
  ~(ffn_bias_2 @ local)
  ~(mask @ local)
  ~mask_type
  =
  let out = globalize_tensor out in
  let src = globalize_tensor src in
  let qkv_weight = globalize_tensor qkv_weight in
  let qkv_bias = globalize_tensor qkv_bias in
  let proj_weight = globalize_tensor proj_weight in
  let proj_bias = globalize_tensor proj_bias in
  let norm_weight_1 = globalize_tensor norm_weight_1 in
  let norm_bias_1 = globalize_tensor norm_bias_1 in
  let norm_weight_2 = globalize_tensor norm_weight_2 in
  let norm_bias_2 = globalize_tensor norm_bias_2 in
  let ffn_weight_1 = globalize_tensor ffn_weight_1 in
  let ffn_bias_1 = globalize_tensor ffn_bias_1 in
  let ffn_weight_2 = globalize_tensor ffn_weight_2 in
  let ffn_bias_2 = globalize_tensor ffn_bias_2 in
  let mask = [%globalize: tensor option] mask in
  [%c.alloc
    ({|CAMLreturn(atg__transformer_encoder_layer_fwd_out(%{out : tensor value}, %{src : tensor value}, %{embed_dim : int}, %{num_heads : int}, %{qkv_weight : tensor value}, %{qkv_bias : tensor value}, %{proj_weight : tensor value}, %{proj_bias : tensor value}, Int_val(%{use_gelu : bool value}), Int_val(%{norm_first : bool value}), %{eps : float}, %{norm_weight_1 : tensor value}, %{norm_bias_1 : tensor value}, %{norm_weight_2 : tensor value}, %{norm_bias_2 : tensor value}, %{ffn_weight_1 : tensor value}, %{ffn_bias_1 : tensor value}, %{ffn_weight_2 : tensor value}, %{ffn_bias_2 : tensor value}, %{mask : tensor option value}, %{mask_type : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _trilinear
  ~(i1 @ local)
  ~(i2 @ local)
  ~(i3 @ local)
  ~expand1
  ~expand2
  ~expand3
  ~sumdim
  ~unroll_dim
  =
  let i1 = globalize_tensor i1 in
  let i2 = globalize_tensor i2 in
  let i3 = globalize_tensor i3 in
  [%c.alloc
    ({|CAMLreturn(atg__trilinear(%{i1 : tensor value}, %{i2 : tensor value}, %{i3 : tensor value}, %{expand1 : int list value}, %{expand2 : int list value}, %{expand3 : int list value}, %{sumdim : int list value}, %{unroll_dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _trilinear_out
  ~(out @ local)
  ~(i1 @ local)
  ~(i2 @ local)
  ~(i3 @ local)
  ~expand1
  ~expand2
  ~expand3
  ~sumdim
  ~unroll_dim
  =
  let out = globalize_tensor out in
  let i1 = globalize_tensor i1 in
  let i2 = globalize_tensor i2 in
  let i3 = globalize_tensor i3 in
  [%c.alloc
    ({|CAMLreturn(atg__trilinear_out(%{out : tensor value}, %{i1 : tensor value}, %{i2 : tensor value}, %{i3 : tensor value}, %{expand1 : int list value}, %{expand2 : int list value}, %{expand3 : int list value}, %{sumdim : int list value}, %{unroll_dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _triton_multi_head_attention
  ~(query @ local)
  ~(key @ local)
  ~(value @ local)
  ~embed_dim
  ~num_head
  ~(qkv_weight @ local)
  ~(qkv_bias @ local)
  ~(proj_weight @ local)
  ~(proj_bias @ local)
  ~(mask @ local)
  =
  let query = globalize_tensor query in
  let key = globalize_tensor key in
  let value = globalize_tensor value in
  let qkv_weight = globalize_tensor qkv_weight in
  let qkv_bias = globalize_tensor qkv_bias in
  let proj_weight = globalize_tensor proj_weight in
  let proj_bias = globalize_tensor proj_bias in
  let mask = [%globalize: tensor option] mask in
  [%c.alloc
    ({|CAMLreturn(atg__triton_multi_head_attention(%{query : tensor value}, %{key : tensor value}, %{value : tensor value}, %{embed_dim : int}, %{num_head : int}, %{qkv_weight : tensor value}, %{qkv_bias : tensor value}, %{proj_weight : tensor value}, %{proj_bias : tensor value}, %{mask : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _triton_multi_head_attention_out
  ~(out @ local)
  ~(query @ local)
  ~(key @ local)
  ~(value @ local)
  ~embed_dim
  ~num_head
  ~(qkv_weight @ local)
  ~(qkv_bias @ local)
  ~(proj_weight @ local)
  ~(proj_bias @ local)
  ~(mask @ local)
  =
  let out = globalize_tensor out in
  let query = globalize_tensor query in
  let key = globalize_tensor key in
  let value = globalize_tensor value in
  let qkv_weight = globalize_tensor qkv_weight in
  let qkv_bias = globalize_tensor qkv_bias in
  let proj_weight = globalize_tensor proj_weight in
  let proj_bias = globalize_tensor proj_bias in
  let mask = [%globalize: tensor option] mask in
  [%c.alloc
    ({|CAMLreturn(atg__triton_multi_head_attention_out(%{out : tensor value}, %{query : tensor value}, %{key : tensor value}, %{value : tensor value}, %{embed_dim : int}, %{num_head : int}, %{qkv_weight : tensor value}, %{qkv_bias : tensor value}, %{proj_weight : tensor value}, %{proj_bias : tensor value}, %{mask : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _triton_scaled_dot_attention ~(q @ local) ~(k @ local) ~(v @ local) ~dropout_p =
  let q = globalize_tensor q in
  let k = globalize_tensor k in
  let v = globalize_tensor v in
  [%c.alloc
    ({|CAMLreturn(atg__triton_scaled_dot_attention(%{q : tensor value}, %{k : tensor value}, %{v : tensor value}, %{dropout_p : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _triton_scaled_dot_attention_out
  ~(out @ local)
  ~(q @ local)
  ~(k @ local)
  ~(v @ local)
  ~dropout_p
  =
  let out = globalize_tensor out in
  let q = globalize_tensor q in
  let k = globalize_tensor k in
  let v = globalize_tensor v in
  [%c.alloc
    ({|CAMLreturn(atg__triton_scaled_dot_attention_out(%{out : tensor value}, %{q : tensor value}, %{k : tensor value}, %{v : tensor value}, %{dropout_p : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _unique (self @ local) ~sorted ~return_inverse =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__unique(%{self : tensor value}, Int_val(%{sorted : bool value}), Int_val(%{return_inverse : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _unique2 (self @ local) ~sorted ~return_inverse ~return_counts =
  let self = globalize_tensor self in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__unique2(%{self : tensor value}, Int_val(%{sorted : bool value}), Int_val(%{return_inverse : bool value}), Int_val(%{return_counts : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _unique2_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  (self @ local)
  ~sorted
  ~return_inverse
  ~return_counts
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let self = globalize_tensor self in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__unique2_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{self : tensor value}, Int_val(%{sorted : bool value}), Int_val(%{return_inverse : bool value}), Int_val(%{return_counts : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _unique_out ~(out0 @ local) ~(out1 @ local) (self @ local) ~sorted ~return_inverse =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__unique_out(%{out0 : tensor value}, %{out1 : tensor value}, %{self : tensor value}, Int_val(%{sorted : bool value}), Int_val(%{return_inverse : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _unpack_dual ~(dual @ local) ~level =
  let dual = globalize_tensor dual in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__unpack_dual(%{dual : tensor value}, %{level : int}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _unsafe_view (self @ local) ~size =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__unsafe_view(%{self : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _unsafe_view_out ~(out @ local) (self @ local) ~size =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__unsafe_view_out(%{out : tensor value}, %{self : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_bicubic2d_aa (self @ local) ~output_size ~align_corners ~scales_h ~scales_w =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_bicubic2d_aa(%{self : tensor value}, %{output_size : int list value}, Int_val(%{align_corners : bool value}), %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_bicubic2d_aa_backward
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~align_corners
  ~scales_h
  ~scales_w
  =
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_bicubic2d_aa_backward(%{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, Int_val(%{align_corners : bool value}), %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_bicubic2d_aa_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~align_corners
  ~scales_h
  ~scales_w
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_bicubic2d_aa_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, Int_val(%{align_corners : bool value}), %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_bicubic2d_aa_out
  ~(out @ local)
  (self @ local)
  ~output_size
  ~align_corners
  ~scales_h
  ~scales_w
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_bicubic2d_aa_out(%{out : tensor value}, %{self : tensor value}, %{output_size : int list value}, Int_val(%{align_corners : bool value}), %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_bicubic2d_aa_vec (input @ local) ~output_size ~align_corners ~scale_factors =
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_bicubic2d_aa_vec(%{input : tensor value}, %{output_size : int list option value}, Int_val(%{align_corners : bool value}), %{scale_factors : float list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_bilinear2d_aa (self @ local) ~output_size ~align_corners ~scales_h ~scales_w
  =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_bilinear2d_aa(%{self : tensor value}, %{output_size : int list value}, Int_val(%{align_corners : bool value}), %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_bilinear2d_aa_backward
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~align_corners
  ~scales_h
  ~scales_w
  =
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_bilinear2d_aa_backward(%{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, Int_val(%{align_corners : bool value}), %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_bilinear2d_aa_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~align_corners
  ~scales_h
  ~scales_w
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_bilinear2d_aa_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, Int_val(%{align_corners : bool value}), %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_bilinear2d_aa_out
  ~(out @ local)
  (self @ local)
  ~output_size
  ~align_corners
  ~scales_h
  ~scales_w
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_bilinear2d_aa_out(%{out : tensor value}, %{self : tensor value}, %{output_size : int list value}, Int_val(%{align_corners : bool value}), %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_bilinear2d_aa_vec (input @ local) ~output_size ~align_corners ~scale_factors
  =
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_bilinear2d_aa_vec(%{input : tensor value}, %{output_size : int list option value}, Int_val(%{align_corners : bool value}), %{scale_factors : float list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_nearest_exact1d (self @ local) ~output_size ~scales =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_nearest_exact1d(%{self : tensor value}, %{output_size : int list value}, %{scales : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_nearest_exact1d_backward
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~scales
  =
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_nearest_exact1d_backward(%{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, %{scales : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_nearest_exact1d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~scales
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_nearest_exact1d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, %{scales : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_nearest_exact1d_out ~(out @ local) (self @ local) ~output_size ~scales =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_nearest_exact1d_out(%{out : tensor value}, %{self : tensor value}, %{output_size : int list value}, %{scales : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_nearest_exact1d_vec (input @ local) ~output_size ~scale_factors =
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_nearest_exact1d_vec(%{input : tensor value}, %{output_size : int list option value}, %{scale_factors : float list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_nearest_exact2d (self @ local) ~output_size ~scales_h ~scales_w =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_nearest_exact2d(%{self : tensor value}, %{output_size : int list value}, %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_nearest_exact2d_backward
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~scales_h
  ~scales_w
  =
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_nearest_exact2d_backward(%{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_nearest_exact2d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~scales_h
  ~scales_w
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_nearest_exact2d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_nearest_exact2d_out
  ~(out @ local)
  (self @ local)
  ~output_size
  ~scales_h
  ~scales_w
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_nearest_exact2d_out(%{out : tensor value}, %{self : tensor value}, %{output_size : int list value}, %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_nearest_exact2d_vec (input @ local) ~output_size ~scale_factors =
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_nearest_exact2d_vec(%{input : tensor value}, %{output_size : int list option value}, %{scale_factors : float list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_nearest_exact3d (self @ local) ~output_size ~scales_d ~scales_h ~scales_w =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_nearest_exact3d(%{self : tensor value}, %{output_size : int list value}, %{scales_d : float option value}, %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_nearest_exact3d_backward
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~scales_d
  ~scales_h
  ~scales_w
  =
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_nearest_exact3d_backward(%{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, %{scales_d : float option value}, %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_nearest_exact3d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  ~output_size
  ~input_size
  ~scales_d
  ~scales_h
  ~scales_w
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_nearest_exact3d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{output_size : int list value}, %{input_size : int list value}, %{scales_d : float option value}, %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_nearest_exact3d_out
  ~(out @ local)
  (self @ local)
  ~output_size
  ~scales_d
  ~scales_h
  ~scales_w
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_nearest_exact3d_out(%{out : tensor value}, %{self : tensor value}, %{output_size : int list value}, %{scales_d : float option value}, %{scales_h : float option value}, %{scales_w : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _upsample_nearest_exact3d_vec (input @ local) ~output_size ~scale_factors =
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg__upsample_nearest_exact3d_vec(%{input : tensor value}, %{output_size : int list option value}, %{scale_factors : float list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _use_cudnn_ctc_loss
  ~(log_probs @ local)
  ~(targets @ local)
  ~input_lengths
  ~target_lengths
  ~blank
  =
  let log_probs = globalize_tensor log_probs in
  let targets = globalize_tensor targets in
  [%c.alloc
    ({|CAMLreturn(atg__use_cudnn_ctc_loss(%{log_probs : tensor value}, %{targets : tensor value}, %{input_lengths : int list value}, %{target_lengths : int list value}, %{blank : int}));|}
     : bool value)]
;;

let _use_cudnn_ctc_loss_tensor
  ~(log_probs @ local)
  ~(targets @ local)
  ~(input_lengths @ local)
  ~(target_lengths @ local)
  ~blank
  =
  let log_probs = globalize_tensor log_probs in
  let targets = globalize_tensor targets in
  let input_lengths = globalize_tensor input_lengths in
  let target_lengths = globalize_tensor target_lengths in
  [%c.alloc
    ({|CAMLreturn(atg__use_cudnn_ctc_loss_tensor(%{log_probs : tensor value}, %{targets : tensor value}, %{input_lengths : tensor value}, %{target_lengths : tensor value}, %{blank : int}));|}
     : bool value)]
;;

let _use_cudnn_rnn_flatten_weight () =
  [%c.alloc ({|CAMLreturn(atg__use_cudnn_rnn_flatten_weight());|} : bool value)]
;;

let _validate_compressed_sparse_indices
  ~is_crow
  ~(compressed_idx @ local)
  ~(plain_idx @ local)
  ~cdim
  ~dim
  ~nnz
  =
  let compressed_idx = globalize_tensor compressed_idx in
  let plain_idx = globalize_tensor plain_idx in
  [%c.alloc
    {|atg__validate_compressed_sparse_indices(Int_val(%{is_crow : bool value}), %{compressed_idx : tensor value}, %{plain_idx : tensor value}, %{cdim : int}, %{dim : int}, %{nnz : int});|}]
;;

let _validate_sparse_bsc_tensor_args
  ~(ccol_indices @ local)
  ~(row_indices @ local)
  ~(values @ local)
  ~size
  =
  let ccol_indices = globalize_tensor ccol_indices in
  let row_indices = globalize_tensor row_indices in
  let values = globalize_tensor values in
  [%c.alloc
    {|atg__validate_sparse_bsc_tensor_args(%{ccol_indices : tensor value}, %{row_indices : tensor value}, %{values : tensor value}, %{size : int list value});|}]
;;

let _validate_sparse_bsr_tensor_args
  ~(crow_indices @ local)
  ~(col_indices @ local)
  ~(values @ local)
  ~size
  =
  let crow_indices = globalize_tensor crow_indices in
  let col_indices = globalize_tensor col_indices in
  let values = globalize_tensor values in
  [%c.alloc
    {|atg__validate_sparse_bsr_tensor_args(%{crow_indices : tensor value}, %{col_indices : tensor value}, %{values : tensor value}, %{size : int list value});|}]
;;

let _validate_sparse_csc_tensor_args
  ~(ccol_indices @ local)
  ~(row_indices @ local)
  ~(values @ local)
  ~size
  =
  let ccol_indices = globalize_tensor ccol_indices in
  let row_indices = globalize_tensor row_indices in
  let values = globalize_tensor values in
  [%c.alloc
    {|atg__validate_sparse_csc_tensor_args(%{ccol_indices : tensor value}, %{row_indices : tensor value}, %{values : tensor value}, %{size : int list value});|}]
;;

let _values (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg__values(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _values_copy (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__values_copy(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _values_copy_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__values_copy_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _version (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg__version(%{self : tensor value}));|} : int64 value)]
;;

let _weight_int4pack_mm
  (self @ local)
  ~(mat2 @ local)
  ~qgroupsize
  ~(qscaleandzeros @ local)
  =
  let self = globalize_tensor self in
  let mat2 = globalize_tensor mat2 in
  let qscaleandzeros = globalize_tensor qscaleandzeros in
  [%c.alloc
    ({|CAMLreturn(atg__weight_int4pack_mm(%{self : tensor value}, %{mat2 : tensor value}, %{qgroupsize : int}, %{qscaleandzeros : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _weight_int4pack_mm_for_cpu
  (self @ local)
  ~(mat2 @ local)
  ~qgroupsize
  ~(qscaleandzeros @ local)
  =
  let self = globalize_tensor self in
  let mat2 = globalize_tensor mat2 in
  let qscaleandzeros = globalize_tensor qscaleandzeros in
  [%c.alloc
    ({|CAMLreturn(atg__weight_int4pack_mm_for_cpu(%{self : tensor value}, %{mat2 : tensor value}, %{qgroupsize : int}, %{qscaleandzeros : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _weight_int8pack_mm (self @ local) ~(mat2 @ local) ~(scales @ local) =
  let self = globalize_tensor self in
  let mat2 = globalize_tensor mat2 in
  let scales = globalize_tensor scales in
  [%c.alloc
    ({|CAMLreturn(atg__weight_int8pack_mm(%{self : tensor value}, %{mat2 : tensor value}, %{scales : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _weight_norm ~(v @ local) ~(g @ local) ~dim =
  let v = globalize_tensor v in
  let g = globalize_tensor g in
  [%c.alloc
    ({|CAMLreturn(atg__weight_norm(%{v : tensor value}, %{g : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _weight_norm_differentiable_backward
  ~(grad_w @ local)
  ~(saved_v @ local)
  ~(saved_g @ local)
  ~(saved_norms @ local)
  ~dim
  =
  let grad_w = globalize_tensor grad_w in
  let saved_v = globalize_tensor saved_v in
  let saved_g = globalize_tensor saved_g in
  let saved_norms = globalize_tensor saved_norms in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__weight_norm_differentiable_backward(%{grad_w : tensor value}, %{saved_v : tensor value}, %{saved_g : tensor value}, %{saved_norms : tensor value}, %{dim : int}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _weight_norm_interface ~(v @ local) ~(g @ local) ~dim =
  let v = globalize_tensor v in
  let g = globalize_tensor g in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__weight_norm_interface(%{v : tensor value}, %{g : tensor value}, %{dim : int}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _weight_norm_interface_backward
  ~(grad_w @ local)
  ~(saved_v @ local)
  ~(saved_g @ local)
  ~(saved_norms @ local)
  ~dim
  =
  let grad_w = globalize_tensor grad_w in
  let saved_v = globalize_tensor saved_v in
  let saved_g = globalize_tensor saved_g in
  let saved_norms = globalize_tensor saved_norms in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__weight_norm_interface_backward(%{grad_w : tensor value}, %{saved_v : tensor value}, %{saved_g : tensor value}, %{saved_norms : tensor value}, %{dim : int}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _weight_norm_interface_backward_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(grad_w @ local)
  ~(saved_v @ local)
  ~(saved_g @ local)
  ~(saved_norms @ local)
  ~dim
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let grad_w = globalize_tensor grad_w in
  let saved_v = globalize_tensor saved_v in
  let saved_g = globalize_tensor saved_g in
  let saved_norms = globalize_tensor saved_norms in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__weight_norm_interface_backward_out(%{out0 : tensor value}, %{out1 : tensor value}, %{grad_w : tensor value}, %{saved_v : tensor value}, %{saved_g : tensor value}, %{saved_norms : tensor value}, %{dim : int}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _weight_norm_interface_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(v @ local)
  ~(g @ local)
  ~dim
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let v = globalize_tensor v in
  let g = globalize_tensor g in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__weight_norm_interface_out(%{out0 : tensor value}, %{out1 : tensor value}, %{v : tensor value}, %{g : tensor value}, %{dim : int}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _wrapped_linear_prepack
  ~(weight @ local)
  ~(weight_scale @ local)
  ~(weight_zero_point @ local)
  ~(bias @ local)
  =
  let weight = globalize_tensor weight in
  let weight_scale = globalize_tensor weight_scale in
  let weight_zero_point = globalize_tensor weight_zero_point in
  let bias = globalize_tensor bias in
  [%c.alloc
    ({|CAMLreturn(atg__wrapped_linear_prepack(%{weight : tensor value}, %{weight_scale : tensor value}, %{weight_zero_point : tensor value}, %{bias : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _wrapped_quantized_linear_prepacked
  (input @ local)
  ~(input_scale @ local)
  ~(input_zero_point @ local)
  ~(packed_weight @ local)
  ~(output_scale @ local)
  ~(output_zero_point @ local)
  ~out_channel
  =
  let input = globalize_tensor input in
  let input_scale = globalize_tensor input_scale in
  let input_zero_point = globalize_tensor input_zero_point in
  let packed_weight = globalize_tensor packed_weight in
  let output_scale = globalize_tensor output_scale in
  let output_zero_point = globalize_tensor output_zero_point in
  [%c.alloc
    ({|CAMLreturn(atg__wrapped_quantized_linear_prepacked(%{input : tensor value}, %{input_scale : tensor value}, %{input_zero_point : tensor value}, %{packed_weight : tensor value}, %{output_scale : tensor value}, %{output_zero_point : tensor value}, %{out_channel : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let abs (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_abs(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let abs_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_abs_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let abs_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_abs_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let absolute (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_absolute(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let absolute_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_absolute_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let absolute_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_absolute_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let acos (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_acos(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let acos_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_acos_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let acos_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_acos_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let acosh (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_acosh(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let acosh_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_acosh_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let acosh_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_acosh_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let adaptive_avg_pool1d (self @ local) ~output_size =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_adaptive_avg_pool1d(%{self : tensor value}, %{output_size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let adaptive_avg_pool1d_out ~(out @ local) (self @ local) ~output_size =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_adaptive_avg_pool1d_out(%{out : tensor value}, %{self : tensor value}, %{output_size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let adaptive_avg_pool2d (self @ local) ~output_size =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_adaptive_avg_pool2d(%{self : tensor value}, %{output_size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let adaptive_avg_pool2d_out ~(out @ local) (self @ local) ~output_size =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_adaptive_avg_pool2d_out(%{out : tensor value}, %{self : tensor value}, %{output_size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let adaptive_avg_pool3d (self @ local) ~output_size =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_adaptive_avg_pool3d(%{self : tensor value}, %{output_size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let adaptive_avg_pool3d_backward
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_adaptive_avg_pool3d_backward(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let adaptive_avg_pool3d_out ~(out @ local) (self @ local) ~output_size =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_adaptive_avg_pool3d_out(%{out : tensor value}, %{self : tensor value}, %{output_size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let adaptive_max_pool1d (self @ local) ~output_size =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_adaptive_max_pool1d(%{self : tensor value}, %{output_size : int list value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let adaptive_max_pool2d (self @ local) ~output_size =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_adaptive_max_pool2d(%{self : tensor value}, %{output_size : int list value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let adaptive_max_pool2d_backward ~(grad_output @ local) (self @ local) ~(indices @ local) =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let indices = globalize_tensor indices in
  [%c.alloc
    ({|CAMLreturn(atg_adaptive_max_pool2d_backward(%{grad_output : tensor value}, %{self : tensor value}, %{indices : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let adaptive_max_pool2d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~(indices @ local)
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let indices = globalize_tensor indices in
  [%c.alloc
    ({|CAMLreturn(atg_adaptive_max_pool2d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{indices : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let adaptive_max_pool2d_out ~(out @ local) ~(indices @ local) (self @ local) ~output_size =
  let out = globalize_tensor out in
  let indices = globalize_tensor indices in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_adaptive_max_pool2d_out(%{out : tensor value}, %{indices : tensor value}, %{self : tensor value}, %{output_size : int list value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let adaptive_max_pool3d (self @ local) ~output_size =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_adaptive_max_pool3d(%{self : tensor value}, %{output_size : int list value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let adaptive_max_pool3d_backward ~(grad_output @ local) (self @ local) ~(indices @ local) =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let indices = globalize_tensor indices in
  [%c.alloc
    ({|CAMLreturn(atg_adaptive_max_pool3d_backward(%{grad_output : tensor value}, %{self : tensor value}, %{indices : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let adaptive_max_pool3d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~(indices @ local)
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let indices = globalize_tensor indices in
  [%c.alloc
    ({|CAMLreturn(atg_adaptive_max_pool3d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{indices : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let adaptive_max_pool3d_out ~(out @ local) ~(indices @ local) (self @ local) ~output_size =
  let out = globalize_tensor out in
  let indices = globalize_tensor indices in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_adaptive_max_pool3d_out(%{out : tensor value}, %{indices : tensor value}, %{self : tensor value}, %{output_size : int list value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let add ?alpha (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_add(%{self : tensor value}, %{other : tensor value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let add_ ?alpha (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_add_(%{self : tensor value}, %{other : tensor value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let add_out ?alpha ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_add_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let add_scalar ?alpha (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_add_scalar(%{self : tensor value}, %{other : scalar value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let add_scalar_ ?alpha (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_add_scalar_(%{self : tensor value}, %{other : scalar value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let add_scalar_out ?alpha ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_add_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let addbmm ?beta ?alpha (self @ local) ~(batch1 @ local) ~(batch2 @ local) =
  let self = globalize_tensor self in
  let batch1 = globalize_tensor batch1 in
  let batch2 = globalize_tensor batch2 in
  [%c.alloc
    ({|CAMLreturn(atg_addbmm(%{self : tensor value}, %{batch1 : tensor value}, %{batch2 : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let addbmm_ ?beta ?alpha (self @ local) ~(batch1 @ local) ~(batch2 @ local) =
  let self = globalize_tensor self in
  let batch1 = globalize_tensor batch1 in
  let batch2 = globalize_tensor batch2 in
  [%c.alloc
    ({|CAMLreturn(atg_addbmm_(%{self : tensor value}, %{batch1 : tensor value}, %{batch2 : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let addbmm_out
  ?beta
  ?alpha
  ~(out @ local)
  (self @ local)
  ~(batch1 @ local)
  ~(batch2 @ local)
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let batch1 = globalize_tensor batch1 in
  let batch2 = globalize_tensor batch2 in
  [%c.alloc
    ({|CAMLreturn(atg_addbmm_out(%{out : tensor value}, %{self : tensor value}, %{batch1 : tensor value}, %{batch2 : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let addcdiv ?value (self @ local) ~(tensor1 @ local) ~(tensor2 @ local) =
  let self = globalize_tensor self in
  let tensor1 = globalize_tensor tensor1 in
  let tensor2 = globalize_tensor tensor2 in
  [%c.alloc
    ({|CAMLreturn(atg_addcdiv(%{self : tensor value}, %{tensor1 : tensor value}, %{tensor2 : tensor value}, %{value : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let addcdiv_ ?value (self @ local) ~(tensor1 @ local) ~(tensor2 @ local) =
  let self = globalize_tensor self in
  let tensor1 = globalize_tensor tensor1 in
  let tensor2 = globalize_tensor tensor2 in
  [%c.alloc
    ({|CAMLreturn(atg_addcdiv_(%{self : tensor value}, %{tensor1 : tensor value}, %{tensor2 : tensor value}, %{value : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let addcdiv_out ?value ~(out @ local) (self @ local) ~(tensor1 @ local) ~(tensor2 @ local)
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let tensor1 = globalize_tensor tensor1 in
  let tensor2 = globalize_tensor tensor2 in
  [%c.alloc
    ({|CAMLreturn(atg_addcdiv_out(%{out : tensor value}, %{self : tensor value}, %{tensor1 : tensor value}, %{tensor2 : tensor value}, %{value : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let addcmul ?value (self @ local) ~(tensor1 @ local) ~(tensor2 @ local) =
  let self = globalize_tensor self in
  let tensor1 = globalize_tensor tensor1 in
  let tensor2 = globalize_tensor tensor2 in
  [%c.alloc
    ({|CAMLreturn(atg_addcmul(%{self : tensor value}, %{tensor1 : tensor value}, %{tensor2 : tensor value}, %{value : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let addcmul_ ?value (self @ local) ~(tensor1 @ local) ~(tensor2 @ local) =
  let self = globalize_tensor self in
  let tensor1 = globalize_tensor tensor1 in
  let tensor2 = globalize_tensor tensor2 in
  [%c.alloc
    ({|CAMLreturn(atg_addcmul_(%{self : tensor value}, %{tensor1 : tensor value}, %{tensor2 : tensor value}, %{value : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let addcmul_out ?value ~(out @ local) (self @ local) ~(tensor1 @ local) ~(tensor2 @ local)
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let tensor1 = globalize_tensor tensor1 in
  let tensor2 = globalize_tensor tensor2 in
  [%c.alloc
    ({|CAMLreturn(atg_addcmul_out(%{out : tensor value}, %{self : tensor value}, %{tensor1 : tensor value}, %{tensor2 : tensor value}, %{value : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let addmm ?beta ?alpha (self @ local) ~(mat1 @ local) ~(mat2 @ local) =
  let self = globalize_tensor self in
  let mat1 = globalize_tensor mat1 in
  let mat2 = globalize_tensor mat2 in
  [%c.alloc
    ({|CAMLreturn(atg_addmm(%{self : tensor value}, %{mat1 : tensor value}, %{mat2 : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let addmm_ ?beta ?alpha (self @ local) ~(mat1 @ local) ~(mat2 @ local) =
  let self = globalize_tensor self in
  let mat1 = globalize_tensor mat1 in
  let mat2 = globalize_tensor mat2 in
  [%c.alloc
    ({|CAMLreturn(atg_addmm_(%{self : tensor value}, %{mat1 : tensor value}, %{mat2 : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let addmm_out ?beta ?alpha ~(out @ local) (self @ local) ~(mat1 @ local) ~(mat2 @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let mat1 = globalize_tensor mat1 in
  let mat2 = globalize_tensor mat2 in
  [%c.alloc
    ({|CAMLreturn(atg_addmm_out(%{out : tensor value}, %{self : tensor value}, %{mat1 : tensor value}, %{mat2 : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let addmv ?beta ?alpha (self @ local) ~(mat @ local) ~(vec @ local) =
  let self = globalize_tensor self in
  let mat = globalize_tensor mat in
  let vec = globalize_tensor vec in
  [%c.alloc
    ({|CAMLreturn(atg_addmv(%{self : tensor value}, %{mat : tensor value}, %{vec : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let addmv_ ?beta ?alpha (self @ local) ~(mat @ local) ~(vec @ local) =
  let self = globalize_tensor self in
  let mat = globalize_tensor mat in
  let vec = globalize_tensor vec in
  [%c.alloc
    ({|CAMLreturn(atg_addmv_(%{self : tensor value}, %{mat : tensor value}, %{vec : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let addmv_out ?beta ?alpha ~(out @ local) (self @ local) ~(mat @ local) ~(vec @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let mat = globalize_tensor mat in
  let vec = globalize_tensor vec in
  [%c.alloc
    ({|CAMLreturn(atg_addmv_out(%{out : tensor value}, %{self : tensor value}, %{mat : tensor value}, %{vec : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let addr ?beta ?alpha (self @ local) ~(vec1 @ local) ~(vec2 @ local) =
  let self = globalize_tensor self in
  let vec1 = globalize_tensor vec1 in
  let vec2 = globalize_tensor vec2 in
  [%c.alloc
    ({|CAMLreturn(atg_addr(%{self : tensor value}, %{vec1 : tensor value}, %{vec2 : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let addr_ ?beta ?alpha (self @ local) ~(vec1 @ local) ~(vec2 @ local) =
  let self = globalize_tensor self in
  let vec1 = globalize_tensor vec1 in
  let vec2 = globalize_tensor vec2 in
  [%c.alloc
    ({|CAMLreturn(atg_addr_(%{self : tensor value}, %{vec1 : tensor value}, %{vec2 : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let addr_out ?beta ?alpha ~(out @ local) (self @ local) ~(vec1 @ local) ~(vec2 @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let vec1 = globalize_tensor vec1 in
  let vec2 = globalize_tensor vec2 in
  [%c.alloc
    ({|CAMLreturn(atg_addr_out(%{out : tensor value}, %{self : tensor value}, %{vec1 : tensor value}, %{vec2 : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let adjoint (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_adjoint(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let affine_grid_generator ~(theta @ local) ~size ~align_corners =
  let theta = globalize_tensor theta in
  [%c.alloc
    ({|CAMLreturn(atg_affine_grid_generator(%{theta : tensor value}, %{size : int list value}, Int_val(%{align_corners : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let affine_grid_generator_backward ~(grad @ local) ~size ~align_corners =
  let grad = globalize_tensor grad in
  [%c.alloc
    ({|CAMLreturn(atg_affine_grid_generator_backward(%{grad : tensor value}, %{size : int list value}, Int_val(%{align_corners : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let affine_grid_generator_out ~(out @ local) ~(theta @ local) ~size ~align_corners =
  let out = globalize_tensor out in
  let theta = globalize_tensor theta in
  [%c.alloc
    ({|CAMLreturn(atg_affine_grid_generator_out(%{out : tensor value}, %{theta : tensor value}, %{size : int list value}, Int_val(%{align_corners : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let alias (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_alias(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let alias_copy (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_alias_copy(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let alias_copy_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_alias_copy_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let align_as (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_align_as(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let align_tensors (tensors @ local) =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_align_tensors(%{tensors : tensor list value}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let all (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_all(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let all_all_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_all_all_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let all_dim (self @ local) ~dim ~keepdim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_all_dim(%{self : tensor value}, %{dim : int}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let all_dims (self @ local) ~dim ~keepdim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_all_dims(%{self : tensor value}, %{dim : int list option value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let all_dims_out ~(out @ local) (self @ local) ~dim ~keepdim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_all_dims_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list option value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let all_out ~(out @ local) (self @ local) ~dim ~keepdim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_all_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let allclose (self @ local) (other @ local) ~rtol ~atol ~equal_nan =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_allclose(%{self : tensor value}, %{other : tensor value}, %{rtol : float}, %{atol : float}, Int_val(%{equal_nan : bool value})));|}
     : bool value)]
;;

let alpha_dropout (input @ local) ~p ~train =
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg_alpha_dropout(%{input : tensor value}, %{p : float}, Int_val(%{train : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let alpha_dropout_ (self @ local) ~p ~train =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_alpha_dropout_(%{self : tensor value}, %{p : float}, Int_val(%{train : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let amax (self @ local) ~dim ~keepdim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_amax(%{self : tensor value}, %{dim : int list value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let amax_out ~(out @ local) (self @ local) ~dim ~keepdim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_amax_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let amin (self @ local) ~dim ~keepdim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_amin(%{self : tensor value}, %{dim : int list value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let amin_out ~(out @ local) (self @ local) ~dim ~keepdim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_amin_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let aminmax (self @ local) ~dim ~keepdim =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_aminmax(%{self : tensor value}, %{dim : int option value}, Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let aminmax_out ~(min @ local) ~(max @ local) (self @ local) ~dim ~keepdim =
  let min = globalize_tensor min in
  let max = globalize_tensor max in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_aminmax_out(%{min : tensor value}, %{max : tensor value}, %{self : tensor value}, %{dim : int option value}, Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let angle (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_angle(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let angle_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_angle_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let any (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_any(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let any_all_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_any_all_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let any_dim (self @ local) ~dim ~keepdim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_any_dim(%{self : tensor value}, %{dim : int}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let any_dims (self @ local) ~dim ~keepdim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_any_dims(%{self : tensor value}, %{dim : int list option value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let any_dims_out ~(out @ local) (self @ local) ~dim ~keepdim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_any_dims_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list option value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let any_out ~(out @ local) (self @ local) ~dim ~keepdim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_any_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arange ~end_ ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_arange(%{end_ : scalar value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arange_start ~start ~end_ ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_arange_start(%{start : scalar value}, %{end_ : scalar value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arange_start_step ?step ~start ~end_ ~options () =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_arange_start_step(%{start : scalar value}, %{end_ : scalar value}, %{step : scalar option value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arccos (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_arccos(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arccos_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_arccos_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arccos_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_arccos_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arccosh (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_arccosh(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arccosh_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_arccosh_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arccosh_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_arccosh_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arcsin (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_arcsin(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arcsin_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_arcsin_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arcsin_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_arcsin_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arcsinh (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_arcsinh(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arcsinh_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_arcsinh_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arcsinh_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_arcsinh_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arctan (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_arctan(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arctan2 (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_arctan2(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arctan2_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_arctan2_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arctan2_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_arctan2_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arctan_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_arctan_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arctan_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_arctan_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arctanh (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_arctanh(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arctanh_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_arctanh_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let arctanh_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_arctanh_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let argmax (self @ local) ~dim ~keepdim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_argmax(%{self : tensor value}, %{dim : int option value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let argmax_out ~(out @ local) (self @ local) ~dim ~keepdim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_argmax_out(%{out : tensor value}, %{self : tensor value}, %{dim : int option value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let argmin (self @ local) ~dim ~keepdim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_argmin(%{self : tensor value}, %{dim : int option value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let argmin_out ~(out @ local) (self @ local) ~dim ~keepdim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_argmin_out(%{out : tensor value}, %{self : tensor value}, %{dim : int option value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let argsort (self @ local) ~dim ~descending =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_argsort(%{self : tensor value}, %{dim : int}, Int_val(%{descending : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let argsort_stable (self @ local) ~stable ~dim ~descending =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_argsort_stable(%{self : tensor value}, Int_val(%{stable : bool value}), %{dim : int}, Int_val(%{descending : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let argsort_stable_out ~(out @ local) (self @ local) ~stable ~dim ~descending =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_argsort_stable_out(%{out : tensor value}, %{self : tensor value}, Int_val(%{stable : bool value}), %{dim : int}, Int_val(%{descending : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let argwhere (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_argwhere(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let as_strided (self @ local) ~size ~stride ~storage_offset =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_as_strided(%{self : tensor value}, %{size : int list value}, %{stride : int list value}, %{storage_offset : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let as_strided_ (self @ local) ~size ~stride ~storage_offset =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_as_strided_(%{self : tensor value}, %{size : int list value}, %{stride : int list value}, %{storage_offset : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let as_strided_copy (self @ local) ~size ~stride ~storage_offset =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_as_strided_copy(%{self : tensor value}, %{size : int list value}, %{stride : int list value}, %{storage_offset : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let as_strided_copy_out ~(out @ local) (self @ local) ~size ~stride ~storage_offset =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_as_strided_copy_out(%{out : tensor value}, %{self : tensor value}, %{size : int list value}, %{stride : int list value}, %{storage_offset : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let as_strided_scatter (self @ local) ~(src @ local) ~size ~stride ~storage_offset =
  let self = globalize_tensor self in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_as_strided_scatter(%{self : tensor value}, %{src : tensor value}, %{size : int list value}, %{stride : int list value}, %{storage_offset : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let as_strided_scatter_out
  ~(out @ local)
  (self @ local)
  ~(src @ local)
  ~size
  ~stride
  ~storage_offset
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_as_strided_scatter_out(%{out : tensor value}, %{self : tensor value}, %{src : tensor value}, %{size : int list value}, %{stride : int list value}, %{storage_offset : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let asin (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_asin(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let asin_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_asin_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let asin_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_asin_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let asinh (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_asinh(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let asinh_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_asinh_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let asinh_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_asinh_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let atan (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_atan(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let atan2 (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_atan2(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let atan2_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_atan2_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let atan2_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_atan2_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let atan_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_atan_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let atan_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_atan_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let atanh (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_atanh(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let atanh_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_atanh_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let atanh_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_atanh_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;
