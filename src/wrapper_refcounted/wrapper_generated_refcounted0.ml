(* THIS FILE IS AUTOMATICALLY GENERATED, DO NOT EDIT BY HAND! *)

open! Core
open C_ffi
open Torch_wrapper_types
open Torch_refcounted_bindings.Type_defs

[%%c {| #include "torch_api.h" |}]

let __and__ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg___and__(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __and__tensor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg___and__tensor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __iand__ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg___iand__(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __iand__tensor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg___iand__tensor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __ilshift__ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg___ilshift__(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __ilshift__tensor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg___ilshift__tensor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __ior__ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg___ior__(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __ior__tensor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg___ior__tensor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __irshift__ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg___irshift__(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __irshift__tensor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg___irshift__tensor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __ixor__ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg___ixor__(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __ixor__tensor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg___ixor__tensor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __lshift__ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg___lshift__(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __lshift__scalar_out_ ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg___lshift__scalar_out_(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __lshift__tensor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg___lshift__tensor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __lshift__tensor_out_ ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg___lshift__tensor_out_(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __or__ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg___or__(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __or__tensor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg___or__tensor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __rshift__ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg___rshift__(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __rshift__scalar_out_ ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg___rshift__scalar_out_(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __rshift__tensor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg___rshift__tensor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __rshift__tensor_out_ ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg___rshift__tensor_out_(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __xor__ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg___xor__(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let __xor__tensor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg___xor__tensor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _adaptive_avg_pool2d (self @ local) ~output_size =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__adaptive_avg_pool2d(%{self : tensor value}, %{output_size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _adaptive_avg_pool2d_backward ~(grad_output @ local) (self @ local) =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__adaptive_avg_pool2d_backward(%{grad_output : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _adaptive_avg_pool2d_backward_out ~(out @ local) ~(grad_output @ local) (self @ local)
  =
  let out = globalize_tensor out in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__adaptive_avg_pool2d_backward_out(%{out : tensor value}, %{grad_output : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _adaptive_avg_pool2d_out ~(out @ local) (self @ local) ~output_size =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__adaptive_avg_pool2d_out(%{out : tensor value}, %{self : tensor value}, %{output_size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _adaptive_avg_pool3d (self @ local) ~output_size =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__adaptive_avg_pool3d(%{self : tensor value}, %{output_size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _adaptive_avg_pool3d_backward ~(grad_output @ local) (self @ local) =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__adaptive_avg_pool3d_backward(%{grad_output : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _adaptive_avg_pool3d_backward_out ~(out @ local) ~(grad_output @ local) (self @ local)
  =
  let out = globalize_tensor out in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__adaptive_avg_pool3d_backward_out(%{out : tensor value}, %{grad_output : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _adaptive_avg_pool3d_out ~(out @ local) (self @ local) ~output_size =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__adaptive_avg_pool3d_out(%{out : tensor value}, %{self : tensor value}, %{output_size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _add_batch_dim (self @ local) ~batch_dim ~level =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__add_batch_dim(%{self : tensor value}, %{batch_dim : int}, %{level : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _add_relu ?alpha (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg__add_relu(%{self : tensor value}, %{other : tensor value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _add_relu_ ?alpha (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg__add_relu_(%{self : tensor value}, %{other : tensor value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _add_relu_out ?alpha ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg__add_relu_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _add_relu_scalar ?alpha (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__add_relu_scalar(%{self : tensor value}, %{other : scalar value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _add_relu_scalar_ ?alpha (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__add_relu_scalar_(%{self : tensor value}, %{other : scalar value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _add_relu_scalar_out ?alpha ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__add_relu_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _addmm_activation
  ?beta
  ?alpha
  (self @ local)
  ~(mat1 @ local)
  ~(mat2 @ local)
  ~use_gelu
  =
  let self = globalize_tensor self in
  let mat1 = globalize_tensor mat1 in
  let mat2 = globalize_tensor mat2 in
  [%c.alloc
    ({|CAMLreturn(atg__addmm_activation(%{self : tensor value}, %{mat1 : tensor value}, %{mat2 : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}, Int_val(%{use_gelu : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _addmm_activation_out
  ?beta
  ?alpha
  ~(out @ local)
  (self @ local)
  ~(mat1 @ local)
  ~(mat2 @ local)
  ~use_gelu
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let mat1 = globalize_tensor mat1 in
  let mat2 = globalize_tensor mat2 in
  [%c.alloc
    ({|CAMLreturn(atg__addmm_activation_out(%{out : tensor value}, %{self : tensor value}, %{mat1 : tensor value}, %{mat2 : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}, Int_val(%{use_gelu : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _aminmax (self @ local) =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__aminmax(%{self : tensor value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _aminmax_dim (self @ local) ~dim ~keepdim =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__aminmax_dim(%{self : tensor value}, %{dim : int}, Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _aminmax_dim_out ~(out0 @ local) ~(out1 @ local) (self @ local) ~dim ~keepdim =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__aminmax_dim_out(%{out0 : tensor value}, %{out1 : tensor value}, %{self : tensor value}, %{dim : int}, Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _aminmax_out ~(out0 @ local) ~(out1 @ local) (self @ local) =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__aminmax_out(%{out0 : tensor value}, %{out1 : tensor value}, %{self : tensor value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _amp_update_scale
  (self @ local)
  ~(growth_tracker @ local)
  ~(found_inf @ local)
  ~scale_growth_factor
  ~scale_backoff_factor
  ~growth_interval
  =
  let self = globalize_tensor self in
  let growth_tracker = globalize_tensor growth_tracker in
  let found_inf = globalize_tensor found_inf in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__amp_update_scale(%{self : tensor value}, %{growth_tracker : tensor value}, %{found_inf : tensor value}, %{scale_growth_factor : float}, %{scale_backoff_factor : float}, %{growth_interval : int}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _amp_update_scale_
  (self @ local)
  ~(growth_tracker @ local)
  ~(found_inf @ local)
  ~scale_growth_factor
  ~scale_backoff_factor
  ~growth_interval
  =
  let self = globalize_tensor self in
  let growth_tracker = globalize_tensor growth_tracker in
  let found_inf = globalize_tensor found_inf in
  [%c.alloc
    ({|CAMLreturn(atg__amp_update_scale_(%{self : tensor value}, %{growth_tracker : tensor value}, %{found_inf : tensor value}, %{scale_growth_factor : float}, %{scale_backoff_factor : float}, %{growth_interval : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _amp_update_scale_out
  ~(out @ local)
  (self @ local)
  ~(growth_tracker @ local)
  ~(found_inf @ local)
  ~scale_growth_factor
  ~scale_backoff_factor
  ~growth_interval
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let growth_tracker = globalize_tensor growth_tracker in
  let found_inf = globalize_tensor found_inf in
  [%c.alloc
    ({|CAMLreturn(atg__amp_update_scale_out(%{out : tensor value}, %{self : tensor value}, %{growth_tracker : tensor value}, %{found_inf : tensor value}, %{scale_growth_factor : float}, %{scale_backoff_factor : float}, %{growth_interval : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _assert_scalar self ~assert_msg =
  [%c.alloc
    {|atg__assert_scalar(%{self : scalar value}, String_val(%{assert_msg : string value}));|}]
;;

let _assert_tensor_metadata ~(a @ local) ~size ~stride ~dtype ~device =
  let a = globalize_tensor a in
  let dtype = Kind.packed_to_int dtype in
  let device = Device.option_to_int device in
  [%c.alloc
    {|atg__assert_tensor_metadata(%{a : tensor value}, %{size : int list option value}, %{stride : int list option value}, %{dtype : int}, %{device : int});|}]
;;

let _autocast_to_full_precision (self @ local) ~cuda_enabled ~cpu_enabled =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__autocast_to_full_precision(%{self : tensor value}, Int_val(%{cuda_enabled : bool value}), Int_val(%{cpu_enabled : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _autocast_to_reduced_precision
  (self @ local)
  ~cuda_enabled
  ~cpu_enabled
  ~cuda_dtype
  ~cpu_dtype
  =
  let self = globalize_tensor self in
  let cuda_dtype = Kind.packed_to_int cuda_dtype in
  let cpu_dtype = Kind.packed_to_int cpu_dtype in
  [%c.alloc
    ({|CAMLreturn(atg__autocast_to_reduced_precision(%{self : tensor value}, Int_val(%{cuda_enabled : bool value}), Int_val(%{cpu_enabled : bool value}), %{cuda_dtype : int}, %{cpu_dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _batch_norm_no_update
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~momentum
  ~eps
  =
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let running_mean = [%globalize: tensor option] running_mean in
  let running_var = [%globalize: tensor option] running_var in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg__batch_norm_no_update(%{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, %{running_mean : tensor option value}, %{running_var : tensor option value}, %{momentum : float}, %{eps : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let _batch_norm_no_update_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  ~(out3 @ local)
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~momentum
  ~eps
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let out3 = globalize_tensor out3 in
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let running_mean = [%globalize: tensor option] running_mean in
  let running_var = [%globalize: tensor option] running_var in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg__batch_norm_no_update_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{out3 : tensor value}, %{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, %{running_mean : tensor option value}, %{running_var : tensor option value}, %{momentum : float}, %{eps : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let _batch_norm_with_update
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~momentum
  ~eps
  =
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let running_mean = globalize_tensor running_mean in
  let running_var = globalize_tensor running_var in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg__batch_norm_with_update(%{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, %{running_mean : tensor value}, %{running_var : tensor value}, %{momentum : float}, %{eps : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let _batch_norm_with_update_functional
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~momentum
  ~eps
  =
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let running_mean = globalize_tensor running_mean in
  let running_var = globalize_tensor running_var in
  let result0, result1, result2, result3, result4, result5 =
    [%c.alloc
      ({|CAMLreturn(atg__batch_norm_with_update_functional(%{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, %{running_mean : tensor value}, %{running_var : tensor value}, %{momentum : float}, %{eps : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor * raw_tensor * raw_tensor)
           value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3
  , wrap_managed_tensor result4
  , wrap_managed_tensor result5 )
;;

let _batch_norm_with_update_out
  ~(out @ local)
  ~(save_mean @ local)
  ~(save_invstd @ local)
  ~(reserve @ local)
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~momentum
  ~eps
  =
  let out = globalize_tensor out in
  let save_mean = globalize_tensor save_mean in
  let save_invstd = globalize_tensor save_invstd in
  let reserve = globalize_tensor reserve in
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let running_mean = globalize_tensor running_mean in
  let running_var = globalize_tensor running_var in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg__batch_norm_with_update_out(%{out : tensor value}, %{save_mean : tensor value}, %{save_invstd : tensor value}, %{reserve : tensor value}, %{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, %{running_mean : tensor value}, %{running_var : tensor value}, %{momentum : float}, %{eps : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let _cast_byte (self @ local) ~non_blocking =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__cast_byte(%{self : tensor value}, Int_val(%{non_blocking : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _cast_char (self @ local) ~non_blocking =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__cast_char(%{self : tensor value}, Int_val(%{non_blocking : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _cast_double (self @ local) ~non_blocking =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__cast_double(%{self : tensor value}, Int_val(%{non_blocking : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _cast_float (self @ local) ~non_blocking =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__cast_float(%{self : tensor value}, Int_val(%{non_blocking : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _cast_half (self @ local) ~non_blocking =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__cast_half(%{self : tensor value}, Int_val(%{non_blocking : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _cast_int (self @ local) ~non_blocking =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__cast_int(%{self : tensor value}, Int_val(%{non_blocking : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _cast_long (self @ local) ~non_blocking =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__cast_long(%{self : tensor value}, Int_val(%{non_blocking : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _cast_short (self @ local) ~non_blocking =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__cast_short(%{self : tensor value}, Int_val(%{non_blocking : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _cdist_backward ~(grad @ local) ~(x1 @ local) ~(x2 @ local) ~p ~(cdist @ local) =
  let grad = globalize_tensor grad in
  let x1 = globalize_tensor x1 in
  let x2 = globalize_tensor x2 in
  let cdist = globalize_tensor cdist in
  [%c.alloc
    ({|CAMLreturn(atg__cdist_backward(%{grad : tensor value}, %{x1 : tensor value}, %{x2 : tensor value}, %{p : float}, %{cdist : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _cdist_backward_out
  ~(out @ local)
  ~(grad @ local)
  ~(x1 @ local)
  ~(x2 @ local)
  ~p
  ~(cdist @ local)
  =
  let out = globalize_tensor out in
  let grad = globalize_tensor grad in
  let x1 = globalize_tensor x1 in
  let x2 = globalize_tensor x2 in
  let cdist = globalize_tensor cdist in
  [%c.alloc
    ({|CAMLreturn(atg__cdist_backward_out(%{out : tensor value}, %{grad : tensor value}, %{x1 : tensor value}, %{x2 : tensor value}, %{p : float}, %{cdist : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _cholesky_solve_helper (self @ local) ~(a @ local) ~upper =
  let self = globalize_tensor self in
  let a = globalize_tensor a in
  [%c.alloc
    ({|CAMLreturn(atg__cholesky_solve_helper(%{self : tensor value}, %{a : tensor value}, Int_val(%{upper : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _cholesky_solve_helper_out ~(out @ local) (self @ local) ~(a @ local) ~upper =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let a = globalize_tensor a in
  [%c.alloc
    ({|CAMLreturn(atg__cholesky_solve_helper_out(%{out : tensor value}, %{self : tensor value}, %{a : tensor value}, Int_val(%{upper : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _chunk_cat (tensors @ local) ~dim ~num_chunks =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg__chunk_cat(%{tensors : tensor list value}, %{dim : int}, %{num_chunks : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _chunk_cat_out ~(out @ local) (tensors @ local) ~dim ~num_chunks =
  let out = globalize_tensor out in
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg__chunk_cat_out(%{out : tensor value}, %{tensors : tensor list value}, %{dim : int}, %{num_chunks : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _coalesce (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg__coalesce(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _coalesce_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__coalesce_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _coalesced (self @ local) ~coalesced =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__coalesced(%{self : tensor value}, Int_val(%{coalesced : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _coalesced_ (self @ local) ~coalesced =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__coalesced_(%{self : tensor value}, Int_val(%{coalesced : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _coalesced_out ~(out @ local) (self @ local) ~coalesced =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__coalesced_out(%{out : tensor value}, %{self : tensor value}, Int_val(%{coalesced : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _compute_linear_combination (input @ local) ~(coefficients @ local) =
  let input = globalize_tensor input in
  let coefficients = globalize_tensor coefficients in
  [%c.alloc
    ({|CAMLreturn(atg__compute_linear_combination(%{input : tensor value}, %{coefficients : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _compute_linear_combination_out ~(out @ local) (input @ local) ~(coefficients @ local)
  =
  let out = globalize_tensor out in
  let input = globalize_tensor input in
  let coefficients = globalize_tensor coefficients in
  [%c.alloc
    ({|CAMLreturn(atg__compute_linear_combination_out(%{out : tensor value}, %{input : tensor value}, %{coefficients : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _conj (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg__conj(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _conj_copy (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg__conj_copy(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _conj_copy_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__conj_copy_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _conj_physical (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__conj_physical(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _conj_physical_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__conj_physical_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _conv_depthwise2d
  (self @ local)
  ~(weight @ local)
  ~kernel_size
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg__conv_depthwise2d(%{self : tensor value}, %{weight : tensor value}, %{kernel_size : int list value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _conv_depthwise2d_out
  ~(out @ local)
  (self @ local)
  ~(weight @ local)
  ~kernel_size
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg__conv_depthwise2d_out(%{out : tensor value}, %{self : tensor value}, %{weight : tensor value}, %{kernel_size : int list value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _convert_indices_from_coo_to_csr (self @ local) ~size ~out_int32 =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__convert_indices_from_coo_to_csr(%{self : tensor value}, %{size : int}, Int_val(%{out_int32 : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _convert_indices_from_coo_to_csr_out ~(out @ local) (self @ local) ~size ~out_int32 =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__convert_indices_from_coo_to_csr_out(%{out : tensor value}, %{self : tensor value}, %{size : int}, Int_val(%{out_int32 : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _convert_indices_from_csr_to_coo
  ~(crow_indices @ local)
  ~(col_indices @ local)
  ~out_int32
  ~transpose
  =
  let crow_indices = globalize_tensor crow_indices in
  let col_indices = globalize_tensor col_indices in
  [%c.alloc
    ({|CAMLreturn(atg__convert_indices_from_csr_to_coo(%{crow_indices : tensor value}, %{col_indices : tensor value}, Int_val(%{out_int32 : bool value}), Int_val(%{transpose : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _convert_indices_from_csr_to_coo_out
  ~(out @ local)
  ~(crow_indices @ local)
  ~(col_indices @ local)
  ~out_int32
  ~transpose
  =
  let out = globalize_tensor out in
  let crow_indices = globalize_tensor crow_indices in
  let col_indices = globalize_tensor col_indices in
  [%c.alloc
    ({|CAMLreturn(atg__convert_indices_from_csr_to_coo_out(%{out : tensor value}, %{crow_indices : tensor value}, %{col_indices : tensor value}, Int_val(%{out_int32 : bool value}), Int_val(%{transpose : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _convert_weight_to_int4pack (self @ local) ~innerktiles =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__convert_weight_to_int4pack(%{self : tensor value}, %{innerktiles : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _convert_weight_to_int4pack_for_cpu (self @ local) ~innerktiles =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__convert_weight_to_int4pack_for_cpu(%{self : tensor value}, %{innerktiles : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _convolution
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  ~transposed
  ~output_padding
  ~groups
  ~benchmark
  ~deterministic
  ~cudnn_enabled
  ~allow_tf32
  =
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg__convolution(%{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{transposed : bool value}), %{output_padding : int list value}, %{groups : int}, Int_val(%{benchmark : bool value}), Int_val(%{deterministic : bool value}), Int_val(%{cudnn_enabled : bool value}), Int_val(%{allow_tf32 : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _convolution_deprecated
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  ~transposed
  ~output_padding
  ~groups
  ~benchmark
  ~deterministic
  ~cudnn_enabled
  =
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg__convolution_deprecated(%{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{transposed : bool value}), %{output_padding : int list value}, %{groups : int}, Int_val(%{benchmark : bool value}), Int_val(%{deterministic : bool value}), Int_val(%{cudnn_enabled : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _convolution_mode
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  ~groups
  =
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg__convolution_mode(%{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{stride : int list value}, String_val(%{padding : string value}), %{dilation : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _convolution_out
  ~(out @ local)
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  ~transposed
  ~output_padding
  ~groups
  ~benchmark
  ~deterministic
  ~cudnn_enabled
  ~allow_tf32
  =
  let out = globalize_tensor out in
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg__convolution_out(%{out : tensor value}, %{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{transposed : bool value}), %{output_padding : int list value}, %{groups : int}, Int_val(%{benchmark : bool value}), Int_val(%{deterministic : bool value}), Int_val(%{cudnn_enabled : bool value}), Int_val(%{allow_tf32 : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _copy_from (self @ local) ~(dst @ local) ~non_blocking =
  let self = globalize_tensor self in
  let dst = globalize_tensor dst in
  [%c.alloc
    ({|CAMLreturn(atg__copy_from(%{self : tensor value}, %{dst : tensor value}, Int_val(%{non_blocking : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _copy_from_and_resize (self @ local) ~(dst @ local) =
  let self = globalize_tensor self in
  let dst = globalize_tensor dst in
  [%c.alloc
    ({|CAMLreturn(atg__copy_from_and_resize(%{self : tensor value}, %{dst : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _copy_from_and_resize_out ~(out @ local) (self @ local) ~(dst @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dst = globalize_tensor dst in
  [%c.alloc
    ({|CAMLreturn(atg__copy_from_and_resize_out(%{out : tensor value}, %{self : tensor value}, %{dst : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _copy_from_out ~(out @ local) (self @ local) ~(dst @ local) ~non_blocking =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dst = globalize_tensor dst in
  [%c.alloc
    ({|CAMLreturn(atg__copy_from_out(%{out : tensor value}, %{self : tensor value}, %{dst : tensor value}, Int_val(%{non_blocking : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _cslt_compress (input @ local) =
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg__cslt_compress(%{input : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _cslt_sparse_mm
  ~(compressed_a @ local)
  ~(dense_b @ local)
  ~(bias @ local)
  ~(alpha @ local)
  ~out_dtype
  ~transpose_result
  ~alg_id
  ~split_k
  ~split_k_one_kernel
  =
  let compressed_a = globalize_tensor compressed_a in
  let dense_b = globalize_tensor dense_b in
  let bias = [%globalize: tensor option] bias in
  let alpha = [%globalize: tensor option] alpha in
  let out_dtype = Kind.packed_to_int out_dtype in
  [%c.alloc
    ({|CAMLreturn(atg__cslt_sparse_mm(%{compressed_a : tensor value}, %{dense_b : tensor value}, %{bias : tensor option value}, %{alpha : tensor option value}, %{out_dtype : int}, Int_val(%{transpose_result : bool value}), %{alg_id : int}, %{split_k : int}, Int_val(%{split_k_one_kernel : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _cslt_sparse_mm_search
  ~(compressed_a @ local)
  ~(dense_b @ local)
  ~(bias @ local)
  ~(alpha @ local)
  ~out_dtype
  ~transpose_result
  =
  let compressed_a = globalize_tensor compressed_a in
  let dense_b = globalize_tensor dense_b in
  let bias = [%globalize: tensor option] bias in
  let alpha = [%globalize: tensor option] alpha in
  let out_dtype = Kind.packed_to_int out_dtype in
  [%c.alloc
    ({|CAMLreturn(atg__cslt_sparse_mm_search(%{compressed_a : tensor value}, %{dense_b : tensor value}, %{bias : tensor option value}, %{alpha : tensor option value}, %{out_dtype : int}, Int_val(%{transpose_result : bool value})));|}
     : int64 value)]
;;

let _ctc_loss
  ~(log_probs @ local)
  ~(targets @ local)
  ~input_lengths
  ~target_lengths
  ~blank
  ~zero_infinity
  =
  let log_probs = globalize_tensor log_probs in
  let targets = globalize_tensor targets in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__ctc_loss(%{log_probs : tensor value}, %{targets : tensor value}, %{input_lengths : int list value}, %{target_lengths : int list value}, %{blank : int}, Int_val(%{zero_infinity : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _ctc_loss_backward
  ~(grad @ local)
  ~(log_probs @ local)
  ~(targets @ local)
  ~input_lengths
  ~target_lengths
  ~(neg_log_likelihood @ local)
  ~(log_alpha @ local)
  ~blank
  ~zero_infinity
  =
  let grad = globalize_tensor grad in
  let log_probs = globalize_tensor log_probs in
  let targets = globalize_tensor targets in
  let neg_log_likelihood = globalize_tensor neg_log_likelihood in
  let log_alpha = globalize_tensor log_alpha in
  [%c.alloc
    ({|CAMLreturn(atg__ctc_loss_backward(%{grad : tensor value}, %{log_probs : tensor value}, %{targets : tensor value}, %{input_lengths : int list value}, %{target_lengths : int list value}, %{neg_log_likelihood : tensor value}, %{log_alpha : tensor value}, %{blank : int}, Int_val(%{zero_infinity : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _ctc_loss_backward_out
  ~(out @ local)
  ~(grad @ local)
  ~(log_probs @ local)
  ~(targets @ local)
  ~input_lengths
  ~target_lengths
  ~(neg_log_likelihood @ local)
  ~(log_alpha @ local)
  ~blank
  ~zero_infinity
  =
  let out = globalize_tensor out in
  let grad = globalize_tensor grad in
  let log_probs = globalize_tensor log_probs in
  let targets = globalize_tensor targets in
  let neg_log_likelihood = globalize_tensor neg_log_likelihood in
  let log_alpha = globalize_tensor log_alpha in
  [%c.alloc
    ({|CAMLreturn(atg__ctc_loss_backward_out(%{out : tensor value}, %{grad : tensor value}, %{log_probs : tensor value}, %{targets : tensor value}, %{input_lengths : int list value}, %{target_lengths : int list value}, %{neg_log_likelihood : tensor value}, %{log_alpha : tensor value}, %{blank : int}, Int_val(%{zero_infinity : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _ctc_loss_backward_tensor
  ~(grad @ local)
  ~(log_probs @ local)
  ~(targets @ local)
  ~(input_lengths @ local)
  ~(target_lengths @ local)
  ~(neg_log_likelihood @ local)
  ~(log_alpha @ local)
  ~blank
  ~zero_infinity
  =
  let grad = globalize_tensor grad in
  let log_probs = globalize_tensor log_probs in
  let targets = globalize_tensor targets in
  let input_lengths = globalize_tensor input_lengths in
  let target_lengths = globalize_tensor target_lengths in
  let neg_log_likelihood = globalize_tensor neg_log_likelihood in
  let log_alpha = globalize_tensor log_alpha in
  [%c.alloc
    ({|CAMLreturn(atg__ctc_loss_backward_tensor(%{grad : tensor value}, %{log_probs : tensor value}, %{targets : tensor value}, %{input_lengths : tensor value}, %{target_lengths : tensor value}, %{neg_log_likelihood : tensor value}, %{log_alpha : tensor value}, %{blank : int}, Int_val(%{zero_infinity : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _ctc_loss_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(log_probs @ local)
  ~(targets @ local)
  ~input_lengths
  ~target_lengths
  ~blank
  ~zero_infinity
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let log_probs = globalize_tensor log_probs in
  let targets = globalize_tensor targets in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__ctc_loss_out(%{out0 : tensor value}, %{out1 : tensor value}, %{log_probs : tensor value}, %{targets : tensor value}, %{input_lengths : int list value}, %{target_lengths : int list value}, %{blank : int}, Int_val(%{zero_infinity : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _ctc_loss_tensor
  ~(log_probs @ local)
  ~(targets @ local)
  ~(input_lengths @ local)
  ~(target_lengths @ local)
  ~blank
  ~zero_infinity
  =
  let log_probs = globalize_tensor log_probs in
  let targets = globalize_tensor targets in
  let input_lengths = globalize_tensor input_lengths in
  let target_lengths = globalize_tensor target_lengths in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__ctc_loss_tensor(%{log_probs : tensor value}, %{targets : tensor value}, %{input_lengths : tensor value}, %{target_lengths : tensor value}, %{blank : int}, Int_val(%{zero_infinity : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _ctc_loss_tensor_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(log_probs @ local)
  ~(targets @ local)
  ~(input_lengths @ local)
  ~(target_lengths @ local)
  ~blank
  ~zero_infinity
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let log_probs = globalize_tensor log_probs in
  let targets = globalize_tensor targets in
  let input_lengths = globalize_tensor input_lengths in
  let target_lengths = globalize_tensor target_lengths in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__ctc_loss_tensor_out(%{out0 : tensor value}, %{out1 : tensor value}, %{log_probs : tensor value}, %{targets : tensor value}, %{input_lengths : tensor value}, %{target_lengths : tensor value}, %{blank : int}, Int_val(%{zero_infinity : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _cudnn_ctc_loss
  ~(log_probs @ local)
  ~(targets @ local)
  ~input_lengths
  ~target_lengths
  ~blank
  ~deterministic
  ~zero_infinity
  =
  let log_probs = globalize_tensor log_probs in
  let targets = globalize_tensor targets in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__cudnn_ctc_loss(%{log_probs : tensor value}, %{targets : tensor value}, %{input_lengths : int list value}, %{target_lengths : int list value}, %{blank : int}, Int_val(%{deterministic : bool value}), Int_val(%{zero_infinity : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _cudnn_ctc_loss_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(log_probs @ local)
  ~(targets @ local)
  ~input_lengths
  ~target_lengths
  ~blank
  ~deterministic
  ~zero_infinity
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let log_probs = globalize_tensor log_probs in
  let targets = globalize_tensor targets in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__cudnn_ctc_loss_out(%{out0 : tensor value}, %{out1 : tensor value}, %{log_probs : tensor value}, %{targets : tensor value}, %{input_lengths : int list value}, %{target_lengths : int list value}, %{blank : int}, Int_val(%{deterministic : bool value}), Int_val(%{zero_infinity : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _cudnn_ctc_loss_tensor
  ~(log_probs @ local)
  ~(targets @ local)
  ~(input_lengths @ local)
  ~(target_lengths @ local)
  ~blank
  ~deterministic
  ~zero_infinity
  =
  let log_probs = globalize_tensor log_probs in
  let targets = globalize_tensor targets in
  let input_lengths = globalize_tensor input_lengths in
  let target_lengths = globalize_tensor target_lengths in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__cudnn_ctc_loss_tensor(%{log_probs : tensor value}, %{targets : tensor value}, %{input_lengths : tensor value}, %{target_lengths : tensor value}, %{blank : int}, Int_val(%{deterministic : bool value}), Int_val(%{zero_infinity : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _cudnn_init_dropout_state ~dropout ~train ~dropout_seed ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg__cudnn_init_dropout_state(%{dropout : float}, Int_val(%{train : bool value}), %{dropout_seed : int}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _cudnn_init_dropout_state_out ~(out @ local) ~dropout ~train ~dropout_seed =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg__cudnn_init_dropout_state_out(%{out : tensor value}, %{dropout : float}, Int_val(%{train : bool value}), %{dropout_seed : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _cudnn_rnn
  (input @ local)
  ~(weight @ local)
  ~weight_stride0
  ~(weight_buf @ local)
  ~(hx @ local)
  ~(cx @ local)
  ~mode
  ~hidden_size
  ~proj_size
  ~num_layers
  ~batch_first
  ~dropout
  ~train
  ~bidirectional
  ~batch_sizes
  ~(dropout_state @ local)
  =
  let input = globalize_tensor input in
  let weight = Torch_local_iterators.List.map_local_input weight ~f:globalize_tensor in
  let weight_buf = [%globalize: tensor option] weight_buf in
  let hx = globalize_tensor hx in
  let cx = [%globalize: tensor option] cx in
  let dropout_state = [%globalize: tensor option] dropout_state in
  let result0, result1, result2, result3, result4 =
    [%c.alloc
      ({|CAMLreturn(atg__cudnn_rnn(%{input : tensor value}, %{weight : tensor list value}, %{weight_stride0 : int}, %{weight_buf : tensor option value}, %{hx : tensor value}, %{cx : tensor option value}, %{mode : int}, %{hidden_size : int}, %{proj_size : int}, %{num_layers : int}, Int_val(%{batch_first : bool value}), %{dropout : float}, Int_val(%{train : bool value}), Int_val(%{bidirectional : bool value}), %{batch_sizes : int list value}, %{dropout_state : tensor option value}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3
  , wrap_managed_tensor result4 )
;;

let _cudnn_rnn_flatten_weight
  ~(weight_arr @ local)
  ~weight_stride0
  ~input_size
  ~mode
  ~hidden_size
  ~proj_size
  ~num_layers
  ~batch_first
  ~bidirectional
  =
  let weight_arr =
    Torch_local_iterators.List.map_local_input weight_arr ~f:globalize_tensor
  in
  [%c.alloc
    ({|CAMLreturn(atg__cudnn_rnn_flatten_weight(%{weight_arr : tensor list value}, %{weight_stride0 : int}, %{input_size : int}, %{mode : int}, %{hidden_size : int}, %{proj_size : int}, %{num_layers : int}, Int_val(%{batch_first : bool value}), Int_val(%{bidirectional : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _cudnn_rnn_flatten_weight_out
  ~(out @ local)
  ~(weight_arr @ local)
  ~weight_stride0
  ~input_size
  ~mode
  ~hidden_size
  ~proj_size
  ~num_layers
  ~batch_first
  ~bidirectional
  =
  let out = globalize_tensor out in
  let weight_arr =
    Torch_local_iterators.List.map_local_input weight_arr ~f:globalize_tensor
  in
  [%c.alloc
    ({|CAMLreturn(atg__cudnn_rnn_flatten_weight_out(%{out : tensor value}, %{weight_arr : tensor list value}, %{weight_stride0 : int}, %{input_size : int}, %{mode : int}, %{hidden_size : int}, %{proj_size : int}, %{num_layers : int}, Int_val(%{batch_first : bool value}), Int_val(%{bidirectional : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _cudnn_rnn_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  ~(out3 @ local)
  ~(out4 @ local)
  (input @ local)
  ~(weight @ local)
  ~weight_stride0
  ~(weight_buf @ local)
  ~(hx @ local)
  ~(cx @ local)
  ~mode
  ~hidden_size
  ~proj_size
  ~num_layers
  ~batch_first
  ~dropout
  ~train
  ~bidirectional
  ~batch_sizes
  ~(dropout_state @ local)
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let out3 = globalize_tensor out3 in
  let out4 = globalize_tensor out4 in
  let input = globalize_tensor input in
  let weight = Torch_local_iterators.List.map_local_input weight ~f:globalize_tensor in
  let weight_buf = [%globalize: tensor option] weight_buf in
  let hx = globalize_tensor hx in
  let cx = [%globalize: tensor option] cx in
  let dropout_state = [%globalize: tensor option] dropout_state in
  let result0, result1, result2, result3, result4 =
    [%c.alloc
      ({|CAMLreturn(atg__cudnn_rnn_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{out3 : tensor value}, %{out4 : tensor value}, %{input : tensor value}, %{weight : tensor list value}, %{weight_stride0 : int}, %{weight_buf : tensor option value}, %{hx : tensor value}, %{cx : tensor option value}, %{mode : int}, %{hidden_size : int}, %{proj_size : int}, %{num_layers : int}, Int_val(%{batch_first : bool value}), %{dropout : float}, Int_val(%{train : bool value}), Int_val(%{bidirectional : bool value}), %{batch_sizes : int list value}, %{dropout_state : tensor option value}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3
  , wrap_managed_tensor result4 )
;;

let _debug_has_internal_overlap (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__debug_has_internal_overlap(%{self : tensor value}));|}
     : int64 value)]
;;

let _dim_arange ~(like @ local) ~dim =
  let like = globalize_tensor like in
  [%c.alloc
    ({|CAMLreturn(atg__dim_arange(%{like : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _dimi (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg__dimi(%{self : tensor value}));|} : int64 value)]
;;

let _dimv (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg__dimv(%{self : tensor value}));|} : int64 value)]
;;

let _dirichlet_grad ~(x @ local) ~(alpha @ local) ~(total @ local) =
  let x = globalize_tensor x in
  let alpha = globalize_tensor alpha in
  let total = globalize_tensor total in
  [%c.alloc
    ({|CAMLreturn(atg__dirichlet_grad(%{x : tensor value}, %{alpha : tensor value}, %{total : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _dirichlet_grad_out ~(out @ local) ~(x @ local) ~(alpha @ local) ~(total @ local) =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  let alpha = globalize_tensor alpha in
  let total = globalize_tensor total in
  [%c.alloc
    ({|CAMLreturn(atg__dirichlet_grad_out(%{out : tensor value}, %{x : tensor value}, %{alpha : tensor value}, %{total : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _dyn_quant_matmul_4bit
  ~(inp @ local)
  ~(packed_weights @ local)
  ~block_size
  ~in_features
  ~out_features
  =
  let inp = globalize_tensor inp in
  let packed_weights = globalize_tensor packed_weights in
  [%c.alloc
    ({|CAMLreturn(atg__dyn_quant_matmul_4bit(%{inp : tensor value}, %{packed_weights : tensor value}, %{block_size : int}, %{in_features : int}, %{out_features : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _dyn_quant_pack_4bit_weight
  ~(weights @ local)
  ~(scales_zeros @ local)
  ~(bias @ local)
  ~block_size
  ~in_features
  ~out_features
  =
  let weights = globalize_tensor weights in
  let scales_zeros = globalize_tensor scales_zeros in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg__dyn_quant_pack_4bit_weight(%{weights : tensor value}, %{scales_zeros : tensor value}, %{bias : tensor option value}, %{block_size : int}, %{in_features : int}, %{out_features : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _efficient_attention_backward
  ~(grad_out_ @ local)
  ~(query @ local)
  ~(key @ local)
  ~(value @ local)
  ~(bias @ local)
  ~(out @ local)
  ~(cu_seqlens_q @ local)
  ~(cu_seqlens_k @ local)
  ~max_seqlen_q
  ~max_seqlen_k
  ~(logsumexp @ local)
  ~dropout_p
  ~(philox_seed @ local)
  ~(philox_offset @ local)
  ~custom_mask_type
  ~bias_requires_grad
  ~scale
  ~num_splits_key
  ~window_size
  ~shared_storage_dqdkdv
  =
  let grad_out_ = globalize_tensor grad_out_ in
  let query = globalize_tensor query in
  let key = globalize_tensor key in
  let value = globalize_tensor value in
  let bias = [%globalize: tensor option] bias in
  let out = globalize_tensor out in
  let cu_seqlens_q = [%globalize: tensor option] cu_seqlens_q in
  let cu_seqlens_k = [%globalize: tensor option] cu_seqlens_k in
  let logsumexp = globalize_tensor logsumexp in
  let philox_seed = globalize_tensor philox_seed in
  let philox_offset = globalize_tensor philox_offset in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg__efficient_attention_backward(%{grad_out_ : tensor value}, %{query : tensor value}, %{key : tensor value}, %{value : tensor value}, %{bias : tensor option value}, %{out : tensor value}, %{cu_seqlens_q : tensor option value}, %{cu_seqlens_k : tensor option value}, %{max_seqlen_q : int}, %{max_seqlen_k : int}, %{logsumexp : tensor value}, %{dropout_p : float}, %{philox_seed : tensor value}, %{philox_offset : tensor value}, %{custom_mask_type : int}, Int_val(%{bias_requires_grad : bool value}), %{scale : float option value}, %{num_splits_key : int option value}, %{window_size : int option value}, Int_val(%{shared_storage_dqdkdv : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let _efficientzerotensor ~size ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg__efficientzerotensor(%{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _efficientzerotensor_out ~(out @ local) ~size =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg__efficientzerotensor_out(%{out : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _embedding_bag
  ~(weight @ local)
  ~(indices @ local)
  ~(offsets @ local)
  ~scale_grad_by_freq
  ~mode
  ~sparse
  ~(per_sample_weights @ local)
  ~include_last_offset
  ~padding_idx
  =
  let weight = globalize_tensor weight in
  let indices = globalize_tensor indices in
  let offsets = globalize_tensor offsets in
  let per_sample_weights = [%globalize: tensor option] per_sample_weights in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg__embedding_bag(%{weight : tensor value}, %{indices : tensor value}, %{offsets : tensor value}, Int_val(%{scale_grad_by_freq : bool value}), %{mode : int}, Int_val(%{sparse : bool value}), %{per_sample_weights : tensor option value}, Int_val(%{include_last_offset : bool value}), %{padding_idx : int}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let _embedding_bag_backward
  ~(grad @ local)
  ~(indices @ local)
  ~(offsets @ local)
  ~(offset2bag @ local)
  ~(bag_size @ local)
  ~(maximum_indices @ local)
  ~num_weights
  ~scale_grad_by_freq
  ~mode
  ~sparse
  ~(per_sample_weights @ local)
  ~padding_idx
  =
  let grad = globalize_tensor grad in
  let indices = globalize_tensor indices in
  let offsets = globalize_tensor offsets in
  let offset2bag = globalize_tensor offset2bag in
  let bag_size = globalize_tensor bag_size in
  let maximum_indices = globalize_tensor maximum_indices in
  let per_sample_weights = [%globalize: tensor option] per_sample_weights in
  [%c.alloc
    ({|CAMLreturn(atg__embedding_bag_backward(%{grad : tensor value}, %{indices : tensor value}, %{offsets : tensor value}, %{offset2bag : tensor value}, %{bag_size : tensor value}, %{maximum_indices : tensor value}, %{num_weights : int}, Int_val(%{scale_grad_by_freq : bool value}), %{mode : int}, Int_val(%{sparse : bool value}), %{per_sample_weights : tensor option value}, %{padding_idx : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _embedding_bag_dense_backward
  ~(grad @ local)
  ~(indices @ local)
  ~(offset2bag @ local)
  ~(bag_size @ local)
  ~(maximum_indices @ local)
  ~num_weights
  ~scale_grad_by_freq
  ~mode
  ~(per_sample_weights @ local)
  ~padding_idx
  =
  let grad = globalize_tensor grad in
  let indices = globalize_tensor indices in
  let offset2bag = globalize_tensor offset2bag in
  let bag_size = globalize_tensor bag_size in
  let maximum_indices = globalize_tensor maximum_indices in
  let per_sample_weights = [%globalize: tensor option] per_sample_weights in
  [%c.alloc
    ({|CAMLreturn(atg__embedding_bag_dense_backward(%{grad : tensor value}, %{indices : tensor value}, %{offset2bag : tensor value}, %{bag_size : tensor value}, %{maximum_indices : tensor value}, %{num_weights : int}, Int_val(%{scale_grad_by_freq : bool value}), %{mode : int}, %{per_sample_weights : tensor option value}, %{padding_idx : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _embedding_bag_dense_backward_out
  ~(out @ local)
  ~(grad @ local)
  ~(indices @ local)
  ~(offset2bag @ local)
  ~(bag_size @ local)
  ~(maximum_indices @ local)
  ~num_weights
  ~scale_grad_by_freq
  ~mode
  ~(per_sample_weights @ local)
  ~padding_idx
  =
  let out = globalize_tensor out in
  let grad = globalize_tensor grad in
  let indices = globalize_tensor indices in
  let offset2bag = globalize_tensor offset2bag in
  let bag_size = globalize_tensor bag_size in
  let maximum_indices = globalize_tensor maximum_indices in
  let per_sample_weights = [%globalize: tensor option] per_sample_weights in
  [%c.alloc
    ({|CAMLreturn(atg__embedding_bag_dense_backward_out(%{out : tensor value}, %{grad : tensor value}, %{indices : tensor value}, %{offset2bag : tensor value}, %{bag_size : tensor value}, %{maximum_indices : tensor value}, %{num_weights : int}, Int_val(%{scale_grad_by_freq : bool value}), %{mode : int}, %{per_sample_weights : tensor option value}, %{padding_idx : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _embedding_bag_forward_only
  ~(weight @ local)
  ~(indices @ local)
  ~(offsets @ local)
  ~scale_grad_by_freq
  ~mode
  ~sparse
  ~(per_sample_weights @ local)
  ~include_last_offset
  ~padding_idx
  =
  let weight = globalize_tensor weight in
  let indices = globalize_tensor indices in
  let offsets = globalize_tensor offsets in
  let per_sample_weights = [%globalize: tensor option] per_sample_weights in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg__embedding_bag_forward_only(%{weight : tensor value}, %{indices : tensor value}, %{offsets : tensor value}, Int_val(%{scale_grad_by_freq : bool value}), %{mode : int}, Int_val(%{sparse : bool value}), %{per_sample_weights : tensor option value}, Int_val(%{include_last_offset : bool value}), %{padding_idx : int}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let _embedding_bag_forward_only_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  ~(out3 @ local)
  ~(weight @ local)
  ~(indices @ local)
  ~(offsets @ local)
  ~scale_grad_by_freq
  ~mode
  ~sparse
  ~(per_sample_weights @ local)
  ~include_last_offset
  ~padding_idx
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let out3 = globalize_tensor out3 in
  let weight = globalize_tensor weight in
  let indices = globalize_tensor indices in
  let offsets = globalize_tensor offsets in
  let per_sample_weights = [%globalize: tensor option] per_sample_weights in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg__embedding_bag_forward_only_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{out3 : tensor value}, %{weight : tensor value}, %{indices : tensor value}, %{offsets : tensor value}, Int_val(%{scale_grad_by_freq : bool value}), %{mode : int}, Int_val(%{sparse : bool value}), %{per_sample_weights : tensor option value}, Int_val(%{include_last_offset : bool value}), %{padding_idx : int}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let _embedding_bag_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  ~(out3 @ local)
  ~(weight @ local)
  ~(indices @ local)
  ~(offsets @ local)
  ~scale_grad_by_freq
  ~mode
  ~sparse
  ~(per_sample_weights @ local)
  ~include_last_offset
  ~padding_idx
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let out3 = globalize_tensor out3 in
  let weight = globalize_tensor weight in
  let indices = globalize_tensor indices in
  let offsets = globalize_tensor offsets in
  let per_sample_weights = [%globalize: tensor option] per_sample_weights in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg__embedding_bag_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{out3 : tensor value}, %{weight : tensor value}, %{indices : tensor value}, %{offsets : tensor value}, Int_val(%{scale_grad_by_freq : bool value}), %{mode : int}, Int_val(%{sparse : bool value}), %{per_sample_weights : tensor option value}, Int_val(%{include_last_offset : bool value}), %{padding_idx : int}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let _embedding_bag_per_sample_weights_backward
  ~(grad @ local)
  ~(weight @ local)
  ~(indices @ local)
  ~(offsets @ local)
  ~(offset2bag @ local)
  ~mode
  ~padding_idx
  =
  let grad = globalize_tensor grad in
  let weight = globalize_tensor weight in
  let indices = globalize_tensor indices in
  let offsets = globalize_tensor offsets in
  let offset2bag = globalize_tensor offset2bag in
  [%c.alloc
    ({|CAMLreturn(atg__embedding_bag_per_sample_weights_backward(%{grad : tensor value}, %{weight : tensor value}, %{indices : tensor value}, %{offsets : tensor value}, %{offset2bag : tensor value}, %{mode : int}, %{padding_idx : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _embedding_bag_per_sample_weights_backward_out
  ~(out @ local)
  ~(grad @ local)
  ~(weight @ local)
  ~(indices @ local)
  ~(offsets @ local)
  ~(offset2bag @ local)
  ~mode
  ~padding_idx
  =
  let out = globalize_tensor out in
  let grad = globalize_tensor grad in
  let weight = globalize_tensor weight in
  let indices = globalize_tensor indices in
  let offsets = globalize_tensor offsets in
  let offset2bag = globalize_tensor offset2bag in
  [%c.alloc
    ({|CAMLreturn(atg__embedding_bag_per_sample_weights_backward_out(%{out : tensor value}, %{grad : tensor value}, %{weight : tensor value}, %{indices : tensor value}, %{offsets : tensor value}, %{offset2bag : tensor value}, %{mode : int}, %{padding_idx : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _embedding_bag_sparse_backward
  ~(grad @ local)
  ~(indices @ local)
  ~(offsets @ local)
  ~(offset2bag @ local)
  ~(bag_size @ local)
  ~num_weights
  ~scale_grad_by_freq
  ~mode
  ~(per_sample_weights @ local)
  ~padding_idx
  =
  let grad = globalize_tensor grad in
  let indices = globalize_tensor indices in
  let offsets = globalize_tensor offsets in
  let offset2bag = globalize_tensor offset2bag in
  let bag_size = globalize_tensor bag_size in
  let per_sample_weights = [%globalize: tensor option] per_sample_weights in
  [%c.alloc
    ({|CAMLreturn(atg__embedding_bag_sparse_backward(%{grad : tensor value}, %{indices : tensor value}, %{offsets : tensor value}, %{offset2bag : tensor value}, %{bag_size : tensor value}, %{num_weights : int}, Int_val(%{scale_grad_by_freq : bool value}), %{mode : int}, %{per_sample_weights : tensor option value}, %{padding_idx : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _empty_affine_quantized ~size ~options ~scale ~zero_point =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg__empty_affine_quantized(%{size : int list value}, %{options_kind : int}, %{options_device : int}, %{scale : float}, %{zero_point : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _empty_affine_quantized_out ~(out @ local) ~size ~scale ~zero_point =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg__empty_affine_quantized_out(%{out : tensor value}, %{size : int list value}, %{scale : float}, %{zero_point : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _empty_per_channel_affine_quantized
  ~size
  ~(scales @ local)
  ~(zero_points @ local)
  ~axis
  ~options
  =
  let scales = globalize_tensor scales in
  let zero_points = globalize_tensor zero_points in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg__empty_per_channel_affine_quantized(%{size : int list value}, %{scales : tensor value}, %{zero_points : tensor value}, %{axis : int}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _empty_per_channel_affine_quantized_out
  ~(out @ local)
  ~size
  ~(scales @ local)
  ~(zero_points @ local)
  ~axis
  =
  let out = globalize_tensor out in
  let scales = globalize_tensor scales in
  let zero_points = globalize_tensor zero_points in
  [%c.alloc
    ({|CAMLreturn(atg__empty_per_channel_affine_quantized_out(%{out : tensor value}, %{size : int list value}, %{scales : tensor value}, %{zero_points : tensor value}, %{axis : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _euclidean_dist ~(x1 @ local) ~(x2 @ local) =
  let x1 = globalize_tensor x1 in
  let x2 = globalize_tensor x2 in
  [%c.alloc
    ({|CAMLreturn(atg__euclidean_dist(%{x1 : tensor value}, %{x2 : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _euclidean_dist_out ~(out @ local) ~(x1 @ local) ~(x2 @ local) =
  let out = globalize_tensor out in
  let x1 = globalize_tensor x1 in
  let x2 = globalize_tensor x2 in
  [%c.alloc
    ({|CAMLreturn(atg__euclidean_dist_out(%{out : tensor value}, %{x1 : tensor value}, %{x2 : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _fake_quantize_learnable_per_channel_affine
  (self @ local)
  ~(scale @ local)
  ~(zero_point @ local)
  ~axis
  ~quant_min
  ~quant_max
  ~grad_factor
  =
  let self = globalize_tensor self in
  let scale = globalize_tensor scale in
  let zero_point = globalize_tensor zero_point in
  [%c.alloc
    ({|CAMLreturn(atg__fake_quantize_learnable_per_channel_affine(%{self : tensor value}, %{scale : tensor value}, %{zero_point : tensor value}, %{axis : int}, %{quant_min : int}, %{quant_max : int}, %{grad_factor : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _fake_quantize_learnable_per_channel_affine_backward
  ~(grad @ local)
  (self @ local)
  ~(scale @ local)
  ~(zero_point @ local)
  ~axis
  ~quant_min
  ~quant_max
  ~grad_factor
  =
  let grad = globalize_tensor grad in
  let self = globalize_tensor self in
  let scale = globalize_tensor scale in
  let zero_point = globalize_tensor zero_point in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__fake_quantize_learnable_per_channel_affine_backward(%{grad : tensor value}, %{self : tensor value}, %{scale : tensor value}, %{zero_point : tensor value}, %{axis : int}, %{quant_min : int}, %{quant_max : int}, %{grad_factor : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _fake_quantize_learnable_per_channel_affine_out
  ~(out @ local)
  (self @ local)
  ~(scale @ local)
  ~(zero_point @ local)
  ~axis
  ~quant_min
  ~quant_max
  ~grad_factor
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let scale = globalize_tensor scale in
  let zero_point = globalize_tensor zero_point in
  [%c.alloc
    ({|CAMLreturn(atg__fake_quantize_learnable_per_channel_affine_out(%{out : tensor value}, %{self : tensor value}, %{scale : tensor value}, %{zero_point : tensor value}, %{axis : int}, %{quant_min : int}, %{quant_max : int}, %{grad_factor : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _fake_quantize_learnable_per_tensor_affine
  (self @ local)
  ~(scale @ local)
  ~(zero_point @ local)
  ~quant_min
  ~quant_max
  ~grad_factor
  =
  let self = globalize_tensor self in
  let scale = globalize_tensor scale in
  let zero_point = globalize_tensor zero_point in
  [%c.alloc
    ({|CAMLreturn(atg__fake_quantize_learnable_per_tensor_affine(%{self : tensor value}, %{scale : tensor value}, %{zero_point : tensor value}, %{quant_min : int}, %{quant_max : int}, %{grad_factor : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _fake_quantize_learnable_per_tensor_affine_backward
  ~(grad @ local)
  (self @ local)
  ~(scale @ local)
  ~(zero_point @ local)
  ~quant_min
  ~quant_max
  ~grad_factor
  =
  let grad = globalize_tensor grad in
  let self = globalize_tensor self in
  let scale = globalize_tensor scale in
  let zero_point = globalize_tensor zero_point in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__fake_quantize_learnable_per_tensor_affine_backward(%{grad : tensor value}, %{self : tensor value}, %{scale : tensor value}, %{zero_point : tensor value}, %{quant_min : int}, %{quant_max : int}, %{grad_factor : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _fake_quantize_learnable_per_tensor_affine_out
  ~(out @ local)
  (self @ local)
  ~(scale @ local)
  ~(zero_point @ local)
  ~quant_min
  ~quant_max
  ~grad_factor
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let scale = globalize_tensor scale in
  let zero_point = globalize_tensor zero_point in
  [%c.alloc
    ({|CAMLreturn(atg__fake_quantize_learnable_per_tensor_affine_out(%{out : tensor value}, %{self : tensor value}, %{scale : tensor value}, %{zero_point : tensor value}, %{quant_min : int}, %{quant_max : int}, %{grad_factor : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _fake_quantize_per_tensor_affine_cachemask_tensor_qparams
  (self @ local)
  ~(scale @ local)
  ~(zero_point @ local)
  ~(fake_quant_enabled @ local)
  ~quant_min
  ~quant_max
  =
  let self = globalize_tensor self in
  let scale = globalize_tensor scale in
  let zero_point = globalize_tensor zero_point in
  let fake_quant_enabled = globalize_tensor fake_quant_enabled in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__fake_quantize_per_tensor_affine_cachemask_tensor_qparams(%{self : tensor value}, %{scale : tensor value}, %{zero_point : tensor value}, %{fake_quant_enabled : tensor value}, %{quant_min : int}, %{quant_max : int}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _fake_quantize_per_tensor_affine_cachemask_tensor_qparams_out
  ~(out0 @ local)
  ~(out1 @ local)
  (self @ local)
  ~(scale @ local)
  ~(zero_point @ local)
  ~(fake_quant_enabled @ local)
  ~quant_min
  ~quant_max
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let self = globalize_tensor self in
  let scale = globalize_tensor scale in
  let zero_point = globalize_tensor zero_point in
  let fake_quant_enabled = globalize_tensor fake_quant_enabled in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__fake_quantize_per_tensor_affine_cachemask_tensor_qparams_out(%{out0 : tensor value}, %{out1 : tensor value}, %{self : tensor value}, %{scale : tensor value}, %{zero_point : tensor value}, %{fake_quant_enabled : tensor value}, %{quant_min : int}, %{quant_max : int}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _fft_c2c (self @ local) ~dim ~normalization ~forward =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__fft_c2c(%{self : tensor value}, %{dim : int list value}, %{normalization : int}, Int_val(%{forward : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _fft_c2c_out ~(out @ local) (self @ local) ~dim ~normalization ~forward =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__fft_c2c_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list value}, %{normalization : int}, Int_val(%{forward : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _fft_c2r (self @ local) ~dim ~normalization ~last_dim_size =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__fft_c2r(%{self : tensor value}, %{dim : int list value}, %{normalization : int}, %{last_dim_size : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _fft_c2r_out ~(out @ local) (self @ local) ~dim ~normalization ~last_dim_size =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__fft_c2r_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list value}, %{normalization : int}, %{last_dim_size : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _fft_r2c (self @ local) ~dim ~normalization ~onesided =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__fft_r2c(%{self : tensor value}, %{dim : int list value}, %{normalization : int}, Int_val(%{onesided : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _fft_r2c_out ~(out @ local) (self @ local) ~dim ~normalization ~onesided =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__fft_r2c_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list value}, %{normalization : int}, Int_val(%{onesided : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _fill_mem_eff_dropout_mask_ (self @ local) ~dropout_p ~seed ~offset =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__fill_mem_eff_dropout_mask_(%{self : tensor value}, %{dropout_p : float}, %{seed : int}, %{offset : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _flash_attention_backward
  ~(grad_out @ local)
  ~(query @ local)
  ~(key @ local)
  ~(value @ local)
  ~(out @ local)
  ~(logsumexp @ local)
  ~(cum_seq_q @ local)
  ~(cum_seq_k @ local)
  ~max_q
  ~max_k
  ~dropout_p
  ~is_causal
  ~(rng_state @ local)
  ~(unused @ local)
  ~scale
  ~window_size_left
  ~window_size_right
  =
  let grad_out = globalize_tensor grad_out in
  let query = globalize_tensor query in
  let key = globalize_tensor key in
  let value = globalize_tensor value in
  let out = globalize_tensor out in
  let logsumexp = globalize_tensor logsumexp in
  let cum_seq_q = globalize_tensor cum_seq_q in
  let cum_seq_k = globalize_tensor cum_seq_k in
  let rng_state = globalize_tensor rng_state in
  let unused = globalize_tensor unused in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__flash_attention_backward(%{grad_out : tensor value}, %{query : tensor value}, %{key : tensor value}, %{value : tensor value}, %{out : tensor value}, %{logsumexp : tensor value}, %{cum_seq_q : tensor value}, %{cum_seq_k : tensor value}, %{max_q : int}, %{max_k : int}, %{dropout_p : float}, Int_val(%{is_causal : bool value}), %{rng_state : tensor value}, %{unused : tensor value}, %{scale : float option value}, %{window_size_left : int option value}, %{window_size_right : int option value}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _foobar (self @ local) ~arg1 ~arg2 ~arg3 =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__foobar(%{self : tensor value}, Int_val(%{arg1 : bool value}), Int_val(%{arg2 : bool value}), Int_val(%{arg3 : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _foobar_out ~(out @ local) (self @ local) ~arg1 ~arg2 ~arg3 =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__foobar_out(%{out : tensor value}, %{self : tensor value}, Int_val(%{arg1 : bool value}), Int_val(%{arg2 : bool value}), Int_val(%{arg3 : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _functional_assert_async (self @ local) ~assert_msg ~(dep_token @ local) =
  let self = globalize_tensor self in
  let dep_token = globalize_tensor dep_token in
  [%c.alloc
    ({|CAMLreturn(atg__functional_assert_async(%{self : tensor value}, String_val(%{assert_msg : string value}), %{dep_token : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _functional_assert_scalar self ~assert_msg ~(dep_token @ local) =
  let dep_token = globalize_tensor dep_token in
  [%c.alloc
    ({|CAMLreturn(atg__functional_assert_scalar(%{self : scalar value}, String_val(%{assert_msg : string value}), %{dep_token : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _functional_sym_constrain_range ~size ~min ~max ~(dep_token @ local) =
  let dep_token = globalize_tensor dep_token in
  [%c.alloc
    ({|CAMLreturn(atg__functional_sym_constrain_range(%{size : scalar value}, %{min : int option value}, %{max : int option value}, %{dep_token : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _functional_sym_constrain_range_for_size ~size ~min ~max ~(dep_token @ local) =
  let dep_token = globalize_tensor dep_token in
  [%c.alloc
    ({|CAMLreturn(atg__functional_sym_constrain_range_for_size(%{size : scalar value}, %{min : int option value}, %{max : int option value}, %{dep_token : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _fused_adagrad
  ~(out @ local)
  (self @ local)
  ~(grads @ local)
  ~(state_sums @ local)
  ~(state_steps @ local)
  ~lr
  ~lr_decay
  ~weight_decay
  ~eps
  ~maximize
  ~(grad_scale @ local)
  ~(found_inf @ local)
  =
  let out = Torch_local_iterators.List.map_local_input out ~f:globalize_tensor in
  let self = Torch_local_iterators.List.map_local_input self ~f:globalize_tensor in
  let grads = Torch_local_iterators.List.map_local_input grads ~f:globalize_tensor in
  let state_sums =
    Torch_local_iterators.List.map_local_input state_sums ~f:globalize_tensor
  in
  let state_steps =
    Torch_local_iterators.List.map_local_input state_steps ~f:globalize_tensor
  in
  let grad_scale = [%globalize: tensor option] grad_scale in
  let found_inf = [%globalize: tensor option] found_inf in
  [%c.alloc
    {|atg__fused_adagrad(%{out : tensor list value}, %{self : tensor list value}, %{grads : tensor list value}, %{state_sums : tensor list value}, %{state_steps : tensor list value}, %{lr : float}, %{lr_decay : float}, %{weight_decay : float}, %{eps : float}, Int_val(%{maximize : bool value}), %{grad_scale : tensor option value}, %{found_inf : tensor option value});|}]
;;

let _fused_adagrad_
  (self @ local)
  ~(grads @ local)
  ~(state_sums @ local)
  ~(state_steps @ local)
  ~lr
  ~lr_decay
  ~weight_decay
  ~eps
  ~maximize
  ~(grad_scale @ local)
  ~(found_inf @ local)
  =
  let self = Torch_local_iterators.List.map_local_input self ~f:globalize_tensor in
  let grads = Torch_local_iterators.List.map_local_input grads ~f:globalize_tensor in
  let state_sums =
    Torch_local_iterators.List.map_local_input state_sums ~f:globalize_tensor
  in
  let state_steps =
    Torch_local_iterators.List.map_local_input state_steps ~f:globalize_tensor
  in
  let grad_scale = [%globalize: tensor option] grad_scale in
  let found_inf = [%globalize: tensor option] found_inf in
  [%c.alloc
    {|atg__fused_adagrad_(%{self : tensor list value}, %{grads : tensor list value}, %{state_sums : tensor list value}, %{state_steps : tensor list value}, %{lr : float}, %{lr_decay : float}, %{weight_decay : float}, %{eps : float}, Int_val(%{maximize : bool value}), %{grad_scale : tensor option value}, %{found_inf : tensor option value});|}]
;;

let _fused_adam
  ~(out @ local)
  (self @ local)
  ~(grads @ local)
  ~(exp_avgs @ local)
  ~(exp_avg_sqs @ local)
  ~(max_exp_avg_sqs @ local)
  ~(state_steps @ local)
  ~lr
  ~beta1
  ~beta2
  ~weight_decay
  ~eps
  ~amsgrad
  ~maximize
  ~(grad_scale @ local)
  ~(found_inf @ local)
  =
  let out = Torch_local_iterators.List.map_local_input out ~f:globalize_tensor in
  let self = Torch_local_iterators.List.map_local_input self ~f:globalize_tensor in
  let grads = Torch_local_iterators.List.map_local_input grads ~f:globalize_tensor in
  let exp_avgs =
    Torch_local_iterators.List.map_local_input exp_avgs ~f:globalize_tensor
  in
  let exp_avg_sqs =
    Torch_local_iterators.List.map_local_input exp_avg_sqs ~f:globalize_tensor
  in
  let max_exp_avg_sqs =
    Torch_local_iterators.List.map_local_input max_exp_avg_sqs ~f:globalize_tensor
  in
  let state_steps =
    Torch_local_iterators.List.map_local_input state_steps ~f:globalize_tensor
  in
  let grad_scale = [%globalize: tensor option] grad_scale in
  let found_inf = [%globalize: tensor option] found_inf in
  [%c.alloc
    {|atg__fused_adam(%{out : tensor list value}, %{self : tensor list value}, %{grads : tensor list value}, %{exp_avgs : tensor list value}, %{exp_avg_sqs : tensor list value}, %{max_exp_avg_sqs : tensor list value}, %{state_steps : tensor list value}, %{lr : float}, %{beta1 : float}, %{beta2 : float}, %{weight_decay : float}, %{eps : float}, Int_val(%{amsgrad : bool value}), Int_val(%{maximize : bool value}), %{grad_scale : tensor option value}, %{found_inf : tensor option value});|}]
;;

let _fused_adam_
  (self @ local)
  ~(grads @ local)
  ~(exp_avgs @ local)
  ~(exp_avg_sqs @ local)
  ~(max_exp_avg_sqs @ local)
  ~(state_steps @ local)
  ~lr
  ~beta1
  ~beta2
  ~weight_decay
  ~eps
  ~amsgrad
  ~maximize
  ~(grad_scale @ local)
  ~(found_inf @ local)
  =
  let self = Torch_local_iterators.List.map_local_input self ~f:globalize_tensor in
  let grads = Torch_local_iterators.List.map_local_input grads ~f:globalize_tensor in
  let exp_avgs =
    Torch_local_iterators.List.map_local_input exp_avgs ~f:globalize_tensor
  in
  let exp_avg_sqs =
    Torch_local_iterators.List.map_local_input exp_avg_sqs ~f:globalize_tensor
  in
  let max_exp_avg_sqs =
    Torch_local_iterators.List.map_local_input max_exp_avg_sqs ~f:globalize_tensor
  in
  let state_steps =
    Torch_local_iterators.List.map_local_input state_steps ~f:globalize_tensor
  in
  let grad_scale = [%globalize: tensor option] grad_scale in
  let found_inf = [%globalize: tensor option] found_inf in
  [%c.alloc
    {|atg__fused_adam_(%{self : tensor list value}, %{grads : tensor list value}, %{exp_avgs : tensor list value}, %{exp_avg_sqs : tensor list value}, %{max_exp_avg_sqs : tensor list value}, %{state_steps : tensor list value}, %{lr : float}, %{beta1 : float}, %{beta2 : float}, %{weight_decay : float}, %{eps : float}, Int_val(%{amsgrad : bool value}), Int_val(%{maximize : bool value}), %{grad_scale : tensor option value}, %{found_inf : tensor option value});|}]
;;

let _fused_adam_tensor_lr_
  (self @ local)
  ~(grads @ local)
  ~(exp_avgs @ local)
  ~(exp_avg_sqs @ local)
  ~(max_exp_avg_sqs @ local)
  ~(state_steps @ local)
  ~(lr @ local)
  ~beta1
  ~beta2
  ~weight_decay
  ~eps
  ~amsgrad
  ~maximize
  ~(grad_scale @ local)
  ~(found_inf @ local)
  =
  let self = Torch_local_iterators.List.map_local_input self ~f:globalize_tensor in
  let grads = Torch_local_iterators.List.map_local_input grads ~f:globalize_tensor in
  let exp_avgs =
    Torch_local_iterators.List.map_local_input exp_avgs ~f:globalize_tensor
  in
  let exp_avg_sqs =
    Torch_local_iterators.List.map_local_input exp_avg_sqs ~f:globalize_tensor
  in
  let max_exp_avg_sqs =
    Torch_local_iterators.List.map_local_input max_exp_avg_sqs ~f:globalize_tensor
  in
  let state_steps =
    Torch_local_iterators.List.map_local_input state_steps ~f:globalize_tensor
  in
  let lr = globalize_tensor lr in
  let grad_scale = [%globalize: tensor option] grad_scale in
  let found_inf = [%globalize: tensor option] found_inf in
  [%c.alloc
    {|atg__fused_adam_tensor_lr_(%{self : tensor list value}, %{grads : tensor list value}, %{exp_avgs : tensor list value}, %{exp_avg_sqs : tensor list value}, %{max_exp_avg_sqs : tensor list value}, %{state_steps : tensor list value}, %{lr : tensor value}, %{beta1 : float}, %{beta2 : float}, %{weight_decay : float}, %{eps : float}, Int_val(%{amsgrad : bool value}), Int_val(%{maximize : bool value}), %{grad_scale : tensor option value}, %{found_inf : tensor option value});|}]
;;

let _fused_adam_tensor_lr_out
  ~(out @ local)
  (self @ local)
  ~(grads @ local)
  ~(exp_avgs @ local)
  ~(exp_avg_sqs @ local)
  ~(max_exp_avg_sqs @ local)
  ~(state_steps @ local)
  ~(lr @ local)
  ~beta1
  ~beta2
  ~weight_decay
  ~eps
  ~amsgrad
  ~maximize
  ~(grad_scale @ local)
  ~(found_inf @ local)
  =
  let out = Torch_local_iterators.List.map_local_input out ~f:globalize_tensor in
  let self = Torch_local_iterators.List.map_local_input self ~f:globalize_tensor in
  let grads = Torch_local_iterators.List.map_local_input grads ~f:globalize_tensor in
  let exp_avgs =
    Torch_local_iterators.List.map_local_input exp_avgs ~f:globalize_tensor
  in
  let exp_avg_sqs =
    Torch_local_iterators.List.map_local_input exp_avg_sqs ~f:globalize_tensor
  in
  let max_exp_avg_sqs =
    Torch_local_iterators.List.map_local_input max_exp_avg_sqs ~f:globalize_tensor
  in
  let state_steps =
    Torch_local_iterators.List.map_local_input state_steps ~f:globalize_tensor
  in
  let lr = globalize_tensor lr in
  let grad_scale = [%globalize: tensor option] grad_scale in
  let found_inf = [%globalize: tensor option] found_inf in
  [%c.alloc
    {|atg__fused_adam_tensor_lr_out(%{out : tensor list value}, %{self : tensor list value}, %{grads : tensor list value}, %{exp_avgs : tensor list value}, %{exp_avg_sqs : tensor list value}, %{max_exp_avg_sqs : tensor list value}, %{state_steps : tensor list value}, %{lr : tensor value}, %{beta1 : float}, %{beta2 : float}, %{weight_decay : float}, %{eps : float}, Int_val(%{amsgrad : bool value}), Int_val(%{maximize : bool value}), %{grad_scale : tensor option value}, %{found_inf : tensor option value});|}]
;;

let _fused_adamw
  ~(out @ local)
  (self @ local)
  ~(grads @ local)
  ~(exp_avgs @ local)
  ~(exp_avg_sqs @ local)
  ~(max_exp_avg_sqs @ local)
  ~(state_steps @ local)
  ~lr
  ~beta1
  ~beta2
  ~weight_decay
  ~eps
  ~amsgrad
  ~maximize
  ~(grad_scale @ local)
  ~(found_inf @ local)
  =
  let out = Torch_local_iterators.List.map_local_input out ~f:globalize_tensor in
  let self = Torch_local_iterators.List.map_local_input self ~f:globalize_tensor in
  let grads = Torch_local_iterators.List.map_local_input grads ~f:globalize_tensor in
  let exp_avgs =
    Torch_local_iterators.List.map_local_input exp_avgs ~f:globalize_tensor
  in
  let exp_avg_sqs =
    Torch_local_iterators.List.map_local_input exp_avg_sqs ~f:globalize_tensor
  in
  let max_exp_avg_sqs =
    Torch_local_iterators.List.map_local_input max_exp_avg_sqs ~f:globalize_tensor
  in
  let state_steps =
    Torch_local_iterators.List.map_local_input state_steps ~f:globalize_tensor
  in
  let grad_scale = [%globalize: tensor option] grad_scale in
  let found_inf = [%globalize: tensor option] found_inf in
  [%c.alloc
    {|atg__fused_adamw(%{out : tensor list value}, %{self : tensor list value}, %{grads : tensor list value}, %{exp_avgs : tensor list value}, %{exp_avg_sqs : tensor list value}, %{max_exp_avg_sqs : tensor list value}, %{state_steps : tensor list value}, %{lr : float}, %{beta1 : float}, %{beta2 : float}, %{weight_decay : float}, %{eps : float}, Int_val(%{amsgrad : bool value}), Int_val(%{maximize : bool value}), %{grad_scale : tensor option value}, %{found_inf : tensor option value});|}]
;;

let _fused_adamw_
  (self @ local)
  ~(grads @ local)
  ~(exp_avgs @ local)
  ~(exp_avg_sqs @ local)
  ~(max_exp_avg_sqs @ local)
  ~(state_steps @ local)
  ~lr
  ~beta1
  ~beta2
  ~weight_decay
  ~eps
  ~amsgrad
  ~maximize
  ~(grad_scale @ local)
  ~(found_inf @ local)
  =
  let self = Torch_local_iterators.List.map_local_input self ~f:globalize_tensor in
  let grads = Torch_local_iterators.List.map_local_input grads ~f:globalize_tensor in
  let exp_avgs =
    Torch_local_iterators.List.map_local_input exp_avgs ~f:globalize_tensor
  in
  let exp_avg_sqs =
    Torch_local_iterators.List.map_local_input exp_avg_sqs ~f:globalize_tensor
  in
  let max_exp_avg_sqs =
    Torch_local_iterators.List.map_local_input max_exp_avg_sqs ~f:globalize_tensor
  in
  let state_steps =
    Torch_local_iterators.List.map_local_input state_steps ~f:globalize_tensor
  in
  let grad_scale = [%globalize: tensor option] grad_scale in
  let found_inf = [%globalize: tensor option] found_inf in
  [%c.alloc
    {|atg__fused_adamw_(%{self : tensor list value}, %{grads : tensor list value}, %{exp_avgs : tensor list value}, %{exp_avg_sqs : tensor list value}, %{max_exp_avg_sqs : tensor list value}, %{state_steps : tensor list value}, %{lr : float}, %{beta1 : float}, %{beta2 : float}, %{weight_decay : float}, %{eps : float}, Int_val(%{amsgrad : bool value}), Int_val(%{maximize : bool value}), %{grad_scale : tensor option value}, %{found_inf : tensor option value});|}]
;;

let _fused_adamw_tensor_lr_
  (self @ local)
  ~(grads @ local)
  ~(exp_avgs @ local)
  ~(exp_avg_sqs @ local)
  ~(max_exp_avg_sqs @ local)
  ~(state_steps @ local)
  ~(lr @ local)
  ~beta1
  ~beta2
  ~weight_decay
  ~eps
  ~amsgrad
  ~maximize
  ~(grad_scale @ local)
  ~(found_inf @ local)
  =
  let self = Torch_local_iterators.List.map_local_input self ~f:globalize_tensor in
  let grads = Torch_local_iterators.List.map_local_input grads ~f:globalize_tensor in
  let exp_avgs =
    Torch_local_iterators.List.map_local_input exp_avgs ~f:globalize_tensor
  in
  let exp_avg_sqs =
    Torch_local_iterators.List.map_local_input exp_avg_sqs ~f:globalize_tensor
  in
  let max_exp_avg_sqs =
    Torch_local_iterators.List.map_local_input max_exp_avg_sqs ~f:globalize_tensor
  in
  let state_steps =
    Torch_local_iterators.List.map_local_input state_steps ~f:globalize_tensor
  in
  let lr = globalize_tensor lr in
  let grad_scale = [%globalize: tensor option] grad_scale in
  let found_inf = [%globalize: tensor option] found_inf in
  [%c.alloc
    {|atg__fused_adamw_tensor_lr_(%{self : tensor list value}, %{grads : tensor list value}, %{exp_avgs : tensor list value}, %{exp_avg_sqs : tensor list value}, %{max_exp_avg_sqs : tensor list value}, %{state_steps : tensor list value}, %{lr : tensor value}, %{beta1 : float}, %{beta2 : float}, %{weight_decay : float}, %{eps : float}, Int_val(%{amsgrad : bool value}), Int_val(%{maximize : bool value}), %{grad_scale : tensor option value}, %{found_inf : tensor option value});|}]
;;

let _fused_adamw_tensor_lr_out
  ~(out @ local)
  (self @ local)
  ~(grads @ local)
  ~(exp_avgs @ local)
  ~(exp_avg_sqs @ local)
  ~(max_exp_avg_sqs @ local)
  ~(state_steps @ local)
  ~(lr @ local)
  ~beta1
  ~beta2
  ~weight_decay
  ~eps
  ~amsgrad
  ~maximize
  ~(grad_scale @ local)
  ~(found_inf @ local)
  =
  let out = Torch_local_iterators.List.map_local_input out ~f:globalize_tensor in
  let self = Torch_local_iterators.List.map_local_input self ~f:globalize_tensor in
  let grads = Torch_local_iterators.List.map_local_input grads ~f:globalize_tensor in
  let exp_avgs =
    Torch_local_iterators.List.map_local_input exp_avgs ~f:globalize_tensor
  in
  let exp_avg_sqs =
    Torch_local_iterators.List.map_local_input exp_avg_sqs ~f:globalize_tensor
  in
  let max_exp_avg_sqs =
    Torch_local_iterators.List.map_local_input max_exp_avg_sqs ~f:globalize_tensor
  in
  let state_steps =
    Torch_local_iterators.List.map_local_input state_steps ~f:globalize_tensor
  in
  let lr = globalize_tensor lr in
  let grad_scale = [%globalize: tensor option] grad_scale in
  let found_inf = [%globalize: tensor option] found_inf in
  [%c.alloc
    {|atg__fused_adamw_tensor_lr_out(%{out : tensor list value}, %{self : tensor list value}, %{grads : tensor list value}, %{exp_avgs : tensor list value}, %{exp_avg_sqs : tensor list value}, %{max_exp_avg_sqs : tensor list value}, %{state_steps : tensor list value}, %{lr : tensor value}, %{beta1 : float}, %{beta2 : float}, %{weight_decay : float}, %{eps : float}, Int_val(%{amsgrad : bool value}), Int_val(%{maximize : bool value}), %{grad_scale : tensor option value}, %{found_inf : tensor option value});|}]
;;

let _fused_dropout (self @ local) ~p =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__fused_dropout(%{self : tensor value}, %{p : float}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _fused_dropout_out ~(out0 @ local) ~(out1 @ local) (self @ local) ~p =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__fused_dropout_out(%{out0 : tensor value}, %{out1 : tensor value}, %{self : tensor value}, %{p : float}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _fused_moving_avg_obs_fq_helper
  (self @ local)
  ~(observer_on @ local)
  ~(fake_quant_on @ local)
  ~(running_min @ local)
  ~(running_max @ local)
  ~(scale @ local)
  ~(zero_point @ local)
  ~averaging_const
  ~quant_min
  ~quant_max
  ~ch_axis
  ~per_row_fake_quant
  ~symmetric_quant
  =
  let self = globalize_tensor self in
  let observer_on = globalize_tensor observer_on in
  let fake_quant_on = globalize_tensor fake_quant_on in
  let running_min = globalize_tensor running_min in
  let running_max = globalize_tensor running_max in
  let scale = globalize_tensor scale in
  let zero_point = globalize_tensor zero_point in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__fused_moving_avg_obs_fq_helper(%{self : tensor value}, %{observer_on : tensor value}, %{fake_quant_on : tensor value}, %{running_min : tensor value}, %{running_max : tensor value}, %{scale : tensor value}, %{zero_point : tensor value}, %{averaging_const : float}, %{quant_min : int}, %{quant_max : int}, %{ch_axis : int}, Int_val(%{per_row_fake_quant : bool value}), Int_val(%{symmetric_quant : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _fused_moving_avg_obs_fq_helper_functional
  (self @ local)
  ~(observer_on @ local)
  ~(fake_quant_on @ local)
  ~(running_min @ local)
  ~(running_max @ local)
  ~(scale @ local)
  ~(zero_point @ local)
  ~averaging_const
  ~quant_min
  ~quant_max
  ~ch_axis
  ~per_row_fake_quant
  ~symmetric_quant
  =
  let self = globalize_tensor self in
  let observer_on = globalize_tensor observer_on in
  let fake_quant_on = globalize_tensor fake_quant_on in
  let running_min = globalize_tensor running_min in
  let running_max = globalize_tensor running_max in
  let scale = globalize_tensor scale in
  let zero_point = globalize_tensor zero_point in
  let result0, result1, result2, result3, result4, result5 =
    [%c.alloc
      ({|CAMLreturn(atg__fused_moving_avg_obs_fq_helper_functional(%{self : tensor value}, %{observer_on : tensor value}, %{fake_quant_on : tensor value}, %{running_min : tensor value}, %{running_max : tensor value}, %{scale : tensor value}, %{zero_point : tensor value}, %{averaging_const : float}, %{quant_min : int}, %{quant_max : int}, %{ch_axis : int}, Int_val(%{per_row_fake_quant : bool value}), Int_val(%{symmetric_quant : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor * raw_tensor * raw_tensor)
           value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3
  , wrap_managed_tensor result4
  , wrap_managed_tensor result5 )
;;

let _fused_moving_avg_obs_fq_helper_out
  ~(out0 @ local)
  ~(out1 @ local)
  (self @ local)
  ~(observer_on @ local)
  ~(fake_quant_on @ local)
  ~(running_min @ local)
  ~(running_max @ local)
  ~(scale @ local)
  ~(zero_point @ local)
  ~averaging_const
  ~quant_min
  ~quant_max
  ~ch_axis
  ~per_row_fake_quant
  ~symmetric_quant
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let self = globalize_tensor self in
  let observer_on = globalize_tensor observer_on in
  let fake_quant_on = globalize_tensor fake_quant_on in
  let running_min = globalize_tensor running_min in
  let running_max = globalize_tensor running_max in
  let scale = globalize_tensor scale in
  let zero_point = globalize_tensor zero_point in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__fused_moving_avg_obs_fq_helper_out(%{out0 : tensor value}, %{out1 : tensor value}, %{self : tensor value}, %{observer_on : tensor value}, %{fake_quant_on : tensor value}, %{running_min : tensor value}, %{running_max : tensor value}, %{scale : tensor value}, %{zero_point : tensor value}, %{averaging_const : float}, %{quant_min : int}, %{quant_max : int}, %{ch_axis : int}, Int_val(%{per_row_fake_quant : bool value}), Int_val(%{symmetric_quant : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _fused_sdp_choice
  ~(query @ local)
  ~(key @ local)
  ~(value @ local)
  ~(attn_mask @ local)
  ~dropout_p
  ~is_causal
  ~scale
  ~enable_gqa
  =
  let query = globalize_tensor query in
  let key = globalize_tensor key in
  let value = globalize_tensor value in
  let attn_mask = [%globalize: tensor option] attn_mask in
  [%c.alloc
    ({|CAMLreturn(atg__fused_sdp_choice(%{query : tensor value}, %{key : tensor value}, %{value : tensor value}, %{attn_mask : tensor option value}, %{dropout_p : float}, Int_val(%{is_causal : bool value}), %{scale : float option value}, Int_val(%{enable_gqa : bool value})));|}
     : int64 value)]
;;

let _fused_sgd
  ~(out @ local)
  (self @ local)
  ~(grads @ local)
  ~(momentum_buffer_list @ local)
  ~weight_decay
  ~momentum
  ~lr
  ~dampening
  ~nesterov
  ~maximize
  ~is_first_step
  ~(grad_scale @ local)
  ~(found_inf @ local)
  =
  let out = Torch_local_iterators.List.map_local_input out ~f:globalize_tensor in
  let self = Torch_local_iterators.List.map_local_input self ~f:globalize_tensor in
  let grads = Torch_local_iterators.List.map_local_input grads ~f:globalize_tensor in
  let momentum_buffer_list =
    Torch_local_iterators.List.map_local_input momentum_buffer_list ~f:globalize_tensor
  in
  let grad_scale = [%globalize: tensor option] grad_scale in
  let found_inf = [%globalize: tensor option] found_inf in
  [%c.alloc
    {|atg__fused_sgd(%{out : tensor list value}, %{self : tensor list value}, %{grads : tensor list value}, %{momentum_buffer_list : tensor list value}, %{weight_decay : float}, %{momentum : float}, %{lr : float}, %{dampening : float}, Int_val(%{nesterov : bool value}), Int_val(%{maximize : bool value}), Int_val(%{is_first_step : bool value}), %{grad_scale : tensor option value}, %{found_inf : tensor option value});|}]
;;

let _fused_sgd_
  (self @ local)
  ~(grads @ local)
  ~(momentum_buffer_list @ local)
  ~weight_decay
  ~momentum
  ~lr
  ~dampening
  ~nesterov
  ~maximize
  ~is_first_step
  ~(grad_scale @ local)
  ~(found_inf @ local)
  =
  let self = Torch_local_iterators.List.map_local_input self ~f:globalize_tensor in
  let grads = Torch_local_iterators.List.map_local_input grads ~f:globalize_tensor in
  let momentum_buffer_list =
    Torch_local_iterators.List.map_local_input momentum_buffer_list ~f:globalize_tensor
  in
  let grad_scale = [%globalize: tensor option] grad_scale in
  let found_inf = [%globalize: tensor option] found_inf in
  [%c.alloc
    {|atg__fused_sgd_(%{self : tensor list value}, %{grads : tensor list value}, %{momentum_buffer_list : tensor list value}, %{weight_decay : float}, %{momentum : float}, %{lr : float}, %{dampening : float}, Int_val(%{nesterov : bool value}), Int_val(%{maximize : bool value}), Int_val(%{is_first_step : bool value}), %{grad_scale : tensor option value}, %{found_inf : tensor option value});|}]
;;

let _fused_sgd_tensor_lr_
  (self @ local)
  ~(grads @ local)
  ~(momentum_buffer_list @ local)
  ~weight_decay
  ~momentum
  ~(lr @ local)
  ~dampening
  ~nesterov
  ~maximize
  ~is_first_step
  ~(grad_scale @ local)
  ~(found_inf @ local)
  =
  let self = Torch_local_iterators.List.map_local_input self ~f:globalize_tensor in
  let grads = Torch_local_iterators.List.map_local_input grads ~f:globalize_tensor in
  let momentum_buffer_list =
    Torch_local_iterators.List.map_local_input momentum_buffer_list ~f:globalize_tensor
  in
  let lr = globalize_tensor lr in
  let grad_scale = [%globalize: tensor option] grad_scale in
  let found_inf = [%globalize: tensor option] found_inf in
  [%c.alloc
    {|atg__fused_sgd_tensor_lr_(%{self : tensor list value}, %{grads : tensor list value}, %{momentum_buffer_list : tensor list value}, %{weight_decay : float}, %{momentum : float}, %{lr : tensor value}, %{dampening : float}, Int_val(%{nesterov : bool value}), Int_val(%{maximize : bool value}), Int_val(%{is_first_step : bool value}), %{grad_scale : tensor option value}, %{found_inf : tensor option value});|}]
;;

let _fused_sgd_tensor_lr_out
  ~(out @ local)
  (self @ local)
  ~(grads @ local)
  ~(momentum_buffer_list @ local)
  ~weight_decay
  ~momentum
  ~(lr @ local)
  ~dampening
  ~nesterov
  ~maximize
  ~is_first_step
  ~(grad_scale @ local)
  ~(found_inf @ local)
  =
  let out = Torch_local_iterators.List.map_local_input out ~f:globalize_tensor in
  let self = Torch_local_iterators.List.map_local_input self ~f:globalize_tensor in
  let grads = Torch_local_iterators.List.map_local_input grads ~f:globalize_tensor in
  let momentum_buffer_list =
    Torch_local_iterators.List.map_local_input momentum_buffer_list ~f:globalize_tensor
  in
  let lr = globalize_tensor lr in
  let grad_scale = [%globalize: tensor option] grad_scale in
  let found_inf = [%globalize: tensor option] found_inf in
  [%c.alloc
    {|atg__fused_sgd_tensor_lr_out(%{out : tensor list value}, %{self : tensor list value}, %{grads : tensor list value}, %{momentum_buffer_list : tensor list value}, %{weight_decay : float}, %{momentum : float}, %{lr : tensor value}, %{dampening : float}, Int_val(%{nesterov : bool value}), Int_val(%{maximize : bool value}), Int_val(%{is_first_step : bool value}), %{grad_scale : tensor option value}, %{found_inf : tensor option value});|}]
;;

let _fw_primal (self @ local) ~level =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__fw_primal(%{self : tensor value}, %{level : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _fw_primal_copy (self @ local) ~level =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__fw_primal_copy(%{self : tensor value}, %{level : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _fw_primal_copy_out ~(out @ local) (self @ local) ~level =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__fw_primal_copy_out(%{out : tensor value}, %{self : tensor value}, %{level : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _gather_sparse_backward (self @ local) ~dim ~(index @ local) ~(grad @ local) =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let grad = globalize_tensor grad in
  [%c.alloc
    ({|CAMLreturn(atg__gather_sparse_backward(%{self : tensor value}, %{dim : int}, %{index : tensor value}, %{grad : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _grid_sampler_2d_cpu_fallback
  (input @ local)
  ~(grid @ local)
  ~interpolation_mode
  ~padding_mode
  ~align_corners
  =
  let input = globalize_tensor input in
  let grid = globalize_tensor grid in
  [%c.alloc
    ({|CAMLreturn(atg__grid_sampler_2d_cpu_fallback(%{input : tensor value}, %{grid : tensor value}, %{interpolation_mode : int}, %{padding_mode : int}, Int_val(%{align_corners : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _grid_sampler_2d_cpu_fallback_backward
  ~(grad_output @ local)
  (input @ local)
  ~(grid @ local)
  ~interpolation_mode
  ~padding_mode
  ~align_corners
  =
  let grad_output = globalize_tensor grad_output in
  let input = globalize_tensor input in
  let grid = globalize_tensor grid in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__grid_sampler_2d_cpu_fallback_backward(%{grad_output : tensor value}, %{input : tensor value}, %{grid : tensor value}, %{interpolation_mode : int}, %{padding_mode : int}, Int_val(%{align_corners : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _grid_sampler_2d_cpu_fallback_out
  ~(out @ local)
  (input @ local)
  ~(grid @ local)
  ~interpolation_mode
  ~padding_mode
  ~align_corners
  =
  let out = globalize_tensor out in
  let input = globalize_tensor input in
  let grid = globalize_tensor grid in
  [%c.alloc
    ({|CAMLreturn(atg__grid_sampler_2d_cpu_fallback_out(%{out : tensor value}, %{input : tensor value}, %{grid : tensor value}, %{interpolation_mode : int}, %{padding_mode : int}, Int_val(%{align_corners : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _has_compatible_shallow_copy_type (self @ local) ~(from @ local) =
  let self = globalize_tensor self in
  let from = globalize_tensor from in
  [%c.alloc
    ({|CAMLreturn(atg__has_compatible_shallow_copy_type(%{self : tensor value}, %{from : tensor value}));|}
     : bool value)]
;;

let _has_same_storage_numel (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg__has_same_storage_numel(%{self : tensor value}, %{other : tensor value}));|}
     : bool value)]
;;

let _histogramdd_bin_edges (self @ local) ~bins ~range ~(weight @ local) ~density =
  let self = globalize_tensor self in
  let weight = [%globalize: tensor option] weight in
  [%c.alloc
    ({|CAMLreturn(atg__histogramdd_bin_edges(%{self : tensor value}, %{bins : int list value}, %{range : float list value}, %{weight : tensor option value}, Int_val(%{density : bool value})));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let _histogramdd_bin_edges_out
  ~(out @ local)
  (self @ local)
  ~bins
  ~range
  ~(weight @ local)
  ~density
  =
  let out = Torch_local_iterators.List.map_local_input out ~f:globalize_tensor in
  let self = globalize_tensor self in
  let weight = [%globalize: tensor option] weight in
  [%c.alloc
    {|atg__histogramdd_bin_edges_out(%{out : tensor list value}, %{self : tensor value}, %{bins : int list value}, %{range : float list value}, %{weight : tensor option value}, Int_val(%{density : bool value}));|}]
;;

let _histogramdd_from_bin_cts (self @ local) ~bins ~range ~(weight @ local) ~density =
  let self = globalize_tensor self in
  let weight = [%globalize: tensor option] weight in
  [%c.alloc
    ({|CAMLreturn(atg__histogramdd_from_bin_cts(%{self : tensor value}, %{bins : int list value}, %{range : float list value}, %{weight : tensor option value}, Int_val(%{density : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _histogramdd_from_bin_cts_out
  ~(out @ local)
  (self @ local)
  ~bins
  ~range
  ~(weight @ local)
  ~density
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weight = [%globalize: tensor option] weight in
  [%c.alloc
    ({|CAMLreturn(atg__histogramdd_from_bin_cts_out(%{out : tensor value}, %{self : tensor value}, %{bins : int list value}, %{range : float list value}, %{weight : tensor option value}, Int_val(%{density : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _histogramdd_from_bin_tensors
  (self @ local)
  ~(bins @ local)
  ~(weight @ local)
  ~density
  =
  let self = globalize_tensor self in
  let bins = Torch_local_iterators.List.map_local_input bins ~f:globalize_tensor in
  let weight = [%globalize: tensor option] weight in
  [%c.alloc
    ({|CAMLreturn(atg__histogramdd_from_bin_tensors(%{self : tensor value}, %{bins : tensor list value}, %{weight : tensor option value}, Int_val(%{density : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _histogramdd_from_bin_tensors_out
  ~(out @ local)
  (self @ local)
  ~(bins @ local)
  ~(weight @ local)
  ~density
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let bins = Torch_local_iterators.List.map_local_input bins ~f:globalize_tensor in
  let weight = [%globalize: tensor option] weight in
  [%c.alloc
    ({|CAMLreturn(atg__histogramdd_from_bin_tensors_out(%{out : tensor value}, %{self : tensor value}, %{bins : tensor list value}, %{weight : tensor option value}, Int_val(%{density : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _indices (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg__indices(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _indices_copy (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__indices_copy(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _indices_copy_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__indices_copy_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _int_mm (self @ local) ~(mat2 @ local) =
  let self = globalize_tensor self in
  let mat2 = globalize_tensor mat2 in
  [%c.alloc
    ({|CAMLreturn(atg__int_mm(%{self : tensor value}, %{mat2 : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _int_mm_out ~(out @ local) (self @ local) ~(mat2 @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let mat2 = globalize_tensor mat2 in
  [%c.alloc
    ({|CAMLreturn(atg__int_mm_out(%{out : tensor value}, %{self : tensor value}, %{mat2 : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _is_all_true (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__is_all_true(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _is_any_true (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__is_any_true(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _is_zerotensor (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg__is_zerotensor(%{self : tensor value}));|} : bool value)]
;;

let _lazy_clone (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg__lazy_clone(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _linalg_check_errors ~(info @ local) ~api_name ~is_matrix =
  let info = globalize_tensor info in
  [%c.alloc
    {|atg__linalg_check_errors(%{info : tensor value}, String_val(%{api_name : string value}), Int_val(%{is_matrix : bool value}));|}]
;;

let _linalg_det ~(a @ local) =
  let a = globalize_tensor a in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__linalg_det(%{a : tensor value}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _linalg_det_result (result @ local) ~(lu @ local) ~(pivots @ local) ~(a @ local) =
  let result = globalize_tensor result in
  let lu = globalize_tensor lu in
  let pivots = globalize_tensor pivots in
  let a = globalize_tensor a in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__linalg_det_result(%{result : tensor value}, %{lu : tensor value}, %{pivots : tensor value}, %{a : tensor value}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _linalg_eigh ~(a @ local) ~uplo ~compute_v =
  let a = globalize_tensor a in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__linalg_eigh(%{a : tensor value}, String_val(%{uplo : string value}), Int_val(%{compute_v : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _linalg_eigh_eigenvalues
  ~(eigenvalues @ local)
  ~(eigenvectors @ local)
  ~(a @ local)
  ~uplo
  ~compute_v
  =
  let eigenvalues = globalize_tensor eigenvalues in
  let eigenvectors = globalize_tensor eigenvectors in
  let a = globalize_tensor a in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__linalg_eigh_eigenvalues(%{eigenvalues : tensor value}, %{eigenvectors : tensor value}, %{a : tensor value}, String_val(%{uplo : string value}), Int_val(%{compute_v : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _linalg_eigvals (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__linalg_eigvals(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _linalg_slogdet ~(a @ local) =
  let a = globalize_tensor a in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg__linalg_slogdet(%{a : tensor value}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let _linalg_slogdet_sign
  ~(sign @ local)
  ~(logabsdet @ local)
  ~(lu @ local)
  ~(pivots @ local)
  ~(a @ local)
  =
  let sign = globalize_tensor sign in
  let logabsdet = globalize_tensor logabsdet in
  let lu = globalize_tensor lu in
  let pivots = globalize_tensor pivots in
  let a = globalize_tensor a in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg__linalg_slogdet_sign(%{sign : tensor value}, %{logabsdet : tensor value}, %{lu : tensor value}, %{pivots : tensor value}, %{a : tensor value}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let _linalg_solve_ex ~(a @ local) ~(b @ local) ~left ~check_errors =
  let a = globalize_tensor a in
  let b = globalize_tensor b in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg__linalg_solve_ex(%{a : tensor value}, %{b : tensor value}, Int_val(%{left : bool value}), Int_val(%{check_errors : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let _linalg_solve_ex_result
  (result @ local)
  ~(lu @ local)
  ~(pivots @ local)
  ~(info @ local)
  ~(a @ local)
  ~(b @ local)
  ~left
  ~check_errors
  =
  let result = globalize_tensor result in
  let lu = globalize_tensor lu in
  let pivots = globalize_tensor pivots in
  let info = globalize_tensor info in
  let a = globalize_tensor a in
  let b = globalize_tensor b in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg__linalg_solve_ex_result(%{result : tensor value}, %{lu : tensor value}, %{pivots : tensor value}, %{info : tensor value}, %{a : tensor value}, %{b : tensor value}, Int_val(%{left : bool value}), Int_val(%{check_errors : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let _linalg_svd ~(a @ local) ~full_matrices ~compute_uv ~driver =
  let a = globalize_tensor a in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__linalg_svd(%{a : tensor value}, Int_val(%{full_matrices : bool value}), Int_val(%{compute_uv : bool value}), %{driver : string option value}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _linalg_svd_u
  ~(u @ local)
  ~(s @ local)
  ~(vh @ local)
  ~(a @ local)
  ~full_matrices
  ~compute_uv
  ~driver
  =
  let u = globalize_tensor u in
  let s = globalize_tensor s in
  let vh = globalize_tensor vh in
  let a = globalize_tensor a in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__linalg_svd_u(%{u : tensor value}, %{s : tensor value}, %{vh : tensor value}, %{a : tensor value}, Int_val(%{full_matrices : bool value}), Int_val(%{compute_uv : bool value}), %{driver : string option value}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _log_softmax (self @ local) ~dim ~half_to_float =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__log_softmax(%{self : tensor value}, %{dim : int}, Int_val(%{half_to_float : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _log_softmax_backward_data ~(grad_output @ local) ~(output @ local) ~dim ~input_dtype =
  let grad_output = globalize_tensor grad_output in
  let output = globalize_tensor output in
  let input_dtype = Kind.packed_to_int input_dtype in
  [%c.alloc
    ({|CAMLreturn(atg__log_softmax_backward_data(%{grad_output : tensor value}, %{output : tensor value}, %{dim : int}, %{input_dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _log_softmax_backward_data_out
  ~(out @ local)
  ~(grad_output @ local)
  ~(output @ local)
  ~dim
  ~input_dtype
  =
  let out = globalize_tensor out in
  let grad_output = globalize_tensor grad_output in
  let output = globalize_tensor output in
  let input_dtype = Kind.packed_to_int input_dtype in
  [%c.alloc
    ({|CAMLreturn(atg__log_softmax_backward_data_out(%{out : tensor value}, %{grad_output : tensor value}, %{output : tensor value}, %{dim : int}, %{input_dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _log_softmax_out ~(out @ local) (self @ local) ~dim ~half_to_float =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__log_softmax_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, Int_val(%{half_to_float : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _logcumsumexp (self @ local) ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__logcumsumexp(%{self : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _logcumsumexp_out ~(out @ local) (self @ local) ~dim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__logcumsumexp_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _lstm_mps
  (input @ local)
  ~(hx @ local)
  ~(params @ local)
  ~has_biases
  ~num_layers
  ~dropout
  ~train
  ~bidirectional
  ~batch_first
  =
  let input = globalize_tensor input in
  let hx = Torch_local_iterators.List.map_local_input hx ~f:globalize_tensor in
  let params = Torch_local_iterators.List.map_local_input params ~f:globalize_tensor in
  let result0, result1, result2, result3, result4, result5 =
    [%c.alloc
      ({|CAMLreturn(atg__lstm_mps(%{input : tensor value}, %{hx : tensor list value}, %{params : tensor list value}, Int_val(%{has_biases : bool value}), %{num_layers : int}, %{dropout : float}, Int_val(%{train : bool value}), Int_val(%{bidirectional : bool value}), Int_val(%{batch_first : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor * raw_tensor * raw_tensor)
           value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3
  , wrap_managed_tensor result4
  , wrap_managed_tensor result5 )
;;

let _lstm_mps_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  ~(out3 @ local)
  ~(out4 @ local)
  ~(out5 @ local)
  (input @ local)
  ~(hx @ local)
  ~(params @ local)
  ~has_biases
  ~num_layers
  ~dropout
  ~train
  ~bidirectional
  ~batch_first
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let out3 = globalize_tensor out3 in
  let out4 = globalize_tensor out4 in
  let out5 = globalize_tensor out5 in
  let input = globalize_tensor input in
  let hx = Torch_local_iterators.List.map_local_input hx ~f:globalize_tensor in
  let params = Torch_local_iterators.List.map_local_input params ~f:globalize_tensor in
  let result0, result1, result2, result3, result4, result5 =
    [%c.alloc
      ({|CAMLreturn(atg__lstm_mps_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{out3 : tensor value}, %{out4 : tensor value}, %{out5 : tensor value}, %{input : tensor value}, %{hx : tensor list value}, %{params : tensor list value}, Int_val(%{has_biases : bool value}), %{num_layers : int}, %{dropout : float}, Int_val(%{train : bool value}), Int_val(%{bidirectional : bool value}), Int_val(%{batch_first : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor * raw_tensor * raw_tensor)
           value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3
  , wrap_managed_tensor result4
  , wrap_managed_tensor result5 )
;;

let _lu_with_info (self @ local) ~pivot ~check_errors =
  let self = globalize_tensor self in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__lu_with_info(%{self : tensor value}, Int_val(%{pivot : bool value}), Int_val(%{check_errors : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _make_dep_token ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg__make_dep_token(%{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _make_dual ~(primal @ local) ~(tangent @ local) ~level =
  let primal = globalize_tensor primal in
  let tangent = globalize_tensor tangent in
  [%c.alloc
    ({|CAMLreturn(atg__make_dual(%{primal : tensor value}, %{tangent : tensor value}, %{level : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _make_dual_copy ~(primal @ local) ~(tangent @ local) ~level =
  let primal = globalize_tensor primal in
  let tangent = globalize_tensor tangent in
  [%c.alloc
    ({|CAMLreturn(atg__make_dual_copy(%{primal : tensor value}, %{tangent : tensor value}, %{level : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _make_dual_copy_out ~(out @ local) ~(primal @ local) ~(tangent @ local) ~level =
  let out = globalize_tensor out in
  let primal = globalize_tensor primal in
  let tangent = globalize_tensor tangent in
  [%c.alloc
    ({|CAMLreturn(atg__make_dual_copy_out(%{out : tensor value}, %{primal : tensor value}, %{tangent : tensor value}, %{level : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _make_per_channel_quantized_tensor
  (self @ local)
  ~(scale @ local)
  ~(zero_point @ local)
  ~axis
  =
  let self = globalize_tensor self in
  let scale = globalize_tensor scale in
  let zero_point = globalize_tensor zero_point in
  [%c.alloc
    ({|CAMLreturn(atg__make_per_channel_quantized_tensor(%{self : tensor value}, %{scale : tensor value}, %{zero_point : tensor value}, %{axis : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _make_per_channel_quantized_tensor_out
  ~(out @ local)
  (self @ local)
  ~(scale @ local)
  ~(zero_point @ local)
  ~axis
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let scale = globalize_tensor scale in
  let zero_point = globalize_tensor zero_point in
  [%c.alloc
    ({|CAMLreturn(atg__make_per_channel_quantized_tensor_out(%{out : tensor value}, %{self : tensor value}, %{scale : tensor value}, %{zero_point : tensor value}, %{axis : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _make_per_tensor_quantized_tensor (self @ local) ~scale ~zero_point =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__make_per_tensor_quantized_tensor(%{self : tensor value}, %{scale : float}, %{zero_point : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _make_per_tensor_quantized_tensor_out ~(out @ local) (self @ local) ~scale ~zero_point
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__make_per_tensor_quantized_tensor_out(%{out : tensor value}, %{self : tensor value}, %{scale : float}, %{zero_point : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _masked_scale (self @ local) ~(mask @ local) ~scale =
  let self = globalize_tensor self in
  let mask = globalize_tensor mask in
  [%c.alloc
    ({|CAMLreturn(atg__masked_scale(%{self : tensor value}, %{mask : tensor value}, %{scale : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _masked_scale_out ~(out @ local) (self @ local) ~(mask @ local) ~scale =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let mask = globalize_tensor mask in
  [%c.alloc
    ({|CAMLreturn(atg__masked_scale_out(%{out : tensor value}, %{self : tensor value}, %{mask : tensor value}, %{scale : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _masked_softmax (self @ local) ~(mask @ local) ~dim ~mask_type =
  let self = globalize_tensor self in
  let mask = globalize_tensor mask in
  [%c.alloc
    ({|CAMLreturn(atg__masked_softmax(%{self : tensor value}, %{mask : tensor value}, %{dim : int option value}, %{mask_type : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _masked_softmax_backward ~(grad_output @ local) ~(output @ local) ~(mask @ local) ~dim
  =
  let grad_output = globalize_tensor grad_output in
  let output = globalize_tensor output in
  let mask = globalize_tensor mask in
  [%c.alloc
    ({|CAMLreturn(atg__masked_softmax_backward(%{grad_output : tensor value}, %{output : tensor value}, %{mask : tensor value}, %{dim : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _masked_softmax_backward_out
  ~(out @ local)
  ~(grad_output @ local)
  ~(output @ local)
  ~(mask @ local)
  ~dim
  =
  let out = globalize_tensor out in
  let grad_output = globalize_tensor grad_output in
  let output = globalize_tensor output in
  let mask = globalize_tensor mask in
  [%c.alloc
    ({|CAMLreturn(atg__masked_softmax_backward_out(%{out : tensor value}, %{grad_output : tensor value}, %{output : tensor value}, %{mask : tensor value}, %{dim : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _masked_softmax_out ~(out @ local) (self @ local) ~(mask @ local) ~dim ~mask_type =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let mask = globalize_tensor mask in
  [%c.alloc
    ({|CAMLreturn(atg__masked_softmax_out(%{out : tensor value}, %{self : tensor value}, %{mask : tensor value}, %{dim : int option value}, %{mask_type : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _mixed_dtypes_linear
  (input @ local)
  ~(weight @ local)
  ~(scale @ local)
  ~(bias @ local)
  ~activation
  =
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let scale = globalize_tensor scale in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg__mixed_dtypes_linear(%{input : tensor value}, %{weight : tensor value}, %{scale : tensor value}, %{bias : tensor option value}, %{activation : string option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _mkldnn_reshape (self @ local) ~shape =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__mkldnn_reshape(%{self : tensor value}, %{shape : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _mkldnn_reshape_out ~(out @ local) (self @ local) ~shape =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__mkldnn_reshape_out(%{out : tensor value}, %{self : tensor value}, %{shape : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _mkldnn_transpose (self @ local) ~dim0 ~dim1 =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__mkldnn_transpose(%{self : tensor value}, %{dim0 : int}, %{dim1 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _mkldnn_transpose_ (self @ local) ~dim0 ~dim1 =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__mkldnn_transpose_(%{self : tensor value}, %{dim0 : int}, %{dim1 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _mkldnn_transpose_out ~(out @ local) (self @ local) ~dim0 ~dim1 =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__mkldnn_transpose_out(%{out : tensor value}, %{self : tensor value}, %{dim0 : int}, %{dim1 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _mps_convolution
  (self @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~padding
  ~stride
  ~dilation
  ~groups
  =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg__mps_convolution(%{self : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{padding : int list value}, %{stride : int list value}, %{dilation : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _mps_convolution_out
  ~(out @ local)
  (self @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~padding
  ~stride
  ~dilation
  ~groups
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg__mps_convolution_out(%{out : tensor value}, %{self : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{padding : int list value}, %{stride : int list value}, %{dilation : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _mps_convolution_transpose
  (self @ local)
  ~(weight @ local)
  ~padding
  ~output_padding
  ~stride
  ~dilation
  ~groups
  =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  [%c.alloc
    ({|CAMLreturn(atg__mps_convolution_transpose(%{self : tensor value}, %{weight : tensor value}, %{padding : int list value}, %{output_padding : int list value}, %{stride : int list value}, %{dilation : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _mps_convolution_transpose_out
  ~(out @ local)
  (self @ local)
  ~(weight @ local)
  ~padding
  ~output_padding
  ~stride
  ~dilation
  ~groups
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  [%c.alloc
    ({|CAMLreturn(atg__mps_convolution_transpose_out(%{out : tensor value}, %{self : tensor value}, %{weight : tensor value}, %{padding : int list value}, %{output_padding : int list value}, %{stride : int list value}, %{dilation : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _native_batch_norm_legit
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~training
  ~momentum
  ~eps
  =
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let running_mean = globalize_tensor running_mean in
  let running_var = globalize_tensor running_var in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__native_batch_norm_legit(%{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, %{running_mean : tensor value}, %{running_var : tensor value}, Int_val(%{training : bool value}), %{momentum : float}, %{eps : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _native_batch_norm_legit_functional
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~training
  ~momentum
  ~eps
  =
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let running_mean = globalize_tensor running_mean in
  let running_var = globalize_tensor running_var in
  let result0, result1, result2, result3, result4 =
    [%c.alloc
      ({|CAMLreturn(atg__native_batch_norm_legit_functional(%{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, %{running_mean : tensor value}, %{running_var : tensor value}, Int_val(%{training : bool value}), %{momentum : float}, %{eps : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3
  , wrap_managed_tensor result4 )
;;

let _native_batch_norm_legit_no_stats
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~training
  ~momentum
  ~eps
  =
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__native_batch_norm_legit_no_stats(%{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, Int_val(%{training : bool value}), %{momentum : float}, %{eps : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _native_batch_norm_legit_no_stats_out
  ~(out @ local)
  ~(save_mean @ local)
  ~(save_invstd @ local)
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~training
  ~momentum
  ~eps
  =
  let out = globalize_tensor out in
  let save_mean = globalize_tensor save_mean in
  let save_invstd = globalize_tensor save_invstd in
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__native_batch_norm_legit_no_stats_out(%{out : tensor value}, %{save_mean : tensor value}, %{save_invstd : tensor value}, %{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, Int_val(%{training : bool value}), %{momentum : float}, %{eps : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _native_batch_norm_legit_no_training
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~momentum
  ~eps
  =
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let running_mean = globalize_tensor running_mean in
  let running_var = globalize_tensor running_var in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__native_batch_norm_legit_no_training(%{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, %{running_mean : tensor value}, %{running_var : tensor value}, %{momentum : float}, %{eps : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _native_batch_norm_legit_no_training_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~momentum
  ~eps
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let running_mean = globalize_tensor running_mean in
  let running_var = globalize_tensor running_var in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__native_batch_norm_legit_no_training_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, %{running_mean : tensor value}, %{running_var : tensor value}, %{momentum : float}, %{eps : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _native_batch_norm_legit_out
  ~(out @ local)
  ~(save_mean @ local)
  ~(save_invstd @ local)
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~training
  ~momentum
  ~eps
  =
  let out = globalize_tensor out in
  let save_mean = globalize_tensor save_mean in
  let save_invstd = globalize_tensor save_invstd in
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let running_mean = globalize_tensor running_mean in
  let running_var = globalize_tensor running_var in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__native_batch_norm_legit_out(%{out : tensor value}, %{save_mean : tensor value}, %{save_invstd : tensor value}, %{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, %{running_mean : tensor value}, %{running_var : tensor value}, Int_val(%{training : bool value}), %{momentum : float}, %{eps : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let _native_multi_head_attention
  ~(query @ local)
  ~(key @ local)
  ~(value @ local)
  ~embed_dim
  ~num_head
  ~(qkv_weight @ local)
  ~(qkv_bias @ local)
  ~(proj_weight @ local)
  ~(proj_bias @ local)
  ~(mask @ local)
  ~need_weights
  ~average_attn_weights
  ~mask_type
  =
  let query = globalize_tensor query in
  let key = globalize_tensor key in
  let value = globalize_tensor value in
  let qkv_weight = globalize_tensor qkv_weight in
  let qkv_bias = globalize_tensor qkv_bias in
  let proj_weight = globalize_tensor proj_weight in
  let proj_bias = globalize_tensor proj_bias in
  let mask = [%globalize: tensor option] mask in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__native_multi_head_attention(%{query : tensor value}, %{key : tensor value}, %{value : tensor value}, %{embed_dim : int}, %{num_head : int}, %{qkv_weight : tensor value}, %{qkv_bias : tensor value}, %{proj_weight : tensor value}, %{proj_bias : tensor value}, %{mask : tensor option value}, Int_val(%{need_weights : bool value}), Int_val(%{average_attn_weights : bool value}), %{mask_type : int option value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _native_multi_head_attention_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(query @ local)
  ~(key @ local)
  ~(value @ local)
  ~embed_dim
  ~num_head
  ~(qkv_weight @ local)
  ~(qkv_bias @ local)
  ~(proj_weight @ local)
  ~(proj_bias @ local)
  ~(mask @ local)
  ~need_weights
  ~average_attn_weights
  ~mask_type
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let query = globalize_tensor query in
  let key = globalize_tensor key in
  let value = globalize_tensor value in
  let qkv_weight = globalize_tensor qkv_weight in
  let qkv_bias = globalize_tensor qkv_bias in
  let proj_weight = globalize_tensor proj_weight in
  let proj_bias = globalize_tensor proj_bias in
  let mask = [%globalize: tensor option] mask in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__native_multi_head_attention_out(%{out0 : tensor value}, %{out1 : tensor value}, %{query : tensor value}, %{key : tensor value}, %{value : tensor value}, %{embed_dim : int}, %{num_head : int}, %{qkv_weight : tensor value}, %{qkv_bias : tensor value}, %{proj_weight : tensor value}, %{proj_bias : tensor value}, %{mask : tensor option value}, Int_val(%{need_weights : bool value}), Int_val(%{average_attn_weights : bool value}), %{mask_type : int option value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _neg_view (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg__neg_view(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _neg_view_copy (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__neg_view_copy(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _neg_view_copy_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__neg_view_copy_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_compute_contiguous_strides_offsets ~(nested_size @ local) =
  let nested_size = globalize_tensor nested_size in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__nested_compute_contiguous_strides_offsets(%{nested_size : tensor value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _nested_from_padded
  ~(padded @ local)
  ~(cpu_nested_shape_example @ local)
  ~fuse_transform_0213
  =
  let padded = globalize_tensor padded in
  let cpu_nested_shape_example = globalize_tensor cpu_nested_shape_example in
  [%c.alloc
    ({|CAMLreturn(atg__nested_from_padded(%{padded : tensor value}, %{cpu_nested_shape_example : tensor value}, Int_val(%{fuse_transform_0213 : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_from_padded_and_nested_example ~(padded @ local) ~(nt_example @ local) =
  let padded = globalize_tensor padded in
  let nt_example = globalize_tensor nt_example in
  [%c.alloc
    ({|CAMLreturn(atg__nested_from_padded_and_nested_example(%{padded : tensor value}, %{nt_example : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_from_padded_and_nested_example_out
  ~(out @ local)
  ~(padded @ local)
  ~(nt_example @ local)
  =
  let out = globalize_tensor out in
  let padded = globalize_tensor padded in
  let nt_example = globalize_tensor nt_example in
  [%c.alloc
    ({|CAMLreturn(atg__nested_from_padded_and_nested_example_out(%{out : tensor value}, %{padded : tensor value}, %{nt_example : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_from_padded_out
  ~(out @ local)
  ~(padded @ local)
  ~(cpu_nested_shape_example @ local)
  ~fuse_transform_0213
  =
  let out = globalize_tensor out in
  let padded = globalize_tensor padded in
  let cpu_nested_shape_example = globalize_tensor cpu_nested_shape_example in
  [%c.alloc
    ({|CAMLreturn(atg__nested_from_padded_out(%{out : tensor value}, %{padded : tensor value}, %{cpu_nested_shape_example : tensor value}, Int_val(%{fuse_transform_0213 : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_from_padded_tensor
  ~(padded @ local)
  ~(offsets @ local)
  ~(dummy @ local)
  ~ragged_idx
  ~(min_seqlen @ local)
  ~(max_seqlen @ local)
  ~sum_s
  =
  let padded = globalize_tensor padded in
  let offsets = globalize_tensor offsets in
  let dummy = globalize_tensor dummy in
  let min_seqlen = [%globalize: tensor option] min_seqlen in
  let max_seqlen = [%globalize: tensor option] max_seqlen in
  [%c.alloc
    ({|CAMLreturn(atg__nested_from_padded_tensor(%{padded : tensor value}, %{offsets : tensor value}, %{dummy : tensor value}, %{ragged_idx : int}, %{min_seqlen : tensor option value}, %{max_seqlen : tensor option value}, %{sum_s : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_get_jagged_dummy ~(any @ local) =
  let any = globalize_tensor any in
  [%c.alloc
    ({|CAMLreturn(atg__nested_get_jagged_dummy(%{any : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_get_lengths (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__nested_get_lengths(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_get_max_seqlen (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__nested_get_max_seqlen(%{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_get_min_seqlen (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__nested_get_min_seqlen(%{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_get_offsets (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__nested_get_offsets(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_get_ragged_idx (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__nested_get_ragged_idx(%{self : tensor value}));|} : int64 value)]
;;

let _nested_get_values (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__nested_get_values(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_get_values_copy (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__nested_get_values_copy(%{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_get_values_copy_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__nested_get_values_copy_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_select_backward ~(grad_output @ local) (self @ local) ~dim ~index =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__nested_select_backward(%{grad_output : tensor value}, %{self : tensor value}, %{dim : int}, %{index : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_sum_backward ~(grad @ local) (self @ local) ~dim ~keepdim =
  let grad = globalize_tensor grad in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__nested_sum_backward(%{grad : tensor value}, %{self : tensor value}, %{dim : int list option value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_view_from_buffer
  (self @ local)
  ~(nested_size @ local)
  ~(nested_strides @ local)
  ~(offsets @ local)
  =
  let self = globalize_tensor self in
  let nested_size = globalize_tensor nested_size in
  let nested_strides = globalize_tensor nested_strides in
  let offsets = globalize_tensor offsets in
  [%c.alloc
    ({|CAMLreturn(atg__nested_view_from_buffer(%{self : tensor value}, %{nested_size : tensor value}, %{nested_strides : tensor value}, %{offsets : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_view_from_buffer_copy
  (self @ local)
  ~(nested_size @ local)
  ~(nested_strides @ local)
  ~(offsets @ local)
  =
  let self = globalize_tensor self in
  let nested_size = globalize_tensor nested_size in
  let nested_strides = globalize_tensor nested_strides in
  let offsets = globalize_tensor offsets in
  [%c.alloc
    ({|CAMLreturn(atg__nested_view_from_buffer_copy(%{self : tensor value}, %{nested_size : tensor value}, %{nested_strides : tensor value}, %{offsets : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_view_from_buffer_copy_out
  ~(out @ local)
  (self @ local)
  ~(nested_size @ local)
  ~(nested_strides @ local)
  ~(offsets @ local)
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let nested_size = globalize_tensor nested_size in
  let nested_strides = globalize_tensor nested_strides in
  let offsets = globalize_tensor offsets in
  [%c.alloc
    ({|CAMLreturn(atg__nested_view_from_buffer_copy_out(%{out : tensor value}, %{self : tensor value}, %{nested_size : tensor value}, %{nested_strides : tensor value}, %{offsets : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_view_from_jagged
  (self @ local)
  ~(offsets @ local)
  ~(dummy @ local)
  ~(lengths @ local)
  ~ragged_idx
  ~(min_seqlen @ local)
  ~(max_seqlen @ local)
  =
  let self = globalize_tensor self in
  let offsets = globalize_tensor offsets in
  let dummy = globalize_tensor dummy in
  let lengths = [%globalize: tensor option] lengths in
  let min_seqlen = [%globalize: tensor option] min_seqlen in
  let max_seqlen = [%globalize: tensor option] max_seqlen in
  [%c.alloc
    ({|CAMLreturn(atg__nested_view_from_jagged(%{self : tensor value}, %{offsets : tensor value}, %{dummy : tensor value}, %{lengths : tensor option value}, %{ragged_idx : int}, %{min_seqlen : tensor option value}, %{max_seqlen : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_view_from_jagged_copy
  (self @ local)
  ~(offsets @ local)
  ~(dummy @ local)
  ~(lengths @ local)
  ~ragged_idx
  ~(min_seqlen @ local)
  ~(max_seqlen @ local)
  =
  let self = globalize_tensor self in
  let offsets = globalize_tensor offsets in
  let dummy = globalize_tensor dummy in
  let lengths = [%globalize: tensor option] lengths in
  let min_seqlen = [%globalize: tensor option] min_seqlen in
  let max_seqlen = [%globalize: tensor option] max_seqlen in
  [%c.alloc
    ({|CAMLreturn(atg__nested_view_from_jagged_copy(%{self : tensor value}, %{offsets : tensor value}, %{dummy : tensor value}, %{lengths : tensor option value}, %{ragged_idx : int}, %{min_seqlen : tensor option value}, %{max_seqlen : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nested_view_from_jagged_copy_out
  ~(out @ local)
  (self @ local)
  ~(offsets @ local)
  ~(dummy @ local)
  ~(lengths @ local)
  ~ragged_idx
  ~(min_seqlen @ local)
  ~(max_seqlen @ local)
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let offsets = globalize_tensor offsets in
  let dummy = globalize_tensor dummy in
  let lengths = [%globalize: tensor option] lengths in
  let min_seqlen = [%globalize: tensor option] min_seqlen in
  let max_seqlen = [%globalize: tensor option] max_seqlen in
  [%c.alloc
    ({|CAMLreturn(atg__nested_view_from_jagged_copy_out(%{out : tensor value}, %{self : tensor value}, %{offsets : tensor value}, %{dummy : tensor value}, %{lengths : tensor option value}, %{ragged_idx : int}, %{min_seqlen : tensor option value}, %{max_seqlen : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _new_zeros_with_same_feature_meta (self @ local) (other @ local) ~self_num_batch_dims =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg__new_zeros_with_same_feature_meta(%{self : tensor value}, %{other : tensor value}, %{self_num_batch_dims : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _new_zeros_with_same_feature_meta_out
  ~(out @ local)
  (self @ local)
  (other @ local)
  ~self_num_batch_dims
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg__new_zeros_with_same_feature_meta_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}, %{self_num_batch_dims : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nnpack_available () =
  [%c.alloc ({|CAMLreturn(atg__nnpack_available());|} : bool value)]
;;

let _nnpack_spatial_convolution
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~padding
  ~stride
  =
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg__nnpack_spatial_convolution(%{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{padding : int list value}, %{stride : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nnpack_spatial_convolution_out
  ~(out @ local)
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~padding
  ~stride
  =
  let out = globalize_tensor out in
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg__nnpack_spatial_convolution_out(%{out : tensor value}, %{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{padding : int list value}, %{stride : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _nnz (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg__nnz(%{self : tensor value}));|} : int64 value)]
;;

let _pack_padded_sequence (input @ local) ~(lengths @ local) ~batch_first =
  let input = globalize_tensor input in
  let lengths = globalize_tensor lengths in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__pack_padded_sequence(%{input : tensor value}, %{lengths : tensor value}, Int_val(%{batch_first : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _pack_padded_sequence_backward
  ~(grad @ local)
  ~input_size
  ~(batch_sizes @ local)
  ~batch_first
  =
  let grad = globalize_tensor grad in
  let batch_sizes = globalize_tensor batch_sizes in
  [%c.alloc
    ({|CAMLreturn(atg__pack_padded_sequence_backward(%{grad : tensor value}, %{input_size : int list value}, %{batch_sizes : tensor value}, Int_val(%{batch_first : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _pack_padded_sequence_out
  ~(out0 @ local)
  ~(out1 @ local)
  (input @ local)
  ~(lengths @ local)
  ~batch_first
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let input = globalize_tensor input in
  let lengths = globalize_tensor lengths in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__pack_padded_sequence_out(%{out0 : tensor value}, %{out1 : tensor value}, %{input : tensor value}, %{lengths : tensor value}, Int_val(%{batch_first : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _pad_circular (self @ local) ~pad =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__pad_circular(%{self : tensor value}, %{pad : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _pad_enum (self @ local) ~pad ~mode ~value =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__pad_enum(%{self : tensor value}, %{pad : int list value}, %{mode : int}, %{value : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _pad_packed_sequence
  ~(data @ local)
  ~(batch_sizes @ local)
  ~batch_first
  ~padding_value
  ~total_length
  =
  let data = globalize_tensor data in
  let batch_sizes = globalize_tensor batch_sizes in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__pad_packed_sequence(%{data : tensor value}, %{batch_sizes : tensor value}, Int_val(%{batch_first : bool value}), %{padding_value : scalar value}, %{total_length : int}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _pdist_backward ~(grad @ local) (self @ local) ~p ~(pdist @ local) =
  let grad = globalize_tensor grad in
  let self = globalize_tensor self in
  let pdist = globalize_tensor pdist in
  [%c.alloc
    ({|CAMLreturn(atg__pdist_backward(%{grad : tensor value}, %{self : tensor value}, %{p : float}, %{pdist : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _pdist_backward_out ~(out @ local) ~(grad @ local) (self @ local) ~p ~(pdist @ local) =
  let out = globalize_tensor out in
  let grad = globalize_tensor grad in
  let self = globalize_tensor self in
  let pdist = globalize_tensor pdist in
  [%c.alloc
    ({|CAMLreturn(atg__pdist_backward_out(%{out : tensor value}, %{grad : tensor value}, %{self : tensor value}, %{p : float}, %{pdist : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _pin_memory (self @ local) ~device =
  let self = globalize_tensor self in
  let device = Device.option_to_int device in
  [%c.alloc
    ({|CAMLreturn(atg__pin_memory(%{self : tensor value}, %{device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _pin_memory_out ~(out @ local) (self @ local) ~device =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let device = Device.option_to_int device in
  [%c.alloc
    ({|CAMLreturn(atg__pin_memory_out(%{out : tensor value}, %{self : tensor value}, %{device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _prelu_kernel (self @ local) ~(weight @ local) =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  [%c.alloc
    ({|CAMLreturn(atg__prelu_kernel(%{self : tensor value}, %{weight : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _prelu_kernel_backward ~(grad_output @ local) (self @ local) ~(weight @ local) =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__prelu_kernel_backward(%{grad_output : tensor value}, %{self : tensor value}, %{weight : tensor value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _print ~s = [%c.alloc {|atg__print(String_val(%{s : string value}));|}]

let _propagate_xla_data (input @ local) ~(output @ local) =
  let input = globalize_tensor input in
  let output = globalize_tensor output in
  [%c.alloc
    {|atg__propagate_xla_data(%{input : tensor value}, %{output : tensor value});|}]
;;

let _remove_batch_dim (self @ local) ~level ~batch_size ~out_dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__remove_batch_dim(%{self : tensor value}, %{level : int}, %{batch_size : int}, %{out_dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _reshape_alias (self @ local) ~size ~stride =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__reshape_alias(%{self : tensor value}, %{size : int list value}, %{stride : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _reshape_alias_copy (self @ local) ~size ~stride =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__reshape_alias_copy(%{self : tensor value}, %{size : int list value}, %{stride : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _reshape_alias_copy_out ~(out @ local) (self @ local) ~size ~stride =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__reshape_alias_copy_out(%{out : tensor value}, %{self : tensor value}, %{size : int list value}, %{stride : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _reshape_copy (self @ local) ~size =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__reshape_copy(%{self : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _reshape_from_tensor (self @ local) ~(shape @ local) =
  let self = globalize_tensor self in
  let shape = globalize_tensor shape in
  [%c.alloc
    ({|CAMLreturn(atg__reshape_from_tensor(%{self : tensor value}, %{shape : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _resize_output (self @ local) ~size ~device =
  let self = globalize_tensor self in
  let device = Device.to_int device in
  [%c.alloc
    ({|CAMLreturn(atg__resize_output(%{self : tensor value}, %{size : int list value}, %{device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _resize_output_ (self @ local) ~size ~device =
  let self = globalize_tensor self in
  let device = Device.to_int device in
  [%c.alloc
    ({|CAMLreturn(atg__resize_output_(%{self : tensor value}, %{size : int list value}, %{device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _resize_output_out ~(out @ local) (self @ local) ~size ~device =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let device = Device.to_int device in
  [%c.alloc
    ({|CAMLreturn(atg__resize_output_out(%{out : tensor value}, %{self : tensor value}, %{size : int list value}, %{device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _rowwise_prune ~(weight @ local) ~(mask @ local) ~compressed_indices_dtype =
  let weight = globalize_tensor weight in
  let mask = globalize_tensor mask in
  let compressed_indices_dtype = Kind.packed_to_int compressed_indices_dtype in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__rowwise_prune(%{weight : tensor value}, %{mask : tensor value}, %{compressed_indices_dtype : int}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _safe_softmax (self @ local) ~dim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg__safe_softmax(%{self : tensor value}, %{dim : int}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sample_dirichlet (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__sample_dirichlet(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _sample_dirichlet_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg__sample_dirichlet_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _saturate_weight_to_fp16 ~(weight @ local) =
  let weight = globalize_tensor weight in
  [%c.alloc
    ({|CAMLreturn(atg__saturate_weight_to_fp16(%{weight : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let _scaled_dot_product_attention_math
  ~(query @ local)
  ~(key @ local)
  ~(value @ local)
  ~(attn_mask @ local)
  ~dropout_p
  ~is_causal
  ~(dropout_mask @ local)
  ~scale
  ~enable_gqa
  =
  let query = globalize_tensor query in
  let key = globalize_tensor key in
  let value = globalize_tensor value in
  let attn_mask = [%globalize: tensor option] attn_mask in
  let dropout_mask = [%globalize: tensor option] dropout_mask in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__scaled_dot_product_attention_math(%{query : tensor value}, %{key : tensor value}, %{value : tensor value}, %{attn_mask : tensor option value}, %{dropout_p : float}, Int_val(%{is_causal : bool value}), %{dropout_mask : tensor option value}, %{scale : float option value}, Int_val(%{enable_gqa : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _scaled_dot_product_attention_math_for_mps
  ~(query @ local)
  ~(key @ local)
  ~(value @ local)
  ~(attn_mask @ local)
  ~dropout_p
  ~is_causal
  ~(dropout_mask @ local)
  ~scale
  =
  let query = globalize_tensor query in
  let key = globalize_tensor key in
  let value = globalize_tensor value in
  let attn_mask = [%globalize: tensor option] attn_mask in
  let dropout_mask = [%globalize: tensor option] dropout_mask in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg__scaled_dot_product_attention_math_for_mps(%{query : tensor value}, %{key : tensor value}, %{value : tensor value}, %{attn_mask : tensor option value}, %{dropout_p : float}, Int_val(%{is_causal : bool value}), %{dropout_mask : tensor option value}, %{scale : float option value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let _scaled_dot_product_cudnn_attention_backward
  ~(grad_out @ local)
  ~(query @ local)
  ~(key @ local)
  ~(value @ local)
  ~(out @ local)
  ~(logsumexp @ local)
  ~(philox_seed @ local)
  ~(philox_offset @ local)
  ~(attn_bias @ local)
  ~(cum_seq_q @ local)
  ~(cum_seq_k @ local)
  ~max_q
  ~max_k
  ~dropout_p
  ~is_causal
  ~scale
  =
  let grad_out = globalize_tensor grad_out in
  let query = globalize_tensor query in
  let key = globalize_tensor key in
  let value = globalize_tensor value in
  let out = globalize_tensor out in
  let logsumexp = globalize_tensor logsumexp in
  let philox_seed = globalize_tensor philox_seed in
  let philox_offset = globalize_tensor philox_offset in
  let attn_bias = globalize_tensor attn_bias in
  let cum_seq_q = globalize_tensor cum_seq_q in
  let cum_seq_k = globalize_tensor cum_seq_k in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg__scaled_dot_product_cudnn_attention_backward(%{grad_out : tensor value}, %{query : tensor value}, %{key : tensor value}, %{value : tensor value}, %{out : tensor value}, %{logsumexp : tensor value}, %{philox_seed : tensor value}, %{philox_offset : tensor value}, %{attn_bias : tensor value}, %{cum_seq_q : tensor value}, %{cum_seq_k : tensor value}, %{max_q : int}, %{max_k : int}, %{dropout_p : float}, Int_val(%{is_causal : bool value}), %{scale : float option value}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;
