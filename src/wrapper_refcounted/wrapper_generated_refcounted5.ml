(* THIS FILE IS AUTOMATICALLY GENERATED, DO NOT EDIT BY HAND! *)

open! Core
open C_ffi
open Torch_wrapper_types
open Torch_refcounted_bindings.Type_defs

[%%c {| #include "torch_api.h" |}]

let median_dim (self @ local) ~dim ~keepdim =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_median_dim(%{self : tensor value}, %{dim : int}, Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let median_dim_values ~(values @ local) ~(indices @ local) (self @ local) ~dim ~keepdim =
  let values = globalize_tensor values in
  let indices = globalize_tensor indices in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_median_dim_values(%{values : tensor value}, %{indices : tensor value}, %{self : tensor value}, %{dim : int}, Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let median_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_median_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let meshgrid (tensors @ local) =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_meshgrid(%{tensors : tensor list value}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let meshgrid_indexing (tensors @ local) ~indexing =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_meshgrid_indexing(%{tensors : tensor list value}, String_val(%{indexing : string value})));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let mh (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_mh(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let min (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_min(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let min_dim (self @ local) ~dim ~keepdim =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_min_dim(%{self : tensor value}, %{dim : int}, Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let min_dim_min ~(min @ local) ~(min_indices @ local) (self @ local) ~dim ~keepdim =
  let min = globalize_tensor min in
  let min_indices = globalize_tensor min_indices in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_min_dim_min(%{min : tensor value}, %{min_indices : tensor value}, %{self : tensor value}, %{dim : int}, Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let min_other (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_min_other(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let min_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_min_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let min_unary_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_min_unary_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let minimum (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_minimum(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let minimum_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_minimum_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let miopen_batch_norm
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~training
  ~exponential_average_factor
  ~epsilon
  =
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  let running_mean = [%globalize: tensor option] running_mean in
  let running_var = [%globalize: tensor option] running_var in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_miopen_batch_norm(%{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{running_mean : tensor option value}, %{running_var : tensor option value}, Int_val(%{training : bool value}), %{exponential_average_factor : float}, %{epsilon : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let miopen_batch_norm_backward
  (input @ local)
  ~(grad_output @ local)
  ~(weight @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~(save_mean @ local)
  ~(save_var @ local)
  ~epsilon
  =
  let input = globalize_tensor input in
  let grad_output = globalize_tensor grad_output in
  let weight = globalize_tensor weight in
  let running_mean = [%globalize: tensor option] running_mean in
  let running_var = [%globalize: tensor option] running_var in
  let save_mean = [%globalize: tensor option] save_mean in
  let save_var = [%globalize: tensor option] save_var in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_miopen_batch_norm_backward(%{input : tensor value}, %{grad_output : tensor value}, %{weight : tensor value}, %{running_mean : tensor option value}, %{running_var : tensor option value}, %{save_mean : tensor option value}, %{save_var : tensor option value}, %{epsilon : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let miopen_batch_norm_backward_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  (input @ local)
  ~(grad_output @ local)
  ~(weight @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~(save_mean @ local)
  ~(save_var @ local)
  ~epsilon
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let input = globalize_tensor input in
  let grad_output = globalize_tensor grad_output in
  let weight = globalize_tensor weight in
  let running_mean = [%globalize: tensor option] running_mean in
  let running_var = [%globalize: tensor option] running_var in
  let save_mean = [%globalize: tensor option] save_mean in
  let save_var = [%globalize: tensor option] save_var in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_miopen_batch_norm_backward_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{input : tensor value}, %{grad_output : tensor value}, %{weight : tensor value}, %{running_mean : tensor option value}, %{running_var : tensor option value}, %{save_mean : tensor option value}, %{save_var : tensor option value}, %{epsilon : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let miopen_batch_norm_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~training
  ~exponential_average_factor
  ~epsilon
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  let running_mean = [%globalize: tensor option] running_mean in
  let running_var = [%globalize: tensor option] running_var in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_miopen_batch_norm_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{running_mean : tensor option value}, %{running_var : tensor option value}, Int_val(%{training : bool value}), %{exponential_average_factor : float}, %{epsilon : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let miopen_convolution
  (self @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~padding
  ~stride
  ~dilation
  ~groups
  ~benchmark
  ~deterministic
  =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_miopen_convolution(%{self : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{padding : int list value}, %{stride : int list value}, %{dilation : int list value}, %{groups : int}, Int_val(%{benchmark : bool value}), Int_val(%{deterministic : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let miopen_convolution_add_relu
  (self @ local)
  ~(weight @ local)
  ~(z @ local)
  ~alpha
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  ~groups
  =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let z = globalize_tensor z in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_miopen_convolution_add_relu(%{self : tensor value}, %{weight : tensor value}, %{z : tensor value}, %{alpha : scalar value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let miopen_convolution_out
  ~(out @ local)
  (self @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~padding
  ~stride
  ~dilation
  ~groups
  ~benchmark
  ~deterministic
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_miopen_convolution_out(%{out : tensor value}, %{self : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{padding : int list value}, %{stride : int list value}, %{dilation : int list value}, %{groups : int}, Int_val(%{benchmark : bool value}), Int_val(%{deterministic : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let miopen_convolution_relu
  (self @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  ~groups
  =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_miopen_convolution_relu(%{self : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let miopen_convolution_transpose
  (self @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~padding
  ~output_padding
  ~stride
  ~dilation
  ~groups
  ~benchmark
  ~deterministic
  =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_miopen_convolution_transpose(%{self : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{padding : int list value}, %{output_padding : int list value}, %{stride : int list value}, %{dilation : int list value}, %{groups : int}, Int_val(%{benchmark : bool value}), Int_val(%{deterministic : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let miopen_convolution_transpose_out
  ~(out @ local)
  (self @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~padding
  ~output_padding
  ~stride
  ~dilation
  ~groups
  ~benchmark
  ~deterministic
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_miopen_convolution_transpose_out(%{out : tensor value}, %{self : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{padding : int list value}, %{output_padding : int list value}, %{stride : int list value}, %{dilation : int list value}, %{groups : int}, Int_val(%{benchmark : bool value}), Int_val(%{deterministic : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let miopen_depthwise_convolution
  (self @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~padding
  ~stride
  ~dilation
  ~groups
  ~benchmark
  ~deterministic
  =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_miopen_depthwise_convolution(%{self : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{padding : int list value}, %{stride : int list value}, %{dilation : int list value}, %{groups : int}, Int_val(%{benchmark : bool value}), Int_val(%{deterministic : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let miopen_depthwise_convolution_out
  ~(out @ local)
  (self @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~padding
  ~stride
  ~dilation
  ~groups
  ~benchmark
  ~deterministic
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_miopen_depthwise_convolution_out(%{out : tensor value}, %{self : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{padding : int list value}, %{stride : int list value}, %{dilation : int list value}, %{groups : int}, Int_val(%{benchmark : bool value}), Int_val(%{deterministic : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let miopen_rnn
  (input @ local)
  ~(weight @ local)
  ~weight_stride0
  ~(hx @ local)
  ~(cx @ local)
  ~mode
  ~hidden_size
  ~num_layers
  ~batch_first
  ~dropout
  ~train
  ~bidirectional
  ~batch_sizes
  ~(dropout_state @ local)
  =
  let input = globalize_tensor input in
  let weight = Torch_local_iterators.List.map_local_input weight ~f:globalize_tensor in
  let hx = globalize_tensor hx in
  let cx = [%globalize: tensor option] cx in
  let dropout_state = [%globalize: tensor option] dropout_state in
  let result0, result1, result2, result3, result4 =
    [%c.alloc
      ({|CAMLreturn(atg_miopen_rnn(%{input : tensor value}, %{weight : tensor list value}, %{weight_stride0 : int}, %{hx : tensor value}, %{cx : tensor option value}, %{mode : int}, %{hidden_size : int}, %{num_layers : int}, Int_val(%{batch_first : bool value}), %{dropout : float}, Int_val(%{train : bool value}), Int_val(%{bidirectional : bool value}), %{batch_sizes : int list value}, %{dropout_state : tensor option value}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3
  , wrap_managed_tensor result4 )
;;

let miopen_rnn_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  ~(out3 @ local)
  ~(out4 @ local)
  (input @ local)
  ~(weight @ local)
  ~weight_stride0
  ~(hx @ local)
  ~(cx @ local)
  ~mode
  ~hidden_size
  ~num_layers
  ~batch_first
  ~dropout
  ~train
  ~bidirectional
  ~batch_sizes
  ~(dropout_state @ local)
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let out3 = globalize_tensor out3 in
  let out4 = globalize_tensor out4 in
  let input = globalize_tensor input in
  let weight = Torch_local_iterators.List.map_local_input weight ~f:globalize_tensor in
  let hx = globalize_tensor hx in
  let cx = [%globalize: tensor option] cx in
  let dropout_state = [%globalize: tensor option] dropout_state in
  let result0, result1, result2, result3, result4 =
    [%c.alloc
      ({|CAMLreturn(atg_miopen_rnn_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{out3 : tensor value}, %{out4 : tensor value}, %{input : tensor value}, %{weight : tensor list value}, %{weight_stride0 : int}, %{hx : tensor value}, %{cx : tensor option value}, %{mode : int}, %{hidden_size : int}, %{num_layers : int}, Int_val(%{batch_first : bool value}), %{dropout : float}, Int_val(%{train : bool value}), Int_val(%{bidirectional : bool value}), %{batch_sizes : int list value}, %{dropout_state : tensor option value}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3
  , wrap_managed_tensor result4 )
;;

let mish (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_mish(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mish_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_mish_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mish_backward ~(grad_output @ local) (self @ local) =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_mish_backward(%{grad_output : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mish_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_mish_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_adaptive_avg_pool2d (self @ local) ~output_size =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_adaptive_avg_pool2d(%{self : tensor value}, %{output_size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_adaptive_avg_pool2d_backward ~(grad_output @ local) (self @ local) =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_adaptive_avg_pool2d_backward(%{grad_output : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_adaptive_avg_pool2d_backward_out
  ~(out @ local)
  ~(grad_output @ local)
  (self @ local)
  =
  let out = globalize_tensor out in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_adaptive_avg_pool2d_backward_out(%{out : tensor value}, %{grad_output : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_adaptive_avg_pool2d_out ~(out @ local) (self @ local) ~output_size =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_adaptive_avg_pool2d_out(%{out : tensor value}, %{self : tensor value}, %{output_size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_convolution
  (self @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~padding
  ~stride
  ~dilation
  ~groups
  =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_convolution(%{self : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{padding : int list value}, %{stride : int list value}, %{dilation : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_convolution_out
  ~(out @ local)
  (self @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~padding
  ~stride
  ~dilation
  ~groups
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_convolution_out(%{out : tensor value}, %{self : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{padding : int list value}, %{stride : int list value}, %{dilation : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_linear (self @ local) ~(weight @ local) ~(bias @ local) =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_linear(%{self : tensor value}, %{weight : tensor value}, %{bias : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_linear_backward_input ~input_size ~(grad_output @ local) ~(weight @ local) =
  let grad_output = globalize_tensor grad_output in
  let weight = globalize_tensor weight in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_linear_backward_input(%{input_size : int list value}, %{grad_output : tensor value}, %{weight : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_linear_backward_input_out
  ~(out @ local)
  ~input_size
  ~(grad_output @ local)
  ~(weight @ local)
  =
  let out = globalize_tensor out in
  let grad_output = globalize_tensor grad_output in
  let weight = globalize_tensor weight in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_linear_backward_input_out(%{out : tensor value}, %{input_size : int list value}, %{grad_output : tensor value}, %{weight : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_linear_backward_weights
  ~(grad_output @ local)
  (input @ local)
  ~(weight @ local)
  ~bias_defined
  =
  let grad_output = globalize_tensor grad_output in
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_mkldnn_linear_backward_weights(%{grad_output : tensor value}, %{input : tensor value}, %{weight : tensor value}, Int_val(%{bias_defined : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let mkldnn_linear_backward_weights_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(grad_output @ local)
  (input @ local)
  ~(weight @ local)
  ~bias_defined
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let grad_output = globalize_tensor grad_output in
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_mkldnn_linear_backward_weights_out(%{out0 : tensor value}, %{out1 : tensor value}, %{grad_output : tensor value}, %{input : tensor value}, %{weight : tensor value}, Int_val(%{bias_defined : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let mkldnn_linear_out ~(out @ local) (self @ local) ~(weight @ local) ~(bias @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_linear_out(%{out : tensor value}, %{self : tensor value}, %{weight : tensor value}, %{bias : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_max_pool2d (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_max_pool2d(%{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_max_pool2d_backward
  ~(grad_output @ local)
  ~(output @ local)
  (input @ local)
  ~kernel_size
  ~stride
  ~padding
  ~dilation
  ~ceil_mode
  =
  let grad_output = globalize_tensor grad_output in
  let output = globalize_tensor output in
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_max_pool2d_backward(%{grad_output : tensor value}, %{output : tensor value}, %{input : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_max_pool2d_backward_out
  ~(out @ local)
  ~(grad_output @ local)
  ~(output @ local)
  (input @ local)
  ~kernel_size
  ~stride
  ~padding
  ~dilation
  ~ceil_mode
  =
  let out = globalize_tensor out in
  let grad_output = globalize_tensor grad_output in
  let output = globalize_tensor output in
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_max_pool2d_backward_out(%{out : tensor value}, %{grad_output : tensor value}, %{output : tensor value}, %{input : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_max_pool2d_out
  ~(out @ local)
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~dilation
  ~ceil_mode
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_max_pool2d_out(%{out : tensor value}, %{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_max_pool3d (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_max_pool3d(%{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_max_pool3d_backward
  ~(grad_output @ local)
  ~(output @ local)
  (input @ local)
  ~kernel_size
  ~stride
  ~padding
  ~dilation
  ~ceil_mode
  =
  let grad_output = globalize_tensor grad_output in
  let output = globalize_tensor output in
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_max_pool3d_backward(%{grad_output : tensor value}, %{output : tensor value}, %{input : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_max_pool3d_backward_out
  ~(out @ local)
  ~(grad_output @ local)
  ~(output @ local)
  (input @ local)
  ~kernel_size
  ~stride
  ~padding
  ~dilation
  ~ceil_mode
  =
  let out = globalize_tensor out in
  let grad_output = globalize_tensor grad_output in
  let output = globalize_tensor output in
  let input = globalize_tensor input in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_max_pool3d_backward_out(%{out : tensor value}, %{grad_output : tensor value}, %{output : tensor value}, %{input : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_max_pool3d_out
  ~(out @ local)
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~dilation
  ~ceil_mode
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_max_pool3d_out(%{out : tensor value}, %{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_reorder_conv2d_weight
  (self @ local)
  ~padding
  ~stride
  ~dilation
  ~groups
  ~input_size
  =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_reorder_conv2d_weight(%{self : tensor value}, %{padding : int list value}, %{stride : int list value}, %{dilation : int list value}, %{groups : int}, %{input_size : int list option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_reorder_conv2d_weight_out
  ~(out @ local)
  (self @ local)
  ~padding
  ~stride
  ~dilation
  ~groups
  ~input_size
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_reorder_conv2d_weight_out(%{out : tensor value}, %{self : tensor value}, %{padding : int list value}, %{stride : int list value}, %{dilation : int list value}, %{groups : int}, %{input_size : int list option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_reorder_conv3d_weight
  (self @ local)
  ~padding
  ~stride
  ~dilation
  ~groups
  ~input_size
  =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_reorder_conv3d_weight(%{self : tensor value}, %{padding : int list value}, %{stride : int list value}, %{dilation : int list value}, %{groups : int}, %{input_size : int list option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_reorder_conv3d_weight_out
  ~(out @ local)
  (self @ local)
  ~padding
  ~stride
  ~dilation
  ~groups
  ~input_size
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_mkldnn_reorder_conv3d_weight_out(%{out : tensor value}, %{self : tensor value}, %{padding : int list value}, %{stride : int list value}, %{dilation : int list value}, %{groups : int}, %{input_size : int list option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mkldnn_rnn_layer
  (input @ local)
  ~(weight0 @ local)
  ~(weight1 @ local)
  ~(weight2 @ local)
  ~(weight3 @ local)
  ~(hx_ @ local)
  ~(cx_ @ local)
  ~reverse
  ~batch_sizes
  ~mode
  ~hidden_size
  ~num_layers
  ~has_biases
  ~bidirectional
  ~batch_first
  ~train
  =
  let input = globalize_tensor input in
  let weight0 = globalize_tensor weight0 in
  let weight1 = globalize_tensor weight1 in
  let weight2 = globalize_tensor weight2 in
  let weight3 = globalize_tensor weight3 in
  let hx_ = globalize_tensor hx_ in
  let cx_ = globalize_tensor cx_ in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg_mkldnn_rnn_layer(%{input : tensor value}, %{weight0 : tensor value}, %{weight1 : tensor value}, %{weight2 : tensor value}, %{weight3 : tensor value}, %{hx_ : tensor value}, %{cx_ : tensor value}, Int_val(%{reverse : bool value}), %{batch_sizes : int list value}, %{mode : int}, %{hidden_size : int}, %{num_layers : int}, Int_val(%{has_biases : bool value}), Int_val(%{bidirectional : bool value}), Int_val(%{batch_first : bool value}), Int_val(%{train : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let mkldnn_rnn_layer_backward
  (input @ local)
  ~(weight1 @ local)
  ~(weight2 @ local)
  ~(weight3 @ local)
  ~(weight4 @ local)
  ~(hx_ @ local)
  ~(cx_tmp @ local)
  ~(output @ local)
  ~(hy_ @ local)
  ~(cy_ @ local)
  ~(grad_output @ local)
  ~(grad_hy @ local)
  ~(grad_cy @ local)
  ~reverse
  ~mode
  ~hidden_size
  ~num_layers
  ~has_biases
  ~train
  ~bidirectional
  ~batch_sizes
  ~batch_first
  ~(workspace @ local)
  =
  let input = globalize_tensor input in
  let weight1 = globalize_tensor weight1 in
  let weight2 = globalize_tensor weight2 in
  let weight3 = globalize_tensor weight3 in
  let weight4 = globalize_tensor weight4 in
  let hx_ = globalize_tensor hx_ in
  let cx_tmp = globalize_tensor cx_tmp in
  let output = globalize_tensor output in
  let hy_ = globalize_tensor hy_ in
  let cy_ = globalize_tensor cy_ in
  let grad_output = [%globalize: tensor option] grad_output in
  let grad_hy = [%globalize: tensor option] grad_hy in
  let grad_cy = [%globalize: tensor option] grad_cy in
  let workspace = globalize_tensor workspace in
  let result0, result1, result2, result3, result4, result5, result6 =
    [%c.alloc
      ({|CAMLreturn(atg_mkldnn_rnn_layer_backward(%{input : tensor value}, %{weight1 : tensor value}, %{weight2 : tensor value}, %{weight3 : tensor value}, %{weight4 : tensor value}, %{hx_ : tensor value}, %{cx_tmp : tensor value}, %{output : tensor value}, %{hy_ : tensor value}, %{cy_ : tensor value}, %{grad_output : tensor option value}, %{grad_hy : tensor option value}, %{grad_cy : tensor option value}, Int_val(%{reverse : bool value}), %{mode : int}, %{hidden_size : int}, %{num_layers : int}, Int_val(%{has_biases : bool value}), Int_val(%{train : bool value}), Int_val(%{bidirectional : bool value}), %{batch_sizes : int list value}, Int_val(%{batch_first : bool value}), %{workspace : tensor value}));|}
       : (raw_tensor
         * raw_tensor
         * raw_tensor
         * raw_tensor
         * raw_tensor
         * raw_tensor
         * raw_tensor)
           value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3
  , wrap_managed_tensor result4
  , wrap_managed_tensor result5
  , wrap_managed_tensor result6 )
;;

let mkldnn_rnn_layer_backward_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  ~(out3 @ local)
  ~(out4 @ local)
  ~(out5 @ local)
  ~(out6 @ local)
  (input @ local)
  ~(weight1 @ local)
  ~(weight2 @ local)
  ~(weight3 @ local)
  ~(weight4 @ local)
  ~(hx_ @ local)
  ~(cx_tmp @ local)
  ~(output @ local)
  ~(hy_ @ local)
  ~(cy_ @ local)
  ~(grad_output @ local)
  ~(grad_hy @ local)
  ~(grad_cy @ local)
  ~reverse
  ~mode
  ~hidden_size
  ~num_layers
  ~has_biases
  ~train
  ~bidirectional
  ~batch_sizes
  ~batch_first
  ~(workspace @ local)
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let out3 = globalize_tensor out3 in
  let out4 = globalize_tensor out4 in
  let out5 = globalize_tensor out5 in
  let out6 = globalize_tensor out6 in
  let input = globalize_tensor input in
  let weight1 = globalize_tensor weight1 in
  let weight2 = globalize_tensor weight2 in
  let weight3 = globalize_tensor weight3 in
  let weight4 = globalize_tensor weight4 in
  let hx_ = globalize_tensor hx_ in
  let cx_tmp = globalize_tensor cx_tmp in
  let output = globalize_tensor output in
  let hy_ = globalize_tensor hy_ in
  let cy_ = globalize_tensor cy_ in
  let grad_output = [%globalize: tensor option] grad_output in
  let grad_hy = [%globalize: tensor option] grad_hy in
  let grad_cy = [%globalize: tensor option] grad_cy in
  let workspace = globalize_tensor workspace in
  let result0, result1, result2, result3, result4, result5, result6 =
    [%c.alloc
      ({|CAMLreturn(atg_mkldnn_rnn_layer_backward_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{out3 : tensor value}, %{out4 : tensor value}, %{out5 : tensor value}, %{out6 : tensor value}, %{input : tensor value}, %{weight1 : tensor value}, %{weight2 : tensor value}, %{weight3 : tensor value}, %{weight4 : tensor value}, %{hx_ : tensor value}, %{cx_tmp : tensor value}, %{output : tensor value}, %{hy_ : tensor value}, %{cy_ : tensor value}, %{grad_output : tensor option value}, %{grad_hy : tensor option value}, %{grad_cy : tensor option value}, Int_val(%{reverse : bool value}), %{mode : int}, %{hidden_size : int}, %{num_layers : int}, Int_val(%{has_biases : bool value}), Int_val(%{train : bool value}), Int_val(%{bidirectional : bool value}), %{batch_sizes : int list value}, Int_val(%{batch_first : bool value}), %{workspace : tensor value}));|}
       : (raw_tensor
         * raw_tensor
         * raw_tensor
         * raw_tensor
         * raw_tensor
         * raw_tensor
         * raw_tensor)
           value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3
  , wrap_managed_tensor result4
  , wrap_managed_tensor result5
  , wrap_managed_tensor result6 )
;;

let mkldnn_rnn_layer_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  ~(out3 @ local)
  (input @ local)
  ~(weight0 @ local)
  ~(weight1 @ local)
  ~(weight2 @ local)
  ~(weight3 @ local)
  ~(hx_ @ local)
  ~(cx_ @ local)
  ~reverse
  ~batch_sizes
  ~mode
  ~hidden_size
  ~num_layers
  ~has_biases
  ~bidirectional
  ~batch_first
  ~train
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let out3 = globalize_tensor out3 in
  let input = globalize_tensor input in
  let weight0 = globalize_tensor weight0 in
  let weight1 = globalize_tensor weight1 in
  let weight2 = globalize_tensor weight2 in
  let weight3 = globalize_tensor weight3 in
  let hx_ = globalize_tensor hx_ in
  let cx_ = globalize_tensor cx_ in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg_mkldnn_rnn_layer_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{out3 : tensor value}, %{input : tensor value}, %{weight0 : tensor value}, %{weight1 : tensor value}, %{weight2 : tensor value}, %{weight3 : tensor value}, %{hx_ : tensor value}, %{cx_ : tensor value}, Int_val(%{reverse : bool value}), %{batch_sizes : int list value}, %{mode : int}, %{hidden_size : int}, %{num_layers : int}, Int_val(%{has_biases : bool value}), Int_val(%{bidirectional : bool value}), Int_val(%{batch_first : bool value}), Int_val(%{train : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let mm (self @ local) ~(mat2 @ local) =
  let self = globalize_tensor self in
  let mat2 = globalize_tensor mat2 in
  [%c.alloc
    ({|CAMLreturn(atg_mm(%{self : tensor value}, %{mat2 : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mm_out ~(out @ local) (self @ local) ~(mat2 @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let mat2 = globalize_tensor mat2 in
  [%c.alloc
    ({|CAMLreturn(atg_mm_out(%{out : tensor value}, %{self : tensor value}, %{mat2 : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mode (self @ local) ~dim ~keepdim =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_mode(%{self : tensor value}, %{dim : int}, Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let mode_values ~(values @ local) ~(indices @ local) (self @ local) ~dim ~keepdim =
  let values = globalize_tensor values in
  let indices = globalize_tensor indices in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_mode_values(%{values : tensor value}, %{indices : tensor value}, %{self : tensor value}, %{dim : int}, Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let moveaxis (self @ local) ~source ~destination =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_moveaxis(%{self : tensor value}, %{source : int list value}, %{destination : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let moveaxis_int (self @ local) ~source ~destination =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_moveaxis_int(%{self : tensor value}, %{source : int}, %{destination : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let movedim (self @ local) ~source ~destination =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_movedim(%{self : tensor value}, %{source : int list value}, %{destination : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let movedim_int (self @ local) ~source ~destination =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_movedim_int(%{self : tensor value}, %{source : int}, %{destination : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mse_loss (self @ local) ~(target @ local) ~reduction =
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_mse_loss(%{self : tensor value}, %{target : tensor value}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mse_loss_backward ~(grad_output @ local) (self @ local) ~(target @ local) ~reduction =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_mse_loss_backward(%{grad_output : tensor value}, %{self : tensor value}, %{target : tensor value}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mse_loss_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~(target @ local)
  ~reduction
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_mse_loss_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{target : tensor value}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mse_loss_out ~(out @ local) (self @ local) ~(target @ local) ~reduction =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_mse_loss_out(%{out : tensor value}, %{self : tensor value}, %{target : tensor value}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let msort (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_msort(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let msort_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_msort_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mt (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_mt(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mul (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_mul(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mul_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_mul_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mul_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_mul_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mul_scalar (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_mul_scalar(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mul_scalar_ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_mul_scalar_(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mul_scalar_out ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_mul_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let multi_margin_loss_backward
  ~(grad_output @ local)
  (self @ local)
  ~(target @ local)
  ~p
  ~margin
  ~(weight @ local)
  ~reduction
  =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let weight = [%globalize: tensor option] weight in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_multi_margin_loss_backward(%{grad_output : tensor value}, %{self : tensor value}, %{target : tensor value}, %{p : scalar value}, %{margin : scalar value}, %{weight : tensor option value}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let multi_margin_loss_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~(target @ local)
  ~p
  ~margin
  ~(weight @ local)
  ~reduction
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let weight = [%globalize: tensor option] weight in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_multi_margin_loss_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{target : tensor value}, %{p : scalar value}, %{margin : scalar value}, %{weight : tensor option value}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let multilabel_margin_loss (self @ local) ~(target @ local) ~reduction =
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_multilabel_margin_loss(%{self : tensor value}, %{target : tensor value}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let multilabel_margin_loss_backward
  ~(grad_output @ local)
  (self @ local)
  ~(target @ local)
  ~reduction
  ~(is_target @ local)
  =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  let is_target = globalize_tensor is_target in
  [%c.alloc
    ({|CAMLreturn(atg_multilabel_margin_loss_backward(%{grad_output : tensor value}, %{self : tensor value}, %{target : tensor value}, %{reduction : int}, %{is_target : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let multilabel_margin_loss_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~(target @ local)
  ~reduction
  ~(is_target @ local)
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  let is_target = globalize_tensor is_target in
  [%c.alloc
    ({|CAMLreturn(atg_multilabel_margin_loss_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{target : tensor value}, %{reduction : int}, %{is_target : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let multilabel_margin_loss_out ~(out @ local) (self @ local) ~(target @ local) ~reduction =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_multilabel_margin_loss_out(%{out : tensor value}, %{self : tensor value}, %{target : tensor value}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let multinomial (self @ local) ~num_samples ~replacement =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_multinomial(%{self : tensor value}, %{num_samples : int}, Int_val(%{replacement : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let multinomial_out ~(out @ local) (self @ local) ~num_samples ~replacement =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_multinomial_out(%{out : tensor value}, %{self : tensor value}, %{num_samples : int}, Int_val(%{replacement : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let multiply (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_multiply(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let multiply_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_multiply_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let multiply_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_multiply_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let multiply_scalar (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_multiply_scalar(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let multiply_scalar_ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_multiply_scalar_(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mv (self @ local) ~(vec @ local) =
  let self = globalize_tensor self in
  let vec = globalize_tensor vec in
  [%c.alloc
    ({|CAMLreturn(atg_mv(%{self : tensor value}, %{vec : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mv_out ~(out @ local) (self @ local) ~(vec @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let vec = globalize_tensor vec in
  [%c.alloc
    ({|CAMLreturn(atg_mv_out(%{out : tensor value}, %{self : tensor value}, %{vec : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mvlgamma (self @ local) ~p =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_mvlgamma(%{self : tensor value}, %{p : int}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mvlgamma_ (self @ local) ~p =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_mvlgamma_(%{self : tensor value}, %{p : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mvlgamma_out ~(out @ local) (self @ local) ~p =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_mvlgamma_out(%{out : tensor value}, %{self : tensor value}, %{p : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nan_to_num (self @ local) ~nan ~posinf ~neginf =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_nan_to_num(%{self : tensor value}, %{nan : float option value}, %{posinf : float option value}, %{neginf : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nan_to_num_ (self @ local) ~nan ~posinf ~neginf =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_nan_to_num_(%{self : tensor value}, %{nan : float option value}, %{posinf : float option value}, %{neginf : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nan_to_num_out ~(out @ local) (self @ local) ~nan ~posinf ~neginf =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_nan_to_num_out(%{out : tensor value}, %{self : tensor value}, %{nan : float option value}, %{posinf : float option value}, %{neginf : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nanmean (self @ local) ~dim ~keepdim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_nanmean(%{self : tensor value}, %{dim : int list option value}, Int_val(%{keepdim : bool value}), %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nanmean_out ~(out @ local) (self @ local) ~dim ~keepdim ~dtype =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_nanmean_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list option value}, Int_val(%{keepdim : bool value}), %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nanmedian (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_nanmedian(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nanmedian_dim (self @ local) ~dim ~keepdim =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_nanmedian_dim(%{self : tensor value}, %{dim : int}, Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let nanmedian_dim_values ~(values @ local) ~(indices @ local) (self @ local) ~dim ~keepdim
  =
  let values = globalize_tensor values in
  let indices = globalize_tensor indices in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_nanmedian_dim_values(%{values : tensor value}, %{indices : tensor value}, %{self : tensor value}, %{dim : int}, Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let nanmedian_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_nanmedian_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nanquantile (self @ local) ~(q @ local) ~dim ~keepdim ~interpolation =
  let self = globalize_tensor self in
  let q = globalize_tensor q in
  [%c.alloc
    ({|CAMLreturn(atg_nanquantile(%{self : tensor value}, %{q : tensor value}, %{dim : int option value}, Int_val(%{keepdim : bool value}), String_val(%{interpolation : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nanquantile_out
  ~(out @ local)
  (self @ local)
  ~(q @ local)
  ~dim
  ~keepdim
  ~interpolation
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let q = globalize_tensor q in
  [%c.alloc
    ({|CAMLreturn(atg_nanquantile_out(%{out : tensor value}, %{self : tensor value}, %{q : tensor value}, %{dim : int option value}, Int_val(%{keepdim : bool value}), String_val(%{interpolation : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nanquantile_scalar (self @ local) ~q ~dim ~keepdim ~interpolation =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_nanquantile_scalar(%{self : tensor value}, %{q : float}, %{dim : int option value}, Int_val(%{keepdim : bool value}), String_val(%{interpolation : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nanquantile_scalar_out ~(out @ local) (self @ local) ~q ~dim ~keepdim ~interpolation =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_nanquantile_scalar_out(%{out : tensor value}, %{self : tensor value}, %{q : float}, %{dim : int option value}, Int_val(%{keepdim : bool value}), String_val(%{interpolation : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nansum (self @ local) ~dim ~keepdim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_nansum(%{self : tensor value}, %{dim : int list option value}, Int_val(%{keepdim : bool value}), %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nansum_out ~(out @ local) (self @ local) ~dim ~keepdim ~dtype =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_nansum_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list option value}, Int_val(%{keepdim : bool value}), %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let narrow (self @ local) ~dim ~start ~length =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_narrow(%{self : tensor value}, %{dim : int}, %{start : int}, %{length : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let narrow_copy (self @ local) ~dim ~start ~length =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_narrow_copy(%{self : tensor value}, %{dim : int}, %{start : int}, %{length : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let narrow_copy_out ~(out @ local) (self @ local) ~dim ~start ~length =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_narrow_copy_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, %{start : int}, %{length : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let narrow_tensor (self @ local) ~dim ~(start @ local) ~length =
  let self = globalize_tensor self in
  let start = globalize_tensor start in
  [%c.alloc
    ({|CAMLreturn(atg_narrow_tensor(%{self : tensor value}, %{dim : int}, %{start : tensor value}, %{length : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let native_batch_norm
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~training
  ~momentum
  ~eps
  =
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let running_mean = [%globalize: tensor option] running_mean in
  let running_var = [%globalize: tensor option] running_var in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_native_batch_norm(%{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, %{running_mean : tensor option value}, %{running_var : tensor option value}, Int_val(%{training : bool value}), %{momentum : float}, %{eps : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let native_batch_norm_out
  ~(out @ local)
  ~(save_mean @ local)
  ~(save_invstd @ local)
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~training
  ~momentum
  ~eps
  =
  let out = globalize_tensor out in
  let save_mean = globalize_tensor save_mean in
  let save_invstd = globalize_tensor save_invstd in
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let running_mean = [%globalize: tensor option] running_mean in
  let running_var = [%globalize: tensor option] running_var in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_native_batch_norm_out(%{out : tensor value}, %{save_mean : tensor value}, %{save_invstd : tensor value}, %{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, %{running_mean : tensor option value}, %{running_var : tensor option value}, Int_val(%{training : bool value}), %{momentum : float}, %{eps : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let native_channel_shuffle (self @ local) ~groups =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_native_channel_shuffle(%{self : tensor value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let native_dropout (input @ local) ~p ~train =
  let input = globalize_tensor input in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_native_dropout(%{input : tensor value}, %{p : float}, Int_val(%{train : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let native_dropout_backward ~(grad_output @ local) ~(mask @ local) ~scale =
  let grad_output = globalize_tensor grad_output in
  let mask = globalize_tensor mask in
  [%c.alloc
    ({|CAMLreturn(atg_native_dropout_backward(%{grad_output : tensor value}, %{mask : tensor value}, %{scale : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let native_dropout_backward_out
  ~(out @ local)
  ~(grad_output @ local)
  ~(mask @ local)
  ~scale
  =
  let out = globalize_tensor out in
  let grad_output = globalize_tensor grad_output in
  let mask = globalize_tensor mask in
  [%c.alloc
    ({|CAMLreturn(atg_native_dropout_backward_out(%{out : tensor value}, %{grad_output : tensor value}, %{mask : tensor value}, %{scale : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let native_dropout_out ~(out0 @ local) ~(out1 @ local) (input @ local) ~p ~train =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let input = globalize_tensor input in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_native_dropout_out(%{out0 : tensor value}, %{out1 : tensor value}, %{input : tensor value}, %{p : float}, Int_val(%{train : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let native_group_norm
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~n
  ~c
  ~hxw
  ~group
  ~eps
  =
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_native_group_norm(%{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, %{n : int}, %{c : int}, %{hxw : int}, %{group : int}, %{eps : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let native_group_norm_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~n
  ~c
  ~hxw
  ~group
  ~eps
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_native_group_norm_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, %{n : int}, %{c : int}, %{hxw : int}, %{group : int}, %{eps : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let native_layer_norm
  (input @ local)
  ~normalized_shape
  ~(weight @ local)
  ~(bias @ local)
  ~eps
  =
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_native_layer_norm(%{input : tensor value}, %{normalized_shape : int list value}, %{weight : tensor option value}, %{bias : tensor option value}, %{eps : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let native_layer_norm_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  (input @ local)
  ~normalized_shape
  ~(weight @ local)
  ~(bias @ local)
  ~eps
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_native_layer_norm_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{input : tensor value}, %{normalized_shape : int list value}, %{weight : tensor option value}, %{bias : tensor option value}, %{eps : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let native_norm ?p (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_native_norm(%{self : tensor value}, %{p : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let native_norm_out ?p ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_native_norm_out(%{out : tensor value}, %{self : tensor value}, %{p : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let native_norm_scalaropt_dim_dtype (self @ local) ~p ~dim ~keepdim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_native_norm_scalaropt_dim_dtype(%{self : tensor value}, %{p : scalar value}, %{dim : int list value}, Int_val(%{keepdim : bool value}), %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let native_norm_scalaropt_dim_dtype_out
  ~(out @ local)
  (self @ local)
  ~p
  ~dim
  ~keepdim
  ~dtype
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_native_norm_scalaropt_dim_dtype_out(%{out : tensor value}, %{self : tensor value}, %{p : scalar value}, %{dim : int list value}, Int_val(%{keepdim : bool value}), %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ne (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_ne(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ne_ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_ne_(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ne_scalar_out ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_ne_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ne_tensor (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_ne_tensor(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ne_tensor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_ne_tensor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ne_tensor_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_ne_tensor_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let neg (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_neg(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let neg_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_neg_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let neg_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_neg_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let negative (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_negative(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let negative_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_negative_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let negative_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_negative_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nested_to_padded_tensor (self @ local) ~padding ~output_size =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_nested_to_padded_tensor(%{self : tensor value}, %{padding : float}, %{output_size : int list option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let new_empty (self @ local) ~size ~options =
  let self = globalize_tensor self in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_new_empty(%{self : tensor value}, %{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let new_empty_out ~(out @ local) (self @ local) ~size =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_new_empty_out(%{out : tensor value}, %{self : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let new_empty_strided (self @ local) ~size ~stride ~options =
  let self = globalize_tensor self in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_new_empty_strided(%{self : tensor value}, %{size : int list value}, %{stride : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let new_empty_strided_out ~(out @ local) (self @ local) ~size ~stride =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_new_empty_strided_out(%{out : tensor value}, %{self : tensor value}, %{size : int list value}, %{stride : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let new_full (self @ local) ~size ~fill_value ~options =
  let self = globalize_tensor self in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_new_full(%{self : tensor value}, %{size : int list value}, %{fill_value : scalar value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let new_full_out ~(out @ local) (self @ local) ~size ~fill_value =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_new_full_out(%{out : tensor value}, %{self : tensor value}, %{size : int list value}, %{fill_value : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let new_ones (self @ local) ~size ~options =
  let self = globalize_tensor self in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_new_ones(%{self : tensor value}, %{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let new_ones_out ~(out @ local) (self @ local) ~size =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_new_ones_out(%{out : tensor value}, %{self : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let new_zeros (self @ local) ~size ~options =
  let self = globalize_tensor self in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_new_zeros(%{self : tensor value}, %{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let new_zeros_out ~(out @ local) (self @ local) ~size =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_new_zeros_out(%{out : tensor value}, %{self : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nextafter (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_nextafter(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nextafter_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_nextafter_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nextafter_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_nextafter_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nll_loss (self @ local) ~(target @ local) ~(weight @ local) ~reduction ~ignore_index =
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let weight = [%globalize: tensor option] weight in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_nll_loss(%{self : tensor value}, %{target : tensor value}, %{weight : tensor option value}, %{reduction : int}, %{ignore_index : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nll_loss2d (self @ local) ~(target @ local) ~(weight @ local) ~reduction ~ignore_index
  =
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let weight = [%globalize: tensor option] weight in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_nll_loss2d(%{self : tensor value}, %{target : tensor value}, %{weight : tensor option value}, %{reduction : int}, %{ignore_index : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nll_loss2d_backward
  ~(grad_output @ local)
  (self @ local)
  ~(target @ local)
  ~(weight @ local)
  ~reduction
  ~ignore_index
  ~(total_weight @ local)
  =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let weight = [%globalize: tensor option] weight in
  let reduction = Reduction.to_int reduction in
  let total_weight = globalize_tensor total_weight in
  [%c.alloc
    ({|CAMLreturn(atg_nll_loss2d_backward(%{grad_output : tensor value}, %{self : tensor value}, %{target : tensor value}, %{weight : tensor option value}, %{reduction : int}, %{ignore_index : int}, %{total_weight : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nll_loss2d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~(target @ local)
  ~(weight @ local)
  ~reduction
  ~ignore_index
  ~(total_weight @ local)
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let weight = [%globalize: tensor option] weight in
  let reduction = Reduction.to_int reduction in
  let total_weight = globalize_tensor total_weight in
  [%c.alloc
    ({|CAMLreturn(atg_nll_loss2d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{target : tensor value}, %{weight : tensor option value}, %{reduction : int}, %{ignore_index : int}, %{total_weight : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nll_loss2d_out
  ~(out @ local)
  (self @ local)
  ~(target @ local)
  ~(weight @ local)
  ~reduction
  ~ignore_index
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let weight = [%globalize: tensor option] weight in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_nll_loss2d_out(%{out : tensor value}, %{self : tensor value}, %{target : tensor value}, %{weight : tensor option value}, %{reduction : int}, %{ignore_index : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nll_loss_backward
  ~(grad_output @ local)
  (self @ local)
  ~(target @ local)
  ~(weight @ local)
  ~reduction
  ~ignore_index
  ~(total_weight @ local)
  =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let weight = [%globalize: tensor option] weight in
  let reduction = Reduction.to_int reduction in
  let total_weight = globalize_tensor total_weight in
  [%c.alloc
    ({|CAMLreturn(atg_nll_loss_backward(%{grad_output : tensor value}, %{self : tensor value}, %{target : tensor value}, %{weight : tensor option value}, %{reduction : int}, %{ignore_index : int}, %{total_weight : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nll_loss_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~(target @ local)
  ~(weight @ local)
  ~reduction
  ~ignore_index
  ~(total_weight @ local)
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let weight = [%globalize: tensor option] weight in
  let reduction = Reduction.to_int reduction in
  let total_weight = globalize_tensor total_weight in
  [%c.alloc
    ({|CAMLreturn(atg_nll_loss_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{target : tensor value}, %{weight : tensor option value}, %{reduction : int}, %{ignore_index : int}, %{total_weight : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nll_loss_nd
  (self @ local)
  ~(target @ local)
  ~(weight @ local)
  ~reduction
  ~ignore_index
  =
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let weight = [%globalize: tensor option] weight in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_nll_loss_nd(%{self : tensor value}, %{target : tensor value}, %{weight : tensor option value}, %{reduction : int}, %{ignore_index : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nll_loss_out
  ~(out @ local)
  (self @ local)
  ~(target @ local)
  ~(weight @ local)
  ~reduction
  ~ignore_index
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let weight = [%globalize: tensor option] weight in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_nll_loss_out(%{out : tensor value}, %{self : tensor value}, %{target : tensor value}, %{weight : tensor option value}, %{reduction : int}, %{ignore_index : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nonzero (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_nonzero(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nonzero_numpy (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_nonzero_numpy(%{self : tensor value}));|} : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let nonzero_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_nonzero_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nonzero_static (self @ local) ~size ~fill_value =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_nonzero_static(%{self : tensor value}, %{size : int}, %{fill_value : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nonzero_static_out ~(out @ local) (self @ local) ~size ~fill_value =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_nonzero_static_out(%{out : tensor value}, %{self : tensor value}, %{size : int}, %{fill_value : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let norm ?p (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_norm(%{self : tensor value}, %{p : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let norm_dtype_out ~(out @ local) (self @ local) ~p ~dim ~keepdim ~dtype =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_norm_dtype_out(%{out : tensor value}, %{self : tensor value}, %{p : scalar value}, %{dim : int list value}, Int_val(%{keepdim : bool value}), %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let norm_except_dim ~(v @ local) ~pow ~dim =
  let v = globalize_tensor v in
  [%c.alloc
    ({|CAMLreturn(atg_norm_except_dim(%{v : tensor value}, %{pow : int}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let norm_out ~(out @ local) (self @ local) ~p ~dim ~keepdim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_norm_out(%{out : tensor value}, %{self : tensor value}, %{p : scalar value}, %{dim : int list value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let norm_scalar_out ?p ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_norm_scalar_out(%{out : tensor value}, %{self : tensor value}, %{p : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let norm_scalaropt_dim (self @ local) ~p ~dim ~keepdim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_norm_scalaropt_dim(%{self : tensor value}, %{p : scalar value}, %{dim : int list value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let norm_scalaropt_dim_dtype (self @ local) ~p ~dim ~keepdim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_norm_scalaropt_dim_dtype(%{self : tensor value}, %{p : scalar value}, %{dim : int list value}, Int_val(%{keepdim : bool value}), %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let norm_scalaropt_dtype (self @ local) ~p ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_norm_scalaropt_dtype(%{self : tensor value}, %{p : scalar value}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let norm_scalaropt_dtype_out ~(out @ local) (self @ local) ~p ~dtype =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_norm_scalaropt_dtype_out(%{out : tensor value}, %{self : tensor value}, %{p : scalar value}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let normal_ (self @ local) ~mean ~std =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_normal_(%{self : tensor value}, %{mean : float}, %{std : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let normal_functional (self @ local) ~mean ~std =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_normal_functional(%{self : tensor value}, %{mean : float}, %{std : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let not_equal (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_not_equal(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let not_equal_ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_not_equal_(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let not_equal_scalar_out ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_not_equal_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let not_equal_tensor (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_not_equal_tensor(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let not_equal_tensor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_not_equal_tensor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let not_equal_tensor_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_not_equal_tensor_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nuclear_norm (self @ local) ~keepdim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_nuclear_norm(%{self : tensor value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nuclear_norm_dim (self @ local) ~dim ~keepdim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_nuclear_norm_dim(%{self : tensor value}, %{dim : int list value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nuclear_norm_dim_out ~(out @ local) (self @ local) ~dim ~keepdim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_nuclear_norm_dim_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let nuclear_norm_out ~(out @ local) (self @ local) ~keepdim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_nuclear_norm_out(%{out : tensor value}, %{self : tensor value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let numpy_t (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_numpy_t(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let one_hot (self @ local) ~num_classes =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_one_hot(%{self : tensor value}, %{num_classes : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ones ~size ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_ones(%{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ones_like (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_ones_like(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ones_like_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_ones_like_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ones_out ~(out @ local) ~size =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_ones_out(%{out : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let orgqr (self @ local) ~(input2 @ local) =
  let self = globalize_tensor self in
  let input2 = globalize_tensor input2 in
  [%c.alloc
    ({|CAMLreturn(atg_orgqr(%{self : tensor value}, %{input2 : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let orgqr_out ~(out @ local) (self @ local) ~(input2 @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let input2 = globalize_tensor input2 in
  [%c.alloc
    ({|CAMLreturn(atg_orgqr_out(%{out : tensor value}, %{self : tensor value}, %{input2 : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ormqr (self @ local) ~(input2 @ local) ~(input3 @ local) ~left ~transpose =
  let self = globalize_tensor self in
  let input2 = globalize_tensor input2 in
  let input3 = globalize_tensor input3 in
  [%c.alloc
    ({|CAMLreturn(atg_ormqr(%{self : tensor value}, %{input2 : tensor value}, %{input3 : tensor value}, Int_val(%{left : bool value}), Int_val(%{transpose : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ormqr_out
  ~(out @ local)
  (self @ local)
  ~(input2 @ local)
  ~(input3 @ local)
  ~left
  ~transpose
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let input2 = globalize_tensor input2 in
  let input3 = globalize_tensor input3 in
  [%c.alloc
    ({|CAMLreturn(atg_ormqr_out(%{out : tensor value}, %{self : tensor value}, %{input2 : tensor value}, %{input3 : tensor value}, Int_val(%{left : bool value}), Int_val(%{transpose : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let outer (self @ local) ~(vec2 @ local) =
  let self = globalize_tensor self in
  let vec2 = globalize_tensor vec2 in
  [%c.alloc
    ({|CAMLreturn(atg_outer(%{self : tensor value}, %{vec2 : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let outer_out ~(out @ local) (self @ local) ~(vec2 @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let vec2 = globalize_tensor vec2 in
  [%c.alloc
    ({|CAMLreturn(atg_outer_out(%{out : tensor value}, %{self : tensor value}, %{vec2 : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let output_nr (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_output_nr(%{self : tensor value}));|} : int64 value)]
;;

let pad (self @ local) ~pad ~mode ~value =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_pad(%{self : tensor value}, %{pad : int list value}, String_val(%{mode : string value}), %{value : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let pad_sequence ~(sequences @ local) ~batch_first ~padding_value ~padding_side =
  let sequences =
    Torch_local_iterators.List.map_local_input sequences ~f:globalize_tensor
  in
  [%c.alloc
    ({|CAMLreturn(atg_pad_sequence(%{sequences : tensor list value}, Int_val(%{batch_first : bool value}), %{padding_value : float}, String_val(%{padding_side : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let pairwise_distance ~(x1 @ local) ~(x2 @ local) ~p ~eps ~keepdim =
  let x1 = globalize_tensor x1 in
  let x2 = globalize_tensor x2 in
  [%c.alloc
    ({|CAMLreturn(atg_pairwise_distance(%{x1 : tensor value}, %{x2 : tensor value}, %{p : float}, %{eps : float}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let pdist (self @ local) ~p =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_pdist(%{self : tensor value}, %{p : float}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let permute (self @ local) ~dims =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_permute(%{self : tensor value}, %{dims : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let permute_copy (self @ local) ~dims =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_permute_copy(%{self : tensor value}, %{dims : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let permute_copy_out ~(out @ local) (self @ local) ~dims =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_permute_copy_out(%{out : tensor value}, %{self : tensor value}, %{dims : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let pin_memory (self @ local) ~device =
  let self = globalize_tensor self in
  let device = Device.option_to_int device in
  [%c.alloc
    ({|CAMLreturn(atg_pin_memory(%{self : tensor value}, %{device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let pinverse (self @ local) ~rcond =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_pinverse(%{self : tensor value}, %{rcond : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let pixel_shuffle (self @ local) ~upscale_factor =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_pixel_shuffle(%{self : tensor value}, %{upscale_factor : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let pixel_shuffle_out ~(out @ local) (self @ local) ~upscale_factor =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_pixel_shuffle_out(%{out : tensor value}, %{self : tensor value}, %{upscale_factor : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let pixel_unshuffle (self @ local) ~downscale_factor =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_pixel_unshuffle(%{self : tensor value}, %{downscale_factor : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let pixel_unshuffle_out ~(out @ local) (self @ local) ~downscale_factor =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_pixel_unshuffle_out(%{out : tensor value}, %{self : tensor value}, %{downscale_factor : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let poisson (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_poisson(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let poisson_nll_loss (input @ local) ~(target @ local) ~log_input ~full ~eps ~reduction =
  let input = globalize_tensor input in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_poisson_nll_loss(%{input : tensor value}, %{target : tensor value}, Int_val(%{log_input : bool value}), Int_val(%{full : bool value}), %{eps : float}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let poisson_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_poisson_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let polar ~(abs @ local) ~(angle @ local) =
  let abs = globalize_tensor abs in
  let angle = globalize_tensor angle in
  [%c.alloc
    ({|CAMLreturn(atg_polar(%{abs : tensor value}, %{angle : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let polar_out ~(out @ local) ~(abs @ local) ~(angle @ local) =
  let out = globalize_tensor out in
  let abs = globalize_tensor abs in
  let angle = globalize_tensor angle in
  [%c.alloc
    ({|CAMLreturn(atg_polar_out(%{out : tensor value}, %{abs : tensor value}, %{angle : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let polygamma ~n (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_polygamma(%{n : int}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let polygamma_ (self @ local) ~n =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_polygamma_(%{self : tensor value}, %{n : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let polygamma_out ~(out @ local) ~n (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_polygamma_out(%{out : tensor value}, %{n : int}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let positive (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_positive(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let pow (self @ local) ~(exponent @ local) =
  let self = globalize_tensor self in
  let exponent = globalize_tensor exponent in
  [%c.alloc
    ({|CAMLreturn(atg_pow(%{self : tensor value}, %{exponent : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let pow_ (self @ local) ~exponent =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_pow_(%{self : tensor value}, %{exponent : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let pow_scalar self ~(exponent @ local) =
  let exponent = globalize_tensor exponent in
  [%c.alloc
    ({|CAMLreturn(atg_pow_scalar(%{self : scalar value}, %{exponent : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let pow_scalar_out ~(out @ local) self ~(exponent @ local) =
  let out = globalize_tensor out in
  let exponent = globalize_tensor exponent in
  [%c.alloc
    ({|CAMLreturn(atg_pow_scalar_out(%{out : tensor value}, %{self : scalar value}, %{exponent : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let pow_tensor_ (self @ local) ~(exponent @ local) =
  let self = globalize_tensor self in
  let exponent = globalize_tensor exponent in
  [%c.alloc
    ({|CAMLreturn(atg_pow_tensor_(%{self : tensor value}, %{exponent : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let pow_tensor_scalar (self @ local) ~exponent =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_pow_tensor_scalar(%{self : tensor value}, %{exponent : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let pow_tensor_scalar_out ~(out @ local) (self @ local) ~exponent =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_pow_tensor_scalar_out(%{out : tensor value}, %{self : tensor value}, %{exponent : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let pow_tensor_tensor_out ~(out @ local) (self @ local) ~(exponent @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let exponent = globalize_tensor exponent in
  [%c.alloc
    ({|CAMLreturn(atg_pow_tensor_tensor_out(%{out : tensor value}, %{self : tensor value}, %{exponent : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let prelu (self @ local) ~(weight @ local) =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  [%c.alloc
    ({|CAMLreturn(atg_prelu(%{self : tensor value}, %{weight : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let prod (self @ local) ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_prod(%{self : tensor value}, %{dtype : int}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let prod_dim_int (self @ local) ~dim ~keepdim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_prod_dim_int(%{self : tensor value}, %{dim : int}, Int_val(%{keepdim : bool value}), %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let prod_int_out ~(out @ local) (self @ local) ~dim ~keepdim ~dtype =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_prod_int_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, Int_val(%{keepdim : bool value}), %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let prod_out ~(out @ local) (self @ local) ~dtype =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_prod_out(%{out : tensor value}, %{self : tensor value}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let put (self @ local) ~(index @ local) ~(source @ local) ~accumulate =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let source = globalize_tensor source in
  [%c.alloc
    ({|CAMLreturn(atg_put(%{self : tensor value}, %{index : tensor value}, %{source : tensor value}, Int_val(%{accumulate : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let put_ (self @ local) ~(index @ local) ~(source @ local) ~accumulate =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let source = globalize_tensor source in
  [%c.alloc
    ({|CAMLreturn(atg_put_(%{self : tensor value}, %{index : tensor value}, %{source : tensor value}, Int_val(%{accumulate : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let put_out ~(out @ local) (self @ local) ~(index @ local) ~(source @ local) ~accumulate =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let source = globalize_tensor source in
  [%c.alloc
    ({|CAMLreturn(atg_put_out(%{out : tensor value}, %{self : tensor value}, %{index : tensor value}, %{source : tensor value}, Int_val(%{accumulate : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let q_per_channel_axis (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_q_per_channel_axis(%{self : tensor value}));|} : int64 value)]
;;

let q_per_channel_scales (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_q_per_channel_scales(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let q_per_channel_scales_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_q_per_channel_scales_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let q_per_channel_zero_points (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_q_per_channel_zero_points(%{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let q_per_channel_zero_points_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_q_per_channel_zero_points_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let q_scale (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_q_scale(%{self : tensor value}));|} : float value)]
;;

let q_zero_point (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_q_zero_point(%{self : tensor value}));|} : int64 value)]
;;

let qr (self @ local) ~some =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_qr(%{self : tensor value}, Int_val(%{some : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let qr_q ~(q @ local) ~(r @ local) (self @ local) ~some =
  let q = globalize_tensor q in
  let r = globalize_tensor r in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_qr_q(%{q : tensor value}, %{r : tensor value}, %{self : tensor value}, Int_val(%{some : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let quantile (self @ local) ~(q @ local) ~dim ~keepdim ~interpolation =
  let self = globalize_tensor self in
  let q = globalize_tensor q in
  [%c.alloc
    ({|CAMLreturn(atg_quantile(%{self : tensor value}, %{q : tensor value}, %{dim : int option value}, Int_val(%{keepdim : bool value}), String_val(%{interpolation : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantile_out ~(out @ local) (self @ local) ~(q @ local) ~dim ~keepdim ~interpolation =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let q = globalize_tensor q in
  [%c.alloc
    ({|CAMLreturn(atg_quantile_out(%{out : tensor value}, %{self : tensor value}, %{q : tensor value}, %{dim : int option value}, Int_val(%{keepdim : bool value}), String_val(%{interpolation : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantile_scalar (self @ local) ~q ~dim ~keepdim ~interpolation =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_quantile_scalar(%{self : tensor value}, %{q : float}, %{dim : int option value}, Int_val(%{keepdim : bool value}), String_val(%{interpolation : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantile_scalar_out ~(out @ local) (self @ local) ~q ~dim ~keepdim ~interpolation =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_quantile_scalar_out(%{out : tensor value}, %{self : tensor value}, %{q : float}, %{dim : int option value}, Int_val(%{keepdim : bool value}), String_val(%{interpolation : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantize_per_channel
  (self @ local)
  ~(scales @ local)
  ~(zero_points @ local)
  ~axis
  ~dtype
  =
  let self = globalize_tensor self in
  let scales = globalize_tensor scales in
  let zero_points = globalize_tensor zero_points in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_quantize_per_channel(%{self : tensor value}, %{scales : tensor value}, %{zero_points : tensor value}, %{axis : int}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantize_per_channel_out
  ~(out @ local)
  (self @ local)
  ~(scales @ local)
  ~(zero_points @ local)
  ~axis
  ~dtype
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let scales = globalize_tensor scales in
  let zero_points = globalize_tensor zero_points in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_quantize_per_channel_out(%{out : tensor value}, %{self : tensor value}, %{scales : tensor value}, %{zero_points : tensor value}, %{axis : int}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantize_per_tensor (self @ local) ~scale ~zero_point ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_quantize_per_tensor(%{self : tensor value}, %{scale : float}, %{zero_point : int}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantize_per_tensor_dynamic (self @ local) ~dtype ~reduce_range =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_quantize_per_tensor_dynamic(%{self : tensor value}, %{dtype : int}, Int_val(%{reduce_range : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantize_per_tensor_dynamic_out ~(out @ local) (self @ local) ~dtype ~reduce_range =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_quantize_per_tensor_dynamic_out(%{out : tensor value}, %{self : tensor value}, %{dtype : int}, Int_val(%{reduce_range : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantize_per_tensor_out ~(out @ local) (self @ local) ~scale ~zero_point ~dtype =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_quantize_per_tensor_out(%{out : tensor value}, %{self : tensor value}, %{scale : float}, %{zero_point : int}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantize_per_tensor_tensor_qparams
  (self @ local)
  ~(scale @ local)
  ~(zero_point @ local)
  ~dtype
  =
  let self = globalize_tensor self in
  let scale = globalize_tensor scale in
  let zero_point = globalize_tensor zero_point in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_quantize_per_tensor_tensor_qparams(%{self : tensor value}, %{scale : tensor value}, %{zero_point : tensor value}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantize_per_tensor_tensor_qparams_out
  ~(out @ local)
  (self @ local)
  ~(scale @ local)
  ~(zero_point @ local)
  ~dtype
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let scale = globalize_tensor scale in
  let zero_point = globalize_tensor zero_point in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_quantize_per_tensor_tensor_qparams_out(%{out : tensor value}, %{self : tensor value}, %{scale : tensor value}, %{zero_point : tensor value}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantize_per_tensor_tensors
  (tensors @ local)
  ~(scales @ local)
  ~(zero_points @ local)
  ~dtype
  =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  let scales = globalize_tensor scales in
  let zero_points = globalize_tensor zero_points in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_quantize_per_tensor_tensors(%{tensors : tensor list value}, %{scales : tensor value}, %{zero_points : tensor value}, %{dtype : int}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let quantize_per_tensor_tensors_out
  ~(out @ local)
  (tensors @ local)
  ~(scales @ local)
  ~(zero_points @ local)
  ~dtype
  =
  let out = Torch_local_iterators.List.map_local_input out ~f:globalize_tensor in
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  let scales = globalize_tensor scales in
  let zero_points = globalize_tensor zero_points in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    {|atg_quantize_per_tensor_tensors_out(%{out : tensor list value}, %{tensors : tensor list value}, %{scales : tensor value}, %{zero_points : tensor value}, %{dtype : int});|}]
;;

let quantized_batch_norm
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(mean @ local)
  ~(var @ local)
  ~eps
  ~output_scale
  ~output_zero_point
  =
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let mean = globalize_tensor mean in
  let var = globalize_tensor var in
  [%c.alloc
    ({|CAMLreturn(atg_quantized_batch_norm(%{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, %{mean : tensor value}, %{var : tensor value}, %{eps : float}, %{output_scale : float}, %{output_zero_point : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantized_batch_norm_out
  ~(out @ local)
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(mean @ local)
  ~(var @ local)
  ~eps
  ~output_scale
  ~output_zero_point
  =
  let out = globalize_tensor out in
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let mean = globalize_tensor mean in
  let var = globalize_tensor var in
  [%c.alloc
    ({|CAMLreturn(atg_quantized_batch_norm_out(%{out : tensor value}, %{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, %{mean : tensor value}, %{var : tensor value}, %{eps : float}, %{output_scale : float}, %{output_zero_point : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantized_gru_cell
  (input @ local)
  ~(hx @ local)
  ~(w_ih @ local)
  ~(w_hh @ local)
  ~(b_ih @ local)
  ~(b_hh @ local)
  ~(packed_ih @ local)
  ~(packed_hh @ local)
  ~(col_offsets_ih @ local)
  ~(col_offsets_hh @ local)
  ~scale_ih
  ~scale_hh
  ~zero_point_ih
  ~zero_point_hh
  =
  let input = globalize_tensor input in
  let hx = globalize_tensor hx in
  let w_ih = globalize_tensor w_ih in
  let w_hh = globalize_tensor w_hh in
  let b_ih = globalize_tensor b_ih in
  let b_hh = globalize_tensor b_hh in
  let packed_ih = globalize_tensor packed_ih in
  let packed_hh = globalize_tensor packed_hh in
  let col_offsets_ih = globalize_tensor col_offsets_ih in
  let col_offsets_hh = globalize_tensor col_offsets_hh in
  [%c.alloc
    ({|CAMLreturn(atg_quantized_gru_cell(%{input : tensor value}, %{hx : tensor value}, %{w_ih : tensor value}, %{w_hh : tensor value}, %{b_ih : tensor value}, %{b_hh : tensor value}, %{packed_ih : tensor value}, %{packed_hh : tensor value}, %{col_offsets_ih : tensor value}, %{col_offsets_hh : tensor value}, %{scale_ih : scalar value}, %{scale_hh : scalar value}, %{zero_point_ih : scalar value}, %{zero_point_hh : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantized_lstm_cell
  (input @ local)
  ~(hx @ local)
  ~(w_ih @ local)
  ~(w_hh @ local)
  ~(b_ih @ local)
  ~(b_hh @ local)
  ~(packed_ih @ local)
  ~(packed_hh @ local)
  ~(col_offsets_ih @ local)
  ~(col_offsets_hh @ local)
  ~scale_ih
  ~scale_hh
  ~zero_point_ih
  ~zero_point_hh
  =
  let input = globalize_tensor input in
  let hx = Torch_local_iterators.List.map_local_input hx ~f:globalize_tensor in
  let w_ih = globalize_tensor w_ih in
  let w_hh = globalize_tensor w_hh in
  let b_ih = globalize_tensor b_ih in
  let b_hh = globalize_tensor b_hh in
  let packed_ih = globalize_tensor packed_ih in
  let packed_hh = globalize_tensor packed_hh in
  let col_offsets_ih = globalize_tensor col_offsets_ih in
  let col_offsets_hh = globalize_tensor col_offsets_hh in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_quantized_lstm_cell(%{input : tensor value}, %{hx : tensor list value}, %{w_ih : tensor value}, %{w_hh : tensor value}, %{b_ih : tensor value}, %{b_hh : tensor value}, %{packed_ih : tensor value}, %{packed_hh : tensor value}, %{col_offsets_ih : tensor value}, %{col_offsets_hh : tensor value}, %{scale_ih : scalar value}, %{scale_hh : scalar value}, %{zero_point_ih : scalar value}, %{zero_point_hh : scalar value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let quantized_max_pool1d (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode
  =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_quantized_max_pool1d(%{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantized_max_pool1d_out
  ~(out @ local)
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~dilation
  ~ceil_mode
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_quantized_max_pool1d_out(%{out : tensor value}, %{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantized_max_pool2d (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode
  =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_quantized_max_pool2d(%{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantized_max_pool2d_out
  ~(out @ local)
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~dilation
  ~ceil_mode
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_quantized_max_pool2d_out(%{out : tensor value}, %{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantized_max_pool3d (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode
  =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_quantized_max_pool3d(%{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantized_max_pool3d_out
  ~(out @ local)
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~dilation
  ~ceil_mode
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_quantized_max_pool3d_out(%{out : tensor value}, %{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantized_rnn_relu_cell
  (input @ local)
  ~(hx @ local)
  ~(w_ih @ local)
  ~(w_hh @ local)
  ~(b_ih @ local)
  ~(b_hh @ local)
  ~(packed_ih @ local)
  ~(packed_hh @ local)
  ~(col_offsets_ih @ local)
  ~(col_offsets_hh @ local)
  ~scale_ih
  ~scale_hh
  ~zero_point_ih
  ~zero_point_hh
  =
  let input = globalize_tensor input in
  let hx = globalize_tensor hx in
  let w_ih = globalize_tensor w_ih in
  let w_hh = globalize_tensor w_hh in
  let b_ih = globalize_tensor b_ih in
  let b_hh = globalize_tensor b_hh in
  let packed_ih = globalize_tensor packed_ih in
  let packed_hh = globalize_tensor packed_hh in
  let col_offsets_ih = globalize_tensor col_offsets_ih in
  let col_offsets_hh = globalize_tensor col_offsets_hh in
  [%c.alloc
    ({|CAMLreturn(atg_quantized_rnn_relu_cell(%{input : tensor value}, %{hx : tensor value}, %{w_ih : tensor value}, %{w_hh : tensor value}, %{b_ih : tensor value}, %{b_hh : tensor value}, %{packed_ih : tensor value}, %{packed_hh : tensor value}, %{col_offsets_ih : tensor value}, %{col_offsets_hh : tensor value}, %{scale_ih : scalar value}, %{scale_hh : scalar value}, %{zero_point_ih : scalar value}, %{zero_point_hh : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let quantized_rnn_tanh_cell
  (input @ local)
  ~(hx @ local)
  ~(w_ih @ local)
  ~(w_hh @ local)
  ~(b_ih @ local)
  ~(b_hh @ local)
  ~(packed_ih @ local)
  ~(packed_hh @ local)
  ~(col_offsets_ih @ local)
  ~(col_offsets_hh @ local)
  ~scale_ih
  ~scale_hh
  ~zero_point_ih
  ~zero_point_hh
  =
  let input = globalize_tensor input in
  let hx = globalize_tensor hx in
  let w_ih = globalize_tensor w_ih in
  let w_hh = globalize_tensor w_hh in
  let b_ih = globalize_tensor b_ih in
  let b_hh = globalize_tensor b_hh in
  let packed_ih = globalize_tensor packed_ih in
  let packed_hh = globalize_tensor packed_hh in
  let col_offsets_ih = globalize_tensor col_offsets_ih in
  let col_offsets_hh = globalize_tensor col_offsets_hh in
  [%c.alloc
    ({|CAMLreturn(atg_quantized_rnn_tanh_cell(%{input : tensor value}, %{hx : tensor value}, %{w_ih : tensor value}, %{w_hh : tensor value}, %{b_ih : tensor value}, %{b_hh : tensor value}, %{packed_ih : tensor value}, %{packed_hh : tensor value}, %{col_offsets_ih : tensor value}, %{col_offsets_hh : tensor value}, %{scale_ih : scalar value}, %{scale_hh : scalar value}, %{zero_point_ih : scalar value}, %{zero_point_hh : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rad2deg (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_rad2deg(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rad2deg_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_rad2deg_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rad2deg_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_rad2deg_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rand ~size ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_rand(%{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rand_like (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_rand_like(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rand_like_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_rand_like_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let rand_out ~(out @ local) ~size =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_rand_out(%{out : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let randint ~high ~size ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_randint(%{high : int}, %{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let randint_like (self @ local) ~high =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_randint_like(%{self : tensor value}, %{high : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let randint_like_low_dtype (self @ local) ~low ~high =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_randint_like_low_dtype(%{self : tensor value}, %{low : int}, %{high : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let randint_like_low_dtype_out ~(out @ local) (self @ local) ~low ~high =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_randint_like_low_dtype_out(%{out : tensor value}, %{self : tensor value}, %{low : int}, %{high : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let randint_like_out ~(out @ local) (self @ local) ~high =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_randint_like_out(%{out : tensor value}, %{self : tensor value}, %{high : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let randint_low ~low ~high ~size ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_randint_low(%{low : int}, %{high : int}, %{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let randint_low_out ~(out @ local) ~low ~high ~size =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_randint_low_out(%{out : tensor value}, %{low : int}, %{high : int}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let randint_out ~(out @ local) ~high ~size =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_randint_out(%{out : tensor value}, %{high : int}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let randn ~size ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_randn(%{size : int list value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let randn_like (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_randn_like(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let randn_like_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_randn_like_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let randn_out ~(out @ local) ~size =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_randn_out(%{out : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let random (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_random(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let random_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_random_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let random_from (self @ local) ~from ~to_ =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_random_from(%{self : tensor value}, %{from : int}, %{to_ : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let random_from_ (self @ local) ~from ~to_ =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_random_from_(%{self : tensor value}, %{from : int}, %{to_ : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let random_from_out ~(out @ local) (self @ local) ~from ~to_ =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_random_from_out(%{out : tensor value}, %{self : tensor value}, %{from : int}, %{to_ : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let random_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_random_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let random_to (self @ local) ~to_ =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_random_to(%{self : tensor value}, %{to_ : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let random_to_ (self @ local) ~to_ =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_random_to_(%{self : tensor value}, %{to_ : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let random_to_out ~(out @ local) (self @ local) ~to_ =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_random_to_out(%{out : tensor value}, %{self : tensor value}, %{to_ : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let randperm ~n ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_randperm(%{n : int}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let randperm_out ~(out @ local) ~n =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_randperm_out(%{out : tensor value}, %{n : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let range ~start ~end_ ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_range(%{start : scalar value}, %{end_ : scalar value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let range_out ?step ~(out @ local) ~start ~end_ () =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_range_out(%{out : tensor value}, %{start : scalar value}, %{end_ : scalar value}, %{step : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let range_out_ ~(out @ local) ~start ~end_ =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_range_out_(%{out : tensor value}, %{start : scalar value}, %{end_ : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let range_step ?step ~start ~end_ ~options () =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_range_step(%{start : scalar value}, %{end_ : scalar value}, %{step : scalar option value}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ravel (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_ravel(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let real (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_real(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let reciprocal (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_reciprocal(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let reciprocal_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_reciprocal_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let reciprocal_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_reciprocal_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let reflection_pad1d (self @ local) ~padding =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_reflection_pad1d(%{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let reflection_pad1d_backward ~(grad_output @ local) (self @ local) ~padding =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_reflection_pad1d_backward(%{grad_output : tensor value}, %{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let reflection_pad1d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~padding
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_reflection_pad1d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let reflection_pad1d_out ~(out @ local) (self @ local) ~padding =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_reflection_pad1d_out(%{out : tensor value}, %{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let reflection_pad2d (self @ local) ~padding =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_reflection_pad2d(%{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let reflection_pad2d_backward ~(grad_output @ local) (self @ local) ~padding =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_reflection_pad2d_backward(%{grad_output : tensor value}, %{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let reflection_pad2d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~padding
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_reflection_pad2d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let reflection_pad2d_out ~(out @ local) (self @ local) ~padding =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_reflection_pad2d_out(%{out : tensor value}, %{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let reflection_pad3d (self @ local) ~padding =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_reflection_pad3d(%{self : tensor value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;
