(* THIS FILE IS AUTOMATICALLY GENERATED, DO NOT EDIT BY HAND! *)

open! Core
open C_ffi
open Torch_wrapper_types
open Torch_refcounted_bindings.Type_defs

[%%c {| #include "torch_api.h" |}]

let atleast_1d (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_atleast_1d(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let atleast_1d_sequence (tensors @ local) =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_atleast_1d_sequence(%{tensors : tensor list value}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let atleast_2d (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_atleast_2d(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let atleast_2d_sequence (tensors @ local) =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_atleast_2d_sequence(%{tensors : tensor list value}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let atleast_3d (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_atleast_3d(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let atleast_3d_sequence (tensors @ local) =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_atleast_3d_sequence(%{tensors : tensor list value}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let avg_pool1d (self @ local) ~kernel_size ~stride ~padding ~ceil_mode ~count_include_pad =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_avg_pool1d(%{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, Int_val(%{ceil_mode : bool value}), Int_val(%{count_include_pad : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let avg_pool1d_out
  ~(out @ local)
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~ceil_mode
  ~count_include_pad
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_avg_pool1d_out(%{out : tensor value}, %{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, Int_val(%{ceil_mode : bool value}), Int_val(%{count_include_pad : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let avg_pool2d
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~ceil_mode
  ~count_include_pad
  ~divisor_override
  =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_avg_pool2d(%{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, Int_val(%{ceil_mode : bool value}), Int_val(%{count_include_pad : bool value}), %{divisor_override : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let avg_pool2d_backward
  ~(grad_output @ local)
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~ceil_mode
  ~count_include_pad
  ~divisor_override
  =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_avg_pool2d_backward(%{grad_output : tensor value}, %{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, Int_val(%{ceil_mode : bool value}), Int_val(%{count_include_pad : bool value}), %{divisor_override : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let avg_pool2d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~ceil_mode
  ~count_include_pad
  ~divisor_override
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_avg_pool2d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, Int_val(%{ceil_mode : bool value}), Int_val(%{count_include_pad : bool value}), %{divisor_override : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let avg_pool2d_out
  ~(out @ local)
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~ceil_mode
  ~count_include_pad
  ~divisor_override
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_avg_pool2d_out(%{out : tensor value}, %{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, Int_val(%{ceil_mode : bool value}), Int_val(%{count_include_pad : bool value}), %{divisor_override : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let avg_pool3d
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~ceil_mode
  ~count_include_pad
  ~divisor_override
  =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_avg_pool3d(%{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, Int_val(%{ceil_mode : bool value}), Int_val(%{count_include_pad : bool value}), %{divisor_override : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let avg_pool3d_backward
  ~(grad_output @ local)
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~ceil_mode
  ~count_include_pad
  ~divisor_override
  =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_avg_pool3d_backward(%{grad_output : tensor value}, %{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, Int_val(%{ceil_mode : bool value}), Int_val(%{count_include_pad : bool value}), %{divisor_override : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let avg_pool3d_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~ceil_mode
  ~count_include_pad
  ~divisor_override
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_avg_pool3d_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, Int_val(%{ceil_mode : bool value}), Int_val(%{count_include_pad : bool value}), %{divisor_override : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let avg_pool3d_out
  ~(out @ local)
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~ceil_mode
  ~count_include_pad
  ~divisor_override
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_avg_pool3d_out(%{out : tensor value}, %{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, Int_val(%{ceil_mode : bool value}), Int_val(%{count_include_pad : bool value}), %{divisor_override : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let baddbmm ?beta ?alpha (self @ local) ~(batch1 @ local) ~(batch2 @ local) =
  let self = globalize_tensor self in
  let batch1 = globalize_tensor batch1 in
  let batch2 = globalize_tensor batch2 in
  [%c.alloc
    ({|CAMLreturn(atg_baddbmm(%{self : tensor value}, %{batch1 : tensor value}, %{batch2 : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let baddbmm_ ?beta ?alpha (self @ local) ~(batch1 @ local) ~(batch2 @ local) =
  let self = globalize_tensor self in
  let batch1 = globalize_tensor batch1 in
  let batch2 = globalize_tensor batch2 in
  [%c.alloc
    ({|CAMLreturn(atg_baddbmm_(%{self : tensor value}, %{batch1 : tensor value}, %{batch2 : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let baddbmm_out
  ?beta
  ?alpha
  ~(out @ local)
  (self @ local)
  ~(batch1 @ local)
  ~(batch2 @ local)
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let batch1 = globalize_tensor batch1 in
  let batch2 = globalize_tensor batch2 in
  [%c.alloc
    ({|CAMLreturn(atg_baddbmm_out(%{out : tensor value}, %{self : tensor value}, %{batch1 : tensor value}, %{batch2 : tensor value}, %{beta : scalar option value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bartlett_window ~window_length ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_bartlett_window(%{window_length : int}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bartlett_window_out ~(out @ local) ~window_length =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_bartlett_window_out(%{out : tensor value}, %{window_length : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bartlett_window_periodic ~window_length ~periodic ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_bartlett_window_periodic(%{window_length : int}, Int_val(%{periodic : bool value}), %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bartlett_window_periodic_out ~(out @ local) ~window_length ~periodic =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_bartlett_window_periodic_out(%{out : tensor value}, %{window_length : int}, Int_val(%{periodic : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let batch_norm
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~training
  ~momentum
  ~eps
  ~cudnn_enabled
  =
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let running_mean = [%globalize: tensor option] running_mean in
  let running_var = [%globalize: tensor option] running_var in
  [%c.alloc
    ({|CAMLreturn(atg_batch_norm(%{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, %{running_mean : tensor option value}, %{running_var : tensor option value}, Int_val(%{training : bool value}), %{momentum : float}, %{eps : float}, Int_val(%{cudnn_enabled : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let batch_norm_backward_elemt
  ~(grad_out @ local)
  (input @ local)
  ~(mean @ local)
  ~(invstd @ local)
  ~(weight @ local)
  ~(sum_dy @ local)
  ~(sum_dy_xmu @ local)
  ~(count @ local)
  =
  let grad_out = globalize_tensor grad_out in
  let input = globalize_tensor input in
  let mean = globalize_tensor mean in
  let invstd = globalize_tensor invstd in
  let weight = [%globalize: tensor option] weight in
  let sum_dy = globalize_tensor sum_dy in
  let sum_dy_xmu = globalize_tensor sum_dy_xmu in
  let count = globalize_tensor count in
  [%c.alloc
    ({|CAMLreturn(atg_batch_norm_backward_elemt(%{grad_out : tensor value}, %{input : tensor value}, %{mean : tensor value}, %{invstd : tensor value}, %{weight : tensor option value}, %{sum_dy : tensor value}, %{sum_dy_xmu : tensor value}, %{count : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let batch_norm_backward_elemt_out
  ~(out @ local)
  ~(grad_out @ local)
  (input @ local)
  ~(mean @ local)
  ~(invstd @ local)
  ~(weight @ local)
  ~(sum_dy @ local)
  ~(sum_dy_xmu @ local)
  ~(count @ local)
  =
  let out = globalize_tensor out in
  let grad_out = globalize_tensor grad_out in
  let input = globalize_tensor input in
  let mean = globalize_tensor mean in
  let invstd = globalize_tensor invstd in
  let weight = [%globalize: tensor option] weight in
  let sum_dy = globalize_tensor sum_dy in
  let sum_dy_xmu = globalize_tensor sum_dy_xmu in
  let count = globalize_tensor count in
  [%c.alloc
    ({|CAMLreturn(atg_batch_norm_backward_elemt_out(%{out : tensor value}, %{grad_out : tensor value}, %{input : tensor value}, %{mean : tensor value}, %{invstd : tensor value}, %{weight : tensor option value}, %{sum_dy : tensor value}, %{sum_dy_xmu : tensor value}, %{count : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let batch_norm_backward_reduce
  ~(grad_out @ local)
  (input @ local)
  ~(mean @ local)
  ~(invstd @ local)
  ~(weight @ local)
  ~input_g
  ~weight_g
  ~bias_g
  =
  let grad_out = globalize_tensor grad_out in
  let input = globalize_tensor input in
  let mean = globalize_tensor mean in
  let invstd = globalize_tensor invstd in
  let weight = [%globalize: tensor option] weight in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg_batch_norm_backward_reduce(%{grad_out : tensor value}, %{input : tensor value}, %{mean : tensor value}, %{invstd : tensor value}, %{weight : tensor option value}, Int_val(%{input_g : bool value}), Int_val(%{weight_g : bool value}), Int_val(%{bias_g : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let batch_norm_backward_reduce_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  ~(out3 @ local)
  ~(grad_out @ local)
  (input @ local)
  ~(mean @ local)
  ~(invstd @ local)
  ~(weight @ local)
  ~input_g
  ~weight_g
  ~bias_g
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let out3 = globalize_tensor out3 in
  let grad_out = globalize_tensor grad_out in
  let input = globalize_tensor input in
  let mean = globalize_tensor mean in
  let invstd = globalize_tensor invstd in
  let weight = [%globalize: tensor option] weight in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg_batch_norm_backward_reduce_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{out3 : tensor value}, %{grad_out : tensor value}, %{input : tensor value}, %{mean : tensor value}, %{invstd : tensor value}, %{weight : tensor option value}, Int_val(%{input_g : bool value}), Int_val(%{weight_g : bool value}), Int_val(%{bias_g : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let batch_norm_elemt
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(mean @ local)
  ~(invstd @ local)
  ~eps
  =
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let mean = globalize_tensor mean in
  let invstd = globalize_tensor invstd in
  [%c.alloc
    ({|CAMLreturn(atg_batch_norm_elemt(%{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, %{mean : tensor value}, %{invstd : tensor value}, %{eps : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let batch_norm_elemt_out
  ~(out @ local)
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(mean @ local)
  ~(invstd @ local)
  ~eps
  =
  let out = globalize_tensor out in
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let mean = globalize_tensor mean in
  let invstd = globalize_tensor invstd in
  [%c.alloc
    ({|CAMLreturn(atg_batch_norm_elemt_out(%{out : tensor value}, %{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, %{mean : tensor value}, %{invstd : tensor value}, %{eps : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let batch_norm_gather_stats
  (input @ local)
  ~(mean @ local)
  ~(invstd @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~momentum
  ~eps
  ~count
  =
  let input = globalize_tensor input in
  let mean = globalize_tensor mean in
  let invstd = globalize_tensor invstd in
  let running_mean = [%globalize: tensor option] running_mean in
  let running_var = [%globalize: tensor option] running_var in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_batch_norm_gather_stats(%{input : tensor value}, %{mean : tensor value}, %{invstd : tensor value}, %{running_mean : tensor option value}, %{running_var : tensor option value}, %{momentum : float}, %{eps : float}, %{count : int}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let batch_norm_gather_stats_out
  ~(out0 @ local)
  ~(out1 @ local)
  (input @ local)
  ~(mean @ local)
  ~(invstd @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~momentum
  ~eps
  ~count
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let input = globalize_tensor input in
  let mean = globalize_tensor mean in
  let invstd = globalize_tensor invstd in
  let running_mean = [%globalize: tensor option] running_mean in
  let running_var = [%globalize: tensor option] running_var in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_batch_norm_gather_stats_out(%{out0 : tensor value}, %{out1 : tensor value}, %{input : tensor value}, %{mean : tensor value}, %{invstd : tensor value}, %{running_mean : tensor option value}, %{running_var : tensor option value}, %{momentum : float}, %{eps : float}, %{count : int}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let batch_norm_gather_stats_with_counts
  (input @ local)
  ~(mean @ local)
  ~(invstd @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~momentum
  ~eps
  ~(counts @ local)
  =
  let input = globalize_tensor input in
  let mean = globalize_tensor mean in
  let invstd = globalize_tensor invstd in
  let running_mean = [%globalize: tensor option] running_mean in
  let running_var = [%globalize: tensor option] running_var in
  let counts = globalize_tensor counts in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_batch_norm_gather_stats_with_counts(%{input : tensor value}, %{mean : tensor value}, %{invstd : tensor value}, %{running_mean : tensor option value}, %{running_var : tensor option value}, %{momentum : float}, %{eps : float}, %{counts : tensor value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let batch_norm_gather_stats_with_counts_out
  ~(out0 @ local)
  ~(out1 @ local)
  (input @ local)
  ~(mean @ local)
  ~(invstd @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~momentum
  ~eps
  ~(counts @ local)
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let input = globalize_tensor input in
  let mean = globalize_tensor mean in
  let invstd = globalize_tensor invstd in
  let running_mean = [%globalize: tensor option] running_mean in
  let running_var = [%globalize: tensor option] running_var in
  let counts = globalize_tensor counts in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_batch_norm_gather_stats_with_counts_out(%{out0 : tensor value}, %{out1 : tensor value}, %{input : tensor value}, %{mean : tensor value}, %{invstd : tensor value}, %{running_mean : tensor option value}, %{running_var : tensor option value}, %{momentum : float}, %{eps : float}, %{counts : tensor value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let batch_norm_stats (input @ local) ~eps =
  let input = globalize_tensor input in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_batch_norm_stats(%{input : tensor value}, %{eps : float}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let batch_norm_stats_out ~(out0 @ local) ~(out1 @ local) (input @ local) ~eps =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let input = globalize_tensor input in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_batch_norm_stats_out(%{out0 : tensor value}, %{out1 : tensor value}, %{input : tensor value}, %{eps : float}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let batch_norm_update_stats
  (input @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~momentum
  =
  let input = globalize_tensor input in
  let running_mean = [%globalize: tensor option] running_mean in
  let running_var = [%globalize: tensor option] running_var in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_batch_norm_update_stats(%{input : tensor value}, %{running_mean : tensor option value}, %{running_var : tensor option value}, %{momentum : float}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let batch_norm_update_stats_out
  ~(out0 @ local)
  ~(out1 @ local)
  (input @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~momentum
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let input = globalize_tensor input in
  let running_mean = [%globalize: tensor option] running_mean in
  let running_var = [%globalize: tensor option] running_var in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_batch_norm_update_stats_out(%{out0 : tensor value}, %{out1 : tensor value}, %{input : tensor value}, %{running_mean : tensor option value}, %{running_var : tensor option value}, %{momentum : float}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let bernoulli (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_bernoulli(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bernoulli_ (self @ local) ~(p @ local) =
  let self = globalize_tensor self in
  let p = globalize_tensor p in
  [%c.alloc
    ({|CAMLreturn(atg_bernoulli_(%{self : tensor value}, %{p : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bernoulli_float_ (self @ local) ~p =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_bernoulli_float_(%{self : tensor value}, %{p : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bernoulli_p (self @ local) ~p =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_bernoulli_p(%{self : tensor value}, %{p : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bernoulli_tensor (self @ local) ~(p @ local) =
  let self = globalize_tensor self in
  let p = globalize_tensor p in
  [%c.alloc
    ({|CAMLreturn(atg_bernoulli_tensor(%{self : tensor value}, %{p : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bilinear ~(input1 @ local) ~(input2 @ local) ~(weight @ local) ~(bias @ local) =
  let input1 = globalize_tensor input1 in
  let input2 = globalize_tensor input2 in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_bilinear(%{input1 : tensor value}, %{input2 : tensor value}, %{weight : tensor value}, %{bias : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let binary_cross_entropy (self @ local) ~(target @ local) ~(weight @ local) ~reduction =
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let weight = [%globalize: tensor option] weight in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_binary_cross_entropy(%{self : tensor value}, %{target : tensor value}, %{weight : tensor option value}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let binary_cross_entropy_backward
  ~(grad_output @ local)
  (self @ local)
  ~(target @ local)
  ~(weight @ local)
  ~reduction
  =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let weight = [%globalize: tensor option] weight in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_binary_cross_entropy_backward(%{grad_output : tensor value}, %{self : tensor value}, %{target : tensor value}, %{weight : tensor option value}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let binary_cross_entropy_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~(target @ local)
  ~(weight @ local)
  ~reduction
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let weight = [%globalize: tensor option] weight in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_binary_cross_entropy_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{target : tensor value}, %{weight : tensor option value}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let binary_cross_entropy_out
  ~(out @ local)
  (self @ local)
  ~(target @ local)
  ~(weight @ local)
  ~reduction
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let weight = [%globalize: tensor option] weight in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_binary_cross_entropy_out(%{out : tensor value}, %{self : tensor value}, %{target : tensor value}, %{weight : tensor option value}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let binary_cross_entropy_with_logits
  (self @ local)
  ~(target @ local)
  ~(weight @ local)
  ~(pos_weight @ local)
  ~reduction
  =
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let weight = [%globalize: tensor option] weight in
  let pos_weight = [%globalize: tensor option] pos_weight in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_binary_cross_entropy_with_logits(%{self : tensor value}, %{target : tensor value}, %{weight : tensor option value}, %{pos_weight : tensor option value}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let binary_cross_entropy_with_logits_out
  ~(out @ local)
  (self @ local)
  ~(target @ local)
  ~(weight @ local)
  ~(pos_weight @ local)
  ~reduction
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let weight = [%globalize: tensor option] weight in
  let pos_weight = [%globalize: tensor option] pos_weight in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_binary_cross_entropy_with_logits_out(%{out : tensor value}, %{self : tensor value}, %{target : tensor value}, %{weight : tensor option value}, %{pos_weight : tensor option value}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bincount (self @ local) ~(weights @ local) ~minlength =
  let self = globalize_tensor self in
  let weights = [%globalize: tensor option] weights in
  [%c.alloc
    ({|CAMLreturn(atg_bincount(%{self : tensor value}, %{weights : tensor option value}, %{minlength : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bincount_out ~(out @ local) (self @ local) ~(weights @ local) ~minlength =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weights = [%globalize: tensor option] weights in
  [%c.alloc
    ({|CAMLreturn(atg_bincount_out(%{out : tensor value}, %{self : tensor value}, %{weights : tensor option value}, %{minlength : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let binomial ~(count @ local) ~(prob @ local) =
  let count = globalize_tensor count in
  let prob = globalize_tensor prob in
  [%c.alloc
    ({|CAMLreturn(atg_binomial(%{count : tensor value}, %{prob : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let binomial_out ~(out @ local) ~(count @ local) ~(prob @ local) =
  let out = globalize_tensor out in
  let count = globalize_tensor count in
  let prob = globalize_tensor prob in
  [%c.alloc
    ({|CAMLreturn(atg_binomial_out(%{out : tensor value}, %{count : tensor value}, %{prob : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_and (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_and(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_and_ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_and_(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_and_scalar_out ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_and_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_and_scalar_tensor self (other @ local) =
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_and_scalar_tensor(%{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_and_scalar_tensor_out ~(out @ local) self (other @ local) =
  let out = globalize_tensor out in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_and_scalar_tensor_out(%{out : tensor value}, %{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_and_tensor (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_and_tensor(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_and_tensor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_and_tensor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_and_tensor_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_and_tensor_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_left_shift (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_left_shift(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_left_shift_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_left_shift_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_left_shift_scalar_tensor self (other @ local) =
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_left_shift_scalar_tensor(%{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_left_shift_scalar_tensor_out ~(out @ local) self (other @ local) =
  let out = globalize_tensor out in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_left_shift_scalar_tensor_out(%{out : tensor value}, %{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_left_shift_tensor_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_left_shift_tensor_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_left_shift_tensor_scalar (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_left_shift_tensor_scalar(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_left_shift_tensor_scalar_ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_left_shift_tensor_scalar_(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_left_shift_tensor_scalar_out ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_left_shift_tensor_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_not (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_bitwise_not(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_not_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_not_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_not_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_not_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_or (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_or(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_or_ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_or_(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_or_scalar_out ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_or_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_or_scalar_tensor self (other @ local) =
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_or_scalar_tensor(%{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_or_scalar_tensor_out ~(out @ local) self (other @ local) =
  let out = globalize_tensor out in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_or_scalar_tensor_out(%{out : tensor value}, %{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_or_tensor (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_or_tensor(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_or_tensor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_or_tensor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_or_tensor_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_or_tensor_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_right_shift (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_right_shift(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_right_shift_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_right_shift_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_right_shift_scalar_tensor self (other @ local) =
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_right_shift_scalar_tensor(%{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_right_shift_scalar_tensor_out ~(out @ local) self (other @ local) =
  let out = globalize_tensor out in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_right_shift_scalar_tensor_out(%{out : tensor value}, %{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_right_shift_tensor_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_right_shift_tensor_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_right_shift_tensor_scalar (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_right_shift_tensor_scalar(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_right_shift_tensor_scalar_ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_right_shift_tensor_scalar_(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_right_shift_tensor_scalar_out ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_right_shift_tensor_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_xor (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_xor(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_xor_ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_xor_(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_xor_scalar_out ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_xor_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_xor_scalar_tensor self (other @ local) =
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_xor_scalar_tensor(%{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_xor_scalar_tensor_out ~(out @ local) self (other @ local) =
  let out = globalize_tensor out in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_xor_scalar_tensor_out(%{out : tensor value}, %{self : scalar value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_xor_tensor (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_xor_tensor(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_xor_tensor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_xor_tensor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bitwise_xor_tensor_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_bitwise_xor_tensor_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let blackman_window ~window_length ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_blackman_window(%{window_length : int}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let blackman_window_out ~(out @ local) ~window_length =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_blackman_window_out(%{out : tensor value}, %{window_length : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let blackman_window_periodic ~window_length ~periodic ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_blackman_window_periodic(%{window_length : int}, Int_val(%{periodic : bool value}), %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let blackman_window_periodic_out ~(out @ local) ~window_length ~periodic =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_blackman_window_periodic_out(%{out : tensor value}, %{window_length : int}, Int_val(%{periodic : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let block_diag (tensors @ local) =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_block_diag(%{tensors : tensor list value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let block_diag_out ~(out @ local) (tensors @ local) =
  let out = globalize_tensor out in
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_block_diag_out(%{out : tensor value}, %{tensors : tensor list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bmm (self @ local) ~(mat2 @ local) =
  let self = globalize_tensor self in
  let mat2 = globalize_tensor mat2 in
  [%c.alloc
    ({|CAMLreturn(atg_bmm(%{self : tensor value}, %{mat2 : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bmm_out ~(out @ local) (self @ local) ~(mat2 @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let mat2 = globalize_tensor mat2 in
  [%c.alloc
    ({|CAMLreturn(atg_bmm_out(%{out : tensor value}, %{self : tensor value}, %{mat2 : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let broadcast_tensors (tensors @ local) =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_broadcast_tensors(%{tensors : tensor list value}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let broadcast_to (self @ local) ~size =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_broadcast_to(%{self : tensor value}, %{size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bucketize (self @ local) ~(boundaries @ local) ~out_int32 ~right =
  let self = globalize_tensor self in
  let boundaries = globalize_tensor boundaries in
  [%c.alloc
    ({|CAMLreturn(atg_bucketize(%{self : tensor value}, %{boundaries : tensor value}, Int_val(%{out_int32 : bool value}), Int_val(%{right : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bucketize_scalar self ~(boundaries @ local) ~out_int32 ~right =
  let boundaries = globalize_tensor boundaries in
  [%c.alloc
    ({|CAMLreturn(atg_bucketize_scalar(%{self : scalar value}, %{boundaries : tensor value}, Int_val(%{out_int32 : bool value}), Int_val(%{right : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bucketize_scalar_out ~(out @ local) self ~(boundaries @ local) ~out_int32 ~right =
  let out = globalize_tensor out in
  let boundaries = globalize_tensor boundaries in
  [%c.alloc
    ({|CAMLreturn(atg_bucketize_scalar_out(%{out : tensor value}, %{self : scalar value}, %{boundaries : tensor value}, Int_val(%{out_int32 : bool value}), Int_val(%{right : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let bucketize_tensor_out
  ~(out @ local)
  (self @ local)
  ~(boundaries @ local)
  ~out_int32
  ~right
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let boundaries = globalize_tensor boundaries in
  [%c.alloc
    ({|CAMLreturn(atg_bucketize_tensor_out(%{out : tensor value}, %{self : tensor value}, %{boundaries : tensor value}, Int_val(%{out_int32 : bool value}), Int_val(%{right : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let can_cast ~from_ ~to_ =
  let from_ = Kind.packed_to_int from_ in
  let to_ = Kind.packed_to_int to_ in
  [%c.alloc ({|CAMLreturn(atg_can_cast(%{from_ : int}, %{to_ : int}));|} : bool value)]
;;

let cartesian_prod (tensors @ local) =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_cartesian_prod(%{tensors : tensor list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cat (tensors @ local) ~dim =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_cat(%{tensors : tensor list value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cat_out ~(out @ local) (tensors @ local) ~dim =
  let out = globalize_tensor out in
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_cat_out(%{out : tensor value}, %{tensors : tensor list value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cauchy (self @ local) ~median ~sigma =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_cauchy(%{self : tensor value}, %{median : float}, %{sigma : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cauchy_ (self @ local) ~median ~sigma =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_cauchy_(%{self : tensor value}, %{median : float}, %{sigma : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cauchy_out ~(out @ local) (self @ local) ~median ~sigma =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_cauchy_out(%{out : tensor value}, %{self : tensor value}, %{median : float}, %{sigma : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ccol_indices (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_ccol_indices(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ccol_indices_copy (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_ccol_indices_copy(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ccol_indices_copy_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_ccol_indices_copy_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cdist ~(x1 @ local) ~(x2 @ local) ~p ~compute_mode =
  let x1 = globalize_tensor x1 in
  let x2 = globalize_tensor x2 in
  [%c.alloc
    ({|CAMLreturn(atg_cdist(%{x1 : tensor value}, %{x2 : tensor value}, %{p : float}, %{compute_mode : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ceil (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_ceil(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ceil_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_ceil_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ceil_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_ceil_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let celu ?alpha (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_celu(%{self : tensor value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let celu_ ?alpha (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_celu_(%{self : tensor value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let celu_out ?alpha ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_celu_out(%{out : tensor value}, %{self : tensor value}, %{alpha : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let chain_matmul ~(matrices @ local) =
  let matrices =
    Torch_local_iterators.List.map_local_input matrices ~f:globalize_tensor
  in
  [%c.alloc
    ({|CAMLreturn(atg_chain_matmul(%{matrices : tensor list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let chain_matmul_out ~(out @ local) ~(matrices @ local) =
  let out = globalize_tensor out in
  let matrices =
    Torch_local_iterators.List.map_local_input matrices ~f:globalize_tensor
  in
  [%c.alloc
    ({|CAMLreturn(atg_chain_matmul_out(%{out : tensor value}, %{matrices : tensor list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let chalf (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_chalf(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let channel_shuffle (self @ local) ~groups =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_channel_shuffle(%{self : tensor value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let channel_shuffle_out ~(out @ local) (self @ local) ~groups =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_channel_shuffle_out(%{out : tensor value}, %{self : tensor value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cholesky (self @ local) ~upper =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_cholesky(%{self : tensor value}, Int_val(%{upper : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cholesky_inverse (self @ local) ~upper =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_cholesky_inverse(%{self : tensor value}, Int_val(%{upper : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cholesky_inverse_out ~(out @ local) (self @ local) ~upper =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_cholesky_inverse_out(%{out : tensor value}, %{self : tensor value}, Int_val(%{upper : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cholesky_out ~(out @ local) (self @ local) ~upper =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_cholesky_out(%{out : tensor value}, %{self : tensor value}, Int_val(%{upper : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cholesky_solve (self @ local) ~(input2 @ local) ~upper =
  let self = globalize_tensor self in
  let input2 = globalize_tensor input2 in
  [%c.alloc
    ({|CAMLreturn(atg_cholesky_solve(%{self : tensor value}, %{input2 : tensor value}, Int_val(%{upper : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cholesky_solve_out ~(out @ local) (self @ local) ~(input2 @ local) ~upper =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let input2 = globalize_tensor input2 in
  [%c.alloc
    ({|CAMLreturn(atg_cholesky_solve_out(%{out : tensor value}, %{self : tensor value}, %{input2 : tensor value}, Int_val(%{upper : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let choose_qparams_optimized (input @ local) ~numel ~n_bins ~ratio ~bit_width =
  let input = globalize_tensor input in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_choose_qparams_optimized(%{input : tensor value}, %{numel : int}, %{n_bins : int}, %{ratio : float}, %{bit_width : int}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let chunk (self @ local) ~chunks ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_chunk(%{self : tensor value}, %{chunks : int}, %{dim : int}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let clamp (self @ local) ~min ~max =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_clamp(%{self : tensor value}, %{min : scalar value}, %{max : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clamp_ (self @ local) ~min ~max =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_clamp_(%{self : tensor value}, %{min : scalar value}, %{max : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clamp_max (self @ local) ~max =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_clamp_max(%{self : tensor value}, %{max : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clamp_max_ (self @ local) ~max =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_clamp_max_(%{self : tensor value}, %{max : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clamp_max_out ~(out @ local) (self @ local) ~max =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_clamp_max_out(%{out : tensor value}, %{self : tensor value}, %{max : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clamp_max_tensor (self @ local) ~(max @ local) =
  let self = globalize_tensor self in
  let max = globalize_tensor max in
  [%c.alloc
    ({|CAMLreturn(atg_clamp_max_tensor(%{self : tensor value}, %{max : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clamp_max_tensor_ (self @ local) ~(max @ local) =
  let self = globalize_tensor self in
  let max = globalize_tensor max in
  [%c.alloc
    ({|CAMLreturn(atg_clamp_max_tensor_(%{self : tensor value}, %{max : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clamp_max_tensor_out ~(out @ local) (self @ local) ~(max @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let max = globalize_tensor max in
  [%c.alloc
    ({|CAMLreturn(atg_clamp_max_tensor_out(%{out : tensor value}, %{self : tensor value}, %{max : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clamp_min (self @ local) ~min =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_clamp_min(%{self : tensor value}, %{min : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clamp_min_ (self @ local) ~min =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_clamp_min_(%{self : tensor value}, %{min : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clamp_min_out ~(out @ local) (self @ local) ~min =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_clamp_min_out(%{out : tensor value}, %{self : tensor value}, %{min : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clamp_min_tensor (self @ local) ~(min @ local) =
  let self = globalize_tensor self in
  let min = globalize_tensor min in
  [%c.alloc
    ({|CAMLreturn(atg_clamp_min_tensor(%{self : tensor value}, %{min : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clamp_min_tensor_ (self @ local) ~(min @ local) =
  let self = globalize_tensor self in
  let min = globalize_tensor min in
  [%c.alloc
    ({|CAMLreturn(atg_clamp_min_tensor_(%{self : tensor value}, %{min : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clamp_min_tensor_out ~(out @ local) (self @ local) ~(min @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let min = globalize_tensor min in
  [%c.alloc
    ({|CAMLreturn(atg_clamp_min_tensor_out(%{out : tensor value}, %{self : tensor value}, %{min : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clamp_out ~(out @ local) (self @ local) ~min ~max =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_clamp_out(%{out : tensor value}, %{self : tensor value}, %{min : scalar value}, %{max : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clamp_tensor (self @ local) ~(min @ local) ~(max @ local) =
  let self = globalize_tensor self in
  let min = [%globalize: tensor option] min in
  let max = [%globalize: tensor option] max in
  [%c.alloc
    ({|CAMLreturn(atg_clamp_tensor(%{self : tensor value}, %{min : tensor option value}, %{max : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clamp_tensor_ (self @ local) ~(min @ local) ~(max @ local) =
  let self = globalize_tensor self in
  let min = [%globalize: tensor option] min in
  let max = [%globalize: tensor option] max in
  [%c.alloc
    ({|CAMLreturn(atg_clamp_tensor_(%{self : tensor value}, %{min : tensor option value}, %{max : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clamp_tensor_out ~(out @ local) (self @ local) ~(min @ local) ~(max @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let min = [%globalize: tensor option] min in
  let max = [%globalize: tensor option] max in
  [%c.alloc
    ({|CAMLreturn(atg_clamp_tensor_out(%{out : tensor value}, %{self : tensor value}, %{min : tensor option value}, %{max : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clip (self @ local) ~min ~max =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_clip(%{self : tensor value}, %{min : scalar value}, %{max : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clip_ (self @ local) ~min ~max =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_clip_(%{self : tensor value}, %{min : scalar value}, %{max : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clip_out ~(out @ local) (self @ local) ~min ~max =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_clip_out(%{out : tensor value}, %{self : tensor value}, %{min : scalar value}, %{max : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clip_tensor (self @ local) ~(min @ local) ~(max @ local) =
  let self = globalize_tensor self in
  let min = [%globalize: tensor option] min in
  let max = [%globalize: tensor option] max in
  [%c.alloc
    ({|CAMLreturn(atg_clip_tensor(%{self : tensor value}, %{min : tensor option value}, %{max : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clip_tensor_ (self @ local) ~(min @ local) ~(max @ local) =
  let self = globalize_tensor self in
  let min = [%globalize: tensor option] min in
  let max = [%globalize: tensor option] max in
  [%c.alloc
    ({|CAMLreturn(atg_clip_tensor_(%{self : tensor value}, %{min : tensor option value}, %{max : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clip_tensor_out ~(out @ local) (self @ local) ~(min @ local) ~(max @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let min = [%globalize: tensor option] min in
  let max = [%globalize: tensor option] max in
  [%c.alloc
    ({|CAMLreturn(atg_clip_tensor_out(%{out : tensor value}, %{self : tensor value}, %{min : tensor option value}, %{max : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clone (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_clone(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let clone_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_clone_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let coalesce (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_coalesce(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let col2im (self @ local) ~output_size ~kernel_size ~dilation ~padding ~stride =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_col2im(%{self : tensor value}, %{output_size : int list value}, %{kernel_size : int list value}, %{dilation : int list value}, %{padding : int list value}, %{stride : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let col2im_out
  ~(out @ local)
  (self @ local)
  ~output_size
  ~kernel_size
  ~dilation
  ~padding
  ~stride
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_col2im_out(%{out : tensor value}, %{self : tensor value}, %{output_size : int list value}, %{kernel_size : int list value}, %{dilation : int list value}, %{padding : int list value}, %{stride : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let col_indices (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_col_indices(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let col_indices_copy (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_col_indices_copy(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let col_indices_copy_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_col_indices_copy_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let column_stack (tensors @ local) =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_column_stack(%{tensors : tensor list value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let column_stack_out ~(out @ local) (tensors @ local) =
  let out = globalize_tensor out in
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_column_stack_out(%{out : tensor value}, %{tensors : tensor list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let combinations (self @ local) ~r ~with_replacement =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_combinations(%{self : tensor value}, %{r : int}, Int_val(%{with_replacement : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let complex ~(real @ local) ~(imag @ local) =
  let real = globalize_tensor real in
  let imag = globalize_tensor imag in
  [%c.alloc
    ({|CAMLreturn(atg_complex(%{real : tensor value}, %{imag : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let complex_out ~(out @ local) ~(real @ local) ~(imag @ local) =
  let out = globalize_tensor out in
  let real = globalize_tensor real in
  let imag = globalize_tensor imag in
  [%c.alloc
    ({|CAMLreturn(atg_complex_out(%{out : tensor value}, %{real : tensor value}, %{imag : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let concat (tensors @ local) ~dim =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_concat(%{tensors : tensor list value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let concat_out ~(out @ local) (tensors @ local) ~dim =
  let out = globalize_tensor out in
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_concat_out(%{out : tensor value}, %{tensors : tensor list value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let concatenate (tensors @ local) ~dim =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_concatenate(%{tensors : tensor list value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let concatenate_out ~(out @ local) (tensors @ local) ~dim =
  let out = globalize_tensor out in
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_concatenate_out(%{out : tensor value}, %{tensors : tensor list value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let conj (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_conj(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let conj_physical (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_conj_physical(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let conj_physical_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_conj_physical_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let conj_physical_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_conj_physical_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let constant_pad_nd ?value (self @ local) ~pad =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_constant_pad_nd(%{self : tensor value}, %{pad : int list value}, %{value : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let constant_pad_nd_out ?value ~(out @ local) (self @ local) ~pad =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_constant_pad_nd_out(%{out : tensor value}, %{self : tensor value}, %{pad : int list value}, %{value : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let contiguous (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_contiguous(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let conv1d
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  ~groups
  =
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_conv1d(%{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let conv1d_padding
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  ~groups
  =
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_conv1d_padding(%{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{stride : int list value}, String_val(%{padding : string value}), %{dilation : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let conv2d
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  ~groups
  =
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_conv2d(%{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let conv2d_padding
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  ~groups
  =
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_conv2d_padding(%{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{stride : int list value}, String_val(%{padding : string value}), %{dilation : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let conv3d
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  ~groups
  =
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_conv3d(%{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let conv3d_padding
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  ~groups
  =
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_conv3d_padding(%{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{stride : int list value}, String_val(%{padding : string value}), %{dilation : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let conv_depthwise3d
  (self @ local)
  ~(weight @ local)
  ~kernel_size
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_conv_depthwise3d(%{self : tensor value}, %{weight : tensor value}, %{kernel_size : int list value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let conv_depthwise3d_out
  ~(out @ local)
  (self @ local)
  ~(weight @ local)
  ~kernel_size
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_conv_depthwise3d_out(%{out : tensor value}, %{self : tensor value}, %{weight : tensor value}, %{kernel_size : int list value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let conv_tbc (self @ local) ~(weight @ local) ~(bias @ local) ~pad =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = globalize_tensor bias in
  [%c.alloc
    ({|CAMLreturn(atg_conv_tbc(%{self : tensor value}, %{weight : tensor value}, %{bias : tensor value}, %{pad : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let conv_tbc_backward
  (self @ local)
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~pad
  =
  let self = globalize_tensor self in
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = globalize_tensor bias in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_conv_tbc_backward(%{self : tensor value}, %{input : tensor value}, %{weight : tensor value}, %{bias : tensor value}, %{pad : int}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let conv_tbc_out ~(out @ local) (self @ local) ~(weight @ local) ~(bias @ local) ~pad =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = globalize_tensor bias in
  [%c.alloc
    ({|CAMLreturn(atg_conv_tbc_out(%{out : tensor value}, %{self : tensor value}, %{weight : tensor value}, %{bias : tensor value}, %{pad : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let conv_transpose1d
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~stride
  ~padding
  ~output_padding
  ~groups
  ~dilation
  =
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_conv_transpose1d(%{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{output_padding : int list value}, %{groups : int}, %{dilation : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let conv_transpose2d
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~stride
  ~padding
  ~output_padding
  ~groups
  ~dilation
  =
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_conv_transpose2d(%{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{output_padding : int list value}, %{groups : int}, %{dilation : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let conv_transpose3d
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~stride
  ~padding
  ~output_padding
  ~groups
  ~dilation
  =
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_conv_transpose3d(%{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{output_padding : int list value}, %{groups : int}, %{dilation : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let convolution
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  ~transposed
  ~output_padding
  ~groups
  =
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_convolution(%{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{transposed : bool value}), %{output_padding : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let convolution_out
  ~(out @ local)
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  ~transposed
  ~output_padding
  ~groups
  =
  let out = globalize_tensor out in
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_convolution_out(%{out : tensor value}, %{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{transposed : bool value}), %{output_padding : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let convolution_overrideable
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  ~transposed
  ~output_padding
  ~groups
  =
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_convolution_overrideable(%{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{transposed : bool value}), %{output_padding : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let convolution_overrideable_out
  ~(out @ local)
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  ~transposed
  ~output_padding
  ~groups
  =
  let out = globalize_tensor out in
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_convolution_overrideable_out(%{out : tensor value}, %{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{transposed : bool value}), %{output_padding : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let copy (self @ local) ~(src @ local) ~non_blocking =
  let self = globalize_tensor self in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_copy(%{self : tensor value}, %{src : tensor value}, Int_val(%{non_blocking : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let copy_out ~(out @ local) (self @ local) ~(src @ local) ~non_blocking =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_copy_out(%{out : tensor value}, %{self : tensor value}, %{src : tensor value}, Int_val(%{non_blocking : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let copy_sparse_to_sparse (self @ local) ~(src @ local) ~non_blocking =
  let self = globalize_tensor self in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_copy_sparse_to_sparse(%{self : tensor value}, %{src : tensor value}, Int_val(%{non_blocking : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let copy_sparse_to_sparse_ (self @ local) ~(src @ local) ~non_blocking =
  let self = globalize_tensor self in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_copy_sparse_to_sparse_(%{self : tensor value}, %{src : tensor value}, Int_val(%{non_blocking : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let copy_sparse_to_sparse_out ~(out @ local) (self @ local) ~(src @ local) ~non_blocking =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_copy_sparse_to_sparse_out(%{out : tensor value}, %{self : tensor value}, %{src : tensor value}, Int_val(%{non_blocking : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let copysign (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_copysign(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let copysign_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_copysign_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let copysign_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_copysign_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let copysign_scalar (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_copysign_scalar(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let copysign_scalar_ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_copysign_scalar_(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let copysign_scalar_out ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_copysign_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let corrcoef (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_corrcoef(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cos (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_cos(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cos_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_cos_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cos_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_cos_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cosh (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_cosh(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cosh_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_cosh_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cosh_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_cosh_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cosine_embedding_loss
  ~(input1 @ local)
  ~(input2 @ local)
  ~(target @ local)
  ~margin
  ~reduction
  =
  let input1 = globalize_tensor input1 in
  let input2 = globalize_tensor input2 in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_cosine_embedding_loss(%{input1 : tensor value}, %{input2 : tensor value}, %{target : tensor value}, %{margin : float}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cosine_similarity ~(x1 @ local) ~(x2 @ local) ~dim ~eps =
  let x1 = globalize_tensor x1 in
  let x2 = globalize_tensor x2 in
  [%c.alloc
    ({|CAMLreturn(atg_cosine_similarity(%{x1 : tensor value}, %{x2 : tensor value}, %{dim : int}, %{eps : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let count_nonzero ~(out @ local) (self @ local) ~dim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_count_nonzero(%{out : tensor value}, %{self : tensor value}, %{dim : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let count_nonzero_out ~(out @ local) (self @ local) ~dim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_count_nonzero_out(%{out : tensor value}, %{self : tensor value}, %{dim : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cov (self @ local) ~correction ~(fweights @ local) ~(aweights @ local) =
  let self = globalize_tensor self in
  let fweights = [%globalize: tensor option] fweights in
  let aweights = [%globalize: tensor option] aweights in
  [%c.alloc
    ({|CAMLreturn(atg_cov(%{self : tensor value}, %{correction : int}, %{fweights : tensor option value}, %{aweights : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cross (self @ local) (other @ local) ~dim =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_cross(%{self : tensor value}, %{other : tensor value}, %{dim : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cross_entropy_loss
  (self @ local)
  ~(target @ local)
  ~(weight @ local)
  ~reduction
  ~ignore_index
  ~label_smoothing
  =
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let weight = [%globalize: tensor option] weight in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_cross_entropy_loss(%{self : tensor value}, %{target : tensor value}, %{weight : tensor option value}, %{reduction : int}, %{ignore_index : int}, %{label_smoothing : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cross_out ~(out @ local) (self @ local) (other @ local) ~dim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_cross_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}, %{dim : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let crow_indices (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_crow_indices(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let crow_indices_copy (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_crow_indices_copy(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let crow_indices_copy_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_crow_indices_copy_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ctc_loss
  ~(log_probs @ local)
  ~(targets @ local)
  ~input_lengths
  ~target_lengths
  ~blank
  ~reduction
  ~zero_infinity
  =
  let log_probs = globalize_tensor log_probs in
  let targets = globalize_tensor targets in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_ctc_loss(%{log_probs : tensor value}, %{targets : tensor value}, %{input_lengths : int list value}, %{target_lengths : int list value}, %{blank : int}, %{reduction : int}, Int_val(%{zero_infinity : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ctc_loss_tensor
  ~(log_probs @ local)
  ~(targets @ local)
  ~(input_lengths @ local)
  ~(target_lengths @ local)
  ~blank
  ~reduction
  ~zero_infinity
  =
  let log_probs = globalize_tensor log_probs in
  let targets = globalize_tensor targets in
  let input_lengths = globalize_tensor input_lengths in
  let target_lengths = globalize_tensor target_lengths in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_ctc_loss_tensor(%{log_probs : tensor value}, %{targets : tensor value}, %{input_lengths : tensor value}, %{target_lengths : tensor value}, %{blank : int}, %{reduction : int}, Int_val(%{zero_infinity : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cudnn_affine_grid_generator ~(theta @ local) ~n ~c ~h ~w =
  let theta = globalize_tensor theta in
  [%c.alloc
    ({|CAMLreturn(atg_cudnn_affine_grid_generator(%{theta : tensor value}, %{n : int}, %{c : int}, %{h : int}, %{w : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cudnn_affine_grid_generator_backward ~(grad @ local) ~n ~c ~h ~w =
  let grad = globalize_tensor grad in
  [%c.alloc
    ({|CAMLreturn(atg_cudnn_affine_grid_generator_backward(%{grad : tensor value}, %{n : int}, %{c : int}, %{h : int}, %{w : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cudnn_affine_grid_generator_backward_out ~(out @ local) ~(grad @ local) ~n ~c ~h ~w =
  let out = globalize_tensor out in
  let grad = globalize_tensor grad in
  [%c.alloc
    ({|CAMLreturn(atg_cudnn_affine_grid_generator_backward_out(%{out : tensor value}, %{grad : tensor value}, %{n : int}, %{c : int}, %{h : int}, %{w : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cudnn_affine_grid_generator_out ~(out @ local) ~(theta @ local) ~n ~c ~h ~w =
  let out = globalize_tensor out in
  let theta = globalize_tensor theta in
  [%c.alloc
    ({|CAMLreturn(atg_cudnn_affine_grid_generator_out(%{out : tensor value}, %{theta : tensor value}, %{n : int}, %{c : int}, %{h : int}, %{w : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cudnn_batch_norm
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~training
  ~exponential_average_factor
  ~epsilon
  =
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  let running_mean = [%globalize: tensor option] running_mean in
  let running_var = [%globalize: tensor option] running_var in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg_cudnn_batch_norm(%{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{running_mean : tensor option value}, %{running_var : tensor option value}, Int_val(%{training : bool value}), %{exponential_average_factor : float}, %{epsilon : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let cudnn_batch_norm_backward
  (input @ local)
  ~(grad_output @ local)
  ~(weight @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~(save_mean @ local)
  ~(save_var @ local)
  ~epsilon
  ~(reservespace @ local)
  =
  let input = globalize_tensor input in
  let grad_output = globalize_tensor grad_output in
  let weight = globalize_tensor weight in
  let running_mean = [%globalize: tensor option] running_mean in
  let running_var = [%globalize: tensor option] running_var in
  let save_mean = [%globalize: tensor option] save_mean in
  let save_var = [%globalize: tensor option] save_var in
  let reservespace = globalize_tensor reservespace in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_cudnn_batch_norm_backward(%{input : tensor value}, %{grad_output : tensor value}, %{weight : tensor value}, %{running_mean : tensor option value}, %{running_var : tensor option value}, %{save_mean : tensor option value}, %{save_var : tensor option value}, %{epsilon : float}, %{reservespace : tensor value}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let cudnn_batch_norm_backward_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  (input @ local)
  ~(grad_output @ local)
  ~(weight @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~(save_mean @ local)
  ~(save_var @ local)
  ~epsilon
  ~(reservespace @ local)
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let input = globalize_tensor input in
  let grad_output = globalize_tensor grad_output in
  let weight = globalize_tensor weight in
  let running_mean = [%globalize: tensor option] running_mean in
  let running_var = [%globalize: tensor option] running_var in
  let save_mean = [%globalize: tensor option] save_mean in
  let save_var = [%globalize: tensor option] save_var in
  let reservespace = globalize_tensor reservespace in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_cudnn_batch_norm_backward_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{input : tensor value}, %{grad_output : tensor value}, %{weight : tensor value}, %{running_mean : tensor option value}, %{running_var : tensor option value}, %{save_mean : tensor option value}, %{save_var : tensor option value}, %{epsilon : float}, %{reservespace : tensor value}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let cudnn_batch_norm_out
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  ~(out3 @ local)
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~training
  ~exponential_average_factor
  ~epsilon
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let out2 = globalize_tensor out2 in
  let out3 = globalize_tensor out3 in
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  let running_mean = [%globalize: tensor option] running_mean in
  let running_var = [%globalize: tensor option] running_var in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg_cudnn_batch_norm_out(%{out0 : tensor value}, %{out1 : tensor value}, %{out2 : tensor value}, %{out3 : tensor value}, %{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{running_mean : tensor option value}, %{running_var : tensor option value}, Int_val(%{training : bool value}), %{exponential_average_factor : float}, %{epsilon : float}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let cudnn_convolution
  (self @ local)
  ~(weight @ local)
  ~padding
  ~stride
  ~dilation
  ~groups
  ~benchmark
  ~deterministic
  ~allow_tf32
  =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  [%c.alloc
    ({|CAMLreturn(atg_cudnn_convolution(%{self : tensor value}, %{weight : tensor value}, %{padding : int list value}, %{stride : int list value}, %{dilation : int list value}, %{groups : int}, Int_val(%{benchmark : bool value}), Int_val(%{deterministic : bool value}), Int_val(%{allow_tf32 : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cudnn_convolution_add_relu
  (self @ local)
  ~(weight @ local)
  ~(z @ local)
  ~alpha
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  ~groups
  =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let z = globalize_tensor z in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_cudnn_convolution_add_relu(%{self : tensor value}, %{weight : tensor value}, %{z : tensor value}, %{alpha : scalar value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cudnn_convolution_add_relu_out
  ~(out @ local)
  (self @ local)
  ~(weight @ local)
  ~(z @ local)
  ~alpha
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  ~groups
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let z = globalize_tensor z in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_cudnn_convolution_add_relu_out(%{out : tensor value}, %{self : tensor value}, %{weight : tensor value}, %{z : tensor value}, %{alpha : scalar value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cudnn_convolution_out
  ~(out @ local)
  (self @ local)
  ~(weight @ local)
  ~padding
  ~stride
  ~dilation
  ~groups
  ~benchmark
  ~deterministic
  ~allow_tf32
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  [%c.alloc
    ({|CAMLreturn(atg_cudnn_convolution_out(%{out : tensor value}, %{self : tensor value}, %{weight : tensor value}, %{padding : int list value}, %{stride : int list value}, %{dilation : int list value}, %{groups : int}, Int_val(%{benchmark : bool value}), Int_val(%{deterministic : bool value}), Int_val(%{allow_tf32 : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cudnn_convolution_relu
  (self @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  ~groups
  =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_cudnn_convolution_relu(%{self : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cudnn_convolution_relu_out
  ~(out @ local)
  (self @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~stride
  ~padding
  ~dilation
  ~groups
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_cudnn_convolution_relu_out(%{out : tensor value}, %{self : tensor value}, %{weight : tensor value}, %{bias : tensor option value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, %{groups : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cudnn_convolution_transpose
  (self @ local)
  ~(weight @ local)
  ~padding
  ~output_padding
  ~stride
  ~dilation
  ~groups
  ~benchmark
  ~deterministic
  ~allow_tf32
  =
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  [%c.alloc
    ({|CAMLreturn(atg_cudnn_convolution_transpose(%{self : tensor value}, %{weight : tensor value}, %{padding : int list value}, %{output_padding : int list value}, %{stride : int list value}, %{dilation : int list value}, %{groups : int}, Int_val(%{benchmark : bool value}), Int_val(%{deterministic : bool value}), Int_val(%{allow_tf32 : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cudnn_convolution_transpose_out
  ~(out @ local)
  (self @ local)
  ~(weight @ local)
  ~padding
  ~output_padding
  ~stride
  ~dilation
  ~groups
  ~benchmark
  ~deterministic
  ~allow_tf32
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let weight = globalize_tensor weight in
  [%c.alloc
    ({|CAMLreturn(atg_cudnn_convolution_transpose_out(%{out : tensor value}, %{self : tensor value}, %{weight : tensor value}, %{padding : int list value}, %{output_padding : int list value}, %{stride : int list value}, %{dilation : int list value}, %{groups : int}, Int_val(%{benchmark : bool value}), Int_val(%{deterministic : bool value}), Int_val(%{allow_tf32 : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cudnn_grid_sampler (self @ local) ~(grid @ local) =
  let self = globalize_tensor self in
  let grid = globalize_tensor grid in
  [%c.alloc
    ({|CAMLreturn(atg_cudnn_grid_sampler(%{self : tensor value}, %{grid : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cudnn_grid_sampler_backward (self @ local) ~(grid @ local) ~(grad_output @ local) =
  let self = globalize_tensor self in
  let grid = globalize_tensor grid in
  let grad_output = globalize_tensor grad_output in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_cudnn_grid_sampler_backward(%{self : tensor value}, %{grid : tensor value}, %{grad_output : tensor value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let cudnn_grid_sampler_backward_out
  ~(out0 @ local)
  ~(out1 @ local)
  (self @ local)
  ~(grid @ local)
  ~(grad_output @ local)
  =
  let out0 = globalize_tensor out0 in
  let out1 = globalize_tensor out1 in
  let self = globalize_tensor self in
  let grid = globalize_tensor grid in
  let grad_output = globalize_tensor grad_output in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_cudnn_grid_sampler_backward_out(%{out0 : tensor value}, %{out1 : tensor value}, %{self : tensor value}, %{grid : tensor value}, %{grad_output : tensor value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let cudnn_grid_sampler_out ~(out @ local) (self @ local) ~(grid @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let grid = globalize_tensor grid in
  [%c.alloc
    ({|CAMLreturn(atg_cudnn_grid_sampler_out(%{out : tensor value}, %{self : tensor value}, %{grid : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cudnn_is_acceptable (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_cudnn_is_acceptable(%{self : tensor value}));|} : bool value)]
;;

let cummax (self @ local) ~dim =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_cummax(%{self : tensor value}, %{dim : int}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let cummax_out ~(values @ local) ~(indices @ local) (self @ local) ~dim =
  let values = globalize_tensor values in
  let indices = globalize_tensor indices in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_cummax_out(%{values : tensor value}, %{indices : tensor value}, %{self : tensor value}, %{dim : int}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let cummaxmin_backward ~(grad @ local) (input @ local) ~(indices @ local) ~dim =
  let grad = globalize_tensor grad in
  let input = globalize_tensor input in
  let indices = globalize_tensor indices in
  [%c.alloc
    ({|CAMLreturn(atg_cummaxmin_backward(%{grad : tensor value}, %{input : tensor value}, %{indices : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cummin (self @ local) ~dim =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_cummin(%{self : tensor value}, %{dim : int}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let cummin_out ~(values @ local) ~(indices @ local) (self @ local) ~dim =
  let values = globalize_tensor values in
  let indices = globalize_tensor indices in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_cummin_out(%{values : tensor value}, %{indices : tensor value}, %{self : tensor value}, %{dim : int}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let cumprod (self @ local) ~dim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_cumprod(%{self : tensor value}, %{dim : int}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cumprod_ (self @ local) ~dim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_cumprod_(%{self : tensor value}, %{dim : int}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cumprod_backward ~(grad @ local) (input @ local) ~dim ~(output @ local) =
  let grad = globalize_tensor grad in
  let input = globalize_tensor input in
  let output = globalize_tensor output in
  [%c.alloc
    ({|CAMLreturn(atg_cumprod_backward(%{grad : tensor value}, %{input : tensor value}, %{dim : int}, %{output : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cumprod_out ~(out @ local) (self @ local) ~dim ~dtype =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_cumprod_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cumsum (self @ local) ~dim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_cumsum(%{self : tensor value}, %{dim : int}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cumsum_ (self @ local) ~dim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_cumsum_(%{self : tensor value}, %{dim : int}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cumsum_out ~(out @ local) (self @ local) ~dim ~dtype =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_cumsum_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cumulative_trapezoid ~(y @ local) ~(x @ local) ~dim =
  let y = globalize_tensor y in
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_cumulative_trapezoid(%{y : tensor value}, %{x : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let cumulative_trapezoid_dx ?dx ~(y @ local) ~dim () =
  let y = globalize_tensor y in
  [%c.alloc
    ({|CAMLreturn(atg_cumulative_trapezoid_dx(%{y : tensor value}, %{dx : scalar option value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let data (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_data(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let deg2rad (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_deg2rad(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let deg2rad_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_deg2rad_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let deg2rad_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_deg2rad_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let dense_dim (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_dense_dim(%{self : tensor value}));|} : int64 value)]
;;

let dequantize (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_dequantize(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let dequantize_self_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_dequantize_self_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let dequantize_tensors (tensors @ local) =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_dequantize_tensors(%{tensors : tensor list value}));|}
     : raw_tensor list value)]
  |> List.map ~f:wrap_managed_tensor
;;

let dequantize_tensors_out ~(out @ local) (tensors @ local) =
  let out = Torch_local_iterators.List.map_local_input out ~f:globalize_tensor in
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    {|atg_dequantize_tensors_out(%{out : tensor list value}, %{tensors : tensor list value});|}]
;;

let det (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_det(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let detach (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_detach(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let detach_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_detach_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let detach_copy (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_detach_copy(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let detach_copy_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_detach_copy_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let diag (self @ local) ~diagonal =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_diag(%{self : tensor value}, %{diagonal : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let diag_embed (self @ local) ~offset ~dim1 ~dim2 =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_diag_embed(%{self : tensor value}, %{offset : int}, %{dim1 : int}, %{dim2 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let diag_embed_out ~(out @ local) (self @ local) ~offset ~dim1 ~dim2 =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_diag_embed_out(%{out : tensor value}, %{self : tensor value}, %{offset : int}, %{dim1 : int}, %{dim2 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let diag_out ~(out @ local) (self @ local) ~diagonal =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_diag_out(%{out : tensor value}, %{self : tensor value}, %{diagonal : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let diagflat (self @ local) ~offset =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_diagflat(%{self : tensor value}, %{offset : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let diagonal (self @ local) ~offset ~dim1 ~dim2 =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_diagonal(%{self : tensor value}, %{offset : int}, %{dim1 : int}, %{dim2 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let diagonal_backward ~(grad_output @ local) ~input_sizes ~offset ~dim1 ~dim2 =
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg_diagonal_backward(%{grad_output : tensor value}, %{input_sizes : int list value}, %{offset : int}, %{dim1 : int}, %{dim2 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let diagonal_backward_out
  ~(out @ local)
  ~(grad_output @ local)
  ~input_sizes
  ~offset
  ~dim1
  ~dim2
  =
  let out = globalize_tensor out in
  let grad_output = globalize_tensor grad_output in
  [%c.alloc
    ({|CAMLreturn(atg_diagonal_backward_out(%{out : tensor value}, %{grad_output : tensor value}, %{input_sizes : int list value}, %{offset : int}, %{dim1 : int}, %{dim2 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let diagonal_copy (self @ local) ~offset ~dim1 ~dim2 =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_diagonal_copy(%{self : tensor value}, %{offset : int}, %{dim1 : int}, %{dim2 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let diagonal_copy_out ~(out @ local) (self @ local) ~offset ~dim1 ~dim2 =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_diagonal_copy_out(%{out : tensor value}, %{self : tensor value}, %{offset : int}, %{dim1 : int}, %{dim2 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let diagonal_scatter (self @ local) ~(src @ local) ~offset ~dim1 ~dim2 =
  let self = globalize_tensor self in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_diagonal_scatter(%{self : tensor value}, %{src : tensor value}, %{offset : int}, %{dim1 : int}, %{dim2 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let diagonal_scatter_out ~(out @ local) (self @ local) ~(src @ local) ~offset ~dim1 ~dim2 =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let src = globalize_tensor src in
  [%c.alloc
    ({|CAMLreturn(atg_diagonal_scatter_out(%{out : tensor value}, %{self : tensor value}, %{src : tensor value}, %{offset : int}, %{dim1 : int}, %{dim2 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let diff (self @ local) ~n ~dim ~(prepend @ local) ~(append @ local) =
  let self = globalize_tensor self in
  let prepend = [%globalize: tensor option] prepend in
  let append = [%globalize: tensor option] append in
  [%c.alloc
    ({|CAMLreturn(atg_diff(%{self : tensor value}, %{n : int}, %{dim : int}, %{prepend : tensor option value}, %{append : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let diff_out ~(out @ local) (self @ local) ~n ~dim ~(prepend @ local) ~(append @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let prepend = [%globalize: tensor option] prepend in
  let append = [%globalize: tensor option] append in
  [%c.alloc
    ({|CAMLreturn(atg_diff_out(%{out : tensor value}, %{self : tensor value}, %{n : int}, %{dim : int}, %{prepend : tensor option value}, %{append : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let digamma (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_digamma(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let digamma_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_digamma_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let digamma_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_digamma_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let dist ?p (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_dist(%{self : tensor value}, %{other : tensor value}, %{p : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let dist_out ?p ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_dist_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}, %{p : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let div (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_div(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let div_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_div_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let div_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_div_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let div_out_mode ~(out @ local) (self @ local) (other @ local) ~rounding_mode =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_div_out_mode(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}, %{rounding_mode : string option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let div_scalar (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_div_scalar(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let div_scalar_ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_div_scalar_(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let div_scalar_mode (self @ local) other ~rounding_mode =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_div_scalar_mode(%{self : tensor value}, %{other : scalar value}, %{rounding_mode : string option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let div_scalar_mode_ (self @ local) other ~rounding_mode =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_div_scalar_mode_(%{self : tensor value}, %{other : scalar value}, %{rounding_mode : string option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let div_scalar_mode_out ~(out @ local) (self @ local) other ~rounding_mode =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_div_scalar_mode_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}, %{rounding_mode : string option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let div_scalar_out ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_div_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let div_tensor_mode (self @ local) (other @ local) ~rounding_mode =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_div_tensor_mode(%{self : tensor value}, %{other : tensor value}, %{rounding_mode : string option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let div_tensor_mode_ (self @ local) (other @ local) ~rounding_mode =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_div_tensor_mode_(%{self : tensor value}, %{other : tensor value}, %{rounding_mode : string option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let divide (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_divide(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let divide_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_divide_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let divide_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_divide_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let divide_out_mode ~(out @ local) (self @ local) (other @ local) ~rounding_mode =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_divide_out_mode(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}, %{rounding_mode : string option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let divide_scalar (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_divide_scalar(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let divide_scalar_ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_divide_scalar_(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let divide_scalar_mode (self @ local) other ~rounding_mode =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_divide_scalar_mode(%{self : tensor value}, %{other : scalar value}, %{rounding_mode : string option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let divide_scalar_mode_ (self @ local) other ~rounding_mode =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_divide_scalar_mode_(%{self : tensor value}, %{other : scalar value}, %{rounding_mode : string option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let divide_tensor_mode (self @ local) (other @ local) ~rounding_mode =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_divide_tensor_mode(%{self : tensor value}, %{other : tensor value}, %{rounding_mode : string option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let divide_tensor_mode_ (self @ local) (other @ local) ~rounding_mode =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_divide_tensor_mode_(%{self : tensor value}, %{other : tensor value}, %{rounding_mode : string option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;
