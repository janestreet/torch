(* THIS FILE IS AUTOMATICALLY GENERATED, DO NOT EDIT BY HAND! *)

open! Core
open C_ffi
open Torch_wrapper_types
open Torch_refcounted_bindings.Type_defs

[%%c {| #include "torch_api.h" |}]

let index_fill_int_scalar_out ~(out @ local) (self @ local) ~dim ~(index @ local) ~value =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  [%c.alloc
    ({|CAMLreturn(atg_index_fill_int_scalar_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, %{index : tensor value}, %{value : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let index_fill_int_tensor (self @ local) ~dim ~(index @ local) ~(value @ local) =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let value = globalize_tensor value in
  [%c.alloc
    ({|CAMLreturn(atg_index_fill_int_tensor(%{self : tensor value}, %{dim : int}, %{index : tensor value}, %{value : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let index_fill_int_tensor_ (self @ local) ~dim ~(index @ local) ~(value @ local) =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let value = globalize_tensor value in
  [%c.alloc
    ({|CAMLreturn(atg_index_fill_int_tensor_(%{self : tensor value}, %{dim : int}, %{index : tensor value}, %{value : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let index_fill_int_tensor_out
  ~(out @ local)
  (self @ local)
  ~dim
  ~(index @ local)
  ~(value @ local)
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let value = globalize_tensor value in
  [%c.alloc
    ({|CAMLreturn(atg_index_fill_int_tensor_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, %{index : tensor value}, %{value : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let index_reduce
  (self @ local)
  ~dim
  ~(index @ local)
  ~(source @ local)
  ~reduce
  ~include_self
  =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let source = globalize_tensor source in
  [%c.alloc
    ({|CAMLreturn(atg_index_reduce(%{self : tensor value}, %{dim : int}, %{index : tensor value}, %{source : tensor value}, String_val(%{reduce : string value}), Int_val(%{include_self : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let index_reduce_
  (self @ local)
  ~dim
  ~(index @ local)
  ~(source @ local)
  ~reduce
  ~include_self
  =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let source = globalize_tensor source in
  [%c.alloc
    ({|CAMLreturn(atg_index_reduce_(%{self : tensor value}, %{dim : int}, %{index : tensor value}, %{source : tensor value}, String_val(%{reduce : string value}), Int_val(%{include_self : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let index_reduce_out
  ~(out @ local)
  (self @ local)
  ~dim
  ~(index @ local)
  ~(source @ local)
  ~reduce
  ~include_self
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  let source = globalize_tensor source in
  [%c.alloc
    ({|CAMLreturn(atg_index_reduce_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, %{index : tensor value}, %{source : tensor value}, String_val(%{reduce : string value}), Int_val(%{include_self : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let index_select (self @ local) ~dim ~(index @ local) =
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  [%c.alloc
    ({|CAMLreturn(atg_index_select(%{self : tensor value}, %{dim : int}, %{index : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let index_select_backward ~(grad @ local) ~self_sizes ~dim ~(index @ local) =
  let grad = globalize_tensor grad in
  let index = globalize_tensor index in
  [%c.alloc
    ({|CAMLreturn(atg_index_select_backward(%{grad : tensor value}, %{self_sizes : int list value}, %{dim : int}, %{index : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let index_select_out ~(out @ local) (self @ local) ~dim ~(index @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let index = globalize_tensor index in
  [%c.alloc
    ({|CAMLreturn(atg_index_select_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, %{index : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let indices (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_indices(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let indices_copy (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_indices_copy(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let indices_copy_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_indices_copy_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let infinitely_differentiable_gelu_backward ~(grad @ local) (self @ local) =
  let grad = globalize_tensor grad in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_infinitely_differentiable_gelu_backward(%{grad : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let inner (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_inner(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let inner_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_inner_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let instance_norm
  (input @ local)
  ~(weight @ local)
  ~(bias @ local)
  ~(running_mean @ local)
  ~(running_var @ local)
  ~use_input_stats
  ~momentum
  ~eps
  ~cudnn_enabled
  =
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  let running_mean = [%globalize: tensor option] running_mean in
  let running_var = [%globalize: tensor option] running_var in
  [%c.alloc
    ({|CAMLreturn(atg_instance_norm(%{input : tensor value}, %{weight : tensor option value}, %{bias : tensor option value}, %{running_mean : tensor option value}, %{running_var : tensor option value}, Int_val(%{use_input_stats : bool value}), %{momentum : float}, %{eps : float}, Int_val(%{cudnn_enabled : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let int_repr (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_int_repr(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let int_repr_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_int_repr_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let inverse (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_inverse(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let inverse_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_inverse_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let is_coalesced (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_is_coalesced(%{self : tensor value}));|} : bool value)]
;;

let is_complex (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_is_complex(%{self : tensor value}));|} : bool value)]
;;

let is_conj (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_is_conj(%{self : tensor value}));|} : bool value)]
;;

let is_distributed (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_is_distributed(%{self : tensor value}));|} : bool value)]
;;

let is_floating_point (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_is_floating_point(%{self : tensor value}));|} : bool value)]
;;

let is_inference (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_is_inference(%{self : tensor value}));|} : bool value)]
;;

let is_leaf (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_is_leaf(%{self : tensor value}));|} : bool value)]
;;

let is_neg (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_is_neg(%{self : tensor value}));|} : bool value)]
;;

let is_nonzero (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_is_nonzero(%{self : tensor value}));|} : bool value)]
;;

let is_pinned (self @ local) ~device =
  let self = globalize_tensor self in
  let device = Device.option_to_int device in
  [%c.alloc
    ({|CAMLreturn(atg_is_pinned(%{self : tensor value}, %{device : int}));|} : bool value)]
;;

let is_same_size (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_is_same_size(%{self : tensor value}, %{other : tensor value}));|}
     : bool value)]
;;

let is_set_to (self @ local) (tensor @ local) =
  let self = globalize_tensor self in
  let tensor = globalize_tensor tensor in
  [%c.alloc
    ({|CAMLreturn(atg_is_set_to(%{self : tensor value}, %{tensor : tensor value}));|}
     : bool value)]
;;

let is_signed (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_is_signed(%{self : tensor value}));|} : bool value)]
;;

let is_vulkan_available () =
  [%c.alloc ({|CAMLreturn(atg_is_vulkan_available());|} : bool value)]
;;

let isclose (self @ local) (other @ local) ~rtol ~atol ~equal_nan =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_isclose(%{self : tensor value}, %{other : tensor value}, %{rtol : float}, %{atol : float}, Int_val(%{equal_nan : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let isfinite (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_isfinite(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let isin ~(elements @ local) ~(test_elements @ local) ~assume_unique ~invert =
  let elements = globalize_tensor elements in
  let test_elements = globalize_tensor test_elements in
  [%c.alloc
    ({|CAMLreturn(atg_isin(%{elements : tensor value}, %{test_elements : tensor value}, Int_val(%{assume_unique : bool value}), Int_val(%{invert : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let isin_scalar_tensor ~element ~(test_elements @ local) ~assume_unique ~invert =
  let test_elements = globalize_tensor test_elements in
  [%c.alloc
    ({|CAMLreturn(atg_isin_scalar_tensor(%{element : scalar value}, %{test_elements : tensor value}, Int_val(%{assume_unique : bool value}), Int_val(%{invert : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let isin_scalar_tensor_out
  ~(out @ local)
  ~element
  ~(test_elements @ local)
  ~assume_unique
  ~invert
  =
  let out = globalize_tensor out in
  let test_elements = globalize_tensor test_elements in
  [%c.alloc
    ({|CAMLreturn(atg_isin_scalar_tensor_out(%{out : tensor value}, %{element : scalar value}, %{test_elements : tensor value}, Int_val(%{assume_unique : bool value}), Int_val(%{invert : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let isin_tensor_scalar ~(elements @ local) ~test_element ~assume_unique ~invert =
  let elements = globalize_tensor elements in
  [%c.alloc
    ({|CAMLreturn(atg_isin_tensor_scalar(%{elements : tensor value}, %{test_element : scalar value}, Int_val(%{assume_unique : bool value}), Int_val(%{invert : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let isin_tensor_scalar_out
  ~(out @ local)
  ~(elements @ local)
  ~test_element
  ~assume_unique
  ~invert
  =
  let out = globalize_tensor out in
  let elements = globalize_tensor elements in
  [%c.alloc
    ({|CAMLreturn(atg_isin_tensor_scalar_out(%{out : tensor value}, %{elements : tensor value}, %{test_element : scalar value}, Int_val(%{assume_unique : bool value}), Int_val(%{invert : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let isin_tensor_tensor_out
  ~(out @ local)
  ~(elements @ local)
  ~(test_elements @ local)
  ~assume_unique
  ~invert
  =
  let out = globalize_tensor out in
  let elements = globalize_tensor elements in
  let test_elements = globalize_tensor test_elements in
  [%c.alloc
    ({|CAMLreturn(atg_isin_tensor_tensor_out(%{out : tensor value}, %{elements : tensor value}, %{test_elements : tensor value}, Int_val(%{assume_unique : bool value}), Int_val(%{invert : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let isinf (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_isinf(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let isinf_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_isinf_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let isnan (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_isnan(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let isnan_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_isnan_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let isneginf (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_isneginf(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let isneginf_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_isneginf_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let isposinf (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_isposinf(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let isposinf_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_isposinf_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let isreal (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_isreal(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let istft
  (self @ local)
  ~n_fft
  ~hop_length
  ~win_length
  ~(window @ local)
  ~center
  ~normalized
  ~onesided
  ~length
  ~return_complex
  =
  let self = globalize_tensor self in
  let window = [%globalize: tensor option] window in
  [%c.alloc
    ({|CAMLreturn(atg_istft(%{self : tensor value}, %{n_fft : int}, %{hop_length : int option value}, %{win_length : int option value}, %{window : tensor option value}, Int_val(%{center : bool value}), Int_val(%{normalized : bool value}), Int_val(%{onesided : bool value}), %{length : int option value}, Int_val(%{return_complex : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let kaiser_window ~window_length ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_kaiser_window(%{window_length : int}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let kaiser_window_beta ~window_length ~periodic ~beta ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_kaiser_window_beta(%{window_length : int}, Int_val(%{periodic : bool value}), %{beta : float}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let kaiser_window_beta_out ~(out @ local) ~window_length ~periodic ~beta =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_kaiser_window_beta_out(%{out : tensor value}, %{window_length : int}, Int_val(%{periodic : bool value}), %{beta : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let kaiser_window_out ~(out @ local) ~window_length =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_kaiser_window_out(%{out : tensor value}, %{window_length : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let kaiser_window_periodic ~window_length ~periodic ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_kaiser_window_periodic(%{window_length : int}, Int_val(%{periodic : bool value}), %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let kaiser_window_periodic_out ~(out @ local) ~window_length ~periodic =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_kaiser_window_periodic_out(%{out : tensor value}, %{window_length : int}, Int_val(%{periodic : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let kl_div (self @ local) ~(target @ local) ~reduction ~log_target =
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_kl_div(%{self : tensor value}, %{target : tensor value}, %{reduction : int}, Int_val(%{log_target : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let kron (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_kron(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let kron_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_kron_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let kthvalue (self @ local) ~k ~dim ~keepdim =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_kthvalue(%{self : tensor value}, %{k : int}, %{dim : int}, Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let kthvalue_values ~(values @ local) ~(indices @ local) (self @ local) ~k ~dim ~keepdim =
  let values = globalize_tensor values in
  let indices = globalize_tensor indices in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_kthvalue_values(%{values : tensor value}, %{indices : tensor value}, %{self : tensor value}, %{k : int}, %{dim : int}, Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let l1_loss (self @ local) ~(target @ local) ~reduction =
  let self = globalize_tensor self in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_l1_loss(%{self : tensor value}, %{target : tensor value}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let layer_norm
  (input @ local)
  ~normalized_shape
  ~(weight @ local)
  ~(bias @ local)
  ~eps
  ~cudnn_enable
  =
  let input = globalize_tensor input in
  let weight = [%globalize: tensor option] weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_layer_norm(%{input : tensor value}, %{normalized_shape : int list value}, %{weight : tensor option value}, %{bias : tensor option value}, %{eps : float}, Int_val(%{cudnn_enable : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lcm (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_lcm(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lcm_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_lcm_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lcm_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_lcm_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ldexp (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_ldexp(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ldexp_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_ldexp_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let ldexp_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_ldexp_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let le (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_le(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let le_ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_le_(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let le_scalar_out ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_le_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let le_tensor (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_le_tensor(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let le_tensor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_le_tensor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let le_tensor_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_le_tensor_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let leaky_relu ?negative_slope (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_leaky_relu(%{self : tensor value}, %{negative_slope : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let leaky_relu_ ?negative_slope (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_leaky_relu_(%{self : tensor value}, %{negative_slope : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let leaky_relu_backward
  ~(grad_output @ local)
  (self @ local)
  ~negative_slope
  ~self_is_result
  =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_leaky_relu_backward(%{grad_output : tensor value}, %{self : tensor value}, %{negative_slope : scalar value}, Int_val(%{self_is_result : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let leaky_relu_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~negative_slope
  ~self_is_result
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_leaky_relu_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{negative_slope : scalar value}, Int_val(%{self_is_result : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let leaky_relu_out ?negative_slope ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_leaky_relu_out(%{out : tensor value}, %{self : tensor value}, %{negative_slope : scalar option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lerp (self @ local) ~(end_ @ local) ~weight =
  let self = globalize_tensor self in
  let end_ = globalize_tensor end_ in
  [%c.alloc
    ({|CAMLreturn(atg_lerp(%{self : tensor value}, %{end_ : tensor value}, %{weight : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lerp_ (self @ local) ~(end_ @ local) ~weight =
  let self = globalize_tensor self in
  let end_ = globalize_tensor end_ in
  [%c.alloc
    ({|CAMLreturn(atg_lerp_(%{self : tensor value}, %{end_ : tensor value}, %{weight : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lerp_scalar_out ~(out @ local) (self @ local) ~(end_ @ local) ~weight =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let end_ = globalize_tensor end_ in
  [%c.alloc
    ({|CAMLreturn(atg_lerp_scalar_out(%{out : tensor value}, %{self : tensor value}, %{end_ : tensor value}, %{weight : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lerp_tensor (self @ local) ~(end_ @ local) ~(weight @ local) =
  let self = globalize_tensor self in
  let end_ = globalize_tensor end_ in
  let weight = globalize_tensor weight in
  [%c.alloc
    ({|CAMLreturn(atg_lerp_tensor(%{self : tensor value}, %{end_ : tensor value}, %{weight : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lerp_tensor_ (self @ local) ~(end_ @ local) ~(weight @ local) =
  let self = globalize_tensor self in
  let end_ = globalize_tensor end_ in
  let weight = globalize_tensor weight in
  [%c.alloc
    ({|CAMLreturn(atg_lerp_tensor_(%{self : tensor value}, %{end_ : tensor value}, %{weight : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lerp_tensor_out ~(out @ local) (self @ local) ~(end_ @ local) ~(weight @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let end_ = globalize_tensor end_ in
  let weight = globalize_tensor weight in
  [%c.alloc
    ({|CAMLreturn(atg_lerp_tensor_out(%{out : tensor value}, %{self : tensor value}, %{end_ : tensor value}, %{weight : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let less (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_less(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let less_ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_less_(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let less_equal (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_less_equal(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let less_equal_ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_less_equal_(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let less_equal_scalar_out ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_less_equal_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let less_equal_tensor (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_less_equal_tensor(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let less_equal_tensor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_less_equal_tensor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let less_equal_tensor_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_less_equal_tensor_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let less_scalar_out ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_less_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let less_tensor (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_less_tensor(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let less_tensor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_less_tensor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let less_tensor_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_less_tensor_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lgamma (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_lgamma(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lgamma_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_lgamma_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lgamma_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_lgamma_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lift (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_lift(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lift_fresh (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_lift_fresh(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lift_fresh_copy (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_lift_fresh_copy(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lift_fresh_copy_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_lift_fresh_copy_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lift_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_lift_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_cholesky (self @ local) ~upper =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_cholesky(%{self : tensor value}, Int_val(%{upper : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_cholesky_ex (self @ local) ~upper ~check_errors =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_cholesky_ex(%{self : tensor value}, Int_val(%{upper : bool value}), Int_val(%{check_errors : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let linalg_cholesky_ex_l ~(l @ local) ~(info @ local) (self @ local) ~upper ~check_errors =
  let l = globalize_tensor l in
  let info = globalize_tensor info in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_cholesky_ex_l(%{l : tensor value}, %{info : tensor value}, %{self : tensor value}, Int_val(%{upper : bool value}), Int_val(%{check_errors : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let linalg_cholesky_out ~(out @ local) (self @ local) ~upper =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_cholesky_out(%{out : tensor value}, %{self : tensor value}, Int_val(%{upper : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_cond (self @ local) ~p =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_cond(%{self : tensor value}, %{p : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_cond_out ~(out @ local) (self @ local) ~p =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_cond_out(%{out : tensor value}, %{self : tensor value}, %{p : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_cond_p_str (self @ local) ~p =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_cond_p_str(%{self : tensor value}, String_val(%{p : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_cond_p_str_out ~(out @ local) (self @ local) ~p =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_cond_p_str_out(%{out : tensor value}, %{self : tensor value}, String_val(%{p : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_cross (self @ local) (other @ local) ~dim =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_cross(%{self : tensor value}, %{other : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_cross_out ~(out @ local) (self @ local) (other @ local) ~dim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_cross_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_det ~(a @ local) =
  let a = globalize_tensor a in
  [%c.alloc ({|CAMLreturn(atg_linalg_det(%{a : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_det_out ~(out @ local) ~(a @ local) =
  let out = globalize_tensor out in
  let a = globalize_tensor a in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_det_out(%{out : tensor value}, %{a : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_diagonal ~(a @ local) ~offset ~dim1 ~dim2 =
  let a = globalize_tensor a in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_diagonal(%{a : tensor value}, %{offset : int}, %{dim1 : int}, %{dim2 : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_eig (self @ local) =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_eig(%{self : tensor value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let linalg_eig_out ~(eigenvalues @ local) ~(eigenvectors @ local) (self @ local) =
  let eigenvalues = globalize_tensor eigenvalues in
  let eigenvectors = globalize_tensor eigenvectors in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_eig_out(%{eigenvalues : tensor value}, %{eigenvectors : tensor value}, %{self : tensor value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let linalg_eigh (self @ local) ~uplo =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_eigh(%{self : tensor value}, String_val(%{uplo : string value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let linalg_eigh_eigvals ~(eigvals @ local) ~(eigvecs @ local) (self @ local) ~uplo =
  let eigvals = globalize_tensor eigvals in
  let eigvecs = globalize_tensor eigvecs in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_eigh_eigvals(%{eigvals : tensor value}, %{eigvecs : tensor value}, %{self : tensor value}, String_val(%{uplo : string value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let linalg_eigvals (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_eigvals(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_eigvals_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_eigvals_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_eigvalsh (self @ local) ~uplo =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_eigvalsh(%{self : tensor value}, String_val(%{uplo : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_eigvalsh_out ~(out @ local) (self @ local) ~uplo =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_eigvalsh_out(%{out : tensor value}, %{self : tensor value}, String_val(%{uplo : string value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_householder_product (input @ local) ~(tau @ local) =
  let input = globalize_tensor input in
  let tau = globalize_tensor tau in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_householder_product(%{input : tensor value}, %{tau : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_householder_product_out ~(out @ local) (input @ local) ~(tau @ local) =
  let out = globalize_tensor out in
  let input = globalize_tensor input in
  let tau = globalize_tensor tau in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_householder_product_out(%{out : tensor value}, %{input : tensor value}, %{tau : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_inv ~(a @ local) =
  let a = globalize_tensor a in
  [%c.alloc ({|CAMLreturn(atg_linalg_inv(%{a : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_inv_ex ~(a @ local) ~check_errors =
  let a = globalize_tensor a in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_inv_ex(%{a : tensor value}, Int_val(%{check_errors : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let linalg_inv_ex_inverse ~(inverse @ local) ~(info @ local) ~(a @ local) ~check_errors =
  let inverse = globalize_tensor inverse in
  let info = globalize_tensor info in
  let a = globalize_tensor a in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_inv_ex_inverse(%{inverse : tensor value}, %{info : tensor value}, %{a : tensor value}, Int_val(%{check_errors : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let linalg_inv_out ~(out @ local) ~(a @ local) =
  let out = globalize_tensor out in
  let a = globalize_tensor a in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_inv_out(%{out : tensor value}, %{a : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_ldl_factor (self @ local) ~hermitian =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_ldl_factor(%{self : tensor value}, Int_val(%{hermitian : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let linalg_ldl_factor_ex (self @ local) ~hermitian ~check_errors =
  let self = globalize_tensor self in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_ldl_factor_ex(%{self : tensor value}, Int_val(%{hermitian : bool value}), Int_val(%{check_errors : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let linalg_ldl_factor_ex_out
  ~(ld @ local)
  ~(pivots @ local)
  ~(info @ local)
  (self @ local)
  ~hermitian
  ~check_errors
  =
  let ld = globalize_tensor ld in
  let pivots = globalize_tensor pivots in
  let info = globalize_tensor info in
  let self = globalize_tensor self in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_ldl_factor_ex_out(%{ld : tensor value}, %{pivots : tensor value}, %{info : tensor value}, %{self : tensor value}, Int_val(%{hermitian : bool value}), Int_val(%{check_errors : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let linalg_ldl_factor_out ~(ld @ local) ~(pivots @ local) (self @ local) ~hermitian =
  let ld = globalize_tensor ld in
  let pivots = globalize_tensor pivots in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_ldl_factor_out(%{ld : tensor value}, %{pivots : tensor value}, %{self : tensor value}, Int_val(%{hermitian : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let linalg_ldl_solve ~(ld @ local) ~(pivots @ local) ~(b @ local) ~hermitian =
  let ld = globalize_tensor ld in
  let pivots = globalize_tensor pivots in
  let b = globalize_tensor b in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_ldl_solve(%{ld : tensor value}, %{pivots : tensor value}, %{b : tensor value}, Int_val(%{hermitian : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_ldl_solve_out
  ~(out @ local)
  ~(ld @ local)
  ~(pivots @ local)
  ~(b @ local)
  ~hermitian
  =
  let out = globalize_tensor out in
  let ld = globalize_tensor ld in
  let pivots = globalize_tensor pivots in
  let b = globalize_tensor b in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_ldl_solve_out(%{out : tensor value}, %{ld : tensor value}, %{pivots : tensor value}, %{b : tensor value}, Int_val(%{hermitian : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_lstsq (self @ local) ~(b @ local) ~rcond ~driver =
  let self = globalize_tensor self in
  let b = globalize_tensor b in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_lstsq(%{self : tensor value}, %{b : tensor value}, %{rcond : float option value}, %{driver : string option value}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let linalg_lstsq_out
  ~(solution @ local)
  ~(residuals @ local)
  ~(rank @ local)
  ~(singular_values @ local)
  (self @ local)
  ~(b @ local)
  ~rcond
  ~driver
  =
  let solution = globalize_tensor solution in
  let residuals = globalize_tensor residuals in
  let rank = globalize_tensor rank in
  let singular_values = globalize_tensor singular_values in
  let self = globalize_tensor self in
  let b = globalize_tensor b in
  let result0, result1, result2, result3 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_lstsq_out(%{solution : tensor value}, %{residuals : tensor value}, %{rank : tensor value}, %{singular_values : tensor value}, %{self : tensor value}, %{b : tensor value}, %{rcond : float option value}, %{driver : string option value}));|}
       : (raw_tensor * raw_tensor * raw_tensor * raw_tensor) value)]
  in
  ( wrap_managed_tensor result0
  , wrap_managed_tensor result1
  , wrap_managed_tensor result2
  , wrap_managed_tensor result3 )
;;

let linalg_lu ~(a @ local) ~pivot =
  let a = globalize_tensor a in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_lu(%{a : tensor value}, Int_val(%{pivot : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let linalg_lu_factor ~(a @ local) ~pivot =
  let a = globalize_tensor a in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_lu_factor(%{a : tensor value}, Int_val(%{pivot : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let linalg_lu_factor_ex ~(a @ local) ~pivot ~check_errors =
  let a = globalize_tensor a in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_lu_factor_ex(%{a : tensor value}, Int_val(%{pivot : bool value}), Int_val(%{check_errors : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let linalg_lu_factor_ex_out
  ~(lu @ local)
  ~(pivots @ local)
  ~(info @ local)
  ~(a @ local)
  ~pivot
  ~check_errors
  =
  let lu = globalize_tensor lu in
  let pivots = globalize_tensor pivots in
  let info = globalize_tensor info in
  let a = globalize_tensor a in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_lu_factor_ex_out(%{lu : tensor value}, %{pivots : tensor value}, %{info : tensor value}, %{a : tensor value}, Int_val(%{pivot : bool value}), Int_val(%{check_errors : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let linalg_lu_factor_out ~(lu @ local) ~(pivots @ local) ~(a @ local) ~pivot =
  let lu = globalize_tensor lu in
  let pivots = globalize_tensor pivots in
  let a = globalize_tensor a in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_lu_factor_out(%{lu : tensor value}, %{pivots : tensor value}, %{a : tensor value}, Int_val(%{pivot : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let linalg_lu_out ~(p @ local) ~(l @ local) ~(u @ local) ~(a @ local) ~pivot =
  let p = globalize_tensor p in
  let l = globalize_tensor l in
  let u = globalize_tensor u in
  let a = globalize_tensor a in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_lu_out(%{p : tensor value}, %{l : tensor value}, %{u : tensor value}, %{a : tensor value}, Int_val(%{pivot : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let linalg_lu_solve ~(lu @ local) ~(pivots @ local) ~(b @ local) ~left ~adjoint =
  let lu = globalize_tensor lu in
  let pivots = globalize_tensor pivots in
  let b = globalize_tensor b in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_lu_solve(%{lu : tensor value}, %{pivots : tensor value}, %{b : tensor value}, Int_val(%{left : bool value}), Int_val(%{adjoint : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_lu_solve_out
  ~(out @ local)
  ~(lu @ local)
  ~(pivots @ local)
  ~(b @ local)
  ~left
  ~adjoint
  =
  let out = globalize_tensor out in
  let lu = globalize_tensor lu in
  let pivots = globalize_tensor pivots in
  let b = globalize_tensor b in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_lu_solve_out(%{out : tensor value}, %{lu : tensor value}, %{pivots : tensor value}, %{b : tensor value}, Int_val(%{left : bool value}), Int_val(%{adjoint : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_matmul (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_matmul(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_matmul_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_matmul_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_matrix_exp (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_matrix_exp(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_matrix_exp_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_matrix_exp_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_matrix_power (self @ local) ~n =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_matrix_power(%{self : tensor value}, %{n : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_matrix_power_out ~(out @ local) (self @ local) ~n =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_matrix_power_out(%{out : tensor value}, %{self : tensor value}, %{n : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_matrix_rank (self @ local) ~tol ~hermitian =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_matrix_rank(%{self : tensor value}, %{tol : float}, Int_val(%{hermitian : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_matrix_rank_atol_rtol_float (self @ local) ~atol ~rtol ~hermitian =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_matrix_rank_atol_rtol_float(%{self : tensor value}, %{atol : float option value}, %{rtol : float option value}, Int_val(%{hermitian : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_matrix_rank_atol_rtol_float_out
  ~(out @ local)
  (self @ local)
  ~atol
  ~rtol
  ~hermitian
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_matrix_rank_atol_rtol_float_out(%{out : tensor value}, %{self : tensor value}, %{atol : float option value}, %{rtol : float option value}, Int_val(%{hermitian : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_matrix_rank_atol_rtol_tensor
  (input @ local)
  ~(atol @ local)
  ~(rtol @ local)
  ~hermitian
  =
  let input = globalize_tensor input in
  let atol = [%globalize: tensor option] atol in
  let rtol = [%globalize: tensor option] rtol in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_matrix_rank_atol_rtol_tensor(%{input : tensor value}, %{atol : tensor option value}, %{rtol : tensor option value}, Int_val(%{hermitian : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_matrix_rank_atol_rtol_tensor_out
  ~(out @ local)
  (input @ local)
  ~(atol @ local)
  ~(rtol @ local)
  ~hermitian
  =
  let out = globalize_tensor out in
  let input = globalize_tensor input in
  let atol = [%globalize: tensor option] atol in
  let rtol = [%globalize: tensor option] rtol in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_matrix_rank_atol_rtol_tensor_out(%{out : tensor value}, %{input : tensor value}, %{atol : tensor option value}, %{rtol : tensor option value}, Int_val(%{hermitian : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_matrix_rank_out ~(out @ local) (self @ local) ~tol ~hermitian =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_matrix_rank_out(%{out : tensor value}, %{self : tensor value}, %{tol : float}, Int_val(%{hermitian : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_matrix_rank_out_tol_tensor
  ~(out @ local)
  (input @ local)
  ~(tol @ local)
  ~hermitian
  =
  let out = globalize_tensor out in
  let input = globalize_tensor input in
  let tol = globalize_tensor tol in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_matrix_rank_out_tol_tensor(%{out : tensor value}, %{input : tensor value}, %{tol : tensor value}, Int_val(%{hermitian : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_matrix_rank_tol_tensor (input @ local) ~(tol @ local) ~hermitian =
  let input = globalize_tensor input in
  let tol = globalize_tensor tol in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_matrix_rank_tol_tensor(%{input : tensor value}, %{tol : tensor value}, Int_val(%{hermitian : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_multi_dot (tensors @ local) =
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_multi_dot(%{tensors : tensor list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_multi_dot_out ~(out @ local) (tensors @ local) =
  let out = globalize_tensor out in
  let tensors = Torch_local_iterators.List.map_local_input tensors ~f:globalize_tensor in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_multi_dot_out(%{out : tensor value}, %{tensors : tensor list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_pinv (self @ local) ~rcond ~hermitian =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_pinv(%{self : tensor value}, %{rcond : float}, Int_val(%{hermitian : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_pinv_atol_rtol_float (self @ local) ~atol ~rtol ~hermitian =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_pinv_atol_rtol_float(%{self : tensor value}, %{atol : float option value}, %{rtol : float option value}, Int_val(%{hermitian : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_pinv_atol_rtol_float_out ~(out @ local) (self @ local) ~atol ~rtol ~hermitian =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_pinv_atol_rtol_float_out(%{out : tensor value}, %{self : tensor value}, %{atol : float option value}, %{rtol : float option value}, Int_val(%{hermitian : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_pinv_atol_rtol_tensor (self @ local) ~(atol @ local) ~(rtol @ local) ~hermitian
  =
  let self = globalize_tensor self in
  let atol = [%globalize: tensor option] atol in
  let rtol = [%globalize: tensor option] rtol in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_pinv_atol_rtol_tensor(%{self : tensor value}, %{atol : tensor option value}, %{rtol : tensor option value}, Int_val(%{hermitian : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_pinv_atol_rtol_tensor_out
  ~(out @ local)
  (self @ local)
  ~(atol @ local)
  ~(rtol @ local)
  ~hermitian
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let atol = [%globalize: tensor option] atol in
  let rtol = [%globalize: tensor option] rtol in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_pinv_atol_rtol_tensor_out(%{out : tensor value}, %{self : tensor value}, %{atol : tensor option value}, %{rtol : tensor option value}, Int_val(%{hermitian : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_pinv_out ~(out @ local) (self @ local) ~rcond ~hermitian =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_pinv_out(%{out : tensor value}, %{self : tensor value}, %{rcond : float}, Int_val(%{hermitian : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_pinv_out_rcond_tensor ~(out @ local) (self @ local) ~(rcond @ local) ~hermitian
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let rcond = globalize_tensor rcond in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_pinv_out_rcond_tensor(%{out : tensor value}, %{self : tensor value}, %{rcond : tensor value}, Int_val(%{hermitian : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_pinv_rcond_tensor (self @ local) ~(rcond @ local) ~hermitian =
  let self = globalize_tensor self in
  let rcond = globalize_tensor rcond in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_pinv_rcond_tensor(%{self : tensor value}, %{rcond : tensor value}, Int_val(%{hermitian : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_qr ~(a @ local) ~mode =
  let a = globalize_tensor a in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_qr(%{a : tensor value}, String_val(%{mode : string value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let linalg_qr_out ~(q @ local) ~(r @ local) ~(a @ local) ~mode =
  let q = globalize_tensor q in
  let r = globalize_tensor r in
  let a = globalize_tensor a in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_qr_out(%{q : tensor value}, %{r : tensor value}, %{a : tensor value}, String_val(%{mode : string value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let linalg_slogdet ~(a @ local) =
  let a = globalize_tensor a in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_slogdet(%{a : tensor value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let linalg_slogdet_out ~(sign @ local) ~(logabsdet @ local) ~(a @ local) =
  let sign = globalize_tensor sign in
  let logabsdet = globalize_tensor logabsdet in
  let a = globalize_tensor a in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_slogdet_out(%{sign : tensor value}, %{logabsdet : tensor value}, %{a : tensor value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let linalg_solve ~(a @ local) ~(b @ local) ~left =
  let a = globalize_tensor a in
  let b = globalize_tensor b in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_solve(%{a : tensor value}, %{b : tensor value}, Int_val(%{left : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_solve_ex ~(a @ local) ~(b @ local) ~left ~check_errors =
  let a = globalize_tensor a in
  let b = globalize_tensor b in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_solve_ex(%{a : tensor value}, %{b : tensor value}, Int_val(%{left : bool value}), Int_val(%{check_errors : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let linalg_solve_ex_out
  (result @ local)
  ~(info @ local)
  ~(a @ local)
  ~(b @ local)
  ~left
  ~check_errors
  =
  let result = globalize_tensor result in
  let info = globalize_tensor info in
  let a = globalize_tensor a in
  let b = globalize_tensor b in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_solve_ex_out(%{result : tensor value}, %{info : tensor value}, %{a : tensor value}, %{b : tensor value}, Int_val(%{left : bool value}), Int_val(%{check_errors : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let linalg_solve_out ~(out @ local) ~(a @ local) ~(b @ local) ~left =
  let out = globalize_tensor out in
  let a = globalize_tensor a in
  let b = globalize_tensor b in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_solve_out(%{out : tensor value}, %{a : tensor value}, %{b : tensor value}, Int_val(%{left : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_solve_triangular (self @ local) ~(b @ local) ~upper ~left ~unitriangular =
  let self = globalize_tensor self in
  let b = globalize_tensor b in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_solve_triangular(%{self : tensor value}, %{b : tensor value}, Int_val(%{upper : bool value}), Int_val(%{left : bool value}), Int_val(%{unitriangular : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_solve_triangular_out
  ~(out @ local)
  (self @ local)
  ~(b @ local)
  ~upper
  ~left
  ~unitriangular
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let b = globalize_tensor b in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_solve_triangular_out(%{out : tensor value}, %{self : tensor value}, %{b : tensor value}, Int_val(%{upper : bool value}), Int_val(%{left : bool value}), Int_val(%{unitriangular : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_svd ~(a @ local) ~full_matrices ~driver =
  let a = globalize_tensor a in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_svd(%{a : tensor value}, Int_val(%{full_matrices : bool value}), %{driver : string option value}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let linalg_svd_u
  ~(u @ local)
  ~(s @ local)
  ~(vh @ local)
  ~(a @ local)
  ~full_matrices
  ~driver
  =
  let u = globalize_tensor u in
  let s = globalize_tensor s in
  let vh = globalize_tensor vh in
  let a = globalize_tensor a in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_linalg_svd_u(%{u : tensor value}, %{s : tensor value}, %{vh : tensor value}, %{a : tensor value}, Int_val(%{full_matrices : bool value}), %{driver : string option value}));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let linalg_svdvals ~(a @ local) ~driver =
  let a = globalize_tensor a in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_svdvals(%{a : tensor value}, %{driver : string option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_svdvals_out ~(out @ local) ~(a @ local) ~driver =
  let out = globalize_tensor out in
  let a = globalize_tensor a in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_svdvals_out(%{out : tensor value}, %{a : tensor value}, %{driver : string option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_tensorinv (self @ local) ~ind =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_tensorinv(%{self : tensor value}, %{ind : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_tensorinv_out ~(out @ local) (self @ local) ~ind =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_tensorinv_out(%{out : tensor value}, %{self : tensor value}, %{ind : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_tensorsolve (self @ local) (other @ local) ~dims =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_tensorsolve(%{self : tensor value}, %{other : tensor value}, %{dims : int list option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_tensorsolve_out ~(out @ local) (self @ local) (other @ local) ~dims =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_tensorsolve_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}, %{dims : int list option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_vander ~(x @ local) ~n =
  let x = globalize_tensor x in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_vander(%{x : tensor value}, %{n : int option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_vecdot ~(x @ local) ~(y @ local) ~dim =
  let x = globalize_tensor x in
  let y = globalize_tensor y in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_vecdot(%{x : tensor value}, %{y : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linalg_vecdot_out ~(out @ local) ~(x @ local) ~(y @ local) ~dim =
  let out = globalize_tensor out in
  let x = globalize_tensor x in
  let y = globalize_tensor y in
  [%c.alloc
    ({|CAMLreturn(atg_linalg_vecdot_out(%{out : tensor value}, %{x : tensor value}, %{y : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linear (input @ local) ~(weight @ local) ~(bias @ local) =
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_linear(%{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linear_out ~(out @ local) (input @ local) ~(weight @ local) ~(bias @ local) =
  let out = globalize_tensor out in
  let input = globalize_tensor input in
  let weight = globalize_tensor weight in
  let bias = [%globalize: tensor option] bias in
  [%c.alloc
    ({|CAMLreturn(atg_linear_out(%{out : tensor value}, %{input : tensor value}, %{weight : tensor value}, %{bias : tensor option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linspace ~start ~end_ ~steps ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_linspace(%{start : scalar value}, %{end_ : scalar value}, %{steps : int}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linspace_out ~(out @ local) ~start ~end_ ~steps =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_linspace_out(%{out : tensor value}, %{start : scalar value}, %{end_ : scalar value}, %{steps : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linspace_scalar_tensor ~start ~(end_ @ local) ~steps ~options =
  let end_ = globalize_tensor end_ in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_linspace_scalar_tensor(%{start : scalar value}, %{end_ : tensor value}, %{steps : int}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linspace_scalar_tensor_out ~(out @ local) ~start ~(end_ @ local) ~steps =
  let out = globalize_tensor out in
  let end_ = globalize_tensor end_ in
  [%c.alloc
    ({|CAMLreturn(atg_linspace_scalar_tensor_out(%{out : tensor value}, %{start : scalar value}, %{end_ : tensor value}, %{steps : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linspace_tensor_scalar ~(start @ local) ~end_ ~steps ~options =
  let start = globalize_tensor start in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_linspace_tensor_scalar(%{start : tensor value}, %{end_ : scalar value}, %{steps : int}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linspace_tensor_scalar_out ~(out @ local) ~(start @ local) ~end_ ~steps =
  let out = globalize_tensor out in
  let start = globalize_tensor start in
  [%c.alloc
    ({|CAMLreturn(atg_linspace_tensor_scalar_out(%{out : tensor value}, %{start : tensor value}, %{end_ : scalar value}, %{steps : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linspace_tensor_tensor ~(start @ local) ~(end_ @ local) ~steps ~options =
  let start = globalize_tensor start in
  let end_ = globalize_tensor end_ in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_linspace_tensor_tensor(%{start : tensor value}, %{end_ : tensor value}, %{steps : int}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let linspace_tensor_tensor_out ~(out @ local) ~(start @ local) ~(end_ @ local) ~steps =
  let out = globalize_tensor out in
  let start = globalize_tensor start in
  let end_ = globalize_tensor end_ in
  [%c.alloc
    ({|CAMLreturn(atg_linspace_tensor_tensor_out(%{out : tensor value}, %{start : tensor value}, %{end_ : tensor value}, %{steps : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_log(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log10 (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_log10(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log10_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_log10_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log10_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_log10_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log1p (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_log1p(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log1p_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_log1p_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log1p_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_log1p_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log2 (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_log2(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log2_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_log2_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log2_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_log2_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_log_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log_normal (self @ local) ~mean ~std =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_log_normal(%{self : tensor value}, %{mean : float}, %{std : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log_normal_ (self @ local) ~mean ~std =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_log_normal_(%{self : tensor value}, %{mean : float}, %{std : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log_normal_out ~(out @ local) (self @ local) ~mean ~std =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_log_normal_out(%{out : tensor value}, %{self : tensor value}, %{mean : float}, %{std : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_log_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log_sigmoid (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_log_sigmoid(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log_sigmoid_backward ~(grad_output @ local) (self @ local) ~(buffer @ local) =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let buffer = globalize_tensor buffer in
  [%c.alloc
    ({|CAMLreturn(atg_log_sigmoid_backward(%{grad_output : tensor value}, %{self : tensor value}, %{buffer : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log_sigmoid_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~(buffer @ local)
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let buffer = globalize_tensor buffer in
  [%c.alloc
    ({|CAMLreturn(atg_log_sigmoid_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{buffer : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log_sigmoid_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_log_sigmoid_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log_softmax (self @ local) ~dim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_log_softmax(%{self : tensor value}, %{dim : int}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let log_softmax_int_out ~(out @ local) (self @ local) ~dim ~dtype =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_log_softmax_int_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logaddexp (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_logaddexp(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logaddexp2 (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_logaddexp2(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logaddexp2_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_logaddexp2_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logaddexp_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_logaddexp_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logcumsumexp (self @ local) ~dim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_logcumsumexp(%{self : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logcumsumexp_out ~(out @ local) (self @ local) ~dim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_logcumsumexp_out(%{out : tensor value}, %{self : tensor value}, %{dim : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logdet (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_logdet(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logical_and (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_logical_and(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logical_and_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_logical_and_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logical_and_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_logical_and_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logical_not (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_logical_not(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logical_not_ (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_logical_not_(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logical_not_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_logical_not_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logical_or (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_logical_or(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logical_or_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_logical_or_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logical_or_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_logical_or_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logical_xor (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_logical_xor(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logical_xor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_logical_xor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logical_xor_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_logical_xor_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logit (self @ local) ~eps =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_logit(%{self : tensor value}, %{eps : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logit_ (self @ local) ~eps =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_logit_(%{self : tensor value}, %{eps : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logit_backward ~(grad_output @ local) (self @ local) ~eps =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_logit_backward(%{grad_output : tensor value}, %{self : tensor value}, %{eps : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logit_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~eps
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_logit_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{eps : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logit_out ~(out @ local) (self @ local) ~eps =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_logit_out(%{out : tensor value}, %{self : tensor value}, %{eps : float option value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logspace ~start ~end_ ~steps ~base ~options =
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_logspace(%{start : scalar value}, %{end_ : scalar value}, %{steps : int}, %{base : float}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logspace_out ~(out @ local) ~start ~end_ ~steps ~base =
  let out = globalize_tensor out in
  [%c.alloc
    ({|CAMLreturn(atg_logspace_out(%{out : tensor value}, %{start : scalar value}, %{end_ : scalar value}, %{steps : int}, %{base : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logspace_scalar_tensor ~start ~(end_ @ local) ~steps ~base ~options =
  let end_ = globalize_tensor end_ in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_logspace_scalar_tensor(%{start : scalar value}, %{end_ : tensor value}, %{steps : int}, %{base : float}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logspace_scalar_tensor_out ~(out @ local) ~start ~(end_ @ local) ~steps ~base =
  let out = globalize_tensor out in
  let end_ = globalize_tensor end_ in
  [%c.alloc
    ({|CAMLreturn(atg_logspace_scalar_tensor_out(%{out : tensor value}, %{start : scalar value}, %{end_ : tensor value}, %{steps : int}, %{base : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logspace_tensor_scalar ~(start @ local) ~end_ ~steps ~base ~options =
  let start = globalize_tensor start in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_logspace_tensor_scalar(%{start : tensor value}, %{end_ : scalar value}, %{steps : int}, %{base : float}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logspace_tensor_scalar_out ~(out @ local) ~(start @ local) ~end_ ~steps ~base =
  let out = globalize_tensor out in
  let start = globalize_tensor start in
  [%c.alloc
    ({|CAMLreturn(atg_logspace_tensor_scalar_out(%{out : tensor value}, %{start : tensor value}, %{end_ : scalar value}, %{steps : int}, %{base : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logspace_tensor_tensor ~(start @ local) ~(end_ @ local) ~steps ~base ~options =
  let start = globalize_tensor start in
  let end_ = globalize_tensor end_ in
  let options_kind, options_device = options in
  let options_kind = Kind.packed_to_int options_kind in
  let options_device = Device.to_int options_device in
  [%c.alloc
    ({|CAMLreturn(atg_logspace_tensor_tensor(%{start : tensor value}, %{end_ : tensor value}, %{steps : int}, %{base : float}, %{options_kind : int}, %{options_device : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logspace_tensor_tensor_out
  ~(out @ local)
  ~(start @ local)
  ~(end_ @ local)
  ~steps
  ~base
  =
  let out = globalize_tensor out in
  let start = globalize_tensor start in
  let end_ = globalize_tensor end_ in
  [%c.alloc
    ({|CAMLreturn(atg_logspace_tensor_tensor_out(%{out : tensor value}, %{start : tensor value}, %{end_ : tensor value}, %{steps : int}, %{base : float}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logsumexp (self @ local) ~dim ~keepdim =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_logsumexp(%{self : tensor value}, %{dim : int list value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let logsumexp_out ~(out @ local) (self @ local) ~dim ~keepdim =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_logsumexp_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list value}, Int_val(%{keepdim : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lstm
  (input @ local)
  ~(hx @ local)
  ~(params @ local)
  ~has_biases
  ~num_layers
  ~dropout
  ~train
  ~bidirectional
  ~batch_first
  =
  let input = globalize_tensor input in
  let hx = Torch_local_iterators.List.map_local_input hx ~f:globalize_tensor in
  let params = Torch_local_iterators.List.map_local_input params ~f:globalize_tensor in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_lstm(%{input : tensor value}, %{hx : tensor list value}, %{params : tensor list value}, Int_val(%{has_biases : bool value}), %{num_layers : int}, %{dropout : float}, Int_val(%{train : bool value}), Int_val(%{bidirectional : bool value}), Int_val(%{batch_first : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let lstm_cell
  (input @ local)
  ~(hx @ local)
  ~(w_ih @ local)
  ~(w_hh @ local)
  ~(b_ih @ local)
  ~(b_hh @ local)
  =
  let input = globalize_tensor input in
  let hx = Torch_local_iterators.List.map_local_input hx ~f:globalize_tensor in
  let w_ih = globalize_tensor w_ih in
  let w_hh = globalize_tensor w_hh in
  let b_ih = [%globalize: tensor option] b_ih in
  let b_hh = [%globalize: tensor option] b_hh in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_lstm_cell(%{input : tensor value}, %{hx : tensor list value}, %{w_ih : tensor value}, %{w_hh : tensor value}, %{b_ih : tensor option value}, %{b_hh : tensor option value}));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let lstm_data
  ~(data @ local)
  ~(batch_sizes @ local)
  ~(hx @ local)
  ~(params @ local)
  ~has_biases
  ~num_layers
  ~dropout
  ~train
  ~bidirectional
  =
  let data = globalize_tensor data in
  let batch_sizes = globalize_tensor batch_sizes in
  let hx = Torch_local_iterators.List.map_local_input hx ~f:globalize_tensor in
  let params = Torch_local_iterators.List.map_local_input params ~f:globalize_tensor in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_lstm_data(%{data : tensor value}, %{batch_sizes : tensor value}, %{hx : tensor list value}, %{params : tensor list value}, Int_val(%{has_biases : bool value}), %{num_layers : int}, %{dropout : float}, Int_val(%{train : bool value}), Int_val(%{bidirectional : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let lstm_mps_backward
  ~(out0 @ local)
  ~(out1 @ local)
  ~(out2 @ local)
  ~(grad_y @ local)
  ~(grad_hy @ local)
  ~(grad_cy @ local)
  ~(z_state @ local)
  ~(cell_state_fwd @ local)
  (input @ local)
  ~(layersoutputs @ local)
  ~(hx @ local)
  ~(params @ local)
  ~has_biases
  ~num_layers
  ~dropout
  ~train
  ~bidirectional
  ~batch_first
  =
  let out0 = globalize_tensor out0 in
  let out1 = Torch_local_iterators.List.map_local_input out1 ~f:globalize_tensor in
  let out2 = Torch_local_iterators.List.map_local_input out2 ~f:globalize_tensor in
  let grad_y = [%globalize: tensor option] grad_y in
  let grad_hy = [%globalize: tensor option] grad_hy in
  let grad_cy = [%globalize: tensor option] grad_cy in
  let z_state = globalize_tensor z_state in
  let cell_state_fwd = globalize_tensor cell_state_fwd in
  let input = globalize_tensor input in
  let layersoutputs = globalize_tensor layersoutputs in
  let hx = Torch_local_iterators.List.map_local_input hx ~f:globalize_tensor in
  let params = Torch_local_iterators.List.map_local_input params ~f:globalize_tensor in
  [%c.alloc
    {|atg_lstm_mps_backward(%{out0 : tensor value}, %{out1 : tensor list value}, %{out2 : tensor list value}, %{grad_y : tensor option value}, %{grad_hy : tensor option value}, %{grad_cy : tensor option value}, %{z_state : tensor value}, %{cell_state_fwd : tensor value}, %{input : tensor value}, %{layersoutputs : tensor value}, %{hx : tensor list value}, %{params : tensor list value}, Int_val(%{has_biases : bool value}), %{num_layers : int}, %{dropout : float}, Int_val(%{train : bool value}), Int_val(%{bidirectional : bool value}), Int_val(%{batch_first : bool value}));|}]
;;

let lt (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_lt(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lt_ (self @ local) other =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_lt_(%{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lt_scalar_out ~(out @ local) (self @ local) other =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_lt_scalar_out(%{out : tensor value}, %{self : tensor value}, %{other : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lt_tensor (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_lt_tensor(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lt_tensor_ (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_lt_tensor_(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lt_tensor_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_lt_tensor_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lu_solve (self @ local) ~(lu_data @ local) ~(lu_pivots @ local) =
  let self = globalize_tensor self in
  let lu_data = globalize_tensor lu_data in
  let lu_pivots = globalize_tensor lu_pivots in
  [%c.alloc
    ({|CAMLreturn(atg_lu_solve(%{self : tensor value}, %{lu_data : tensor value}, %{lu_pivots : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lu_solve_out ~(out @ local) (self @ local) ~(lu_data @ local) ~(lu_pivots @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let lu_data = globalize_tensor lu_data in
  let lu_pivots = globalize_tensor lu_pivots in
  [%c.alloc
    ({|CAMLreturn(atg_lu_solve_out(%{out : tensor value}, %{self : tensor value}, %{lu_data : tensor value}, %{lu_pivots : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let lu_unpack ~(lu_data @ local) ~(lu_pivots @ local) ~unpack_data ~unpack_pivots =
  let lu_data = globalize_tensor lu_data in
  let lu_pivots = globalize_tensor lu_pivots in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_lu_unpack(%{lu_data : tensor value}, %{lu_pivots : tensor value}, Int_val(%{unpack_data : bool value}), Int_val(%{unpack_pivots : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let lu_unpack_out
  ~(p @ local)
  ~(l @ local)
  ~(u @ local)
  ~(lu_data @ local)
  ~(lu_pivots @ local)
  ~unpack_data
  ~unpack_pivots
  =
  let p = globalize_tensor p in
  let l = globalize_tensor l in
  let u = globalize_tensor u in
  let lu_data = globalize_tensor lu_data in
  let lu_pivots = globalize_tensor lu_pivots in
  let result0, result1, result2 =
    [%c.alloc
      ({|CAMLreturn(atg_lu_unpack_out(%{p : tensor value}, %{l : tensor value}, %{u : tensor value}, %{lu_data : tensor value}, %{lu_pivots : tensor value}, Int_val(%{unpack_data : bool value}), Int_val(%{unpack_pivots : bool value})));|}
       : (raw_tensor * raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1, wrap_managed_tensor result2
;;

let margin_ranking_loss
  ~(input1 @ local)
  ~(input2 @ local)
  ~(target @ local)
  ~margin
  ~reduction
  =
  let input1 = globalize_tensor input1 in
  let input2 = globalize_tensor input2 in
  let target = globalize_tensor target in
  let reduction = Reduction.to_int reduction in
  [%c.alloc
    ({|CAMLreturn(atg_margin_ranking_loss(%{input1 : tensor value}, %{input2 : tensor value}, %{target : tensor value}, %{margin : float}, %{reduction : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let masked_fill (self @ local) ~(mask @ local) ~value =
  let self = globalize_tensor self in
  let mask = globalize_tensor mask in
  [%c.alloc
    ({|CAMLreturn(atg_masked_fill(%{self : tensor value}, %{mask : tensor value}, %{value : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let masked_fill_ (self @ local) ~(mask @ local) ~value =
  let self = globalize_tensor self in
  let mask = globalize_tensor mask in
  [%c.alloc
    ({|CAMLreturn(atg_masked_fill_(%{self : tensor value}, %{mask : tensor value}, %{value : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let masked_fill_scalar_out ~(out @ local) (self @ local) ~(mask @ local) ~value =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let mask = globalize_tensor mask in
  [%c.alloc
    ({|CAMLreturn(atg_masked_fill_scalar_out(%{out : tensor value}, %{self : tensor value}, %{mask : tensor value}, %{value : scalar value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let masked_fill_tensor (self @ local) ~(mask @ local) ~(value @ local) =
  let self = globalize_tensor self in
  let mask = globalize_tensor mask in
  let value = globalize_tensor value in
  [%c.alloc
    ({|CAMLreturn(atg_masked_fill_tensor(%{self : tensor value}, %{mask : tensor value}, %{value : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let masked_fill_tensor_ (self @ local) ~(mask @ local) ~(value @ local) =
  let self = globalize_tensor self in
  let mask = globalize_tensor mask in
  let value = globalize_tensor value in
  [%c.alloc
    ({|CAMLreturn(atg_masked_fill_tensor_(%{self : tensor value}, %{mask : tensor value}, %{value : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let masked_fill_tensor_out ~(out @ local) (self @ local) ~(mask @ local) ~(value @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let mask = globalize_tensor mask in
  let value = globalize_tensor value in
  [%c.alloc
    ({|CAMLreturn(atg_masked_fill_tensor_out(%{out : tensor value}, %{self : tensor value}, %{mask : tensor value}, %{value : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let masked_scatter (self @ local) ~(mask @ local) ~(source @ local) =
  let self = globalize_tensor self in
  let mask = globalize_tensor mask in
  let source = globalize_tensor source in
  [%c.alloc
    ({|CAMLreturn(atg_masked_scatter(%{self : tensor value}, %{mask : tensor value}, %{source : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let masked_scatter_ (self @ local) ~(mask @ local) ~(source @ local) =
  let self = globalize_tensor self in
  let mask = globalize_tensor mask in
  let source = globalize_tensor source in
  [%c.alloc
    ({|CAMLreturn(atg_masked_scatter_(%{self : tensor value}, %{mask : tensor value}, %{source : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let masked_scatter_backward ~(grad_output @ local) ~(mask @ local) ~sizes =
  let grad_output = globalize_tensor grad_output in
  let mask = globalize_tensor mask in
  [%c.alloc
    ({|CAMLreturn(atg_masked_scatter_backward(%{grad_output : tensor value}, %{mask : tensor value}, %{sizes : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let masked_scatter_out ~(out @ local) (self @ local) ~(mask @ local) ~(source @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let mask = globalize_tensor mask in
  let source = globalize_tensor source in
  [%c.alloc
    ({|CAMLreturn(atg_masked_scatter_out(%{out : tensor value}, %{self : tensor value}, %{mask : tensor value}, %{source : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let masked_select (self @ local) ~(mask @ local) =
  let self = globalize_tensor self in
  let mask = globalize_tensor mask in
  [%c.alloc
    ({|CAMLreturn(atg_masked_select(%{self : tensor value}, %{mask : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let masked_select_backward ~(grad @ local) (input @ local) ~(mask @ local) =
  let grad = globalize_tensor grad in
  let input = globalize_tensor input in
  let mask = globalize_tensor mask in
  [%c.alloc
    ({|CAMLreturn(atg_masked_select_backward(%{grad : tensor value}, %{input : tensor value}, %{mask : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let masked_select_out ~(out @ local) (self @ local) ~(mask @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let mask = globalize_tensor mask in
  [%c.alloc
    ({|CAMLreturn(atg_masked_select_out(%{out : tensor value}, %{self : tensor value}, %{mask : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let matmul (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_matmul(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let matmul_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_matmul_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let matrix_exp (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_matrix_exp(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let matrix_exp_backward (self @ local) ~(grad @ local) =
  let self = globalize_tensor self in
  let grad = globalize_tensor grad in
  [%c.alloc
    ({|CAMLreturn(atg_matrix_exp_backward(%{self : tensor value}, %{grad : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let matrix_h (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_matrix_h(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let matrix_power (self @ local) ~n =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_matrix_power(%{self : tensor value}, %{n : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let matrix_power_out ~(out @ local) (self @ local) ~n =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_matrix_power_out(%{out : tensor value}, %{self : tensor value}, %{n : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let max (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_max(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let max_dim (self @ local) ~dim ~keepdim =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_max_dim(%{self : tensor value}, %{dim : int}, Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let max_dim_max ~(max @ local) ~(max_values @ local) (self @ local) ~dim ~keepdim =
  let max = globalize_tensor max in
  let max_values = globalize_tensor max_values in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_max_dim_max(%{max : tensor value}, %{max_values : tensor value}, %{self : tensor value}, %{dim : int}, Int_val(%{keepdim : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let max_other (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_max_other(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let max_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_max_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let max_pool1d (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_max_pool1d(%{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let max_pool1d_with_indices
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~dilation
  ~ceil_mode
  =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_max_pool1d_with_indices(%{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let max_pool2d (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_max_pool2d(%{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let max_pool2d_backward
  ~(grad_output @ local)
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~dilation
  ~ceil_mode
  =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_max_pool2d_backward(%{grad_output : tensor value}, %{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let max_pool2d_backward_out
  ~(out @ local)
  ~(grad_output @ local)
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~dilation
  ~ceil_mode
  =
  let out = globalize_tensor out in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_max_pool2d_backward_out(%{out : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let max_pool2d_with_indices
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~dilation
  ~ceil_mode
  =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_max_pool2d_with_indices(%{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let max_pool2d_with_indices_backward
  ~(grad_output @ local)
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~dilation
  ~ceil_mode
  ~(indices @ local)
  =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let indices = globalize_tensor indices in
  [%c.alloc
    ({|CAMLreturn(atg_max_pool2d_with_indices_backward(%{grad_output : tensor value}, %{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value}), %{indices : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let max_pool2d_with_indices_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~dilation
  ~ceil_mode
  ~(indices @ local)
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let indices = globalize_tensor indices in
  [%c.alloc
    ({|CAMLreturn(atg_max_pool2d_with_indices_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value}), %{indices : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let max_pool2d_with_indices_out
  ~(out @ local)
  ~(indices @ local)
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~dilation
  ~ceil_mode
  =
  let out = globalize_tensor out in
  let indices = globalize_tensor indices in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_max_pool2d_with_indices_out(%{out : tensor value}, %{indices : tensor value}, %{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let max_pool3d (self @ local) ~kernel_size ~stride ~padding ~dilation ~ceil_mode =
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_max_pool3d(%{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let max_pool3d_with_indices
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~dilation
  ~ceil_mode
  =
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_max_pool3d_with_indices(%{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let max_pool3d_with_indices_backward
  ~(grad_output @ local)
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~dilation
  ~ceil_mode
  ~(indices @ local)
  =
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let indices = globalize_tensor indices in
  [%c.alloc
    ({|CAMLreturn(atg_max_pool3d_with_indices_backward(%{grad_output : tensor value}, %{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value}), %{indices : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let max_pool3d_with_indices_backward_grad_input
  ~(grad_input @ local)
  ~(grad_output @ local)
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~dilation
  ~ceil_mode
  ~(indices @ local)
  =
  let grad_input = globalize_tensor grad_input in
  let grad_output = globalize_tensor grad_output in
  let self = globalize_tensor self in
  let indices = globalize_tensor indices in
  [%c.alloc
    ({|CAMLreturn(atg_max_pool3d_with_indices_backward_grad_input(%{grad_input : tensor value}, %{grad_output : tensor value}, %{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value}), %{indices : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let max_pool3d_with_indices_out
  ~(out @ local)
  ~(indices @ local)
  (self @ local)
  ~kernel_size
  ~stride
  ~padding
  ~dilation
  ~ceil_mode
  =
  let out = globalize_tensor out in
  let indices = globalize_tensor indices in
  let self = globalize_tensor self in
  let result0, result1 =
    [%c.alloc
      ({|CAMLreturn(atg_max_pool3d_with_indices_out(%{out : tensor value}, %{indices : tensor value}, %{self : tensor value}, %{kernel_size : int list value}, %{stride : int list value}, %{padding : int list value}, %{dilation : int list value}, Int_val(%{ceil_mode : bool value})));|}
       : (raw_tensor * raw_tensor) value)]
  in
  wrap_managed_tensor result0, wrap_managed_tensor result1
;;

let max_unary_out ~(out @ local) (self @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  [%c.alloc
    ({|CAMLreturn(atg_max_unary_out(%{out : tensor value}, %{self : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let max_unpool2d (self @ local) ~(indices @ local) ~output_size =
  let self = globalize_tensor self in
  let indices = globalize_tensor indices in
  [%c.alloc
    ({|CAMLreturn(atg_max_unpool2d(%{self : tensor value}, %{indices : tensor value}, %{output_size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let max_unpool2d_out ~(out @ local) (self @ local) ~(indices @ local) ~output_size =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let indices = globalize_tensor indices in
  [%c.alloc
    ({|CAMLreturn(atg_max_unpool2d_out(%{out : tensor value}, %{self : tensor value}, %{indices : tensor value}, %{output_size : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let max_unpool3d (self @ local) ~(indices @ local) ~output_size ~stride ~padding =
  let self = globalize_tensor self in
  let indices = globalize_tensor indices in
  [%c.alloc
    ({|CAMLreturn(atg_max_unpool3d(%{self : tensor value}, %{indices : tensor value}, %{output_size : int list value}, %{stride : int list value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let max_unpool3d_out
  ~(out @ local)
  (self @ local)
  ~(indices @ local)
  ~output_size
  ~stride
  ~padding
  =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let indices = globalize_tensor indices in
  [%c.alloc
    ({|CAMLreturn(atg_max_unpool3d_out(%{out : tensor value}, %{self : tensor value}, %{indices : tensor value}, %{output_size : int list value}, %{stride : int list value}, %{padding : int list value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let maximum (self @ local) (other @ local) =
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_maximum(%{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let maximum_out ~(out @ local) (self @ local) (other @ local) =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let other = globalize_tensor other in
  [%c.alloc
    ({|CAMLreturn(atg_maximum_out(%{out : tensor value}, %{self : tensor value}, %{other : tensor value}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mean (self @ local) ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_mean(%{self : tensor value}, %{dtype : int}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mean_dim (self @ local) ~dim ~keepdim ~dtype =
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_mean_dim(%{self : tensor value}, %{dim : int list option value}, Int_val(%{keepdim : bool value}), %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mean_dtype_out ~(out @ local) (self @ local) ~dtype =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_mean_dtype_out(%{out : tensor value}, %{self : tensor value}, %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let mean_out ~(out @ local) (self @ local) ~dim ~keepdim ~dtype =
  let out = globalize_tensor out in
  let self = globalize_tensor self in
  let dtype = Kind.packed_to_int dtype in
  [%c.alloc
    ({|CAMLreturn(atg_mean_out(%{out : tensor value}, %{self : tensor value}, %{dim : int list option value}, Int_val(%{keepdim : bool value}), %{dtype : int}));|}
     : raw_tensor value)]
  |> wrap_managed_tensor
;;

let median (self @ local) =
  let self = globalize_tensor self in
  [%c.alloc ({|CAMLreturn(atg_median(%{self : tensor value}));|} : raw_tensor value)]
  |> wrap_managed_tensor
;;
